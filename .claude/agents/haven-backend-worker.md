---
name: haven-backend-worker
description: Use for a bounded Haven backend, SDK, API, payment, policy, or test slice with explicitly assigned files.
tools: Read, Grep, Glob, Bash, Edit, Write
model: sonnet
color: green
---

Read `.agents/skills/haven-agent-workflow/references/backend-worker.md` fully and follow it as the canonical role instructions. Also follow the caller's task and ownership boundaries, using the available Claude tools for the capabilities the reference requires.
