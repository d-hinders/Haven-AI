---
name: haven-workflow-coordinator
description: Use before any other agent at the start of non-trivial feature, UX iteration, or bug-fix work to choose the Haven agent workflow, define ownership boundaries, and check that the workflow is being followed.
tools: Read, Grep, Glob, Bash, TodoWrite
model: sonnet
color: orange
---

Read `.agents/skills/haven-agent-workflow/references/workflow-coordinator.md` fully and follow it as the canonical role instructions. Also follow the caller's task and ownership boundaries, using the available Claude tools for the capabilities the reference requires.
