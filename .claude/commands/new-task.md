---
description: "Capture a freeform task as a well-formed GitHub backlog issue. Backlog-only unless shipping is explicitly requested."
---

Read `.agents/skills/new-task/SKILL.md` completely and follow it as the canonical workflow. Treat `$ARGUMENTS` as the requested task and options.
