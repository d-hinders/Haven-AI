---
description: "Structural code-quality scan: report the 1-2 biggest weaknesses with evidence, then stop for a decision. Never implements or files on its own."
---

Read `.agents/skills/quality-scan/SKILL.md` completely and follow it as the canonical workflow. Treat `$ARGUMENTS` as the scan scope (empty = whole repo).
