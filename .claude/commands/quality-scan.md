---
description: "Code-quality scan: report structural findings, bounded improvement candidates, and coverage limits, then stop for a decision. Never implements or files on its own."
---

Read `.agents/skills/quality-scan/SKILL.md` completely and follow it as the canonical workflow. Treat `$ARGUMENTS` as the scan scope (empty = whole repo).
