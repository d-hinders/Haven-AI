---
description: "Ship one Haven production release — bump and contract docs when a version moves, PR to dev, promotion to main, then verified closeout in production and on npm."
---

Read and follow [the canonical skill](../../.agents/skills/release/SKILL.md). Treat $ARGUMENTS as the requested version or release scope (empty = work it out in preflight; a release that moves no version is a valid shape and the skill routes it).
