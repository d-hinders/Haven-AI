---
description: "Ship one ready Haven GitHub issue through implementation, review, PR creation, and the correct merge gate."
---

Read `.agents/skills/ship-next/SKILL.md` completely and follow it as the canonical workflow. Treat `$ARGUMENTS` as the queue selector, issue, or freeform task. When invoked through `/loop`, complete exactly one issue and stop so the loop can invoke the command again.
