{
  "$comment": [
    "ROOT-GUARD OWNERSHIP MANIFEST (#1624, epic #1621).",
    "",
    "Haven has guards that live OUTSIDE the tree they police: a lint under",
    "scripts/ that polices packages/backend, a ratchet that polices",
    "packages/frontend. Only the policed package's CI job actually runs them.",
    "So a pull request that ONLY weakens such a guard -- widening an exemption",
    "regex, deleting a refusal, loosening a rule -- would go green with the",
    "guard never running, unless the CI change classifier is told that the",
    "guard belongs to that job.",
    "",
    "That knowledge used to be four bespoke arms of a case statement. It is",
    "data now, and this file is the single place it lives. The classifier",
    "(scripts/ci/change-classifier.mjs) turns each entry into a routing rule;",
    "nothing else decides it.",
    "",
    "FIELDS",
    "",
    "  path     Exact repo-root-relative path. NOT a glob -- see below.",
    "  jobs     The output flags whose CI jobs own this file. These are the",
    "           jobs that would actually catch a regression in it.",
    "  runsVia  The npm script(s) those jobs run to exercise the guard. This is",
    "           what makes `jobs` a CHECKED claim rather than an assertion:",
    "           scripts/ci/root-guard-ownership.test.mjs reads .github/workflows/ci.yml",
    "           and fails if a named job does not actually run one of these.",
    "           Two shapes (#2743): a bare name is a ROOT package.json script",
    "           (`lint:deps`); `<script> -w <workspace>` is that WORKSPACE's",
    "           (`test -w packages/sdk`), for a guard whose only real check is a",
    "           package's own suite and which therefore has no root script to",
    "           hang ownership on. Both are validated against the package.json",
    "           that must define them. Every owning job's block must contain AT",
    "           LEAST ONE entry as a whole `npm run <entry>` command -- not every",
    "           entry in every block, since frontend_checks legitimately runs",
    "           `test -w packages/frontend` and never `test -w packages/sdk`.",
    "  reason   Why this ownership, in a sentence someone can disagree with.",
    "",
    "NOT ONLY GUARDS (#2727). An entry may also be a CANONICAL SOURCE whose",
    "pinned copies live in other packages -- same failure shape, since the",
    "copy's pin test runs only in the copy's own job. For such an entry",
    "`runsVia` names a script the owning job genuinely runs, but it is not",
    "always the whole proof: where a derivation cannot be read back, the job",
    "is ROUTED so its own pin test runs, and `runsVia` is the routing hook",
    "that makes the ownership expressible and checkable here.",
    "",
    "EXACT PATHS ONLY, deliberately. The epic asks for exact paths before glob",
    "semantics, and a guard manifest is where a too-clever glob does the most",
    "damage: `scripts/*.mjs` would silently claim every future script in that",
    "directory for the backend job. A new guard gets a new line. The one place",
    "globs remain is the root-CONFIG rule in the classifier (workflow files,",
    "package.json, tsconfig*.json), which forces the full matrix rather than",
    "naming an owner -- a different question, and its glob behaviour is",
    "characterized by rows in scripts/ci/routing-matrix.mjs.",
    "",
    "ADDING A GUARD: add the entry here, and add a matrix row in",
    "scripts/ci/routing-matrix.mjs. The tests fail until you do both -- an",
    "entry nothing exercises is the failure mode this epic exists to remove.",
    "",
    "NOT a completeness claim. This file says where the guards it lists belong;",
    "it does not assert that every guard in the repo is listed. Enforcing that",
    "is #1626."
  ],
  "guards": [
    {
      "path": ".dependency-cruiser.cjs",
      "jobs": [
        "backend"
      ],
      "runsVia": [
        "lint:deps"
      ],
      "reason": "The module-boundary rule set (#982). It is authoritative over the prose in the architecture doc, so weakening a rule here is exactly the change that must re-run the gate enforcing it."
    },
    {
      "path": "scripts/dep-lint.mjs",
      "jobs": [
        "backend"
      ],
      "runsVia": [
        "lint:deps"
      ],
      "reason": "The dependency-boundary gate itself. It scans packages/backend and packages/core but is only ever run by backend_checks."
    },
    {
      "path": "scripts/dep-lint.test.mjs",
      "jobs": [
        "backend"
      ],
      "runsVia": [
        "lint:deps:test"
      ],
      "reason": "The gate's own self-test. A lint nobody tests can start passing vacuously, so loosening the test must run it."
    },
    {
      "path": "scripts/db-mock-ratchet.mjs",
      "jobs": [
        "backend"
      ],
      "runsVia": [
        "lint:db-mocks"
      ],
      "reason": "The shrink-only ratchet that stops the positional-mock pattern growing back in packages/backend tests (#1219)."
    },
    {
      "path": "scripts/db-mock-ratchet.test.mjs",
      "jobs": [
        "backend"
      ],
      "runsVia": [
        "lint:db-mocks:test"
      ],
      "reason": "The ratchet's self-test, for the same reason as dep-lint's: a ratchet whose test is weakened can ratchet nothing."
    },
    {
      "path": "scripts/retired-rail-prose-ratchet.mjs",
      "jobs": [
        "backend"
      ],
      "runsVia": [
        "lint:retired-rail-prose"
      ],
      "reason": "The shrink-only ratchet that stops the retired-rail prose phrase family growing back across packages/** (#2685). It scans all packages but only backend_checks runs it."
    },
    {
      "path": "scripts/retired-rail-prose-ratchet.test.mjs",
      "jobs": [
        "backend"
      ],
      "runsVia": [
        "lint:retired-rail-prose:test"
      ],
      "reason": "The ratchet's self-test, for the same reason as dep-lint's: a ratchet whose test is weakened can ratchet nothing."
    },
    {
      "path": "scripts/lint-request-schemas.mjs",
      "jobs": [
        "backend"
      ],
      "runsVia": [
        "lint:request-schemas"
      ],
      "reason": "The shrink-only ratchet over the request-validation rollout (#3029, epic #3028): per route file, whether any registered route is still shadowed and how many hand-rolled typeof request guards remain. It scans packages/backend routes but only backend_checks runs it."
    },
    {
      "path": "scripts/lint-request-schemas.test.mjs",
      "jobs": [
        "backend"
      ],
      "runsVia": [
        "lint:request-schemas:test"
      ],
      "reason": "The ratchet's self-test, for the same reason as dep-lint's: a ratchet whose test is weakened can ratchet nothing."
    },
    {
      "path": "scripts/lint-next-steps.mjs",
      "jobs": [
        "backend",
        "mcp_server",
        "signer",
        "mcp"
      ],
      "runsVia": [
        "lint:next-steps"
      ],
      "reason": "The shrink-only ratchet over the typed next-step contract (#3104, epic #3105): per file, emission blocks that name neither a tool nor next_tool_omitted_reason, and discovery entries without suggested_arguments, across the hosted server, the signer and the local runtime. Baseline committed at zero. It scans three packages, so it runs in each package's job and in backend_checks beside the request-schema ratchet."
    },
    {
      "path": "scripts/lint-next-steps.test.mjs",
      "jobs": [
        "backend",
        "mcp_server",
        "signer",
        "mcp"
      ],
      "runsVia": [
        "lint:next-steps:test"
      ],
      "reason": "The ratchet's self-test, for the same reason as dep-lint's: a ratchet whose test is weakened can ratchet nothing."
    },
    {
      "path": "scripts/generate-api-types.mjs",
      "jobs": [
        "backend"
      ],
      "runsVia": [
        "check:api-types"
      ],
      "reason": "Generates packages/core/src/api-types.ts from the backend OpenAPI spec. Backend rather than frontend despite writing into core: the drift check that would fail runs only in backend_checks."
    },
    {
      "path": "scripts/lib/ratchet.mjs",
      "jobs": [
        "backend",
        "frontend"
      ],
      "runsVia": [
        "lint:db-mocks",
        "lint:wire-types"
      ],
      "reason": "The shared ratchet ENGINE, backing NINE gates as of #3131 (eight as of #3104, seven as of #3029, six as of #2747): the backend db-mock, retired-rail-prose and request-schema ratchets, the frontend wire-type ratchet (#1447), the frontend copy lint, packages/frontend/scripts/design-lint, scripts/docs/ui-gate-wording, the typed next-step ratchet (#3104), and the MCP\u2194CLI vocabulary guard (#3131). An earlier version of this sentence said two, which is what it backed at #1447. Routing it to one job would leave the other surface's gates unguarded, which is the whole reason two jobs are named here."
    },
    {
      "path": "scripts/lib/ratchet.test.mjs",
      "jobs": [
        "backend",
        "frontend"
      ],
      "runsVia": [
        "lint:ratchet:test"
      ],
      "reason": "The engine's own self-test, which did not exist until #2759 — six gates imported the module and each gate's suite covered only the paths that gate happens to take, so the engine's contract was tested by nobody. Both surface jobs, mirroring the engine above: a weakening reachable from either surface must run it."
    },
    {
      "path": "scripts/lint-wire-types.mjs",
      "jobs": [
        "frontend"
      ],
      "runsVia": [
        "lint:wire-types"
      ],
      "reason": "The wire-type ratchet (#1447). It polices packages/frontend hooks and types, and only frontend_checks runs it."
    },
    {
      "path": "scripts/lint-wire-types.test.mjs",
      "jobs": [
        "frontend"
      ],
      "runsVia": [
        "lint:wire-types:test"
      ],
      "reason": "Its self-test. Widening the exemption regex or deleting the --update refusal without running the gate is the specific failure this covers."
    },
    {
      "path": "scripts/network-map-pins.test.mjs",
      "jobs": [
        "backend",
        "sdk",
        "signer"
      ],
      "runsVia": [
        "lint:network-pins"
      ],
      "reason": "The network-map pin test (#1478) spans backend, SDK and signer sources, and all three jobs run it. Naming fewer would let a drift ship past the job that would have caught it."
    },
    {
      "path": "scripts/ci/visual-baseline-inventory.mjs",
      "jobs": [
        "frontend"
      ],
      "runsVia": [
        "visual:baselines"
      ],
      "reason": "The visual gate's honesty instrument (#2318): it names the baselines that were actually compared, and refuses an EMPTY baseline tree — the case where the pixel gate passes having compared nothing. It reads packages/frontend/e2e/__screenshots__ but lives under scripts/, and the job that reads it beside the tick (design_visual) is gated on the frontend surface, so a pull request touching only this file must be routed to frontend_checks to run it at all."
    },
    {
      "path": "scripts/ci/visual-baseline-inventory.test.mjs",
      "jobs": [
        "frontend"
      ],
      "runsVia": [
        "visual:baselines:test"
      ],
      "reason": "Its self-test. A report nobody tests can start answering vacuously, which is the exact defect the report exists to expose, so weakening the test must run it."
    },
    {
      "path": "scripts/lint-migration-constraint-scope.mjs",
      "jobs": [
        "backend"
      ],
      "runsVia": [
        "lint:migration-constraint-scope"
      ],
      "reason": "Refuses a pg_constraint lookup in packages/backend/src/db/migrations/** that is not anchored to a relation or schema (#2702). `conname` is not unique across schemas, so an unanchored lookup is answered by another schema's copy and the constraint is silently skipped — measured at 185 and 193 schemas missing three constraints. It lives under scripts/ and only backend_checks runs it, so a PR that only weakens the guard would not route to the job that runs it."
    },
    {
      "path": "scripts/lint-migration-constraint-scope.test.mjs",
      "jobs": [
        "backend"
      ],
      "runsVia": [
        "lint:migration-constraint-scope:test"
      ],
      "reason": "The guard's own fixtures, and they are load-bearing rather than decoration: with the repo already clean, neutering the guard's scope predicate left it GREEN (measured). These tests are the half that can go red, including a fixture in the multi-line shape a hand-written grep missed."
    },
    {
      "path": "packages/sdk/src/agent-guidance.ts",
      "jobs": [
        "sdk",
        "cli",
        "frontend"
      ],
      "runsVia": [
        "lint:runbook-parity"
      ],
      "reason": "The canonical agent runbook, and the source of several pinned derivations that live in other packages so those packages stay installable alone. Two are FULL-TEXT copies (packages/cli/src/agent-guidance-text.ts, packages/frontend/public/for-agents.md) and lint:runbook-parity verifies both. The frontend holds two more that it cannot: src/lib/agent-onboarding-prompt.ts copies one constant, and src/lib/agent-skill-bundle.ts embeds text composed from four of them via sdk/src/skill-content.ts -- resolved text, not an extractable literal. Each derivation is byte-pinned by a test in ITS OWN package's job, and before #2727 an SDK-only change routed code, sdk, backend, connect, mcp, mcp_server and signer: neither cli nor frontend, so none of those pin tests ran. #2713 edited this file and left the CLI copy stale; the frontend copy survived only because that PR happened to touch frontend files too. dev did not even go red -- cli_checks was skipped, so the stale copy was simply carried until an unrelated backend PR (#2719) regenerated it. All three jobs are named so every pin test runs on a change here."
    },
    {
      "path": "packages/cli/scripts/sync-agent-guidance.mjs",
      "jobs": [
        "sdk",
        "cli",
        "frontend"
      ],
      "runsVia": [
        "lint:runbook-parity"
      ],
      "reason": "The generator and verifier for the runbook copies above. It lives under packages/cli/ but reads packages/sdk/, so a change here can break a check that any of the three jobs running lint:runbook-parity performs -- and these rules match before the packages/cli/* arm, so without the explicit cli in jobs a change to this file would stop routing the CLI's own suite (#2727)."
    },
    {
      "path": "packages/sdk/src/skill-content.ts",
      "jobs": [
        "sdk",
        "frontend"
      ],
      "runsVia": [
        "test -w packages/sdk",
        "test -w packages/frontend"
      ],
      "reason": "The canonical generic payment skill, and the second SDK source with a cross-package pin. packages/frontend/src/lib/agent-skill-bundle.ts keeps a deliberately decoupled inline copy so the frontend deploys standalone, and packages/frontend/src/lib/__tests__/agent-skill-bundle.test.ts imports THIS file directly to assert byte-for-byte parity -- a test that runs only in frontend_checks. #2727 closed the same hole for agent-guidance.ts and left this one open (#2743): an SDK-only change here routed sdk but not frontend, so mutating a heading exited 0 on lint:runbook-parity (which reads only the two full-text runbook copies, and this is not one of them) while agent-skill-bundle.test.ts failed in a job that never ran. That copy is resolved text composed at build time, so no readable-back lint can cover it; the frontend's own suite is the check, which is why runsVia names it by workspace rather than inventing a second spelling of a check frontend_checks already runs."
    },
    {
      "path": "packages/backend/src/infra/chain/x402-binding-signer.ts",
      "jobs": [
        "backend",
        "mcp_server"
      ],
      "runsVia": [
        "test -w packages/backend",
        "test -w packages/mcp-server"
      ],
      "reason": "The producer of the Haven-signed x402 expected context. packages/mcp-server/src/x402-expected-wire-contract.test.ts imports THIS file across the package boundary to prove the hosted/signer/backend contract byte-for-byte -- the only runtime import of backend/src from any other package -- and that test runs only in mcp_server_checks. #3023 changed this file's import graph (config.ts, which requires DATABASE_URL at evaluation) on a backend-only PR; the MCP job did not run, dev runs skip it by surface, and the contract proof was absent until the next mcp-server PR failed (#3046). Same shape #2727/#2743 closed for the SDK's canonical sources."
    },
    {
      "path": "packages/backend/src/config/boolean-flag.ts",
      "jobs": [
        "backend",
        "mcp_server"
      ],
      "runsVia": [
        "test -w packages/backend",
        "test -w packages/mcp-server"
      ],
      "reason": "The only module x402-binding-signer.ts reaches inside the backend (#3046). It must stay dependency-free so the binding signer stays importable without the backend runtime env; an import added here re-creates the #3046 trap in the mcp-server job, so a change routes that job too."
    },
    {
      "path": ".env.example",
      "jobs": [
        "backend"
      ],
      "runsVia": [
        "test -w packages/backend"
      ],
      "reason": "The configuration mirror the backend suite pins in BOTH directions: packages/backend/src/docs-drift/env-example-drift.test.ts asserts every variable the backend reads is documented here and every documented key is read somewhere. Its only gated runner is backend_checks' own vitest, so before this entry a PR editing only .env.example routed nowhere and skipped the drift test on exactly the edit it exists to catch (quality scan 2026-09-22, candidate C4)."
    },
    {
      "path": "scripts/lint-request-schemas-baseline.json",
      "jobs": [
        "backend"
      ],
      "runsVia": [
        "lint:request-schemas"
      ],
      "reason": "The committed baseline the request-schema ratchet (#3029, epic #3028) compares packages/backend route counts against. Lowering a count without the code change that earns it is the exact weakening the ratchet refuses, and the gate runs only in backend_checks — the ratchet and its self-test were registered but the data they compare against was not, so a baseline-only edit routed nowhere (quality scan 2026-09-22, candidate C4)."
    },
    {
      "path": "scripts/lint-next-steps-baseline.json",
      "jobs": [
        "backend",
        "mcp_server",
        "signer",
        "mcp"
      ],
      "runsVia": [
        "lint:next-steps"
      ],
      "reason": "The committed baseline of the typed next-step ratchet (#3104, epic #3105), read by lint:next-steps everywhere that lint runs — the same four jobs as the ratchet itself, because lowering a count here weakens the same contract the ratchet polices. The ratchet and its self-test were registered while the data they compare against was not, so a baseline-only edit routed nowhere (quality scan 2026-09-22, candidate C4)."
    },
    {
      "path": "scripts/vitest/assert-fresh-dist.mjs",
      "jobs": [
        "connect",
        "mcp_server",
        "qa_agent"
      ],
      "runsVia": [
        "test -w packages/connect",
        "test -w packages/mcp-server",
        "test -w packages/qa-agent"
      ],
      "reason": "The vitest global-setup hook that refuses to run a package's suite against a stale build of @haven_ai/sdk, mcp or signer. Three packages' suites import it and each runs it inside its own gated job, so a weakening that made the check vacuous is caught only if every consumer's job re-runs — the #3046 shape, and no dependents fan-out reaches any of the three packages (quality scan 2026-09-22, candidate C4)."
    },
    {
      "path": "scripts/lib/lint-escapes.mjs",
      "jobs": [
        "frontend"
      ],
      "runsVia": [
        "design:lint -w packages/frontend"
      ],
      "reason": "The shared escape-marker helper for the frontend line-scanning gates: design-lint and the copy lint both import isEscaped from it. The copy lint runs unconditionally, but design:lint runs inside the gated frontend_checks job, so a PR that only weakens escape recognition skipped the one gated job that consumes it (quality scan 2026-09-22, candidate C4)."
    }
  ]
}
