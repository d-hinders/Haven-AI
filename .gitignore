node_modules/
dist/
*.tsbuildinfo
.next/
.env
*.env.local
*.log
.DS_Store
*.docx
output/playwright/
packages/frontend/playwright-report/
packages/frontend/test-results/

# Docs coupling-gate output (generated by scripts/docs/coupling-gate.mjs)
coupling-comment.md
# Design-system coupling-gate output (#898)
design-system-coupling-comment.md

# Agent worktrees — transient per-agent checkouts, never committed
# (tracked .claude config like agents/ and skills/ is intentionally NOT ignored)
.claude/worktrees/

# Local session reports (never commit)
haven-*.pdf
*.local.mts

# iCloud/macOS resurrects deleted files as "<name> 2.<ext>" duplicates and
# leaves build tarballs around — never commit these (they broke the docs
# coupling gate on #969, 2026-07-23; same class as the earlier docs junk).
* 2.ts
* 2.tsx
* 2.mts
* 2.js
*.tgz
