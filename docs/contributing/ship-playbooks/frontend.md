---
owner: "@d-hinders"
status: current
covers: []  # narrative — process playbook
last-verified: "2026-06-29"
---

# Frontend playbook

Loaded by `/ship-next` for `area:frontend` issues. **Stub — filled in [#654](https://github.com/d-hinders/Haven-AI/issues/654).**

When complete, this playbook will require, by reference: the `AGENTS.md` "Required Reading For UI Work" set (design system, copy guidelines, screen recipes, design review), reuse-first component usage, the Captain Self-Check Preflight for the touched UI traps, browser **or** named headless verification, an advisory design-review pass, and the UI merge policy (auto-merge clean UI, pause on a flagged UX issue).

Until #654 lands, follow `AGENTS.md` → "Required Reading For UI Work" and `docs/product/design-review.md` directly.
