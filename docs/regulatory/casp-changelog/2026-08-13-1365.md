- **#1365** — reporting-feed recovery gaps closed, both sides observability/liveness with the double-post guard intact: (1) a connector-level skip is now a REAL `skipped` ledger row with its reason preserved (previously mis-recorded as `pushed` with NULL `external_ref` and the reason dropped — the dashboard showed "Synced" for a payment never delivered and the backfill never revisited it); skipped rows are re-claimable exactly like failed ones (lifecycle-tested: skip → re-claim → exactly one push). (2) `POST /accounting/reporting/reopen/:paymentId` is the ONE sanctioned path that flips a `pushed` row back to retryable, and it is verification-gated server-side: the route re-runs the #1362 read-back and reopens only when Fortnox ITSELF confirms the pushed invoice no longer exists; an invoice that still exists refuses with nothing written (mutation-tested — removing the registered-check flips the test), and the row-state half (`pushed → failed` only, via a status-predicated UPDATE) is separately tested. No accounting assertion is added (the feed remains non-asserting, #491), no payment authority, custody, or route touches money — this is bookkeeping-delivery liveness for records of ALREADY-settled payments. Perimeter unchanged.
