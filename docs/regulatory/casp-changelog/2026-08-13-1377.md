- **#1377** — Connect-agent flow polish (rename + single-USDC budget step + step-4 shell) plus ONE new backend read: `GET /agent-connection-setups/:setupId/connector-status`, a narrow, read-only status endpoint the local connector polls after registering so it can wait for the user's budget approval and end with a line naming the granted authority. Authenticated with the pending agent API key (deliberately usable while `setup_pending`-scoped); it reveals only the setup status plus — once active — the approved-budget summary (token, amount, reset period), returns 404 for setups not owned by the presented key and 401 for revoked/missing keys, and grants no payment authority: nothing signs, funds, or moves value through it, and the #1073/#1074 approval verification (Haven verifies live on-chain authority rather than accepting the client's word) is untouched. The connector's wait is bounded (5 s polls, 3-minute cap, clean guidance on timeout — never a hang) and skipped in `--json` automation mode. Frontend changes are presentation-only: the single-budget step still submits the byte-identical setup payload, and approval routes are unchanged. Perimeter unchanged.
