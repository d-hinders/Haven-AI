- **#1379** — the published Connect CLI now resolves the npm `.bin` symlink before deciding whether it is the program entrypoint, with a packed-artifact smoke proving `haven-connect --version` and `--help` actually execute through that path. The frontend adds a one-minute, pre-registration recovery note only while the authenticated setup status remains `awaiting_connection`; it offers a copy of the existing local command and cancellation before a fresh one-time setup, and does not infer a connection from local files or turn a status-read error into a verdict. No key generation, credential storage, agent registration semantics, approval route, signature, funding, or on-chain authority changes; the setup still grants no spending authority until the user's existing approval completes. Perimeter unchanged.
