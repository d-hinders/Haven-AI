- **#1386** — Connect now reports completed local runtime installation status before it begins its bounded budget-approval poll. The report contains only runtime readiness metadata and remains best-effort: a failure is logged without activating the agent, changing a budget, exposing credentials, or altering any on-chain authority. The existing dashboard approval controls and pending-approval boundary remain unchanged; the order merely prevents the connector and dashboard from waiting on one another. A regression test pins report-before-first-poll ordering, and the existing failure-path test pins non-fatal reporting. Perimeter unchanged.
