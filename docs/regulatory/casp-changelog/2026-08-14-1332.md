- **#1332** — guidance-surface parity: Connect setup now installs the generic payment skill via each runtime's DOCUMENTED instruction mechanism, not only Claude's skills folder. Hermes gets the canonical `SKILL.md` byte-for-byte in `$HERMES_HOME/skills/haven-pay/` (Hermes auto-discovers the same front-matter format); Codex CLI/Desktop get the skill BODY as a marker-delimited managed section in the global `~/.codex/AGENTS.md` — re-runs replace the section in place, everything outside Haven's own markers is preserved byte-for-byte, and a damaged marker pair fails CLOSED with the file untouched (mutation direction covered by tests: orphaned-marker fixtures assert the file is left byte-identical). Runtimes without a documented instruction file (Cursor, VS Code, Claude Desktop, `other`) get nothing written; the MCP server-level initialize instructions remain their baseline. The substance is the single canonical string in `packages/sdk/src/skill-content.ts` (`HAVEN_SKILL_BODY_MD` is a mechanical front-matter strip of it, derived at module load, never a hand-maintained fork; the frontend download parity test still pins the canonical bytes). Authority analysis: this is orientation prose only — static, secret-free by construction (tests assert no API key or private-key material in any written file), installed only after the runtime config write succeeded, and a failed install never fails setup. No tool schema, refusal, route, signature, credential handling, or consent hash changes — neither consent surface reads instruction/skill text. Perimeter unchanged.
