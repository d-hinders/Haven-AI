- **#1426** — presentational only: the connector's budget-approval celebration line now phrases every dashboard-offered reset period with the dashboard's own sentence words (`per week`, `per month`, `in total`) instead of raw minutes ("per 10080 minutes"), with a parity test pinning each phrasing. The described grant, its amount, its enforcement, and every credential path are untouched — this changes how an already-granted authority is WORDED in terminal output, nothing about what was granted or how it is enforced on-chain. The mapping is deliberately duplicated from the frontend rather than shared through `@haven_ai/core` (a private workspace package that published packages must never depend on); the tests are the coupling. Perimeter unchanged.
