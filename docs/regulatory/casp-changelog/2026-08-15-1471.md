- **#1471** — the backend's x402 network gate now accepts the bare literal `'base-sepolia'`, resolving the one divergence between the four network-string maps in the SDK's favour — and the deciding evidence was the SPEC, not preference: the x402 library's own `NetworkSchema` enum lists `'base-sepolia'` as a canonical network identifier (no CAIP form appears in that enum at all), so a real merchant sends the bare string and a gate rejecting it made the client call an option payable and then 400 on it — undercutting #1453's property that selection is decided once and is trustworthy. The #1478 pin test did its job in its first week and in BOTH directions: shrinking `DOCUMENTED_DIVERGENCE` to empty proved the divergence really closed, and the widened backend immediately FAILED the symmetry check because the SDK client's `chainIdFromNetwork` lacked the alias — forcing every copy to widen in the same change, which is precisely the drift class the test exists to make impossible. All four maps now agree exactly; the documented-divergence list is empty, and any future entry is a recorded decision with #1471 as the precedent for how one gets resolved. Authority unchanged: a network string maps to a chain id or the request 400s exactly as before; testnet-only surface (Base Sepolia), no mainnet behaviour touched, no new route, no key material. Merge-order note recorded for honesty: this change could not land before the #1478 pin test it edits — shipping them in the wrong order would have broken dev's CI on a guard doing its job.
