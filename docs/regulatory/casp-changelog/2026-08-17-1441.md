- **#1441** — The demo merchant gained `vpn_legacy`, the one catalogue entry advertising **eip3009 alone**, and `x402-hosted-mcp-signer` now buys it. That leg covers the funding-leg topology from the `haven_pay_mcp_tool` entry point, and the funding leg only exists on eip3009 — but every other product also advertises erc7710, and #1450's preference rule is unconditional on the merchant side, so a delegation-rail client always took erc7710 and the funding leg never ran. The leg's invariant ("two on-chain legs, zero delegate residual") had nothing to assert, so it skipped PERMANENTLY, and under `QA_REQUIRE_ALL_LEGS=1` that single unreachable leg blocked the whole money-flow suite from ever reporting green — including on runs where every other scenario passed. **No authority, custody, or execution boundary moves.** This is a testnet fixture's catalogue plus one QA scenario's target: scheme selection itself is untouched (#1450's rule is correct and unchanged — this gives it a merchant where it has exactly one option to choose), and nothing changes about what may be spent, by whom, or under which caveats. A dedicated product rather than narrowing an existing one, which is the recorded lesson of #1513: making `vpn_pro` 3009-only silently changed what every other leg was buying and had to be reverted. The erc7710 branch in the leg becomes a FAILURE rather than a skip, because against a merchant offering no erc7710 option an erc7710 quote can only mean scheme selection ignored the advertised methods, or the merchant stopped serving the 3009-only product — both real defects on the funding path, and this leg is now what catches them; its message names the plan and what that plan advertises so a triager can tell those two causes apart. Worth recording for the coverage claim rather than any perimeter change: the funding-leg bridge is the rail that deliberately reintroduces a transient delegate hot balance, so it is the one whose live coverage matters most, and it had been unexercised from this entry point for as long as the preference rule has existed. Mutation-proven, and the mutation found a real gap: widening `vpn_legacy` back to `SUPPORTED_SETTLEMENT_METHODS` — the exact change its own prose comment warns against — initially broke NO test, so the comment was the only thing holding the property this fix depends on; three `products.test.ts` cases now fail under that mutation, including one asserting it remains the ONLY 3009-only product so the leg keeps an unambiguous target. Perimeter unchanged.
