- **#1441** — QA-harness only: the `x402-hosted-mcp-signer` money-flow leg now sends `max_amount_human: '1'` on its `haven_pay_mcp_tool` call, and two tests pin that the request carries a cap in exactly one spelling. **No product code changed** — not a route, not a tool schema, not a signing path, not a caveat, not a credential scope — so nothing in the x402 execution sequence or the custody perimeter moves, and every claim in `04-x402-payment-sequence.md` reads identically before and after. The cap requirement itself is unchanged behaviour shipped in #1351 (`readMaxAmountCap(args, { required: true })`); this change makes the harness *satisfy* an existing contract it had drifted out of, rather than relaxing one. The direction matters for this doc's purposes: the leg was being refused with `INVALID_INPUT` **before the merchant was contacted and before any funding intent existed**, so no payment authority was ever exercised by the failing runs, and the fix does not grant any — it restores a test's ability to reach the hosted-topology seam (#1154) that the leg exists to cover. Worth recording as a verification GAP rather than a verification: because this leg was red from 2026-08-15, the hosted MCP + local edge signer topology — the DEFAULT topology every connect-flow user runs — has had no passing money-flow evidence since then, and `x402-erc7710-hosted` remains red for a separate, unresolved reason, so the erc7710 half of that topology still has none. That gap is stated, not closed, by this change; it is tracked on #1441 and was accepted on the record via the `qa-override` on promotion #1504. Mutation-proven by the two new argument-level assertions, which fail if a future edit drops the cap or sends both spellings. Perimeter unchanged.
