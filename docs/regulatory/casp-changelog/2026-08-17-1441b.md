- **#1441 (closeout)** — QA-harness only, no product code. Two changes. (1) `x402-hosted-mcp-signer` now SKIPS, with a reason, when the hosted quote comes back erc7710-shaped, instead of failing on a `payload_hash` that scheme never carries. The leg's invariant is the FUNDING-LEG topology ("both on-chain legs succeed, delegate left with zero residual"), and #1450's preference rule makes the hosted tool select erc7710 whenever the merchant advertises it — so against the dev merchant that invariant is *unreachable*, not violated. The skip is not a coverage hole and the reason string says where the coverage lives: hosted erc7710 by `x402-erc7710-hosted`, hosted 3009 by `x402-catalog-guided-purchase`, which drives the SAME hosted MCP + local signer topology through `haven_prepare_catalog_purchase` and proves both on-chain legs plus a zero delegate residual on every run. What this leg alone still covers is the `haven_pay_mcp_tool` entry point on the funding path, which is why it is retained rather than deleted, and `QA_REQUIRE_ALL_LEGS=1` still escalates the skip to a run failure for an operator who wants that entry point exercised against a 3009-only merchant. (2) Reverts the #1509 diagnostic probes from `x402-erc7710-hosted` now that they have done their job — they localised the failure to *after* a successful settle (`after_quote=pending_signature after_sign=pending_signature after_settle_refused=submitted`), which is what identified the real cause in #1511. **Nothing about authority, custody, or execution changes**: no route, no tool schema, no signing path, no caveat, no credential scope is touched, and the assertions that make the remaining legs meaningful — scheme reported, delegate EOA untouched, both on-chain legs, zero residual — are all retained. Mutation-proven: disabling the new skip guard fails the two tests that assert it, while the test pinning that a 3009 quote is UNAFFECTED correctly stays green, which is what stops the guard from silently swallowing the funding path it exists to protect. Perimeter unchanged.
