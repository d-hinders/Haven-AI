- **#1441 (closeout, corrected)** — QA fixture only; no product code, no Haven-side behaviour. The previous attempt made `x402-hosted-mcp-signer` SKIP on an erc7710 quote, reasoning that its funding-leg invariant was unreachable once #1450 made Haven prefer erc7710 wherever a merchant advertises it, and that the coverage existed elsewhere. The live run rejected that reasoning on the repo's own terms: `QA_REQUIRE_ALL_LEGS=1` is set, and the runner refused the run with "green-with-skips: 1 scenario(s) NEVER RAN". The policy is stronger than "covered somewhere" — a leg that does not RUN is not coverage — and it is right, because the topology this leg guards is the one #1138 broke while every gate stayed green. So the leg is made runnable instead: the demo merchant's `vpn_pro` becomes deliberately **EIP-3009-only** (`settlementMethods: ['eip3009']`), and the leg buys `{plan:'pro'}` at both the quote and the settle. **This does not weaken the preference rule and changes no Haven code**: #1450 still prefers erc7710 wherever it is advertised, and withholding it on ONE otherwise-unused demo product simply reproduces a merchant shape that dominates the real world (≈every live x402 merchant is EIP-3009-only) so the funding-leg path stays exercised end to end. No other leg is affected — every other scenario buys `vpn_basic`, and `storage_50gb` remains the verify-without-settle sweep fixture. The skip branch is retained but re-scoped from "expected outcome" to **fixture-regression detector**: reaching it now means `vpn_pro` started advertising erc7710 again or the leg was repointed, and its message names the exact file and field to check; under `QA_REQUIRE_ALL_LEGS=1` that skip fails the run, which is the intended alarm. Verified: all 56 demo-merchant tests pass with the narrowed product (none pinned its settlement methods), and new assertions pin that BOTH the quote and the settle buy `pro` — a mismatch there would settle a different purchase than the one authorized. Perimeter unchanged.
