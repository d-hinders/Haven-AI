- **#1503** — Release `0.1.24-alpha.0`: a version bump across the five published packages (`sdk`, `signer`, `mcp`, `connect`, `cli`) plus `mcp-server`'s self-reported version, produced mechanically by `scripts/release-bump.mjs`. The diff contains **no executable behaviour**: version fields, internal cross-package dep pins, the six source version constants (`MCP_VERSION`, `SIGNER_VERSION`, `HOSTED_SERVER_VERSION`, `CONNECTOR_VERSION`, `CLI_VERSION`, connect's `runtime-manifest`), and the matching `package-lock.json` entries. No route, no credential scope, no key material, no signing path, and no authority boundary is added, widened, or moved by this change — every custody and authority claim in the parent doc reads identically before and after it. What the release *distributes* is the already-merged erc7710 client work from epic #1450, whose perimeter analyses are their own shards (`2026-08-15-1452.md`, `-1453.md`, `-1454.md`, `-1474.md`, `-1476.md`, `-1482.md`); this entry deliberately does not restate or re-verify them, because publishing reviewed code to npm is a distribution event, not a second authority change. The one boundary worth stating positively: the bump moves `connect`'s pinned `sdkVersion`/`signerVersion` from `0.1.23-alpha.2` to `0.1.24-alpha.0`, so a newly connected agent installs the signer that carries #1455's local caveat verification and #1476's refusal of an unbound delegation payload — both **refusals**, so the reachable signing surface narrows rather than widens. Private workspace consumers (`backend`, `qa-agent`) keep their `"*"` pins per the release contract, so neither silently resolves a stale registry tarball. Verified by the release script's own post-build assertions (every version constant re-read from the rebuilt `dist`), `scripts/verify-connect-bundle.mjs`, and `smoke:pack -w packages/connect` launching the packed MCP, signer and connector tarballs. Stated rather than smoothed over: this release was promoted under a `qa-override`, so the money-flow QA evidence covering the promoted head is a recorded human acceptance rather than a green harness run — the outstanding red legs are tracked on #1441 and are unrelated to the contents of this diff. Perimeter unchanged.
