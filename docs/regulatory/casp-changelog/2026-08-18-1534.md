- **#1534** — QA harness only. **No authority, custody, or execution boundary moves**: no route, tool schema, capability, signing path, caveat, or credential scope changes, no product code is touched, and every claim in `casp-risk-guardrails.md` and `04-x402-payment-sequence.md` reads identically before and after. It reaches this log because `packages/qa-agent/src/scenarios/x402-hosted-mcp-signer.ts` sits inside a contract doc's `covers:` globs. Three money-flow legs now supply their own per-attempt x402 idempotency key instead of inheriting the SDK's synthesised 5-minute-bucket one. Recorded because of what the defect it fixes says about the EVIDENCE this repo's promotion gate runs on, rather than about the perimeter. `qa-dev.yml` retries the whole suite ~30s after a failure — inside that bucket — so a leg supplying no key had attempt 2 collapse onto attempt 1's already-settled payment and be refused by the #1521 guard. The guard was RIGHT: it correctly declined to re-authorize a payment whose funds had already reached the merchant. The defect was that the retry asked it to. Consequence worth stating plainly: on 2026-08-18 `x402-sweep-recovery` PASSED on attempt 1 and FAILED on attempt 2 for this reason, attempt 1 failed 2 scenarios while attempt 2 failed 4, and the two sets barely overlapped — so a red money-flow run could not be read as evidence of a regression at all, because the retry was testing a slate the previous attempt had dirtied. A promotion gate keyed on that run's colour was keyed on noise. Nothing about what an agent may spend changes; what changes is whether a failing run means anything. Scope was established by reading every surviving leg rather than assumed: `x402-delegation-3009` already had the helper (#1520), `x402-delegation-3009-sweep` supplied NO key at all and is the actual gap, `x402-catalog-guided-purchase` and `x402-hosted-mcp-signer` hand-rolled `Date.now()` keys that are per-attempt-distinct in practice but duplicate the helper and collide within a millisecond, and the three erc7710 legs send no key by design — they have no funding leg to strand, the backend stores null so the unique index cannot collide, and all three passed both attempts in run 32111752993. The durable half is a guard test derived from the sources rather than a list of leg names: a leg buying through `client.fetch` must supply the helper's key, and no leg may hand-roll one. That derivation is deliberate — #1520 fixed exactly the two legs it happened to touch and stopped, which is the same hand-maintained-list defect #1526 corrected twice in one day. Mutation-proven in both directions: reverting the sweep-leg fix and reintroducing a `Date.now()` key each fail a distinct test. Perimeter unchanged.
