- **Release 0.1.26-alpha.0** — Version strings only across the five published packages, their internal dep pins, the source version constants, and the Supported Runtime Manifest table. **No authority, custody, or execution boundary moves**: no route, tool schema, capability, signing path, caveat, or credential scope changes in this diff, every claim in `casp-risk-guardrails.md` reads identically before and after, and `publish.yml` derives the `alpha` dist-tag from the prerelease version so `latest` is untouched. Recorded, as with 0.1.25-alpha.0, for what the bump CARRIES: #1521 changed `packages/sdk/src/client.ts`, `types.ts`, `provider.ts` and `index.ts` AFTER 0.1.25-alpha.0 was published, and `publish.yml` skips versions already on npm — so promoting without a bump would leave the same version string holding different code on npm and in-repo, with SDK consumers silently retaining the pre-#1521 behaviour: on an idempotency replay of an already-settled x402 payment, the published SDK still mints a fresh EIP-3009 authorization against a delegate whose funds are spent and returns it paired with the original payment's tx hash. That is a payer-facing reporting defect (funds moved, payer told it failed), never a double-spend — the unfundable authorization cannot settle. Verified the release carries the fix rather than assuming: the built SDK `dist/index.js` and `dist/index.cjs` each contain `X402AlreadySettledError` (5 occurrences). First release cut under the #1526 pin rules, and they held: `mcp-server`'s version moves in lockstep for `HOSTED_SERVER_VERSION` coherence while its `"*"` workspace pins are untouched — checked by `verifyPrivateConsumersUnpinned` inside the bump itself, not just by the PR lint. The lockfile change is 11 version-string lines; the local npm's 38 spurious `dev`/`peer` metadata insertions were stripped diff-aware (insertions only, pre-existing lines untouched) and the result re-validated as JSON, with `npm ci --dry-run` consistent. Perimeter unchanged.
