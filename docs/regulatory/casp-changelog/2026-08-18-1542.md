- **#1542** — Connect completion handoff made approval-aware (epic #1541, from the 2026-08-18 field test): the approval wait now checks once before announcing itself (so an already-approved budget never prints the contradictory "waiting for you to approve… / Budget approved 🎉" pair), the printed next steps are shaped by the wait's observed outcome (approved → confirmation instead of a re-request; ended → fresh-connection guidance; pending/skipped → the full instruction unchanged), the connector uses one name for the one gate ("approve the budget", replacing "agent rules" in its output), and the restart step states why it survives in-process verification (session/app-scoped runtimes read MCP config only at start-up). Orientation prose, its ordering, and poll timing (first check immediate, same 5 s cadence and 3-minute bound thereafter) only: no credential handling changed, no new capability, no route or signing surface touched, the wait still uses the read-only setup-scoped `connector-status` endpoint, the `--json` structured outcome contract is byte-identical (structured mode still skips the wait), and the no-secret output boundaries (no keys, no credential paths, abbreviated delegate address) hold. What may be spent, by whom, and under which caveats is untouched. Perimeter unchanged.
