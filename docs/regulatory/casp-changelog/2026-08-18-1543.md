- **#1543** — The connector now sends an early, best-effort install-status report the moment the runtime MCP config write settles (epic #1541, from the 2026-08-18 field test), so the dashboard can expose its budget-approval controls without waiting for the network probes and skill install — a tail the approval decision does not depend on. Readiness-metadata timing only, no authority surface moves: the report goes to the same setup-scoped `install-status` endpoint with the same sanitizer and per-key merge (a strict subset of the fields the existing final report already sends — config/consent booleans, restart and next-action state; no probe verdict, no skill state), authenticated by the same `setup_pending`-scoped agent API key. An install-status report — early or final — remains incapable of activating an agent, granting, or changing budget authority: approval still requires the owner's dashboard action, and the budget-approval route verifies the signed delegation authority server-side regardless of any client-reported state (`routes/agent-connection-setups.ts`). Failure of the early report is silent and costs only the head start; the final report remains authoritative and overwrites the same keys. No credential handling, signing surface, route, or schema change; the no-secret output boundaries hold (the early snapshot carries booleans and enum-like strings, no paths or key material). Perimeter unchanged.
