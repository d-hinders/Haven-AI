- **#1544** — Re-run/collision behavior of the connect flow is characterized, not changed (epic #1541): new tests pin that a re-run of an already-consumed setup command fails before any credential write or runtime-config change — the backend refuses the consumed setup at resolve, and refuses at register in the concurrent-run race, both shapes pinned (any key pair minted for a refused attempt is discarded from memory, never persisted) — that a fresh setup on a configured machine writes the new agent's credentials into a sibling per-agent directory while the previous agent's files stay byte-identical, and that the managed `haven`/`haven-signer` MCP entries are replaced — never duplicated — while unrelated entries are preserved. A README section documents the same answers, including that a re-run does not revoke the previous agent (revocation stays a dashboard action; authority stays whatever the on-chain rules say). Tests and documentation only: no production code path, credential handling, route, signing surface, or authority changes — zero behavior change by construction. The characterization surfaced one opt-in-path footgun, filed as the follow-up #1569 rather than fixed here (Claude Code local-stdio mode does not replace a pre-existing `haven` entry on re-run, leaving the runtime wired to the previous agent's wrapper — a wiring/staleness issue, not a spend-authority one: the stale wrapper still holds only that agent's own credentials and its authority remains its own on-chain rules). Perimeter unchanged.
