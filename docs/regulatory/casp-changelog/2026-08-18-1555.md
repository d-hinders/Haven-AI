- **#1555** — Migration `061_outbound_txs` + its repository land the durable record for epic #1554 (the outbound-tx queue that will replace the "interim" in-process relayer serialisation). **Deliberately behaviour-neutral: no production code path reads or writes the table yet** — the schema review is isolated from any call-site change, which arrive as epic slices #1556–#1559. The table records the lifecycle of relayer broadcasts (queued → broadcast → mined | replaced | failed) with a lease-based claim (`claimed_at` under `FOR UPDATE SKIP LOCKED`) so a process crash between claim and broadcast leaves a recoverable row rather than a phantom state. **No authority, custody, or execution boundary moves.** The relayer stays gas-only; nothing changes about what may be spent, by whom, or under which caveats; no route, rail, or signing surface is touched. The repository is FAIL-CLOSED (these rows are the authority for "what did we broadcast", unlike the fail-open watermark optimisation), its seven queries are registered in the db-schema-smoke PREPARE list, and the claim's two load-bearing properties are proven against real Postgres: two claimants can never take the same row, and a claimant holding a lock cannot stall the lane (mutation-proven — removing `SKIP LOCKED` fails the stall test). Perimeter unchanged.
