- **#1545** — Copy and documentation only, closing out epic #1541's field-test feedback: (1) the dashboard-generated setup prompt gains one sentence telling an agent operator the connector's existing `--json` structured-output mode exists — the pasted command itself is unchanged, the flag is opt-in, and the sentence describes output format only (stdout record, stderr progress), adding no approved action to the prompt's consent enumeration; (2) the connect flow's approval gate is renamed to "approve the budget" in the setup prompt's final line and the dashboard's waiting/manual-path copy, converging on the term the connector's wait loop (#1542) and the delegation-rail approval screens already use — a wording change in orientation strings, with the rules-review screens' product vocabulary untouched; (3) the connect README documents why there is no pre-registration confirmation prompt (consent = the dashboard-minted one-time setup prompt; setups stay cancellable; a registered agent starts `pending_approval` with zero spend authority) and that `--json` runs skip the interactive approval wait. No route behavior, credential handling, signing surface, schema, or authority change anywhere in the diff — the touched backend file's only change is inside the `buildSetupPrompt` string builder. What may be spent, by whom, and under which caveats is untouched. Perimeter unchanged.
