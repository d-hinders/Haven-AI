- **#1547** — The guided catalog purchase preflight (`haven_prepare_catalog_purchase`, hosted MCP) now honours the #1450 settlement-scheme owner decision: it runs the same `selectX402SettlementScheme` (#1453) that `haven_pay_mcp_tool` runs, and on a delegation-rail account whose merchant advertises `assetTransferMethod: "erc7710"` it prepares the DIRECT-settlement child (`prepareX402Erc7710`, #1456) instead of the EIP-3009 funding intent it was hard-wired to. Risk-reducing by construction: on the preferred scheme the funding leg is ABSENT — no transient delegate-EOA hot balance, no sweep exposure, one on-chain hop treasury→merchant — which is the entire structural argument recorded for #1450. Spend authority is unchanged: the budget delegation's on-chain caveat enforcers remain the gate on either scheme, the cap is still enforced against the live quote before anything is prepared, and the delegation-rail over-budget refusal still fires before any intent exists. The SDK's `prepareX402Erc7710` gains an optional `mcpCallContext` passthrough (persisted by the already-existing backend path, `delegation-authorize.ts`) so the settle leg rehydrates the merchant call by `payment_id` on this scheme too — the agent relays ids and signatures, never merchant payloads. Same PR, per the acceptance criteria: the `x402-catalog-guided-purchase` QA leg's money proof is now scheme-aware — on erc7710 a reported `funding_tx_hash` is a FAILURE (the 3009 proof inverted), treasury debit must equal merchant credit, and the delegate EOA must be untouched; the 3009 proof stays for the fallback shape. Also in scope, advice surfaces only: catalog discovery's `suggested_tool` for MCP entries now names `haven_prepare_catalog_purchase` (the structured field contradicted the prose that prefers the guided path, and structured fields win by the server's own instructions), and the signer version pre-check guidance across quote/pay/resume descriptions and `signer_compatibility.check` now tells agents to branch on the signer's machine-readable version-mismatch refusal (#1309) instead of comparing against an MCP initialize result most agent harnesses cannot read — enforcement location unchanged (the signer's signing-time refusal was always the only hard gate). No route, credential-handling, or schema change. Perimeter unchanged.
