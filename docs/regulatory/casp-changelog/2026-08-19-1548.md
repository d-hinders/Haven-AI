- **#1548** — Guidance text only, on the hosted MCP surface: the required spending cap on paid MCP purchases gains a documented convention for the common case where the user stated no cap ("buy X"). The convention — stated in the hosted server instructions and the `haven_prepare_catalog_purchase` / `haven_quote_catalog_purchase` / `haven_pay_mcp_tool` descriptions, pinned by a server test — is: quote first, then set `max_amount_human` to the live quoted amount; never invent headroom; a price rise between quote and prepare then refuses safely at the existing cap check and the agent re-quotes and confirms with the user; a user-stated cap always wins. Zero behavior change: `readMaxAmountCap`, cap enforcement, request/response schemas, and every payment/settlement path are untouched — the cap remains required and remains enforced against the live quote before any funding intent or settlement child exists. Risk posture unchanged or mildly improved: previously each agent improvised its own cap when the user gave none (a live run produced ~6× the price from a private heuristic — silent headroom); the convention removes that improvised headroom and makes agent behavior uniform. No route, credential-handling, signing, or schema change. Perimeter unchanged.
