- **#1549** — Response-size compaction on the hosted MCP purchase tools, extending the #1272 compact contract: `haven_pay_mcp_tool` and `haven_prepare_catalog_purchase` no longer echo the raw merchant 402 `payment_required` in their default responses — since #1355 Haven persists it at authorize and the local signer fetches it (with the typed data and expected context) by `payment_id`, so the echo was the largest repeated block on every purchase, pure agent-context cost, and one more bulky artifact an agent could mangle in relay. `include_signing_payload=true` restores it verbatim (the same replay escape as `typed_data`, same idempotency-key semantics), which is what an older signer/backend or the step-by-step `haven_x402_sign_header` path uses; the QUOTE tools (`haven_quote_x402`, `haven_resume_x402_payment`) keep returning it unconditionally — there it is the product. Also shrinks the static `signer_compatibility.check` prose (~half), keeping the #1309 machine fields (`x402_expected_context_version`, `signer_capability`, `check`, `fallback`) and the standing recovery instructions intact. Deliberately KEPT: the Haven-signed `x402.expected` block verbatim including `auth` (it is the signed binding the verbatim-fallback contract relays; wire-contract tests pin it), `x402.accepted`, and the #1308/#1318 top-level and summary fields. No payment, settlement, signing, or authority change: what may be spent, the cap checks, and every wire request are untouched — only response fields the fast path never read are trimmed. The two QA legs that relayed the echo now sign by `payment_id` alone and FAIL if the echo reappears. Fixture-measured: 379B saved per purchase on the minimal single-option 402 (real multi-option 402s are larger), plus ~280B from the check shrink, per response. Perimeter unchanged.
