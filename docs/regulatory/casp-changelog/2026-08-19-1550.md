- **#1550** — The demo merchant (packages/demo-merchant-mcp) answers in English by default, with Swedish behind a per-call `locale: 'sv'` option, and gains a `result_detail: 'summary'` option that drops the rendered ASCII invoice + bookkeeping JSON from the tool TEXT while keeping the structured `summary` object and the `x-receipt-json` response header byte-identical. Presentation copy only — no payment, settlement, signing, or authority surface moves: the x402 challenge/settlement machinery (x402.ts, http.ts payment interception) is untouched apart from the 402 challenge's human-readable `description` suffix switching to English, and the invoice DOCUMENT (`InvoiceJson` — the Swedish bookkeeping artifact feeding `x-receipt-json` and the accounting fixtures) is deliberately NOT localized: keys, values, and the `beskrivning` row stay Swedish on every locale (pinned by test). The per-payment replay cache is keyed per (locale, result_detail) variant so a duplicate call replays byte-identical text without re-settling (one `submit`, pinned by test). No route, credential-handling, or schema change. Perimeter unchanged.
