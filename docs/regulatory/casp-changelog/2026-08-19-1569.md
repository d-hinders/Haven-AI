- **#1569** — The Claude Code local-stdio config writer (`configureClaudeCode`, opt-in `--local` topology) now removes the stale managed `haven`/`haven-signer` MCP entries before re-adding them — the exact remove-first sequence its hosted sibling always used, because `claude mcp add-json` fails on a name collision. Before this, a second setup on an already-configured workspace collided, reported `claude_code_config_failed`, and left the runtime silently wired to the PREVIOUS agent's local MCP wrapper (found by the #1544 characterization review). MCP wiring only, no authority surface moves: which agent a runtime talks to changes, but each agent's spend authority remains its own on-chain rules and its own locally held keys — the stale wrapper never held another agent's credentials, and the fix removes only the two Haven-managed entry names, touching no unrelated MCP servers. One new failure state is accepted deliberately: a genuine CLI error between the remove and the add now leaves NO `haven` entry rather than a stale wrong-agent one — fail-closed for wiring, with the existing `claude_code_config_failed` recovery (rerun setup) working cleanly from that state since the sequence is idempotent from any start. A stateful-fake regression test enforces the CLI's collision semantics and pins that a re-run re-points the entry at the new agent's wrapper; the README's known-exception paragraph is removed, making the #1544 "replaced, never duplicated" claim universally true. No route, credential-handling, signing, or schema change. Perimeter unchanged.
