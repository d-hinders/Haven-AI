- **#1587** — The connector's hosted+signer setup now PROVES the local signer with a real MCP stdio handshake (initialize → tools/list → required-tools check, the required list DERIVED from the pinned `@haven_ai/signer` package so it cannot drift) before reporting the signer configured. Previously the "signer ok" verdict was a credential-file stat plus consent flag — which is how the 2026-08-18 external Codex Desktop test got a setup that exited 0 while the signer was unstartable, failing only mid-payment. `localSignerConfigured` is now true only after the handshake; failure yields `local_signer_probe_<status>` (`process_error`/`timeout`/`missing_tools`/`bad_response`), a human message naming the failure, and the re-run command as recovery. The early dashboard snapshot (#1543) is unchanged — probe verdicts refine the final report, never gate the unlock. **No authority, custody, or execution boundary moves**: the probe is read-only (`tools/list`), signs nothing, and the signer key never leaves the machine — this narrows only whether a BROKEN signer setup can be reported healthy. Mutation-proven: ignoring the probe verdict fails all three failure-path tests. Perimeter unchanged.
