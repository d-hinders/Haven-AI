- **#1588** — Payment-tool guidance responses gain a runtime-neutral tool-resolution pair: alongside the existing `next_tool` (Claude-family namespaced `mcp__<server>__<tool>`, kept **byte-identical** — pinned by the existing characterization assertions), every guidance block now also carries `next_tool_server` (the logical server, `haven`/`haven-signer`) and `next_tool_name` (the bare tool name). Codex names MCP servers by config key (`haven_signer`, underscore per TOML), so the prefixed identifier matched nothing callable there — the 2026-08-18 external test's cross-runtime gap. The pair is DERIVED from `next_tool` at the single emission point (`buildAgentGuidance`), so the three fields cannot drift; description prose that spells the `mcp__…` form now carries a runtime-naming hedge, enforced by a registration-derived scanner test (any description containing `mcp__` without the hedge fails). **Strictly additive response fields + prose: no authority, custody, or execution boundary moves** — nothing changes in what can be signed, spent, or settled; only how a non-Claude runtime finds the same signer tool it was already authorized to call. The installed skill text and its byte-parity frontend copy updated in lockstep. Perimeter unchanged.
