- **#1589** — `@haven_ai/connect` gains `--doctor` (read-only diagnosis) and `--repair`. Doctor checks, without a setup token and without printing any secret (asserted by test): runtime config present and pointing at the prepared wrapper (flagging the retired npx shape), credential files parse, the pinned signer runtime matches the manifest (flagging stale/empty dirs), the hosted MCP answers an authorized `tools/list`, and the local signer completes a REAL stdio handshake — whose initialize payload now also surfaces the signer's advertised compat versions (probe extended to carry it; one handshake, not two). An un-acknowledged consent gate is reported as "consent missing" with the ack re-run as the repair, never as a broken signer. Every failing check names one concrete repair; exit is non-zero on any failure; `--json` emits the machine-readable report. Repair re-runs only what setup owns — reinstall the pinned runtime, rewrite wrapper + sidecar + runtime config from the STORED identity — and **never touches credentials, never mints identity, never needs a new token** (refusal tested). **No authority, custody, or execution boundary moves**: doctor's single spawn is the same read-only handshake setup uses; nothing signs, nothing spends. This is the tooling that turns the 2026-08-18 "hand-build an MCP client to debug setup" failure into one command. Perimeter unchanged.
