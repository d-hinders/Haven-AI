- **#1590** — `haven_get_agent` now names its readiness signal for what it actually covers: `spend_authority_readiness` (hosted identity + on-chain remaining spend authority), with `readiness` kept as a **deprecated same-value alias** — pinned byte-identical by the existing characterization tests, removal earliest after the next release train. The bare name invited a real misread: in the 2026-08-18 external test an agent took `readiness: "ready"` as end-to-end payment readiness while the local signer was unstartable — a state the hosted server structurally cannot see. Every agent-visible description of the signal (SDK tool description, installed skill + its byte-parity frontend copy, hosted server instructions, exported type JSDoc) now states the local-signer exclusion explicitly and points at the verification that DOES cover it (any signer tool call, or `connect --doctor`). **Strictly additive field + prose: no authority, custody, or execution boundary moves** — the derivation is unchanged, nothing new can be spent, and no rail or approval semantics change. Perimeter unchanged.
