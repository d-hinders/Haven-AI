- **#1591** — the hosted MCP server's tool-description prose is cut roughly in half (30,609 → 15,568 UTF-8 bytes, −49.1%) and the flow-generic guidance (sign by `payment_id` only, EIP-3009 vs erc7710 settle shapes, expiry → same-`idempotency_key` re-run, the version-mismatch refusal branch, stranded-funds recovery via `haven_sweep_delegate`) is consolidated ONCE into the server instructions instead of being repeated per tool — external-tester feedback (epic #1585) showed the repetition burying the per-tool deltas. **Prose only: no tool schema, capability, request/response shape, authority, custody, or execution boundary moves** — every refusal (one-cap rule, both-or-neither, over-budget, version-mismatch) is enforced in code and on-chain exactly as before; descriptions were never an enforcement surface. Internal issue archaeology (`#N` references) moved from agent-visible strings into code comments. A new test file guards the outcome three ways, each mutation-proven: a shrink-only size ratchet (≤60% of the pre-trim baseline), a ban on `#N` archaeology in any agent-visible description or instruction, and spot-pins that the shared guidance lives on the instructions and is not re-duplicated per tool. Perimeter unchanged.
