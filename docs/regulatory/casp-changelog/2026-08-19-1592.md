- **#1592** — `merchant_catalog.price_display` normalized to `<human amount> <asset code>` (`'$0.0015 USDC'` → `'0.0015 USDC'`): migration 062 strips the `$` prefix from rows matching the exact seeded `$… USDC` double-statement shape (idempotent; already-normalized, NULL, and genuinely-USD displays untouched — proven on real Postgres incl. an `updated_at`-unchanged check, and the scope guard is mutation-tested), and the two probe-side writers in `modules/catalog/merchant-catalog.ts` stop emitting the prefix so nothing recreates the old form. **Display-only data: no authority, custody, or execution boundary moves** — `price_display` is an indicative label; every payment is capped against the merchant's live quote and enforced on-chain exactly as before, and `price_atomic` (the only field any flow computes with) is untouched. Perimeter unchanged.
