- **#1593** — the local-stdio MCP runtime install (`packages/connect/src/local-mcp-runtime.ts`) gains the #1586 hardening its own comment already named it a twin of: the spurious 120s npm timeout is replaced by the shared honest budget (`SIGNER_INSTALL_TIMEOUT_MS`, 600s) and the install emits 15s `onProgress` heartbeats, threaded through the real `installRuntime` entry point (mutation-proven both ways: an unthreaded callback fails the integration test, a killed heartbeat fails both tests). **Setup tooling only: no authority, custody, or execution boundary moves** — nothing about what may be signed or spent changes; a legitimate cold-cache install simply stops failing spuriously and silently mid-setup. The local topology already fails closed structurally (no npx fallback exists on that path). Perimeter unchanged.
