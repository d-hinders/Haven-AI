- **#1605** — comment-only correction of four stale `until #N` code comments that claimed to wait for work that has since shipped (worst: `routes/hybrid-accounts.ts` still said "a rail value nothing routes to until #829" — #829's delegation payment rail has been live for weeks). Each comment now describes the CURRENT state with the issue reference kept as history. **Zero behavior change: not one byte of executable code, SQL, or schema moves** — the two migration files' `up()`/`down()` bodies are untouched (verified by diff: docstring lines only). No authority, custody, or execution boundary is described differently than it actually is — that misdescription is exactly what the change removes. Perimeter unchanged.
