- **Release 0.1.27-alpha.0** — Version strings only across the five published packages, their internal dep pins, the source version constants, and the Supported Runtime Manifest table. **No authority, custody, or execution boundary moves**: no route, tool schema, capability, signing path, caveat, or credential scope changes in this diff — verified rather than asserted (`git diff origin/dev` outside docs and the lockfile contains no line that is not a version string or a `@haven_ai/*` pin), every claim in `casp-risk-guardrails.md` reads identically before and after, and `publish.yml` derives the `alpha` dist-tag from the prerelease version so `latest` is untouched. Recorded, as with 0.1.26-alpha.0, for what the bump CARRIES: #1593, #1595, #1597 and #1598 changed `packages/connect/src/**`, `packages/mcp-server/src/**` and `packages/sdk/src/**` AFTER 0.1.26-alpha.0 was published, and `publish.yml` skips versions already on npm — so promoting without a bump leaves the same version string holding different code on npm and in-repo, and `npx @haven_ai/connect@alpha` keeps handing users a connector with no `--doctor`. The carried surface is setup tooling and reporting, not perimeter: `--doctor` reads local state and reports, and `--repair` rewrites runtime config **from credentials the user already holds** — it mints no authority, widens no scope, and refuses outright rather than clobbering a local-stdio topology it cannot correctly rewrite (#1589 review, HIGH). Verified the release carries the fixes rather than assuming: the built connect `dist/cli.cjs` contains `doctor` (25 occurrences) and `0.1.27-alpha.0` (3), and the SDK `dist/index.js`/`dist/index.cjs` each contain `spend_authority_readiness` (4) — the #1598 rename that names the signal for what it covers, an additive field with `readiness` retained as a same-value deprecated alias, so no consumer loses a field. Second release cut under the #1526 pin rules and they held again: `mcp-server`'s version moves in lockstep for `HOSTED_SERVER_VERSION` coherence while its `"*"` workspace pins are untouched — checked by `verifyPrivateConsumersUnpinned` inside the bump and by `lint:workspace-pins` (11 workspaces, both directions). The lockfile change is 11 version-string lines; the local npm's 37 spurious `dev`/`peer` metadata insertions were stripped by restoring the file and re-applying only the version substitution, then re-validated as JSON with `npm ci --dry-run` consistent — the same defect, and the same remedy, as 0.1.26-alpha.0. Perimeter unchanged.
