- **#1614** — SDK characterization and structural-boundary tests now pin the existing `HavenClient` public facade, representative request/payment behavior, private responsibility ownership, and local dependency fan-in. The change is test-only: no runtime module, public declaration, route, credential handling, signer, payment instruction, refusal, or on-chain authority changed. API authentication remains identity only, every executable transfer still requires the same external signature and on-chain controls, and Haven gains no custody or discretionary execution capability. Perimeter unchanged.
