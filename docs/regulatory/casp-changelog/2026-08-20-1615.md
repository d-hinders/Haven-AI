- **#1615** — `HavenClient` now composes internal Haven-API transport, payment-state, and payment-mapper modules; the public facade, wire URLs/methods/payloads, default and scoped headers, timeout/error/status semantics, receipt verification, and payment return shapes are characterization-tested unchanged. The relocation adds no signer, x402 lifecycle, merchant transport, sweep policy, route, credential, or on-chain-control behavior: API authentication remains identity-only, signatures and existing on-chain constraints remain the payment authority, and the delegate key stays local. Perimeter unchanged.
