- **#1617** — `HavenClient` delegates authenticated account/allowance/receipt reads and sweep orchestration to internal SDK modules without changing its public facade, wire contracts, or payment lifecycle. The local sweep path still uses only the caller-held delegate key and always transfers to the authenticated agent's Safe; no Haven service receives a key or gains signing authority. Hosted prepare/submit still carries an already-signed authorization through the API/relayer path, so API credentials remain identity-only and never sufficient to spend. Chain/token selection, exact signed authorization validation, on-chain policy, custody boundaries, and external user revocation remain unchanged. Perimeter unchanged.
