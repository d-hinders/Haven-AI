- **#1620** — closes epic #1613. The last two responsibility clusters left `HavenClient`: merchant delivery with its evidence trail (`merchant-completion.ts`) and the agent-tool adapter (`tool-adapter.ts`). **Behaviour-preserving: no authority, custody or execution boundary moves, no wire contract changes, and nothing new is grantable** — the facade keeps every exported method and public type, and the structural pin over its public contract is untouched, which is the measure of that. The merchant module's asymmetry moved verbatim and is worth restating: a merchant that REJECTS a retry after Haven already moved money is written down as a reconciliation event, a merchant that accepts is written down as evidence plus its own receipt when it offers one, and **neither write may ever change the caller-visible outcome** — the resource is already paid for, so bookkeeping that threw would turn a completed payment into a reported failure. Every one of those calls therefore still swallows. The module is scheme-neutral by construction, which is what lets the #1508 no-funding-leg path finish through the same door as the 3009 path. New cross-protocol contract tests pin what the decomposition must NOT change: every module shares ONE Haven transport by identity rather than by equal configuration, so a caller's `withRequestContext` headers and configured timeout reach every protocol alike; the EIP-3009 receipt cache is owned by exactly one module, since two protocols sharing a cache keyed by an authorization's expiry is the shape #1521 came from. Mutation-proven — give one module its own transport and its request loses the caller's header while the others keep it, a divergence no functional test would have noticed. `HavenClient` ends the epic at 1,500 lines from 3,597, with 4 private members from 31. Perimeter unchanged.
