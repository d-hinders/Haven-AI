- **Release 0.1.28-alpha.0** — Version strings only across the five published packages, their internal dep pins, the source version constants, and the Supported Runtime Manifest table. **No authority, custody, or execution boundary moves**: no route, tool schema, capability, signing path, caveat, or credential scope changes in this diff — verified rather than asserted (`git diff origin/dev` outside docs and the lockfile contains no line that is not a version string or a `@haven_ai/*` pin), every claim in `casp-risk-guardrails.md` reads identically before and after, and `publish.yml` derives the `alpha` dist-tag from the prerelease version so `latest` is untouched. Recorded, as with 0.1.27-alpha.0, for what the bump CARRIES, which this time is the largest change to the SDK's money path in a single release: the #1620 epic decomposed `packages/sdk/src/client.ts` from ~2500 lines to a compatibility facade, extracting the EIP-3009 funding-leg lifecycle (#1618), the erc7710 no-funding-leg lifecycle (#1619), the Haven API transport and payment mapping (#1634), the paid-MCP merchant transport (#1636), and account reads plus delegate sweeps (#1655), all landed behind the characterization tests written first for exactly this purpose (#1631, #1614). A refactor of the settlement lifecycle is precisely the change class where a silent surface move would matter, so compatibility was **measured against the published artifact rather than inferred from the epic's intent**: the built `dist/index.d.ts` was diffed against the `@haven_ai/sdk@0.1.27-alpha.0` tarball fetched from npm, and of 155 top-level declarations, **zero were added and zero removed**; the 227-line type diff contains no changed line that is not a `private` member or a comment. What moved is which object owns a code path, never what a consumer may call or what authority it carries — the two settlement schemes stay separated on the same boundary the perimeter analysis already draws between them (erc7710 has no funding leg to share, so the delegate EOA never holds the money on that path, and the 3009 bridge keeps its bounded, swept hot balance). Third release cut under the #1526 pin rules and they held again: `mcp-server`'s version moves in lockstep for `HOSTED_SERVER_VERSION` coherence while its `"*"` workspace pins are untouched — checked by `verifyPrivateConsumersUnpinned` inside the bump and by `lint:workspace-pins` (11 workspaces, both directions). The lockfile change is its 11 real version-string lines; the local npm again inserted spurious `dev`/`peer` metadata (38 lines this time), stripped by restoring the file and re-applying only the version substitution, then re-validated as JSON with `npm ci --dry-run` clean — the same defect, and the same remedy, as 0.1.26-alpha.0 and 0.1.27-alpha.0, now three releases running. Perimeter unchanged.
