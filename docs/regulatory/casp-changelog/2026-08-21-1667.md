- **#1667** — erc7710 authorize now deploys the settlement child's delegator — the delegate hybrid smart account — when it is still counterfactual, fixing a fresh agent's first-ever erc7710 payment reverting `InvalidEOASignature` at merchant redemption (the DelegationManager verifies the child's signature via EIP-1271 only against deployed code; ecrecover against the counterfactual account recovers the delegate EOA ≠ delegator). The deploy is the **same permissionless 4337 factory call** treasury activation already performs (#860, `ensureHybridDeployed`): it instantiates the account with the delegate EOA as its only owner baked into the deterministic address and grants the deployer nothing — Haven's relayer signs a plain transaction to the audited factory and gains no authority over the account or its funds. No custody moves: the account's signer stays the agent's delegate key, spend authority remains the signed budget delegation plus the child's on-chain caveats (exact amount, merchant pin, ≤600 s expiry, redeemer pin), and no policy check is bypassed — the deploy sits after the budget-delegation lookup and hourly cap, before any intent row, and fails closed (retryable 502; relayer gas budget breach → 429 via the #717 spend guard, so the shared sponsor cannot be drained through this new call site). Mutation coverage: `x402-delegation.test.ts` (#1667 block) pins the deploy call with agent attribution, the fail-closed 502 with no intent created, the 429 mapping, and that the EIP-3009 shape never triggers it (its funding UserOp carries initCode). Perimeter unchanged.
