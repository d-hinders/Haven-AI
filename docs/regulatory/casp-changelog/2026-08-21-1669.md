- **#1669** — the deliberate stranded-funds test fixture in the merchant catalog is now labelled as what it is, in every place an agent or a client could look. **No authority, custody or execution boundary moves and nothing new is grantable** — one catalog row's name, description and category change (migration 064), and the `GET /catalog` spec gains the honest sentence about what `active` means: verification exercises the 402 CHALLENGE only, so `active` says the merchant answers, never that it settles. The fixture itself is WANTED (2026-08-21 owner decision): it simulates the stranding failure mode and exercised the real recovery path — expiry → sweep → the #1269 below-minimum mapping — correctly when it fired live. What was missing was disclosure: the row read like any purchasable service, an agent paid it expecting delivery, and had to diagnose a silent empty-402 and a stranded balance. Now the name carries an unmissable marker, the description states plainly that the funding leg succeeds, the merchant never settles, and delivery must not be expected, and `category: 'test-fixture'` gives pre-filtering clients a structural signal independent of prose. The label survives both catalog writers — refresh updates price/status fields only (pinned by a test against the SQL itself, so a future name-write cannot silently revert the label), and discovery ingest is `ON CONFLICT DO NOTHING`. Perimeter unchanged.
