- **#1688** — a superseded agent credential directory whose API key still authenticates is now a FAILING `--doctor` check and a named heads-up in the connect completion output, instead of a cosmetic note. **No authority, custody or execution boundary moves and nothing new is grantable** — this is diagnostics over an existing state: a setup re-run mints a NEW agent and retires nothing (the connector never deletes directories, registration only collides on delegate address, and cancel deliberately refuses to auto-revoke an approved agent), so a host that started before the re-run keeps authenticating — and spending — as the agent the user believes they replaced, silently. The check probes each unselected directory with ITS OWN stored key: still-live ⇒ failure naming the agent id with the revoke-then-remove repair; already-revoked ⇒ informational pass; unreachable ⇒ a note that is never a false alarm and never a clean bill. **Connect never revokes and never deletes credentials itself — it reports, the user decides** — and the repair text says so explicitly. No secret material appears in either output form, asserted in tests. The condition detected is spend-relevant; the change itself moves no money and grants no authority — it makes an existing silent spend-capable state visible, which is the #1694 prerequisite this closes. Perimeter unchanged.
