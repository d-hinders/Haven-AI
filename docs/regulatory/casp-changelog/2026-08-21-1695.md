- **#1695** — `connect`'s runtime config writers are parametrized on an MCP server-name pair (`haven-<slug>` / `haven-signer-<slug>`, plus a per-pair Hermes env key), the foundation slice of epic #1694's multi-agent capability. **No custody or authority boundary moves.** This is config *wiring* only: which name a runtime uses to address an MCP server. No route, credential scope, key-material handling, signing path, or spend authority is added, widened, or moved — a named pair carries exactly the same hosted-URL+API-key / signer-wrapper content the bare pair always carried, and which agent may spend remains governed by the on-chain delegation and the #1690 signer-side payer guard, neither of which this diff touches. The unnamed path is byte-identical to before (pinned by characterization tests captured pre-change), so no currently-wired host's behaviour changes. Two boundary observations stated positively: (a) surgicality is the security property here — a writer now removes/updates only the entries it owns by name, which *removes* the #1569 clobbering class where a re-run silently re-pointed a runtime at a different agent's credentials (mutation-proven: widening the Claude Code remove-scope to the bare names fails the test); (b) on Hermes, each named pair gets its own dotenv key (`MCP_HAVEN_<SLUG>_API_KEY`) precisely so two agents can never share one identity variable and silently authenticate as whichever setup ran last. Secret hygiene unchanged: API keys land only in the owner-only config/env files they always landed in. No CLI flag is exposed yet (#1696 wires `--name`); this slice is unreachable from user input. Perimeter unchanged.
