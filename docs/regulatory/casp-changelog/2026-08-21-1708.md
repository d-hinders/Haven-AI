- **#1708** — Design-token plumbing only: `globals.css` gains an **additive** channel form (`--v2-<name>-rgb: R G B`) beside each existing `#RRGGBB` declaration, `tailwind.config.js` reads those channels through Tailwind's `<alpha-value>` placeholder, and 68 `ring-[var(--v2-*)]/N` classes across 39 files become theme aliases (`ring-brand/30`). The classes being replaced **compiled to nothing** — a bare `var()` colour has no channels for Tailwind to re-compose, so the opacity variant was silently dropped and `--tw-ring-color` kept its preflight default, rendering every focus ring blue-500/50 instead of brand indigo. **No custody or authority boundary moves, and no behaviour of any kind changes beyond pixels.** Nothing here touches payment execution, agent authority, spend limits, signing, relaying, x402/MPP flows, credential handling or any route: the two files the CASP `covers:` mapping implicates (`ApprovalQueue.tsx`, and the same file under the x402 sequence doc) each change exactly one focus-ring utility class inside an existing `className` string — no logic, no props, no copy, no conditional, no data. The approval queue's user-facing meaning is untouched: the same approvals render, the same actions are offered, the same refusals apply; the keyboard-focus indicator on those controls simply becomes visible in the brand colour rather than an unintended blue, which if anything *improves* the human circuit-breaker surface by making the focused control unambiguous. Solid usages (`bg-brand`, `text-brand`, `border-border`) are pinned to the same rendered colour by a compiled-CSS assertion, the two token forms are pinned to each other by a hex↔channel sync test, and the dead-class shape is pinned by a regression test that reproduces the old bare-`var()` theme and asserts it emits no `--tw-ring-color` at all. All three guards, plus the call-site guard, are mutation-proven. Perimeter unchanged.
