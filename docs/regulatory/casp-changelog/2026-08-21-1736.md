- **#1736** — GATE COVERAGE, not perimeter: this document's `covers:` list is widened so that every path on `ship-next`'s money-path file list is gated by a contract doc, and a body section (`What covers: must span`) names that list as the maintenance reference so the two cannot drift apart again silently. **No code changed, no route, module, authority or custody boundary moves, and nothing new is grantable** — what changes is which future PRs are ASKED the perimeter question. The defect was the most deceptive kind: `rails/allowance-module.ts` was listed while the entire delegation rail built beside it was not, so the counterfactual account deploy (`rails/hybrid-provisioning.ts` — the code that provisions the account holding user funds and decides when an outbound-tx record closes `failed`) tripped the strict gate never. The audit behind the change is the part worth keeping: against the money-path list, **23 files were outside this doc's `covers:` and 13 were gated by NO contract doc at all** — eleven `rails/` files (all five `delegation-*`, `execution-rail`, `hybrid-provisioning`, `sweep`, `allowance-nonce-coordinator`), `routes/approvals.ts` and `routes/safe-exec.ts` (the approval queue and user-signed execution), plus the relayer/outbound machinery (`infra/relayer-spend-guard.ts`, `relayer-balance-monitor.ts`, `outbound-queue.ts`, `outbound-bump-worker.ts`) that literally broadcasts and fee-replaces real transactions. The remaining ten were gated only by `delegation-rail-security-model.md` or `04-x402-payment-sequence.md` — a different obligation from a perimeter analysis. The widening is by directory glob where a whole layer is the boundary (`rails/**`, `infra/relayer*.ts`, `infra/outbound-*.ts`) precisely so a new rail file is covered the day it is written rather than the day someone notices. File granularity is kept deliberately: `hybrid-provisioning.ts` also hosts the pure `computeHybridAccountAddress`, but that derives the address that will hold user funds, so read-only is not perimeter-irrelevant, and the gate's remedy is a one-paragraph shard cheap enough that over-inclusion costs little where under-inclusion costs the guarantee. `satisfied-by: docs/regulatory/casp-changelog/**` was confirmed unchanged, so every newly-covered path resolves to "write a shard", never "edit this parent doc" (the #1496 collision lesson). Mutation-proven in the only way a coverage fix can be: with `BASE_SHA`/`HEAD_SHA` set over a diff touching `rails/hybrid-provisioning.ts`, `coupling-gate.mjs --strict` exits **0 on `origin/dev`** (the hole) and exits **1 naming this doc** with the covers change applied — the gate's silence and its demand observed in the same command, not asserted. Deliberately excluded, with the reasoning recorded in the doc body rather than left as a gap: `scripts/release-bump.mjs` and `.github/workflows/publish.yml`, which ship spend-authority code but move no funds and shift no authority boundary (#1739 holds that decision). Perimeter unchanged; what changed is that the perimeter now gets asked.
