- **#1709** — Converted the 91 `border-[var(--v2-*)]/N` call-sites in
  `packages/frontend/src` to the channel-backed theme aliases (`border-danger/20`
  etc.) that #1708 introduced, restoring tinted borders that had been compiling
  to nothing on Tailwind v3.4. Three of the touched files are covered by
  contract docs — `ApprovalQueue.tsx` (x402 sequence, CASP guardrails),
  `AccountSignersCard.tsx` (delegation-rail security model) and
  `reporting/page.tsx` (CASP guardrails) — which is why this shard exists; the
  change in each is a **class-string rename only**, verified by reading the
  diff for those files rather than assuming it from the sweep's shape. No new
  authority, custody, route, perimeter, or key-handling surface: no logic, prop,
  handler, request, or copy changes anywhere in the diff, and no backend file is
  touched. The approval queue's reject/approve controls and the account signer
  management flow behave identically; what changes is that a border which
  previously rendered no colour at all now renders the soft tint its class
  always claimed. Mutation-proven: reverting a converted border to the dead
  bare-`var()` form, reverting the bracketed-alpha form, and breaking a channel
  token in `tailwind.config.js` each turn a named guard red, and all restore
  green. Perimeter unchanged.
