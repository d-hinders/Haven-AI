- **#1739** — Decides the question [#1736](https://github.com/d-hinders/Haven-AI/issues/1736) deferred, and reverses its answer: `scripts/release-bump.mjs` and `.github/workflows/publish.yml` join this doc's `covers:`. No executable behaviour changes — the diff is front-matter, a doc section, a test comment and one missing path in a test fixture; no route, credential scope, key material, signing path, version field or dep pin moves, and every custody and authority claim in the parent doc reads identically before and after. The deferral was overturned because it analysed the wrong event: `covers:` gates *edits to a file*, never *runs of it*, so covering the bump script puts no shard on a release cut and the cost the issue weighed ("every release PR then writes a shard") never existed — release PRs already write shards, forced by the version constants a bump rewrites under `packages/mcp/src/`, `packages/signer/src/`, `packages/connect/src/` and `packages/mcp-server/src/`, per the release-shard convention in `casp-changelog/README.md` (#1789). The event that *was* ungated is a change to the machinery deciding which artifact reaches a user's machine — the five published versions, the cross-package pins, connect's pinned `sdkVersion`/`signerVersion` (which signer a newly connected agent installs) and the release-time wildcard-pin re-check the script holds so a release cannot depend on a lint having been run. Measured, not asserted: before this change `node scripts/docs/coupling-gate.mjs --strict --changed=scripts/release-bump.mjs` exited 0 with three advisory findings and no contract doc, across nine commits in ninety days; after it, the run exits 1 naming this doc. `publish.yml` is covered on the same footing — its two existing contract docs describe the branch model and the runtime pins, which is a different obligation from a perimeter analysis rather than a substitute for one (#1736's own rule, applied to #1736's own deferred case, as is the rule that a voluntarily-written shard — #1790's `2026-08-22-1790.md`, on this very file — is evidence of a missing gate). The [#1503](casp-changelog/2026-08-17-1503.md) precedent is **upheld, not overturned**: publishing already-reviewed code to npm remains a distribution event and not a second authority change, which is why a release shard may stay thin; it answered what a release owes, never what changing the releaser owes. Recorded as a standing constraint because #1790 established that a bump may write a contract doc: **a CASP shard is never generated** — the gate satisfies a doc on file presence alone, so a script-written shard would excuse it with content no human read, which is precisely the circularity #1790's review caught. Nothing generates a perimeter argument and nothing may start. Perimeter unchanged; the set of changes *asked* the perimeter question widens.
