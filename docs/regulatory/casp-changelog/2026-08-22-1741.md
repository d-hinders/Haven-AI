- **#1741 / #1746** — Focus-indicator styling only, and the same two files the CASP `covers:` mapping implicates as in #1708 (`ApprovalQueue.tsx`, and the same file under the x402 sequence doc), where the change is once again confined to focus-ring utility classes inside an existing `className` string. Two things change across the frontend: the single focus-ring opacity moves `/30` (and the form family's `/20`) to `/80`, which is the lowest 10%-step alpha at which every ring/background pair in the product clears WCAG's 3:1 non-text-contrast bar — `ring-brand/30` measured **1.60:1** on white and the form family's `/20` measured **1.36:1**; and the two families collapse into one, so every focus ring is `focus-visible:` + `ring-2` + `/80`, with offsets standardised at `-2` where an offset is used at all. Three latent defects are fixed in passing, all of them *invisible* rings rather than merely faint ones: `Toast.tsx`'s `ring-current/30` compiled to no `--tw-ring-color` whatsoever (the #1708 failure mode in a second shape — `currentColor` has no channels for Tailwind to re-compose either, so that ring rendered preflight blue-500/50 on all three tones); the shell's skip-link pill carried a brand ring on a `--v2-ink` fill, which tops out at 2.58:1 at *full* opacity and is now `ring-white/80` at 10.76:1; and two brand-**filled** buttons (`WalletButton`, `ErrorBoundary`) carried un-offset brand rings that composited brand-over-brand at ~1.0:1, now offset against the page exactly as `ui/Button` already was. **No custody or authority boundary moves, and no behaviour changes beyond pixels and one focus selector.** Nothing here touches payment execution, agent authority, spend limits, signing, relaying, x402/MPP flows, credential handling, routes, props, copy, conditionals or data. The approval queue's user-facing meaning is untouched: the same approvals render, the same actions are offered, the same refusals apply. The one behavioural delta anywhere is that a *clicked* `<select>` no longer retains a ring after its dropdown closes (text inputs are unaffected — the Selectors-4 heuristic makes a focused text field match `:focus-visible` however it was focused); this is a deliberate consistency decision recorded at the call-site. If anything the human circuit-breaker surface improves again: the focused control on the approval queue now has a focus indicator that provably clears the accessibility bar rather than one at 1.60:1. Every guard is mutation-proven, and the contrast assertions are re-derived from `globals.css` against the **compiled** CSS rather than asserted from token values. Perimeter unchanged.
