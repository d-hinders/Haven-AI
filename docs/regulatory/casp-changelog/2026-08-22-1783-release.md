- **Release 0.1.29-alpha.0** — Version strings only across the five published packages, their internal dep pins, the source version constants, and the Supported Runtime Manifest table. **No authority, custody, or execution boundary moves**: no route, tool schema, capability, signing path, caveat, or credential scope changes in this diff — verified rather than asserted (`git diff origin/dev` outside docs and the lockfile contains no line that is not a version string or a `@haven_ai/*` pin; the lockfile's own diff is 11 version lines and nothing else, the #1663 structural rewrite holding for a fourth consecutive cut with no hand-repair), every claim in `casp-risk-guardrails.md` reads identically before and after, and `publish.yml` derives the `alpha` dist-tag from the prerelease version so `latest` is untouched. Recorded, as with prior cuts, for what the bump CARRIES — and this one is unusual in that the gap between npm and the repo was not merely stale but **actively user-visible, and was measured rather than inferred**: `0.1.28-alpha.0` was published 2026-08-21 07:05Z, and #1672 merged to `dev` at 09:50Z, 2h45m later. Unpacking the published tarball shows it contains none of #1672's markers (`runtime-force`, `overrodeHint`, `detected; ignoring` — zero matches each, against 4/3/1 in the local build) and none of #1719's, so `npx @haven_ai/connect@alpha` currently resolves to a connector predating detection-first runtime resolution entirely. Production is unaffected today because `main` does not carry #1672 either (checked on unshallowed history: `dev` is 61 commits ahead, `main` zero ahead), so the deployed dashboard still emits `--runtime` and stays consistent with that connector — the mismatch is confined to the dev environment, and this release is what makes the two consistent again going forward. The connect work reaching users here is a **setup-time** surface throughout: which MCP config file the connector writes, and how it refuses when it cannot tell. That distinction is the whole authority argument — #1719's new rungs decide a config *location*, never what may be signed or spent, and its security property runs the protective direction: a wrong resolution would write an API key and a delegate key into a client the user does not use, so the connector refuses rather than guesses (an unrecognised runtime name with nothing detected refuses outright; the installed-client scan populates choices and never auto-selects; every new refusal precedes `resolveSetup`, key generation and any credential write, pinned by mutation-proof tests). The delegate key is still generated locally and never leaves the machine, Haven still receives only the public address plus a proof signature, and budgets and on-chain caveats are untouched. One carried change does sit nearer the money path and is called out rather than folded into the summary: **#1690** widens the x402 expected-context to version 3 (payer identity), which ADDS a signer-side refusal — the signer declines another agent's quote naming both identities — and adds optional passthrough fields; a refusal added is a narrowing of authority, not a widening, and the backend still emits v2 until an operator flips `X402_EMIT_PAYER_CONTEXT` per environment. **#1756** bounds the SDK delegate sweep's confirmation waits and reports an expiry instead of hanging the caller: a liveness fix on an existing path, moving no funds that were not already being swept. Perimeter unchanged.
