- **#1789 / #1795** — Release-path documentation corrections, plus one operator-facing string in `scripts/release-bump.mjs`. The shard exists because the diff touches a file on the money-path list (`scripts/release-bump.mjs`), not because anything about spend authority moved: the change to that file is a `log()` line and its comment, rewriting the CASP shard filename the sign-off tells a release-cutter to use (`<date>-<pr>-release.md` → the actual release version, #1789) — no version, cross-package pin, dist-tag, build step, publish trigger or verification changed, and every existing guard in `scripts/release-bump.test.mjs` still passes unmodified. The remaining files are prose: `scripts/README.md`'s published-package count (stale at "four" since `@haven_ai/cli` became published; the set is now derived from each workspace's `private` flag and guarded, rather than restated a fourth time), `docs/regulatory/casp-changelog/README.md`'s release-shard naming convention, and `.agents/skills/release/SKILL.md`'s copy of it. Nothing here changes what a shard must CONTAIN — the perimeter argument, the authority/custody analysis and the closing verdict are unchanged obligations; only the filename becomes knowable before the pull request exists, which removes a circular step that previously produced a mislabelled compliance record (`2026-08-22-1783-release.md`, written on a wrong guess). No new authority, custody, route, signing surface or credential path; Haven still holds no keys and no API credential is made sufficient to spend. Mutation-proven rather than asserted: reverting the sign-off line fails the new #1789 guard, reverting either count fails its #1795 guard, and flipping a workspace's `private` flag fails the derived-set guards while the prose stands still. Perimeter unchanged.
