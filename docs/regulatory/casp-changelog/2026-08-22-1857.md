- **#1857** — Ten raw unicode arrows in gated frontend surfaces become lucide icons. The only file on this doc's `covers:` list that the diff touches is `packages/frontend/src/components/AddFundsModal.tsx`, and the change there is two `<Button>` call sites: the literal `→` is removed from the button's children and the primitive's **existing** `trailingIcon` prop is passed instead, so `ui/Button` renders lucide `ArrowRight` at `w-3.5 h-3.5` in the slot it already had. **No authority, custody or execution boundary moves, and nothing new is grantable.** No route, request, delegation, allowance, caveat, approval, policy evaluation, signing path or credential read appears anywhere in the diff. The two buttons' `onClick` handlers (`handleBuyWithCard`, `handleReceiveInstead`) are untouched — same functions, same arguments, same call sites; only what is painted *inside* the button changes.

  **Not money-path, and stated as a decision rather than an omission.** The `ship-next` money-path file list is not hit by any file in this diff, and the diff moves no money and instructs nobody about moving any: the two labels are `Buy with card` and `Show receive address`, and both keep those exact words. This differs from #1844 and #1852, which were recorded as *money-path by consequence* because they changed what a dialog **told a human about where to send funds**. Nothing here changes what any surface tells a human. The funding/custody surfaces in the wider diff (`AddFundsModal`, and `app/(authenticated)/custody/page.tsx`, which is not on this doc's `covers:`) are named only because their **rendered baselines** move, not because their behaviour does.

  **Strictly presentational, and the scope of "presentational" is worth pinning.** Three shapes of edit, and each is closed:

  1. **Glyph → primitive.** A raw `↗`/`→`/`&rarr;` text node is deleted and an `<Icon icon={ExternalLink|ArrowRight}>` is rendered in its place. `Icon` is the shared wrapper — decorative and `aria-hidden` by default — and the glyphs it replaces were either bare text nodes or already `aria-hidden`. So the **accessible name of every affected control is unchanged**: the arrow contributed nothing to it before (it was decoration) and contributes nothing now. No link target, `href`, `target`, `rel`, `onClick` or route changes anywhere in the diff.
  2. **Layout class.** Five anchors/links/spans gain `inline-flex items-center gap-1` so the icon sits on the text's centre line instead of wherever the label's typeface put the glyph. This changes box layout only; no colour token, size token, spacing scale value or z-index is introduced.
  3. **Copy.** Two strings on `/design-system` lose an arrow *inside prose* — `mt-2 title→body` becomes `mt-2 title to body`, and a caption that advertised `Address`'s `↗` as the pattern now names the lucide icon. `/design-system` is a developer-facing showcase route; it moves no funds and describes no user obligation.

  **The one shared primitive, and why its blast radius is presentational too.** `components/haven/Address.tsx` is the canonical on-chain address display, so this diff moves rendered output wherever it is rendered *with* an `href` — and that set was **enumerated rather than assumed**, because "shared primitive" invites an overstatement. In production it is exactly one surface: `components/ApprovalQueue.tsx:250` and `:257`, the "Agent spending wallet" / "Merchant" lines in the approval-detail expansion, which is genuinely money-adjacent. The `/design-system` showcase is the only other site. Every other `<Address>` call site (`contacts/page.tsx:219,315`, `ReceiveFundsModal.tsx:170`) omits `href`, so the anchor branch never renders there and they are untouched. In particular the transaction surfaces do **not** go through this branch: `TransactionActivityRow`, `TransactionDetailPanel` and `TransactionsTable` render their own explorer-link components, which this diff does not touch. What changes on the affected surface is the **trailing external-link affordance only**: the address `value`, the truncation rule, the tooltip's full-address disclosure, the copy affordance and the `href` are all untouched, so nothing about *which* address a user reads, or *what* they can copy, moves. The change makes `Address` agree with `TransactionActivityRow` and `SendModal`, which already rendered this exact affordance with lucide `ExternalLink`; the primitive was the outlier, not the new case.

  **Verified mechanically, from a clean shell.** `docs/product/design-system.md` § 5 ships a runnable `perl` census of unicode arrows over gated surfaces. Extracted byte-for-byte from the Markdown and run under `env -i PATH=/usr/bin:/bin:/usr/local/bin bash`: **15 lines before the diff, 5 after** — the 5 being the one allowlisted site (`TransactionMovement.tsx:55`) plus four JSX-comment continuation lines a line-based search cannot distinguish from markup. The clean-shell requirement is not ceremony: an earlier draft of that check invoked `rg`, which on this machine is a shell function, so it printed `command not found` and its empty output read as a clean codebase.
