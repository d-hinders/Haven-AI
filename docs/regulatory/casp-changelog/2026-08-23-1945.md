- **#1945** — A presentation-layer change only: the four `--v2-shadow-*` elevation tokens paint again. `shadow-[var(--v2-shadow-card)]` and its siblings compiled to a shadow **colour** (`--tw-shadow-color: var(…); --tw-shadow: var(--tw-shadow-colored)`) against a variable nothing sets, so the computed `box-shadow` was `none` and every `Card`, every `ui/Modal` panel, every popover, tooltip and toast rendered flat. 67 call sites across 43 files now use the Tailwind theme utility (`shadow-card`, `shadow-card-raised`, `shadow-button`, `shadow-modal`, `shadow-popover`), `tailwind.config.js` gains the two `boxShadow` entries that were missing, and `src/__tests__/shadow-token.test.ts` compiles the stylesheet to keep the dead spelling out. **No authority, custody or execution boundary moves; nothing new is grantable, revocable or spendable.** Not money-path on either half of the union: the issue carries `code-quality` / `area:frontend` only, and the diff touches no path in `.github/money-path-globs.json` — no route, schema, migration, tool schema or signing scheme is involved, and no backend or SDK file is touched at all.

  This shard exists because `casp-risk-guardrails.md` declares `covers:` over the product surfaces this diff edits, not because a guardrail moved. It is worth being precise about that, because a diff spanning 43 frontend files reads as broad and is in fact extremely narrow: **every one of the 67 edits is a single class token inside a `className` string.** No component changes its props, its conditionals, its data flow, its copy, or what it renders — only how one CSS declaration is spelled. Nothing in the diff reads, writes, transmits or displays a value; the set of information reachable in the UI is byte-for-byte what it was.

  **What a user actually sees change, stated plainly rather than minimised.** Elevation returns app-wide: card and panel edges regain a 1–2px contact shadow and a soft ambient spread, modal panels regain the 12px lift that separates them from the backdrop, and popovers/tooltips/toasts regain the shadow that distinguishes a floating layer from the page. Dimensions do not change — a `box-shadow` paints outside the border box and does not participate in layout — so nothing reflows, no control moves, and no disclosure, warning or confirmation is repositioned, resized, obscured or made harder to reach. The approval and budget surfaces that carry the risk-disclosure copy (`ApprovalRequiredBanner`, `ApprovalNotifications`, `EditAgentModal`, `AddFundsModal`, `SendModal`) are all in the touched set and all change only by gaining the elevation the design system already specified for them; if anything, a dialog that is visually separated from the page behind it is easier to read as a distinct decision point, which is the direction this section cares about.

  **The regulatory-relevant residual, named rather than assumed away.** The committed visual baselines were recorded from the broken CSS, so they currently assert the flat render as correct. They are regenerated here, Linux-rendered through the *Update visual baselines* workflow, and the resulting red on `Design visual regression` is the fix working rather than a defect. That means the usual guarantee — "the visual gate would have caught an unintended change to a disclosure surface" — is temporarily not available on this one PR, because the gate's reference is what is being replaced. It is replaced by an explicit **prediction, made before regenerating**: the only differences should be a continuous colour difference at card, panel and popover edges, at unchanged element dimensions, with no text reflow and no change to any rendered string. Any diff not explained by that prediction is investigated rather than blessed, and the regenerated images are read against it. Both independent review passes (`haven-reviewer` and `haven-design-reviewer`) run over rendered evidence, and the design pass is the one that matters here.

  **Mutation-proven, so the new guard is evidence rather than decoration.** Reintroducing `shadow-[var(--v2-shadow-card-raised)]` at one call site turns exactly two named assertions red; deleting a `boxShadow` theme entry turns three red; and breaking the positive control itself (`.v2-modal-backdrop`) turns the control red and nothing else — which is what makes a "paints nothing" verdict from this instrument a measurement rather than a broken reader.
