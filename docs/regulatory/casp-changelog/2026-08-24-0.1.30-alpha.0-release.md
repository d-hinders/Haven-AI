- **Release 0.1.30-alpha.0** — The release diff itself changes only version strings across the five published packages and private lockstep `mcp-server`, exact internal pins for published consumers, mirrored source constants, the deterministic lockfile version lines, and the generated Supported Runtime Manifest rows. It carries the connector work merged after 0.1.29-alpha.0: **#1700** adds the client half of `--rekey` / `--rekey-finish`, with the replacement key generated locally, stored mode `0600`, never returned in CLI or JSON output and never sent to Haven; the existing owner-authenticated dashboard flow remains the only authority that can replace the agent key. **#1702** ships documentation that distinguishes agent key rotation from account recovery; it changes guidance only and grants Haven no key access or recovery authority. **#1878** reports the resolved MCP server name as a nullable, non-secret display label that is never used for identity or authorization. **#1910 / #1911 / #1915** make repair target the correct named MCP pair and make abandoned parked-key state observable without returning private material, deleting files, issuing a new network mutation, or changing what is grantable, revocable or spendable. Those carried changes were independently perimeter-reviewed in their issue shards; this cut adds no route, signing path, caveat, allowance, credential scope, custody surface or discretionary execution, and `publish.yml` derives the prerelease `alpha` tag so `latest` remains untouched. Perimeter unchanged.
