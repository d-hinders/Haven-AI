# 2026-08-25 — #1905: money-path perimeter widened to the edge signer package and the core machine-payment domain (with #1896/#1903)

Pure classification/coverage change to the money-path guardrail — **no payment-execution, authority, or custody behavior changes**. The substantive rules of this doc are untouched; only the perimeter it asks about grew.

Delta surfaces on the CASP perimeter:

1. `packages/signer/**` joined the runtime money-path list (`globs`, and `covers:` here). Rationale is the list's own: "signing schemes are spend authority". The SDK entry point was already listed; the edge-signer package that actually holds the delegate key material was on no list, and is the stronger case (#1896). A change to the signer package now gets the money-path label, `money.md`, and the CASP perimeter question.
2. `packages/core/src/machine-payment-lifecycle.ts` joined `globs` and `covers:`: the machine-payment domain moved verbatim to `@haven_ai/core` by #987, so the backend path the list guarded is a 12-line re-export shim — the real code now lives here (#1905).
3. `packages/frontend/src/lib/signer.ts` and `packages/frontend/src/hooks/useAgentRekey.ts` joined the money-path **control** surface (`controlGlobs`, labelled + human-reviewed, no QA-freshness re-run): the client that picks which signer signs a spend-authority action was invisible to the list (#1903). They are decision surfaces, not money-moving code, and are not added to this doc's `covers:` for that reason — the deployed-backend money-flow harness cannot exercise them.

The CASP perimeter question ("does this change payment execution / agent authority / custody?") now reaches signer-package and core-machine-payment diffs that previously slipped past the label. Non-custody rules (Red Lines) unchanged and re-verified unaffected: this PR touches no enforcement code.
