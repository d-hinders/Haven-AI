# 2026-08-25 — #2020: retire the agent_allowances surface (epic #1440)

Every reader and writer of `agent_allowances` is removed (#2020): `GET /agents`
no longer touches the table on any path (proven by a real-DB test that DROPs
it and exercises every surviving repository read), the PUT/DELETE per-token
allowance routes become 410 tombstones, `POST /agents` refuses a non-empty
`allowances` array instead of writing mirror rows, the connection-setup copy —
the table's last writer — is gone, and `GET /machine-payments/allowances`
answers the fail-closed 410 on the retired AllowanceModule rail (reversing
#1986's left-readable decision on the recorded owner call of 2026-08-25: the
accounts are emptied and unsupported, per the #2021 readability waiver) while
serving the delegation rail unchanged from the derived `agent_delegations`
view. Authority and custody narrow, never widen: the rows removed were a
DISPLAY mirror — the AllowanceModule grant on-chain was always the authority,
that rail's spend paths have answered 410 since #1986, and the delegation
rail's budgets remain owner-signed delegations enforced by on-chain caveat
enforcers, untouched here. No signing scheme, key custody, funding, sweep, or
settlement surface changes; nothing new is writable. The change deletes
spend-authority *configuration display* for a retired rail and closes its last
read, so the CASP perimeter shrinks. Perimeter unchanged in kind, reduced in
surface.
