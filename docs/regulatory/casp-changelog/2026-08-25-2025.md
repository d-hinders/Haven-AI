- **#2025** — the owner-scoped delegation lifecycle now refuses a revoked agent before it builds or activates a fresh budget delegation (HTTP 409), while deliberately preserving `pending_approval` first-grant activation and owner-facing reads, signer recovery, and per-hash/batch revocation. The build insert is conditioned on the current non-revoked lifecycle row and activation locks/re-checks that row inside its transaction, so a revoke cannot race a new pending or active row into existence; route tests cover the early refusal and active/pending positive controls. This is an off-chain fail-closed eligibility check only: owner signatures and the on-chain delegation caveats remain the actual authority, Haven gains no key or signing role, and users retain independent on-chain revocation. Perimeter narrowed.
