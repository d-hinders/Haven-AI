- **#2044** — **Three unfalsifiable spies removed from one of CASP Red Line #4's two executable proofs, and the three that remain are mutation-proven able to fail.** Test-only; no runtime code, no route, no schema, no authority and no custody surface is touched, and nothing here moves a wei. It is recorded as a money-path change anyway because the file it edits is a *regulatory proof* of the non-custody perimeter, and the failure being fixed is the one that makes such a proof worthless: `packages/backend/src/routes/__tests__/non-custody-onchain-gate.contract.test.ts` built six spies over `rails/allowance-module.js` and asserted they were never called, but #1987 had deleted three of those names — `generateTransferHash`, `recoverSigner`, `executeAllowanceTransfer` — along with the AllowanceModule rail's execution half. Because `vi.mock(..., () => allowanceMocks)` replaces the module wholesale, the factory simply invented three functions production does not have, and the suite then asserted nobody called them. **No edit to production code could turn those assertions red**: you cannot re-add a call to a function that does not exist. An empty-set guard, in a suite an auditor is invited to read as coverage of a red line. Disclosed by #2004 rather than introduced by it, and deliberately filed rather than widened into that diff.

  **What was removed, and what carries the capability instead.** The three dead names are gone from the mock factory, `generateTransferHash` is gone from `expectNoOffChainCoverageArithmetic()`, and both `expect(...executeAllowanceTransfer).not.toHaveBeenCalled()` assertions are gone. **No coverage is lost.** The claim those assertions gestured at — that the retired rail cannot execute an AllowanceModule transfer — is carried by the structural import-binding assertion added by #1987, which still names all three symbols in its banned list, is falsifiable by a single edit, and carries a positive control proving the extractor can say yes before a "no" counts. That was verified, not asserted: a **rail-resurrection mutation** that re-added an `executeAllowanceTransfer` export to `rails/allowance-module.ts` *and* imported it into `routes/payments.ts` turned exactly **one** assertion red — the structural one, by name — while the other five cases stayed green. A stale `vi.mock` of `infra/repositories/allowance-nonce-watermarks.js`, a module #1987 also deleted, was removed from the same file for the same reason: it stubbed nothing and read as coverage of a path that no longer exists. Six sibling suites still carry the same stale stub; they are filed as #2048 rather than widened into a regulatory-proof PR.

  **The remaining three spies were measured rather than reasoned about, and the measurement corrected #2004's own inventory.** That inventory recorded the spies as unable to influence the code under test, because `routes/payments.ts` imports nothing from `rails/allowance-module.js` — which is true and was re-verified here. But the module is on the route's **transitive** graph (`routes/payments.ts` → `modules/mpp/index.ts` → `modules/mpp/allowances.ts`), so the mock is live and a re-added call on the payment path does trip the spy. Proven per name, each mutation confirmed to have LANDED by diff before its colour was read, and each restored from a task-named backup verified by content hash rather than by memory: a `computeEffectiveAllowance` call added after the retirement gate reddens all three delegation cases at that named line; a `getTokenAllowance` call added **before** the gate reddens the two `#1986 RETIREMENT` cases as well, which is what makes their claim ("refused before ANY on-chain allowance read") a genuine ordering assertion rather than a restatement of the 410; and `getLatestBlockTimeSec` — the block-time read that fed the legacy arithmetic, promoted into the helper to replace the dead `generateTransferHash` slot — reddens all six by the same shape of edit. The first of those mutations also demonstrated a shadowing effect worth recording: with both `computeEffectiveAllowance` and `getTokenAllowance` called, only the first named line reports, so each spy needed its own mutation to be proven **by name**.

  **Positive control.** A mutation the suite makes no claim about — rewording the native-token refusal string in the delegation branch — **SURVIVED**, 6/6 green, confirmed landed by diff. The rig can report survival and is not merely red under any edit.

  **What this does not claim.** The retained spies remain a supporting guard, not the red line's proof; the load-bearing assertions are unchanged and untouched by this diff — the structural import-binding check (#1987) and the deployed-enforcer probes in `non-custody-onchain-enforcer.contract.test.ts` (#2004) — and the `redeemDelegations` caveat-stack-composition remainder stated by #2004 is still open and still not claimed by either file. Perimeter unchanged; what changes is that every assertion in this suite can now fail for the reason it states.
