- **#2049** — **CASP Red Line #4's sole structural guard matched ONE regex pattern — spanning three usage variants of a single import shape — against nine shapes a reintroduction could take; it now parses the module's import STRUCTURE, and every shape it claims is mutation-proven by name.** Test-only; no runtime code, no route, no schema, no authority and no custody surface is touched, and nothing here moves a wei. Recorded as a money-path change for the same reason #2044 was: the file it edits is a *regulatory proof* of the non-custody perimeter, and #2044 made this particular assertion **load-bearing** — three vacuous spies were removed there on the stated ground that the capability they gestured at is carried by the structural banned-import check. That check was mutation-proven at the time, but only on the shape it could see.

  **The gap, measured rather than taken from the ticket.** `paymentPathImportBindings()` matched `import\s+(?:type\s+)?\{([^}]*)\}\s+from\s+'[^']+'` — a NAMED clause in SINGLE quotes. That is one pattern, and it matched three usage variants of the shape it knew (named, aliased, multi-line). Run against every shape a reintroduction could take, it missed **eight** more: `import * as AM from '…'`; `await import('…')`, destructured or whole-namespace; a *computed* dynamic specifier; a `createRequire(import.meta.url)('…')`; `export { … } from '…'`; `export * from '…'`; a side-effect `import '…'`; and — not in the ticket, found while measuring — an ordinary named clause written with **double quotes**. Orthogonal to all eight, and found later still while probing the new rule's own normalization, a specifier can carry a URL **query suffix** (`…/allowance-module.js?bust=1`) — Node's ESM loader resolves that to the same module, the standard cache-busting idiom — so the first version of the widened guard would itself have missed it. Reachability was checked per shape rather than assumed, because a shape that cannot occur is not a hole: `packages/backend` is `"type": "module"`, so a bare `require` is a runtime `ReferenceError` and only reachable via `createRequire`; a **default** import of `rails/allowance-module.ts` does not compile at all (TS1192 — the module has no default export), so it is covered but not currently expressible; and of the ten banned symbol names, only three (`computeEffectiveAllowance`, `getTokenAllowance`, `getLatestBlockTimeSec`) are still live exports — the other seven are tombstones from #1987, kept so a re-created symbol lands on a guard rather than on nothing.

  **What replaced it, and why not a wider regex.** `paymentPathImports()` parses `routes/payments.ts` with the TypeScript parser and returns structure: bound names (named specifiers by BOTH original name and local alias, default bindings, namespace locals), names re-exported from another module, static module specifiers in any clause form, every string literal in the file's **code**, and a count of dynamic `import()` calls with a non-literal specifier. A regex grown to cover eight cases is itself hard to falsify and stops matching silently when a ninth appears. Parsing also buys back the property the original regex existed to protect: comments are not AST nodes, so `payments.ts` naming a retired symbol in prose above the retirement gate still cannot make the assertion red — the reason a plain `toContain` was rejected in the first place. Five ordered rules now carry the claim: (1) no banned name **bound**, (2) none **re-exported**, (3) no **static** reference to a retired-rail module in any clause form, (4) no retired-rail module named in any **runtime** `import()`/`require()`, (5) no **computed** dynamic specifier on this route at all. The positive control was widened with it and still runs first.

  **Mutation evidence — one mutation per shape, each confirmed LANDED by diff before its colour was read, each restored from a task-named backup verified by SHA-256, each red reported at its own named line.** A named clause in double quotes → rule (1), `computeEffectiveAllowance must not be imported…`. `export { computeEffectiveAllowance } from` → rule (2). `import * as AM from`, `export * from`, and a side-effect `import '…'` → rule (3), each independently. `await import('…')` destructured → rule (4); a computed `import(_mod)` whose path is still a literal somewhere in the file → rule (4); a `createRequire(import.meta.url)('…')` → rule (4). A genuinely assembled specifier (`'../rails/' + _seg`, with no banned literal anywhere in the file) → rule (5). A query-suffixed `import('…/allowance-module.js?bust=1')` → rule (4), after `bannedModuleRefs()` was taught to strip `?`/`#` — that shape was measured MISSING against the first version of this fix and is the reason the normalization is not merely an extension strip. All ten killed. Two observations kept rather than smoothed over: the namespace mutation left every retained spy GREEN — an unused binding is invisible to `not.toHaveBeenCalled()`, which is precisely the exposure #2049 named — and the `createRequire` mutation additionally broke two unrelated cases with a 500, which is the ESM runtime confirming that shape's difficulty; the structural assertion still reddened at its own line.

  **Positive control.** An unrelated edit on the payment path — a new local string constant in the delegation branch, itself containing the words "allowance rail" — **SURVIVED**, 6/6 green, confirmed landed by diff. A guard widened until everything is red is not a guard, and rule (4)'s literal scan in particular had to be shown not to fire on ordinary copy.

  **What this still cannot see, named in the assertion's own comment as well as here.** (1) **Transitive reach** — it reads ONE file; an allowed module that itself imports the arithmetic and re-exposes it is invisible, and that path is real (`routes/payments.ts` → `modules/mpp/index.ts` → `modules/mpp/allowances.ts`, #2044). What covers a transitive *call* is the retained spy set. (2) **Other payment surfaces** — `routes/x402.ts` and the machine-payment path are not read here. (3) **Arithmetic re-implemented inline**, importing nothing: no import-shaped guard can see it, and the spies cannot either. (4) **`eval` / `new Function` indirection** — rule (5) catches a computed `import()`, not a module name assembled and eval'd. Rule (4) also structurally subsumes rule (3) for any literal specifier; (3) is kept so the failure names the static shape rather than just the string, and that is stated in the code rather than left to be re-derived.

  **What this does not claim.** The perimeter is unchanged — no enforcement, no custody boundary and no on-chain behaviour is altered by a test-only diff. The `redeemDelegations` caveat-stack-composition remainder stated by #2004 is still open and still not claimed here. What changes is that the assertion #2044 made load-bearing now fails for eight shapes it previously could not see, and names the four it still cannot.
