- **#1991** — the parent guardrails doc itself was rewritten control-by-control
  against the delegation rail (epic #1440 slice 8): every control that named the
  Safe AllowanceModule as a LIVE gate now names its delegation-rail equivalent
  (on-chain caveat enforcers replace on-chain allowances; owner-signed
  delegations replace owner-approved Safe transactions; the #832 exit story
  replaces "Safe-compatible UIs"; no approval queue — over-budget REVERTS), with
  the mapping anchored to `docs/security/delegation-rail-security-model.md` §2.
  Remaining Safe mentions are deliberate: the retirement record (#1984 inflow
  closed, #1986 fail-closed 410, #1987/#1988/#1989 machinery deleted, #2020
  report surface retired), the surviving owner-signed remnant (`/safe/exec`,
  which carries #1229 recovery and is owner authority, not a policy rail), and
  the frozen historical EOF log. One custody statement moved in the honest
  direction: instead of asserting key-purity, the doc now DISCLOSES the
  owner-accepted #1985 artifact — the production relayer key owns four retired
  test dust Safes on Base mainnet (~$0.14 total, delegate-extractable bounded at
  $0.02), full evidence in #1985's closing comment — and growing that set is a
  named feature-review trigger. Red Line #4's executable-proof section (#2004)
  is unchanged; the surrounding prose now cites it as the final-gate proof. This
  is a documentation change only: no code, no route, no key role, no refusal,
  and no authority moved — the doc's claims were brought back into agreement
  with enforcement that already shipped. Perimeter unchanged (and now stated
  against the rail that actually enforces it).
