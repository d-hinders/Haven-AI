- **#2063** — De-queue the surviving queue-and-approve copy on every product and agent-facing text surface outside `/design-system` (the follow-up census #2066/#1947 filed). **Prose only, and the posture can only strengthen**: no route, tool schema, refusal, signature, credential handling, or consent-hash input changes — neither consent surface reads instruction/skill text (the #1332 analysis, unchanged), so nothing here re-triggers consent.

  **What changed and why it matters under CASP**: the marketing landing (`app/page.tsx`, including the `>$100 / Requires approval` policy tile), signup, `UsingYourAgentInfo`, `EditAgentModal`, the connect `ReviewStep`, `AgentDetailClient`, the generated agent handoff (`lib/agent-handoff.ts`) and the generic payment skill (`lib/agent-skill-bundle.ts` + its canonical source `packages/sdk/src/skill-content.ts`) all still described a discretionary per-payment approval queue inside the payment path. That framing is false on the only live rail — the delegation rail declines out-of-policy payments during prepare, on-chain, with nothing written (`routes/payments.ts`; both retired rails answer 410 at every agent-payment entry, #1986) — and it is also the *worse* regulatory story, since it implies Haven operates a discretionary step over user funds. The new copy states the true model: budgets, recipients and expiry are enforced on-chain; out-of-policy payments are declined before any money moves; the only approval that exists is the owner's one-time budget-grant/revoke signature (kept everywhere it is described). No claim that Haven holds funds, controls keys, or transfers money on the user's behalf. The `pending_approvals` stat on `AgentDetailClient` is untouched — its deletion belongs to #2055. Perimeter unchanged.
