- **#2155** — `connect` tombstone records now SURVIVE deletion of the retired agent directory: `writeAgentTombstone` mirrors the record under `~/.haven/tombstones/<agent_id>.json` (0600, same redaction, same no-key boundary) and returns `recordPath`; `--tombstone` output names the mirror; `--doctor` lists mirrored records whose credential dir no longer exists as `retired (dir removed)` — informational, never a failure, and read from the doctor's own `homeDir` so an explicit `--credentials-dir` never consults the machine-wide default root (REGRESSION B2 discipline). **No custody or authority boundary moves.** The mirror is the same record a tombstone already was — `agent_id` / `retired_at` / redacted `reason` / optional `replaced_by` only, no key material, no signing path, nothing revoked, replicate-or-delete never implied: the #1688 rule ("connect reports, the user decides") is preserved, and this is mutation-proven in two directions: (a) the mirrored file contains neither `identity.json` nor `signer.json` contents even when a secret-shaped `--reason` is pasted (the redaction test now checks the mirror explicitly); (b) a retiree whose dir is then fully deleted stays visible to `--doctor` and `readTombstoneRecords`, while a record with no dir holds no key and can never fail the doctor — a tombstone remains a marker, never a revocation, so it can never green-wash a key that still spends. The reachable signing surface is unchanged: the retired path was already a non-signing diagnostic stub that exits 1 (the mirror adds no executable surface at all). Secret hygiene carries over from #1681 verbatim: pasted `sk_agent_`-shaped text is redacted at the write layer before it can reach either copy, and `--tombstone` CLI output stays redacted. No route, credential scope, key-material handling, or signing path is added or widened; the parent doc's custody and authority claims read identically before and after this diff. Perimeter unchanged.
