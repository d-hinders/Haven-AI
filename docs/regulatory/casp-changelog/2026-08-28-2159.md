- **#2159** — adds a deterministic Base Sepolia QA leg for the #2145 EIP-3009 crash shape and a tightly bounded `MERCHANT_REPORT_GRACE_MIN_OVERRIDE`: it can only shorten the status-recovery wait to 0–15 minutes when the deployment serves exactly chain 84532, and startup rejects it on mainnet, mixed, or unbounded deployments. The production default remains 15 minutes. The QA leg signs the existing funding redemption with the agent-held delegate key, reads the existing server-derived recovery status, and resumes the original merchant request through the SDK gate; it neither creates authority nor changes a signed amount, recipient, asset, route, or on-chain caveat. Its balance assertions and failure cleanup make stranded delegate funds observable and sweepable, not executable by Haven. Perimeter unchanged.
