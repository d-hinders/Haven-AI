- **#2169** — `connect` gains `--unwire <dir> | --name <slug>`: tombstone-first removal of ONE agent's wiring — the hosted + signer MCP pair out of every runtime config it appears in (Hermes YAML, Codex TOML, the Cursor / VS Code / Insiders / Claude Desktop JSON configs), the Hermes dotenv API-key line (`MCP_HAVEN_API_KEY` / `MCP_HAVEN_<SLUG>_API_KEY`), and the TARGET credential directory's own local key material (`signer.json`, any abandoned `rekey-pending.json`, the stored `identity.json` API key), with the #2155 mirrored tombstone kept as the surviving record. **No custody or authority boundary moves, in either direction.** It removes LOCAL key material but never revokes anything on the backend and never signs or transfers: revocation stays an owner action on the Haven agent page, so a local teardown complements — and never replaces — the backend revoke (the #1688 rule "connect reports, the user decides" is preserved verbatim, and the CLI output says exactly that). The spend-capable surface only NARROWS here: the removed local half is the same signing path setup created, so no reachable signing, route, credential-scope or key-material-handling surface is added or widened. The one new risk class is the inverse of the writer's: a remover that touches another agent's pair. It is closed the way `agentIsWired` reasons — an UNNAMED pair (shared `haven` / `haven-signer` names and `MCP_HAVEN_API_KEY`) is only removed when this directory's wrapper is the one the config launches (or its key is the one the env holds), and the command REFUSES rather than guess — mutation-proven by the two-unnamed-agents test in `unwire.test.ts`. Secret hygiene carries over: no key material is ever printed, the tamper-safe unreadable-config path never rewrites a file holding another server's bearer token, and the tombstone mirror retains the same redaction and no-key boundary as #1681/#2155. Perimeter unchanged.
