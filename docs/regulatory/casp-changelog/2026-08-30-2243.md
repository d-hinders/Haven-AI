- **#2243** — Documentation only: `packages/signer/README.md` (the published `@haven_ai/signer` npm landing page) re-verified against `packages/signer/src/**` after ~three months of drift, plus a scoped `last-verified` bump in `scripts/docs/package-docs.mjs` and one corrected sentence in `docs/operations/mcp-runtime-compatibility.md`. **No executable file changes**, so no authority, custody, route or key-handling surface moves: `git diff --stat` is three Markdown files and one docs-tooling manifest whose only edit is a `last-verified` string and a comment. The perimeter classification comes from the file half — `packages/signer/` is a money-path glob (`.github/money-path-globs.json`, #1896: the edge-signer package holds the delegate key material) — and this diff matches it only through the README, which ships to npm but executes nothing. The custody invariant the doc describes is **unchanged and was re-verified rather than assumed**: the delegate key is loaded from `HAVEN_DELEGATE_KEY` or the credential file's `delegate_key`, stays in `core.ts`, is never a field of any request or response (`sign-context.ts` transmits only a Bearer API key read from a sibling `identity.json`), and never appears in an audit row (`SigningAuditEntry` in `audit.ts` carries timestamp, tool, payload hash, delegate address, and optionally account address and chain id — no key, signature or payment header). What the change corrects is the doc's **account** of that invariant, in the direction of accuracy: the README asserted "the signer makes no network calls — it can't leak the key to Haven or anyone else", which #1263 made false (an authenticated read-only `GET /x402/:id/sign-context`), and derived a true guarantee from a false premise; it now states the guarantee directly and names the one network call. Filed as its own defect, #2242. The same absolute was found and narrowed in the skew section of `mcp-runtime-compatibility.md`, with that section's conclusion unaffected. The README additionally now documents guards that were already enforced in code but undocumented — local erc7710 settlement-child caveat verification against pinned enforcer addresses (#1455), the unbound-delegation refusal (#1476), the payer-delegate guard (#1690), typed-data-vs-hash mode binding (#1138) — and re-labels retired-rail framing on the EIP-3009 funding leg (`haven_submit -> funds **Safe** -> delegate EOA` becomes `funds **account** -> delegate EOA`; the leg still exists on that scheme, but the Safe rail cannot spend since #1440/#1986, so the funding hop is from the delegation-rail account). Review also caught one **overstatement** in the new refusals list and it was corrected before merge: the gasless-sweep destination check in `core.ts` is conditional on the local credential carrying `safe_address`, so the README now states the `from` check as unconditional and the `to` check as credential-dependent rather than presenting both as independent local re-derivation. Nothing is mutation-tested here because nothing executable changed; the doc gates (`docs:check`, `docs:coupling`) are the applicable checks and both are green. Perimeter unchanged.
