- **#2257** — Haven's exploratory Haven-as-merchant x402 resource surface is retired: the six `/x402/resources*` and `/x402/receipts` operations, their AllowanceModule transaction verifier, and the two backing tables are removed. The router now leaves those paths as ordinary 404s, and migration 073 drops the child receipts table before the resource table without touching unrelated payment history. This narrows the merchant-acceptance and public-payment-verification perimeter; it does not remove or alter the live delegation-rail x402 flow in `routes/x402.ts` / `modules/x402/`, the settlement verifier, the standalone demo merchant, or the read-only `rails/allowance-module.ts` infrastructure. No private keys, signer, on-chain authority, or payment control moved. The contract and loop-harness docs were re-read, the OpenAPI paths were removed and regenerated, and the current perimeter metadata no longer names the deleted route. Perimeter narrowed.
