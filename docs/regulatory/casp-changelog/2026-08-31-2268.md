- **#2268** — `.github/workflows/qa-dev.yml` is a `controlGlob` in
  `.github/money-path-globs.json`, so this change is labelled money-path even
  though it moves no funds. **Its diff in that file is comment-only** — the `on:`
  block, the `concurrency` group, every job, step, `if:` condition and secret
  reference are byte-identical to `origin/dev`, verified by diffing the file with
  comment lines stripped. Nothing about which code the money-flow harness runs,
  which backend it runs against, or which runs `scripts/ci/qa-freshness.mjs`
  accepts as covering a promotion has changed; `qa-freshness.mjs`, `dev-gate.yml`,
  `money-path-globs.json` and the labeler are untouched. `scripts/ci/qa-freshness.mjs`
  — the freshness gate itself, and also a `controlGlob` — is edited in this PR and its
  diff is likewise **comment-only**, verified the same way: its `KNOWN LIMIT` header
  block asserted that the `dev-deployed` trigger makes the run's `headSha` and the
  deployed SHA coincide, which is true of a trigger that fires and vacuous for one that
  never has. No exported function, threshold, glob, exit code or `qa-override` path is
  touched, so what the gate accepts as covering a promotion is bit-for-bit what it
  accepted before. The substantive change is
  a **strengthening** of the release/QA control surface and lives entirely in
  `scripts/ci/guard-freshness.mjs`, the non-gating #2208 reporter: it now also
  watches `qa-dev.yml`'s `repository_dispatch` (`dev-deployed`) trigger, which had
  fired zero times across the repository's whole history (156 runs, 2026-06-30 to
  2026-08-31, counted against the Actions API) while the operations doc described
  it as live. That silence had been shifting promotion freshness onto the nightly
  cron alone and putting pressure on the `qa-override` bypass, so making it
  audible narrows the route past the gate rather than widening it. The reporter
  cannot pass, fail or skip any gate — it opens a `ci-health` issue and nothing
  else. Mutation-proven in both directions: deleting the `repository_dispatch`
  block from `qa-dev.yml` turns the registry-coupling test red by name, and
  widening `countedEvents` to include the live `schedule`/`workflow_dispatch`
  events — the masking failure the guard exists to prevent — turns its own test
  red by name; a positive control asserts the guard stays silent when a genuine
  `dev-deployed` dispatch is fresh, so it cannot pass by alarming on everything.
  No new authority, custody, route, credential or key material: the remaining
  half of the fix is an operator adding a deploy hook in a third-party dashboard,
  described in `docs/operations/agent-qa.md` with no token value anywhere in this
  repository. Perimeter unchanged.
