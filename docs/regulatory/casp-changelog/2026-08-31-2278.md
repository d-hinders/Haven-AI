- **#2278** — the generated Connect setup prompt now tells an executing agent
  that the unchanged command downloads the published npm package and contacts
  the Haven API over the network; a sandboxed runner is expected to enable
  network access or request network access escalation, which changes the execution environment
  rather than the command and is not a third command modification. This is
  served guidance only: the command, its API response shape, setup-token flow,
  credential boundaries, signing path, approval verification, and on-chain
  authority are unchanged. The prompt still says the connector generates the
  signing key locally and sends Haven only the public signing address plus proof,
  and the existing two-permitted-changes restriction is pinned unchanged by the
  route test alongside the new sentence. No private key is exposed, no API
  credential becomes sufficient to spend, and no payment, delegation, relay, or
  custody behavior changes. Perimeter unchanged.
