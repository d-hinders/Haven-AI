- **#2279** — `@haven_ai/connect` terminal-narration timing only; **no spend
  authority, key role, approval authority, or perimeter moved**. Two changes:
  (1) an imperative approve-the-budget call-to-action prints when the #1543
  early install-status report is delivered — the moment the dashboard's
  approval controls actually unlock (`runtimeConfigured || installErrored`) —
  instead of the ask arriving only after the install tail. The CTA stays
  silent in two cases: the report never arrived (button may not be live), or
  the delivered errorCode's own completion handoff will recommend starting a
  fresh connection — approving first would spend the approval on a setup the
  run is about to disown (`manual_runtime_setup_required`, whose handoff
  still recommends approving, keeps the CTA);
  (2) the approval wait loop's 30-second reminder now states elapsed time,
  poll cadence, and the 3-minute give-up bound. Custody and authority posture
  unchanged: the approval itself remains a dashboard action by the owner —
  the connector still only *reads* the narrow connector-status endpoint and
  cannot activate an agent or change budget authority (the existing
  failed-report-is-metadata-only property is untouched); the on-chain budget
  delegation, its caveat enforcers, and the signing paths are not touched.
  `--json` contract unchanged: the CTA and reminder are stderr progress prose;
  the outcome object, stdout purity, and `schema_version` are untouched. No
  secrets enter any new line (amounts/labels were already printed by the
  existing celebration path; the new lines carry no credential material).
