- **#2293** — The default `SWEEP_MIN_USDC` floor moves from 1 USDC to 0.01 USDC, and the below-floor prepare response now names the current balance, configured floor, and accumulation behavior; the existing `<` boundary means an exact 10,000 atomic-unit balance remains sweepable. Production must also set `SWEEP_MIN_USDC=0.01` and redeploy because an existing environment override takes precedence over the code default; the exact live recovery remains an operator verification item. This adjusts the threshold on a pre-existing recovery path: the sweep still creates only a delegate-to-the-user's Haven wallet EIP-3009 authorization after the balance meets the floor, the local signer remains the only holder of the delegate key, and the relayer pays gas. No new authority, custody relationship, route, signed-intent mutation, or API-credential spending ability is introduced. Perimeter unchanged.
