- **#2302** — Discovery-source attribution and crawler telemetry only. A nullable `source` TEXT column on `agent_connection_setups` (migration 074) records where a connect setup came from (e.g. the /402 page, a registry listing); the slug is sanitized at the route, degrades to NULL rather than refusing, and is echoed into the `agent_created` onboarding-event metadata. A new Next.js middleware logs structured fetch events for the static discovery artifacts when the User-Agent is a known AI crawler — log lines only, no database write, no new backend endpoint (an unauthenticated ingest route was deliberately rejected as an abuse surface). No route gains or changes authority: setup creation still requires the same user auth, registration the same one-shot token + proof signature, and no signing, custody, spending, approval or revocation path is touched. Perimeter unchanged.
