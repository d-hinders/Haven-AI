- **#2323** — CI gate logic and its documentation only. **No executable payment behaviour changes:** no route, tool schema, signing scheme, delegation, caveat, budget, balance or wire format is touched, and no key or signature is created, moved or validated differently. The perimeter classification comes from the file half alone — `scripts/docs/coupling-gate.mjs` is a **`controlGlobs`** entry in `.github/money-path-globs.json` (#2200), labelled money-path so a human reads a change that could weaken the gate, and excluded from the `qa-freshness` re-run because re-running the money-flow harness proves nothing about a docs gate. This document's own `covers:` does **not** name `scripts/docs/**`, so this diff does not implicate it; the shard is written because the money-path classification stands on the control glob, not because the coupling gate demanded one. **What was wrong.** `docs:coupling` treated a CASP shard as clearing the parent doc *entirely*: `implicatedDocs` matched the doc's `satisfied-by:` glob and `continue`d **before** the `covers:` test, so a parent cleared by an added shard was not merely un-blocked — it was never **named**, in either posture. Measured on `origin/dev` against this document's real front matter with one covered code file, using the gate's own exported pure core: covered code alone → 1 finding, parent named, both postures; covered code + an ADDED shard → **0 findings, parent absent**, both postures; covered code + an EDITED shard → 1 finding (the #2192 rule, intact). The green tick read as "the coupled docs are consistent with this change"; what it asserted was "a shard exists that claims to cover this change". Those are different claims and the second is weaker exactly where the first matters — a regulatory document describing an ordering the code no longer has. The shard is written by the person making the change, so shard-satisfies-parent is self-certification: the author asserts the change is documented and the gate accepts the assertion as the evidence. **What it cost, reconstructed rather than recounted.** #2274 (PR #2322) moved the retired-rail 410 above token resolution on `POST /payments` and `POST /x402/authorize`, wrote a correct shard (`docs/regulatory/casp-changelog/2026-08-31-2274.md`) and went green, while this document's #2245 Current-state blockquote still listed token resolution among the checks preceding the x402 rail 410 — a sentence that same diff had just made false, in the document the shard was satisfying. `haven-doc-reviewer` caught it as a blocking finding; the implementing agent recorded that it would otherwise have shipped. Re-running #2274's real file set (`routes/payments.ts`, `routes/x402.ts`, its shard) through the pre-#2323 gate reproduces the silence exactly: **15 advisory docs listed and neither shard-cleared contract doc among them** — the second being `docs/architecture/04-x402-payment-sequence.md`, whose own `last-verified` chain shows it had the same paragraph wrong and was corrected in a later pass. So the defect cost two documents, not the one the issue named. **What changed, and the half that deliberately did not.** The BLOCKING half is byte-identical: a qualifying ADDED shard still clears `--strict`, and `#1366`/`#1496`/`#2192` semantics are unchanged, pinned by the existing tests plus a new negative control (strip the shard from #2274's diff → exit 1 and a hard BLOCK, not a re-read note; and a test-only money-path diff with no shard still blocks, so the `#1076` incidental carve-out is intact). Requiring a parent edit instead — the strictest option, and the one rejected — would reinstate the `last-verified` line collision that #1366 moved verification records into shards to escape (four PRs in one day) and that #1496 saw three more of, and would buy a rubber-stamped date, which is worse than a stale one because the weekly staleness audit ranks on it. What is added is REPORTING: a shard-cleared parent now appears in its own section of the advisory comment — *"Parent docs cleared by a shard — body not re-read"* — naming the shard that cleared it, the changed files to re-read it against, and why a green tick is not evidence there. Its own section rather than a bullet in the existing list, because that list is where "the one ⚠️ finding that mattered on #1076 was skimmed past in a list of eleven". The finding is keyed on the `covers:` match, so a PR that writes a shard but changes nothing the parent describes stays silent; and the shard-cleared match set is noise-filtered, so a test-only money-path PR draws no re-read request. **What this is honestly worth.** No gate can verify that a body was re-read — it can only refuse to hide the doc, and the section lands in an advisory comment that exits 0. The mechanical half is *routing*: `ship-next`'s doc-reviewer step runs over the docs the gate implicates, and a shard-cleared parent was not in that set, so on #2274 `haven-doc-reviewer` was pointed at this document by luck rather than by the gate's output. It now is, by construction. `haven-doc-reviewer` remains the control that reads the body. **This document's § "the perimeter argument" blockquote was re-read and deliberately NOT edited.** It says a `satisfied-by` declaration "is satisfied by *presence*, so a script-written shard would excuse the gate with content no human read". That remains true after this change — the gate is still excused by presence; what is new is that it now says so out loud. Nothing generates a perimeter argument, and this change does not make one generable. **Where else the same self-satisfying shape lives**, surveyed and reported rather than fixed: `covers:` front matter (the author declares the scope and `validate-frontmatter.mjs` only checks each glob resolves to ≥1 real file, never that the list is complete — related but not identical, being a standing owner declaration rather than a per-change assertion, and this document's `covers:` is the one with an independent antidote — `scripts/ci/money-path.test.mjs` pins it against `.github/money-path-globs.json`, though the pin is ONE-directional and stating it as bidirectional was itself an unmeasured instrument claim, caught by `haven-doc-reviewer` on this PR: the test asserts every money-path glob is matched by something in `covers:` — a floor — and nothing asserts the reverse, so an unrelated entry added to `covers:` is checked by nothing, measured by appending a marketing-page glob and watching all 10 tests stay green); the `last-verified` chain check (`chain-integrity.mjs` verifies containment of prior issue references and nothing about the note's truth, and its `chain-reset(#N)` hatch is author-written — different failure mode, losing history rather than shipping a false claim, and it already applies this lesson one level down by requiring the parenthesised issue number so prose *about* a reset cannot excuse one); and `EXEMPT_PACKAGE_DOCS` in `package-docs.mjs`, where a file leaves the system on an author-written reason string that check (4b) only tests for non-emptiness. Perimeter unchanged; nothing is grantable that was not grantable before, and no gate was weakened — the blocking surface is unchanged and the reporting surface strictly grew.
