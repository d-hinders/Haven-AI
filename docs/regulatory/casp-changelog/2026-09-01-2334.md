- **#2334** — **agent-facing prose only; no route, tool schema, signing scheme,
  delegation, caveat, wire format or custody boundary moves.** The generic
  payment skill (`packages/sdk/src/skill-content.ts`, the canonical copy
  `packages/connect` auto-installs, mirrored byte-for-byte in the frontend
  download `packages/frontend/src/lib/agent-skill-bundle.ts`) told the agent
  that the pay-tool result's `amount` / `amount_atomic` "is the amount **Haven
  authorizes** for that call". That is the § Product Copy Rules attribution
  inversion in the one artifact a human downloads from the connect-agent
  success screen and an agent then reads as instructions — it names Haven as
  the party granting spend authority, which is the Core Design Principle's
  explicit "must never be" (`Haven backend + Haven database policy + Haven
  discretion`).
  **What the field actually is, measured rather than paraphrased.**
  `amount_atomic` is the MERCHANT's own quoted price, relayed unchanged.
  `packages/sdk/src/x402-protocol.ts:227` sets `amountAtomic:
  x402AuthorizationAmount(option)`; `packages/sdk/src/x402.ts:59-60` resolves
  that to `option.maxAmountRequired ?? option.amount` — fields of the
  merchant's own HTTP 402 `PaymentRequired` response. On the settlement path
  the same value arrives as `amountRaw`
  (`packages/backend/src/modules/x402/delegation-authorize.ts:394`) and becomes
  the child delegation's `maxAmount`
  (`packages/backend/src/modules/x402/x402-delegation.ts:172`). The repository
  already states this in one place — `packages/mcp-server/src/tools.ts:497`,
  "amount/amount_atomic here are the live merchant quote" — so the skill was
  the outlier, not the code. Haven mints nothing here and widens nothing: the
  authority is the owner-signed `erc20PeriodTransfer` budget delegation built
  unsigned at `packages/backend/src/rails/delegation-policy.ts:119` and signed
  by the account owner, and the amount is bounded on-chain by the
  `ERC20PeriodTransferEnforcer` caveat, exercised during `prepareRedemption`'s
  gas estimation (`packages/backend/src/rails/delegation-rail.ts`) so an
  out-of-policy redemption reverts before any intent row is written. The
  backend's own `remainingAtomic < amountRaw` decline
  (`delegation-authorize.ts:324`) is the "optional pre-check that mirrors
  on-chain rules" this document permits, not the authority.
  **The corrected sentence, and why each clause is true.** "the merchant's own
  quoted price for that call" — the provenance above. "a ceiling the merchant
  settles at or below" — unchanged, and correct: `maxAmountRequired` is a
  maximum the merchant may charge. "It is a price, not an approval: the payment
  goes through only if it also fits the cap you set and the on-chain budget the
  user signed, which is enforced on-chain rather than by Haven" — the caller's
  cap and the caveat stack, in that order, and the closing clause is the
  § Product Copy Rules USE form ("Haven cannot move funds outside the limits
  you approve") stated positively. No avoid-listed phrase is used, and the
  wording is consistent with the skill's own opening paragraph, which already
  says every payment is checked against the on-chain budget before money moves.
  **Both copies moved in one commit**, as the byte-parity test requires; that
  the single-file fix is a red build rather than a silent fork was verified by
  mutation (reverting only the SDK copy fails `agent-skill-bundle.test.ts`'s
  cross-package parity case, exit 1; both copies corrected, 9/9 pass).
  **Sweep.** Every authority verb in both copies was re-read
  (`/usr/bin/grep -niE 'authoriz|authoris|permit|grant|approv|allows?|on your
  behalf|entitle|empower'`, control 26 `Haven` hits in the SDK copy), as was
  every sentence in which Haven is the grammatical actor. This was the only
  attribution claim: the remaining Haven-as-actor sentences are mechanical
  (rehydrates context, completes the merchant leg, never contacted the
  merchant) and the other `authorizing` occurrence attributes the act to the
  user. One out-of-scope near neighbour is recorded and filed rather than
  folded in: `packages/signer/src/core.ts:571,616` and its documented copy at
  `docs/operations/mcp-runtime-compatibility.md:779` say rewriting a version
  field "would misrepresent what Haven authorised" — there "authorised" plausibly
  means Haven's signed context BINDING rather than spend authority, so it needs
  its own judgement, and `packages/signer/` is money-path in its own right.
  **A guard now exists where none did, with its ceiling written down.**
  `scripts/frontend-copy-lint.mjs` — which since #2317 does scan both of these
  files — was measured green on the exact defect (mutated tree, pre-change
  script, exit 0), because its `BANNED` list held terminology only. Five literal
  attribution phrases are added (`haven authorizes|authorises|approves|grants|
  permits`), proven by mutation to take the same tree to exit 1 naming both
  files. It is a single-line literal match and cannot read attribution: a
  reworded inversion or a line break between the two words evades it, and
  `scripts/frontend-copy-lint.test.mjs` now asserts both evasions explicitly so
  a green run is never read as "attribution was checked". Human review remains
  the control; this is the literal floor under it.
  Local gates: `npm run lint:copy` exit 0, `npm run lint:copy:test` 17/17,
  frontend `agent-skill-bundle.test.ts` 9/9.
  No key, no signature, no delegation, no caveat, no budget and no balance is
  touched by this diff. Haven still holds nothing, signs no settlement, and
  grants no spend authority. Perimeter unchanged; a downloadable instruction
  that claimed Haven's authority now names the owner's.
