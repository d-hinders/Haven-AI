- **#2347** — Comment and agent-facing-string wording only, across the two CASP-`covers:`-mapped packages #2334 deliberately left out of its own scope. **No executable behaviour changes:** no route, tool schema, signing scheme, delegation, caveat, budget, balance or wire format is touched, and no key or signature is created, moved or validated differently. The perimeter classification comes from the file half — `packages/signer/**` is a glob in `.github/money-path-globs.json` (#1896) — with `packages/mcp-server/src/**` and `packages/signer/**` both `covers:`-mapped by this document; the diff is nonetheless a comment/string-only change in listed files with review-confirmed zero behavioural delta, the same treatment #2247 took. **What was wrong and what the code actually does.** `packages/mcp-server/src/tools.ts`'s pre-funding price guard documented `authorizedAtomic` as "the amount **Haven would authorize** for the call" — the § Core Design Principle inversion #2334 fixed in the shipped `SKILL.md`, and plausibly its source, since the shipped sentence reads as a paraphrase of this comment. Re-derived from current `dev` rather than taken from #2334's summary: the value is `option.maxAmountRequired ?? option.amount` (`packages/sdk/src/x402.ts`, `optionAuthorizationAmount`, exported to the hosted server as `x402AuthorizationAmount`) — fields read straight off the **merchant's** 402 response — and it travels as `amountRaw` into the child delegation's `maxAmount`. Payment authority is elsewhere and unchanged: the owner-signed `erc20PeriodTransfer` budget delegation, built **unsigned** by `buildBudgetDelegation` (`packages/backend/src/rails/delegation-policy.ts`, whose return type is `Omit<Delegation, 'signature'>`) and signed by the account owner, enforced on-chain by `ERC20PeriodTransferEnforcer`. The comment now says so, and names the funding-leg sense in which the surrounding identifiers legitimately read "authorization". **A third instance, agent-facing, that #2347's own body did not know about.** The `ERC7710_RAIL_REQUIRED` refusal shipped to agents read "so Haven refuses rather **than authorize** on a guess" — Haven as the grammatical actor of an authority verb, in a live payment refusal. It now reads "refuses rather than **proceed on a guess**", matching the sibling refusal in the same file ("Haven refuses rather than proceed on an unchecked ...") and what the file's own test already calls the behaviour ("refuses rather than guessing a rail it could not see"). The `code`, `statusCode`, `nextAction` and `suggestedTool` are untouched. No test asserted the changed prose, so two literal pins are added to that existing test — the corrected phrase present, and `not.toContain('than authorize')` — and mutation-proven: restoring the pristine `origin/dev` `tools.ts` takes the test from exit 0 to exit 1 naming the missing phrase, and restoring from a `cp` backup (`diff -q` byte-identical) returns it to exit 0. Neither assertion interprets a sentence, per the #2163 rework cap; the copy lint could not have covered it in any case (see below). **The signer half was judged, not rewritten, and the original reading was the correct one.** `packages/signer/src/core.ts`'s version-skew refusal said rewriting the version field "would misrepresent what Haven **authorised**". That is not the CASP inversion: Haven really does sign the expected-context binding (`HAVEN_X402_BINDING_SIGNER`), so there is something of Haven's to misrepresent, and the sentence never claimed Haven confers spend authority. It is changed to "**declared**" for one reason: "authorised" was the single place in the package using a broad authority verb for the act, on a refusal an agent reads inside a payment flow, where it invites exactly the #2334 misreading. "Declared" is not a fourth coinage — it is the word `packages/signer/src/settlement-child.ts` already uses for this exact binding ("proves Haven **declared** a payload … it says nothing about what the payload MEANS"), which is the narrowest and most load-bearing of the neighbours; the others say "Haven **authored** the authorization" (`packages/signer/src/tools.ts`) and "Haven's **binding** over the authorization" (`packages/sdk/src/sweep.ts`). The property claimed is unchanged; only the verb naming it. The documented copy at `docs/operations/mcp-runtime-compatibility.md` moves with it and says why. **Sweep, and what it can and cannot show.** A control-first Haven-as-grammatical-actor sweep was run against `origin/dev` at `a36fd686` (the branch point), over all 1770 tracked files there for `authoriz|authoris|permit|grant|approv|allows?|on your behalf|entitle|empower` (controls: 789 files contain "Haven", 1030 contain an authority verb — a zero was never believed on its own), in two passes because a line-anchored search is not sufficient over wrapped prose or wrapped comments: the line-anchored pass returned 363 hits, and **missed the very defect this issue names**, because `Haven would` and `authorize` sit on different lines of the JSDoc. A second pass matching across newlines found 29 newline-spanning hits, which is where both `tools.ts` instances surfaced. Every hit in both passes was read. Everything else is correct as written and was left alone. The closest call, named explicitly because both review passes independently re-raised it: `packages/mcp-server/src/tools.ts`'s `DELEGATION_BUDGET_EXCEEDED` refusal says "The amount **this purchase** would authorize (…)". It WAS examined in this sweep and deliberately kept — the grammatical actor is the purchase, not Haven, and the sentence it sits in immediately attributes the limit to "the agent's remaining active delegation budget" and tells the reader to "ask the wallet owner to grant or raise the budget", which is the correct attribution. Changing it would be uniformity, not accuracy. Also left alone: "return to Haven to approve" (Haven is the place, the user is the actor), `haven_get_allowances` / `return_to_haven_for_wallet_approval` identifiers, the x402/EIP-3009 protocol nouns (`authorizeX402`, `TransferWithAuthorization`, `POST /x402/authorize`), and the many correct attributions already in the tree (`sdk/src/tools.ts` "Haven relays signed transactions only; the agent key authorizes payment and on-chain limits enforce spend"; `mcp-server/src/auth.ts` "The token authorizes nothing on its own"). **The guard was measured, not extended, and the measurement is the finding.** #2334 added five literal attribution phrases to `scripts/frontend-copy-lint.mjs` and recorded two evasions in its own test file. Run against this issue's three defects, the guard fires on the #2334 control and misses all three — and two of those misses are new evasion classes it had not recorded: a **modal/auxiliary form** ("Haven **would** authorize") evades the adjacent-literal list even fully unwrapped, and so does any **non-adjacent** construction ("Haven refuses rather than authorize"); separately, `haven authorised`/`haven authorized` (**past tense**) is a one-line gap in the list, whose present-tense sibling `haven authorises` is caught. The list is not extended here for three reasons: two of the three shapes cannot be expressed as a single-line literal at all, and reaching them means interpreting a sentence, which the #2163 rework caps put out of scope; all three files live in `packages/mcp-server/` and `packages/signer/`, which are in neither `SCAN_DIRS` nor `SCAN_FILES`, so no phrase addition would have fired regardless; and `scripts/frontend-copy-lint.mjs` was under a live coordination claim (#2333) on #1289, where the past-tense gap is reported instead. Human review remains the control for attribution, exactly as #2334 wrote down. **One pre-existing, already-tracked gap independently re-found by the sweep and NOT fixed here:** `packages/frontend/src/components/UsingYourAgentInfo.tsx` still carries the two avoid-listed § Product Copy Rules phrases this document's own "Known compliance gap" paragraph names, tracked by **#2246** (open). Out of scope and left to its tracker. Perimeter unchanged; nothing is grantable that was not grantable before.
