- **#2363** — A doc comment and a test. **No production behaviour changes at all**: no schema, no handler, no tool description string, no membership of `STRICT_INPUT_TOOLS`, no route, no signing scheme, no migration, no funds moved, no authority granted or removed. `haven_complete_mcp_tool` is permissive before this change and permissive after it, and the pin asserting so is strengthened rather than relaxed. **Perimeter unchanged.**

  **Why a compliance shard exists for a comment.** `docs/regulatory/casp-risk-guardrails.md` `covers: packages/mcp-server/src/**` by glob, so any edit to that tree implicates it and the coupling gate blocks until it is touched or a shard clears it. Recorded plainly so a later reader does not infer a regulatory event from the shard's existence: the shard is the routing mechanism, not evidence that the perimeter moved. **This diff is also not money-path by either half of ship-next § Merge Gate's union** — `packages/mcp-server/src/tools.ts` appears in neither `globs` nor `controlGlobs` of `.github/money-path-globs.json` (checked, not assumed), and issue #2363 carries `code-quality` + `area:mcp` and no `money-path` label. It is comment-only in a money-path-*adjacent* file, and the review confirmed zero behavioural change.

  **The defect, and why it is not a tense nit.** The `STRICT_INPUT_TOOLS` doc block justified `haven_complete_mcp_tool`'s exclusion by saying Haven's auto-installed `SKILL.md` **"says"** — present tense — "Pass `payment_required`, `arguments`, and `mcp_transport` verbatim from the quote/prepare result." #2353's PR #2359 deleted that sentence from both byte-pinned copies (`packages/sdk/src/skill-content.ts` and `packages/frontend/src/lib/agent-skill-bundle.ts`). The comment is **load-bearing**: it is the recorded reason the tool sits outside the strict set, so it is what a contributor reads when deciding whether the exclusion can be lifted. Arguing from a falsified premise, it pointed at the **wrong conclusion** — "the guidance is still wrong, so we still cannot" — when the true state is "the guidance is fixed, and what remains is a **rollout** question": the skill is auto-installed, an unknown number of agents carry the old copy on disk, and flipping strictness before it propagates would still turn Haven's own documented flow into a hard 400, now for a reason that exists nowhere in the repository. The same block also claimed #2353 owns "both byte-pinned copies … and this tool's switch"; #2359 shipped the copies and deliberately not the switch, so that clause described a PR that does not exist. Both are rewritten. #2353 stays open for the rollout decision; #2366 (spelling convergence) and #2349 (batch 3) are linked from the comment.

  **Neighbouring bullets audited for the same staleness class, and the sound ones are reported alongside the changed one.** A justification comment ages the moment the thing it justifies changes, and three exclusions/inclusions in this block were re-decided the same day (#2312/PR #2355, #2348/PR #2368). Read against the live `toolSchemas` map via `tsx`, not by eye: 22 hosted schemas (the "all 22 byte-identical advertisement" claim holds); 7 strict tools, batch 1's three plus batch 2's four; `haven_quote_x402` declares `url`/`method`/`headers` only, so the "no field to route a body to" bullet stands; `haven_pay_x402_quote.payment_required` is genuinely required (`isOptional() === false`), so the "already failed loudly" bullet stands; `haven_send.idempotency_key` is declared and optional; `haven_get_agent` and `haven_get_allowances` are both `{}`, so the empty-schema exclusion stands; `haven_complete_mcp_tool` declares six fields and `payment_required` is not among them, so "has never declared" stands; #2349 is open, so "batched, not exempted" stands. **One stale bullet, seven sound.** Scope of the search for further instances: `/usr/bin/grep` over `packages/mcp-server/src/**/*.ts` for `SKILL.md`, `skill text`, `skill-content`, `agent-skill` and for `says`/`said`/`tells`/`teaches` — the only present-tense claim about shipped skill text in the tree was this one, and `strict-tool-input.test.ts:195` already said "used to instruct". Control search (`haven_` in `tools.ts`) returns **311**, so the instrument can say yes.

  **Enforcement decision, argued rather than assumed.** The proposal considered was pinning the quoted sentence against the real `skill-content.ts` string. In the direction it is usually meant — "the quote must still be found in the skill" — it is **backwards here**, because the quote is of a sentence that has been deleted on purpose; such a check would fail on the corrected tree. The correct-direction pin already exists (`packages/sdk/src/skill-content.test.ts`: the deleted imperative stays deleted, the correction stays present, mutation-proven under #2353). What was missing was not an assertion but a **location**: a revert of the skill text went red only in the SDK's suite, with a message about the skill, and nothing told whoever repaired it that this exclusion's rationale had silently inverted. So a `#2363` block in `packages/mcp-server/src/strict-tool-input.test.ts` re-asserts the same two literals *in the suite of the file that carries the comment*, plus a control that the skill still discusses the tool at all, plus a re-assertion that the tool is still absent from `STRICT_INPUT_TOOLS`. Two literal string matches, no sentence interpretation — `ship-next` § Rework caps rule 1. **The broader "skill prose vs tool schemas" guard is deliberately NOT proposed**: #2359 prototyped and rejected it on measurement (1 true positive, 10 false positives, all ten from sentence-spanning segmentation), recorded in `docs/product/copy-guidelines.md` § Enforcement, and suppressing them needs the prose-layout allowlist that failed four times on #2131.

  **Mutation-proven, judged on process exit code, restored from `cp` backups and verified byte-identical with `diff -q` rather than by inverting the edit.** Baseline: 37/37 green, exit 0. **Mutation A** — rewrite the shipped skill's `haven_complete_mcp_tool` paragraph back to "Pass `payment_required`, `arguments`, and `mcp_transport` verbatim" in `packages/sdk/src/skill-content.ts`: **exit 1**, both premise assertions red ("the deleted imperative stays deleted", "the correction stays present"), control green — the check goes red exactly when the comment's premise stops holding. **Mutation B** — add `haven_complete_mcp_tool` to `STRICT_INPUT_TOOLS`: **exit 1**, the permissiveness assertion red and the two skill-text assertions green, so the two failure modes are distinguishable rather than one blunt red. Restores verified with `diff -q` against the pre-mutation copies (exit 0 both times), then 37/37 green again at exit 0. Full `packages/mcp-server` suite: **364/364, exit 0**; `typecheck` exit 0.

  **Both parent docs this shard clears were read, not merely cleared (#2323/#2367).** The coupling gate names `casp-risk-guardrails.md` and `04-x402-payment-sequence.md` as `[shard-satisfied]` with the body not re-read. `casp-risk-guardrails.md`: its `covers:` list is the gate and `packages/mcp-server/src/**` is inside it; this change adds no glob, moves no boundary, and touches no Use/Avoid copy rule — no edit needed, `last-verified` deliberately not bumped because the document was not re-verified as a whole. `04-x402-payment-sequence.md`: searched for every claim this diff could stale; its strictness paragraph (~line 503) is about the **nested** `mcp_transport` object and its `additionalProperties: false` advertisement, not about top-level strictness on `haven_complete_mcp_tool`, and its `#1307` settle-by-`payment_id` paragraph matches the six fields the tool declares today — accurate as written, not edited. `docs/architecture/08-local-vs-hosted-mcp.md` (advisory) was read: #2353 already corrected it to the past tense there, which is precisely the asymmetry #2363 closes — the doc was right and the code comment was wrong — and its "what pins the two halves apart is `strict-tool-input.test.ts`" sentence stays true of the file this change adds a third pin to. Not edited, and that is a reported outcome rather than an omission.

  Perimeter unchanged; an exclusion rationale that argued from a premise its own fix had falsified now states the real blocker, and the premise it rests on is pinned where the rationale lives.
