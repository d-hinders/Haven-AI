- **#2260** — Removes the retired `contracts.allowanceModule` field from the
  shared chain registry, its backend and frontend structural mirrors, and both
  registry snapshots. This is a completion of the Safe / AllowanceModule
  retirement, not a change to payment execution: the last backend reader was
  removed by #2259, and this diff does not change a route, signing scheme,
  agent authority, relayer behavior, on-chain state, migration, or stored
  record. The remaining `rails/allowance-module.ts` exports stay byte-for-byte
  behaviorally the same; they provide shared provider, relayer-wallet, and
  token-balance reads and do not construct an AllowanceModule contract. The
  removed addresses were configuration residue only, so no funds move and no
  existing on-chain permission is changed or made less externally revocable.
  The core type removal is checked by typechecks, while backend and frontend
  snapshots pin the resulting registry shape. Together with the exact reader
  audit, those checks show no consumer still requires the field. Perimeter
  unchanged.
