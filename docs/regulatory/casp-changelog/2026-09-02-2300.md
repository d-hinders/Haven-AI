- **#2300** — the money-path perimeter is **WIDENED by one entry**, and nothing
  else moves. `packages/mcp-server/src/**` joins `globs` in
  `.github/money-path-globs.json` (and `.github/labeler.yml` and the `ship-next`
  Merge Gate, which CI pins to it in both directions). **No executable payment
  behaviour changes:** no route, tool schema, signing scheme, delegation, caveat,
  budget, balance or wire format is touched, no key or signature is created,
  moved or validated differently, and no file under `packages/mcp-server/` is
  edited by this change. The classification comes from the file half twice
  over: `.github/money-path-globs.json` and `scripts/ci/qa-freshness.mjs` are
  both `controlGlobs`, so this PR labels itself money-path and is the human read
  the tier exists to force. This document's `covers:` does not name either, so
  the coupling gate did not demand this shard; it is written because a
  perimeter change is a perimeter analysis by definition.
  **What the entry guards.** The hosted MCP server (`packages/mcp-server/`,
  Docker-deployed from `dev`, never on npm) constructs and relays payments over
  Streamable HTTP and holds no key — the edge signer signs (§ *Hard Architecture
  Invariants*, `docs/architecture/08-local-vs-hosted-mcp.md`). Its `tools.ts`
  is where a caller's intent becomes a sequence of Haven API calls, and that
  sequencing is a money decision: #2282 (PR #2296) was `haven_settle_mcp_tool`
  relaying the funding userop *before* checking the merchant-call context
  existed, stranding the caller with a `funded_but_unsettled` intent —
  reproduced on dev with real Base Sepolia transactions; #2312 (PR #2355)
  measured over a real transport that the MCP SDK strips undeclared keys before
  the handler, so a caller's `tx_hash` was silently dropped on
  `haven_report_x402_outcome`; #2348 (PR #2368) measured that a stripped
  `idempotencyKey` on `haven_send` sent `POST /payments` with no
  `idempotency_key` at all — a retry was a second spend — and on
  `haven_pay_mcp_tool` *replaced* the replay scope with a 300 s bucketed hash of
  the merchant quote; #2051 was a merchant-steerable cap bypass in the same file,
  measured at 900 USDC authorized against a stated 1 USDC cap. PR #2296 changed
  `tools.ts` and carried `area:mcp, area:docs` — the #1896 shape, a file whose
  omission gave the right answer for the wrong reason, because a human had
  labelled the issue.
  **Why runtime `globs` and not `controlGlobs`, on the file's own test.**
  `controlGlobs` are excluded from the `qa-freshness` re-run because "re-running
  the money-flow harness proves nothing about a CI config change". Here it
  proves exactly what the gate asks: the hosted MCP deploys from `dev` on
  Railway and `qa-dev.yml` drives that deployment through `QA_HOSTED_MCP_URL`
  with the real `@haven_ai/signer` in-process — `x402-hosted-mcp-signer`,
  `x402-erc7710-hosted` and `x402-catalog-guided-purchase` all call
  `haven_pay_mcp_tool` / `haven_settle_mcp_tool` on the deployed server. That is
  the argument the frontend decision surfaces (#1903) could not make and the
  reason they are control globs. **A correction to the issue's premise, recorded
  because it changes what the tier buys:** `qa-freshness` does not key on the PR
  label. `evaluate()` diffs the promotion head against the green run's SHA and
  matches the changed files against `globs` alone — so the `money-path` label on
  issue #2282 never reached the gate, and a control-glob entry would have
  labelled PR #2296 and still let it promote on a run that never exercised it.
  Proven with the gate's own pure core over PR #2296's real six-file set:
  before, `evaluate()` → `ok` with zero matches on either list; after,
  three `packages/mcp-server/src/*` matches, `money_path_after_run` on the dev
  path and `hotfix_money_path` on the hotfix path.
  **Scope, measured before choosing.** Across 1156 first-parent commits on `dev`
  in the 90 days to 2026-09-02, 55 touched `packages/mcp-server`, 53 under
  `src/`, and 2 exclusively `README.md` / `Dockerfile` / `package.json` /
  `tsconfig` / `tsup` / `vitest` config — build and prose plumbing the harness
  cannot distinguish a change to. `src/**` matches this document's existing
  `covers:` entry exactly, so the #1899 pin (now a floor over 31 of 47 globs,
  corrected from the "30 of 46" the #2323 shard and
  `docs-quality-system.md` carried — stale via this change, not wrong when
  written) held with no `covers:` widening; the whole-package glob would have
  forced a Dockerfile into a `contract: true` doc's `covers:`. Label cost: 28 of
  the 55 commits carried no money-path label through any other file and now
  would; comparable to the already-listed `packages/signer/**` at 79 commits in
  the same window.
  **One consequence the tier carries, closed in the same change and
  mutation-proven.** `scripts/release-bump.mjs` writes `HOSTED_SERVER_VERSION`
  into `packages/mcp-server/src/server.ts` on every cut, always after the last
  green run — the #2164 shape that made the gate refuse every release by
  construction until `SIGNER_VERSION` was allowlisted. `RELEASE_BUMP_VERSION_CONSTANTS`
  in `qa-freshness.mjs` now names `HOSTED_SERVER_VERSION`; the symbol-pairing
  rule, the per-hunk check and the fail-closed direction are untouched, and a
  new negative pins that `MCP_VERSION` / `CONNECTOR_VERSION` / `CLI_VERSION`
  (whose files sit on no glob) stay refused. Mutations, `cp`-restored
  byte-identically: removing the glob from the JSON fails three
  `money-path.test.mjs` cases including the new one by name; removing the
  constant from the allowlist fails exactly the new `qa-freshness.test.mjs`
  case. Nothing is grantable that was not grantable before; no refusal is
  removed; the blocking surface strictly grew. **Perimeter widened, deliberately;
  authority, custody and every route unchanged.**
