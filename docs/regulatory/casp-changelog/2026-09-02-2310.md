- **#2310** — **Untagged `@haven_ai/*` install commands tagged `@alpha` across the docs; docs-only, zero production changes**: no signature, no refusal, no schema, no route, no tool, no migration, no funds moved, no authority granted or removed)Skip, no budget/custody/approval surface touched. The diff is nine `.md` files, and every line changes one thing: the package-spec token on an install/npx command (`npm install @haven_ai/sdk` → `npm install @haven_ai/sdk@alpha`, `npm i -g @haven_ai/cli` → `npm i -g @haven_ai/cli@alpha`, `npx @haven_ai/signer` → `npx @haven_ai/signer@alpha`, `npx @haven_ai/mcp` → `npx @haven_ai/mcp@alpha`; the connect one-liner was already `@alpha` and is left alone). The repo fix for the signer's false security claim shipped under #2243 — the dist-tag half of this issue is a release action with an owner and is NOT in this change: no `npm publish`, no registry write.

  **Why a compliance shard exists for a README token change.** `packages/signer/**` is on `globs` in `.github/money-path-globs.json` and in `casp-risk-guardrails.md` `covers:`, and `packages/signer/README.md` changed — so the blocking contract-doc gate fires by the **path half** even though #2310 carries no `money-path` label and touches no signing/runtime code (identical shape to the #2395 precedent, a docs-comment PR gated by the same glob). The shard is the routing mechanism, not evidence the perimeter moved.

  **Why the change is strictly protective.** On 2026-08-31 the five published packages had `latest` dist-tags 18–35 versions behind the alpha train — including `@haven_ai/signer@latest` = `0.1.0-alpha`, whose published README still carries the #2242 false claim until #2243's corrected prose ships there. An untagged `npx @haven_ai/signer` / `npm install @haven_ai/sdk` / `npx @haven_ai/mcp` / `npm i -g @haven_ai/cli` in the docs resolves to that stale `latest`; tagging the token routes first-contact installs to `@alpha` (0.1.34-alpha.0, measured from the registry 2026-09-02). Verified rather than asserted: `git diff` against `origin/dev` contains no non-`.md` path, and re-grep of the scoped paths confirms no untagged `@haven_ai/*` npm/npx command remains (remaining hits are known false positives — the README prose sentence at README.md:566, the unpublished workspace package `@haven_ai/mcp-server` `/` `packages/mcp-server/README.md:164`, whose name does not exist on the registry, and the immutable changelog shards). No tag token selects which signing, budget or custody code runs; it selects which published artifact a first-contact reader receivesheb. The parent doc's Red Lines, Product Copy rules and `covers:` span are unaffected — no glob, boundary or rule moves, and install-command spelling is not a claim any CASP section makes; not re-verified as a whole, `last-verified` deliberately not bumped. Perimeter unchanged.
