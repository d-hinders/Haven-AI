- **#2392** — Two more emitters of the delegation-rail budget projection gain an `expectMatchesSpec` round trip, and the projection's own emitter list is corrected. **Perimeter unchanged** — no production code path changes: the diff is two route-test files (`routes/__tests__/dashboard.test.ts`, `routes/__tests__/agents.test.ts`), a header-comment edit in `rails/delegation-budget-view.ts` (the only reason this shard exists — that file sits on the `rails/delegation-*.ts` money-path glob and under this doc's `rails/**` cover, and the hunk is nine comment lines with zero behavioural change, confirmed by `git diff` containing no non-comment line), the matching row in `05-agent-api-openapi.md`, two DESCRIPTION-only strings in `openapi/spec.ts` (its #2295 header comment and the served `AgentAllowance.description`, which named the same phantom `PATCH /agents/{id}` — raised by `haven-reviewer` on this PR; no schema, type, pattern or required list changes) with `packages/core/src/api-types.ts` regenerated for that one JSDoc line (`check:api-types` in sync), and this record. No route, response code, required-field list, schema, signing scheme, migration or on-chain envelope moves; no funds move; no authority is granted, widened or narrowed. **What was measured rather than assumed.** Against `origin/dev` `18f66e97`, the view has exactly five non-test callers — `GET /agents`, `GET /agents/{id}`, `PUT /agents/{id}` (`routes/agents.ts`), `GET /dashboard/overview` (`routes/dashboard.ts`) and `GET /machine-payments/allowances` (`modules/mpp/allowances.ts`) — while its header named a `PATCH /agents/{id}` that has never existed (zero `patch:` operations in `openapi/spec.ts`, no PATCH registration) and omitted the PUT; `POST /agents` is not a caller at all, it answers with a literal `allowances: []`. Both newly pinned routes AGREE with their spec entries today: `GET /dashboard/overview` emits `agents[].allowances[]` as `{tokenSymbol, allowanceAmount, resetPeriodMin}` with `allowanceAmount` the human-decimal string from `formatTokenValue(budget_atomic, decimals)` — one nesting above the atomic `budget_atomic` the SQL returns (`listActiveDelegations`: `SELECT id, agent_id, chain_id, token_address, budget_atomic, period_seconds FROM agent_delegations WHERE … status = 'active'`) — matching `DashboardAgentAllowance`; `POST /agents` emits the `INSERT … RETURNING` row plus `safe_address/safe_name/safe_chain_id`, `api_key`, `allowances: []` and `passport_requested`, matching `CreateAgentResponse` (`allOf: [Agent, {api_key, passport_requested}]`). The read side is unchanged: the projection remains read/reporting only and enforcement stays the on-chain delegation, exactly as the file's header states. **Mutation-proven in both directions, each on process exit code, each restored from a `cp` backup and checked with `diff -q`, never by inverting the edit.** `GET /dashboard/overview`, route side: renaming the emitted `allowanceAmount` → exit 1, `must have required property 'allowanceAmount'` + `must NOT have additional properties ('allowance_amount')`; emitting it as a number → exit 1, `/agents/0/allowances/0/allowanceAmount: must be string`; making the view emit the atomic `budget_atomic` → exit 1 on the retained literal (`"1.00"` expected, `"1000000"` received) while the schema round trip PASSES, which is why the literal is kept beside the round trip and not replaced by it (the spec types that field as a bare `string`, and even the named `allowanceHumanAmount` pattern admits a bare integer by design). Spec side: `allowanceAmount` retyped `integer` → exit 1, `must be integer`; the required entry renamed → exit 1, `must have required property 'allowance_amount'`. `POST /agents`, route side: `allowances: []` dropped → exit 1, `/: must have required property 'allowances'`; `passport_requested` emitted as a string → exit 1, `/passport_requested: must be boolean`. Spec side: the `api_key` pattern changed to `^sk_live_` → exit 1, `/api_key: must match pattern "^sk_live_"`; `archived_at` added to `Agent.required` → exit 1, `/: must have required property 'archived_at'`. Candidate: both files green, 27/27; the mutation edits to `routes/dashboard.ts`, `routes/agents.ts`, `openapi/spec.ts` and the view's function body are byte-identical to `origin/dev` in the commit. **This parent doc was read, not merely cleared:** § *Payment-Related Merge Checklist* (every item holds — no key custody, no credential-as-instrument, no off-chain-only control, no envelope mutation, no copy change), § *What `covers:` must span* (`rails/**` is the gate that fired; nothing is added to or removed from any list) and § *The agent-authority surface is in scope* (`routes/agents.ts` is covered; it is not edited here). Nothing in its body is stale for this change and its `last-verified` is deliberately not bumped. Perimeter unchanged.
