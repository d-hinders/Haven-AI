- **#2395** — **Two doc comments. No production behaviour changes at all**: no signature, no refusal, no schema, no route, no tool, no migration, no funds moved, no authority granted or removed. `packages/signer/src/core.ts` changes only the JSDoc on `X402ExpectedPayment.resourceUrl` and `.merchantTo` (the interface, every check that reads those fields — `assertX402MatchesExpected`, `assertExpectedBinding`, `verifySettlementChild` — and every byte of executable code are untouched: `git diff` on the file is 15 added / 2 removed lines, all inside `/** … */`); `scripts/docs/package-docs.mjs` changes one clause of the signer README's `// #2243:` verification note, a governance manifest the runtime never loads. **Perimeter unchanged.**

  **Why a compliance shard exists for two comments.** `packages/signer/**` is on `globs` in `.github/money-path-globs.json` (#1896 — the edge-signer package holds the delegate key), so this PR is money-path by the **file half** of ship-next § Merge Gate's union even though issue #2395 carries `code-quality` + `area:mcp` + `area:docs` and no `money-path` label; and `casp-risk-guardrails.md` declares `covers: packages/signer/**`, so the blocking contract-doc gate fires on any edit under that path, comments included. The shard is the routing mechanism, not evidence that the perimeter moved. Two consequences are accepted rather than dodged: the `money-path` label lands, and the `qa-freshness` promotion gate will count `packages/signer/src/core.ts` as changed after the last green money-flow run. Its only exemption, `isVersionOnlyDiff` (#2164), does **not** apply — it pairs `SIGNER_VERSION` / `"version"` / `@haven_ai/*` pin lines by symbol and answers `false` to anything else, and it was run rather than reasoned about: `false` on this diff, `true` on a control diff bumping `SIGNER_VERSION` in place. There is no comment-only exemption in that gate, and none is proposed here.

  **The defect, and why it is the #2363 class.** Both JSDoc lines said the resource / merchant recipient "was funded by hosted `haven_x402_authorize`". That name was a never-registered #314 alias of `haven_pay_x402_quote` (`legacyToolSchemas`, `tools.ts:596` before PR #2394), and #2394 deleted it outright, so the comment explained a security-relevant field — the two values the signer refuses a merchant header against — by reference to a tool that does not exist, pointing the next reader at the wrong mental model (the same class as #2363, a comment justifying an exclusion by a sentence #2353 had deleted). The rewrite names what actually sets the field today, established from `packages/mcp-server/src/tools.ts` on current `dev` rather than from the issue: `buildX402SigningContext` ("Shape returned by haven_pay_x402_quote and used by haven_resume_x402_payment") relays `intent.resourceUrl` / `intent.merchantTo` into `x402.expected`, and the signer checks them in `assertX402MatchesExpected` (header resource and `payTo`) and, on erc7710, `verifySettlementChild` (the child's payee caveat). The meaning — which field is set by which step, and which check consumes it — is unchanged and now stated. The manifest note's present-tense "a deprecated alias for …" is rewritten to describe the alias as the thing it was at #2243's time and record its deletion, without carrying the dead name forward.

  **Sweep, control-first, on the worktree at `origin/dev` `659c14f8` (after #2394).** `/usr/bin/grep -rn` with build directories excluded over `packages docs scripts .agents .claude .github` and the four root gravity files: control `haven_pay_x402_quote` **172** before the edit, **173** after (the new JSDoc names it once). `haven_x402_authorize` before: eight lines — the three this PR rewrites plus five that are correct history and are left alone (`tools.ts:547`, `strict-tool-input.test.ts:292/300`, `sdk/src/tool-descriptions.test.ts:50`, `docs/architecture/08-local-vs-hosted-mcp.md:111`, and the immutable #2349 shard). After: those five only. `haven_list_transactions`: history only, same five files minus the SDK test. `HostedToolNameLegacy` / `legacyToolSchemas`: zero hits, exit 1, against the control above.

  **No consent surface moves.** `packages/signer/src/consent.ts` hashes identity + tool names + `SIGNER_CONSENT_SURFACE_VERSION`; none of those read a JSDoc. No operator is re-prompted.

  **Both parent docs this shard clears were read, not merely cleared (#2323/#2367), and the one contract doc the shard does NOT clear was re-verified in scope.** `docs/architecture/04-x402-payment-sequence.md` (`covers: packages/signer/src/core.ts`, shard-satisfied): its decomposed sequence diagram already names `haven_pay_x402_quote { payment_required }` as the hosted step that yields `{ payment_id, payload_hash, x402.expected }`, and its #1189 note says the signer reconstructs the resource/merchant context from the value Haven SIGNED — both consistent with the corrected JSDoc; accurate, not edited, `last-verified` not bumped. `docs/operations/mcp-runtime-compatibility.md` (`contract: true`, `covers: packages/signer/**`, no `satisfied-by:` convention, so the strict gate BLOCKED on it): its only `core.ts` citations are in § *Signer / hosted-MCP version skew* — `SUPPORTED_X402_EXPECTED_VERSIONS`, `assertSupportedBindingVersion`, `HavenUnsupportedSignerVersionError` — the version handshake, which this change does not touch; that section was re-read, nothing in it names which hosted tool sets the two fields, nothing edited, and its `last-verified` chain is prepended per #1496 with the prior note preserved byte-verbatim (asserted programmatically: the new line ends with the old comment body, 384,171 bytes, and every other line is identical). `casp-risk-guardrails.md`: its § *What `covers:` must span* is the gate and `packages/signer/**` is inside it; this change adds no glob, moves no boundary, and touches no Red Line or Product Copy rule — no edit, `last-verified` deliberately not bumped because the document was not re-verified as a whole. `docs/architecture/07-edge-signer.md` (`covers: packages/signer/**`, advisory): its two decomposed-flow diagrams already name `haven_pay_x402_quote -> { payment_id, payload_hash, x402.expected }` as the hosted step, which is exactly what the corrected JSDoc now says — accurate, not edited, `last-verified` not bumped. `packages/signer/README.md` (governed in the manifest, advisory): its bridge section names the same tool and the same five refusal fields ("amount, merchant recipient, resource URL, token asset or network"); read for this claim only, accurate, not re-verified as a whole, `last-verified` not bumped — the manifest edit is to the historical #2243 note, not a verification of the README.

  Perimeter unchanged; a JSDoc that explained the signer's two header-refusal fields by reference to a deleted alias now names the tool and the checks that actually set and consume them.
