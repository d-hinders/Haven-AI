- **#2353 (switch)** — **The last permissive money-path tool now refuses an undeclared argument. `haven_complete_mcp_tool` moved from `PERMISSIVE_INPUT_TOOLS` to `STRICT_INPUT_TOOLS`, and the shipped guidance it was held back for has already been corrected and shipped to npm.** No route, schema shape, migration, signing scheme, allowance, custody relationship or settlement path changes. What changes is refusal behaviour on ONE tool: an argument the tool does not declare (the defect case is `payment_required`) is now REFUSED at both enforcement layers instead of silently stripped, on a tool whose merchant call spends against a 402 Haven rehydrates from the payment record (by `payment_id`, since #1307). The advertised JSON Schema is unchanged — every hosted schema already said `additionalProperties: false`, so permissiveness was a contract mismatch, not an honest option (#2312's finding).

  **Why the switch is safe now, and what it waited for.** #2359 (2026-09-01) fixed the shipped `SKILL.md` in both byte-pinned copies (`packages/sdk/src/skill-content.ts` + its frontend twin) — the skill now says `payment_id` + `payment_header` ONLY and names `payment_required` as a field the tool does not take — and deliberately did NOT flip the tool, because the skill is auto-installed and an unknown number of agents carried the old copy on disk. The propagation condition the code comments named is met and measured: `@haven_ai/sdk@0.1.34-alpha.0` (which carries #2359's corrected skill through the connect install path) was published 2026-09-01T19:21:21Z on the `alpha` dist-tag that `npx @haven_ai/connect@alpha` resolves (checked live against the npm registry, not remembered). ~44 hours of propagation at claim time.

  **Both enforcement layers were switched, not just the registration.** Found by the direct-embedder path going red, not by reading: the transport-level registration (`toolInputSchema` → `.strict()`) refuses for MCP callers, but the `haven_complete_mcp_tool` handler still called permissive `parse`, so an embedder importing `createToolHandlers` directly had the key silently stripped and the call proceeded (the test observed the handler RUN — `API_ERROR` from the stub's empty rehydration answer — where every other strict tool refuses with `INVALID_INPUT` before the handler). The handler now calls `parseStrict` like every other strict tool; both layers read their refusal text from the one declaration in `STRICT_INPUT_TOOLS`, so they cannot drift. The refusal names `payment_required` and states where the value actually comes from (the payment record by `payment_id`), so an agent holding the old skill copy is told what is wrong and where the 402 really comes from, rather than receiving a success that pinned nothing.

  **The two pins that held the halves apart were flipped deliberately, with the reversal recorded where they live.** `strict-tool-input.test.ts`'s `#2353` block was a characterization test of the silent strip (its header said the switch "goes red and whoever flips it has to come here and say so" — this is that). It now asserts the refusal over the real transport (`Client` → `InMemoryTransport` → `buildHostedMcpServer`) using the EXACT argument shape the old skill instructed, plus the new other-half assertion (the same call WITHOUT the undeclared key still reaches the handler), keeping the declared-arg-survives control. The `#2312` CONTROL drops `haven_complete_mcp_tool` from its permissive list (two tools remain, both `{}`-schema), and #2363's premise block — which existed because the comment's premise changed under it once before — is re-based with the switch recorded in its own header. The anti-vacuity membership pin counts 20 strict / 2 permissive. Mutation-proven in the red direction too (below).

  **Residual risk, stated rather than implied away.** An agent still carrying a PRE-0.1.34 auto-installed skill copy sends `payment_required` and now meets a hard refusal where it previously met a silent success. That is the intended change — the refusal is actionable (names the key, names the rehydration) and the old behaviour was the defect (success while spending against a 402 the caller never saw) — but it IS a behaviour change on the documented flow for that population, which is why the propagation evidence is part of the record and why the refusal message is self-explanatory. Auto-install happens on connect runs; an agent connected BEFORE 2026-09-01T19:21Z that has not re-run connect carries the stale copy until it does.

  **Scope of the check behind "nothing else goes stale".** Searched (`/usr/bin/grep`, control >0 before any zero believed) for every place asserting the tool's permissiveness or the 19/3 split: `STRICT_INPUT_TOOLS`/`PERMISSIVE_INPUT_TOOLS` comments and their doc block (tools.ts — rewritten: the exclusion bullet is kept under its heading with the reversal recorded in place, so a reader arriving from the #2312 story finds it where they would look); the two pins in `strict-tool-input.test.ts` (flipped, with the history in their headers); `docs/architecture/08-local-vs-hosted-mcp.md` (paragraph rewritten + counts 20/2); `docs/architecture/06-hosted-mcp-connect-flow.md` (strictness paragraph + counts); `docs/operations/mcp-runtime-compatibility.md` (strict-input Troubleshooting bullet, with the operator-facing pre-0.1.34 diagnosis); `docs/product/copy-guidelines.md` (the § Enforcement #2353 bullet's "pins the strip itself" — found by the in-session reviewer sweep as a present-tense claim this PR falsifies; corrected to "pins the refusal" with the flip recorded). The shipped skill copies, `skill-content.test.ts`, and the frontend parity suite are untouched and still green — the guidance was already correct.

  **Mutation-proven, judged on process exit code, restored from `cp` backups and verified byte-identical (`sha256sum` + `diff -q`) rather than by inverting the edit.** (1) Removing `.strict()` from the registered schema for this tool alone → the transport refusal tests go red while the declared-args test stays green. (2) Reverting the handler `parseStrict` → `parse` → the direct-embedder refusal test goes red (`INVALID_INPUT` expectation fails on the strip). (3) Reverting the `#2349` membership count → red. All restores verified byte-identical; the shipped-skill copies were never touched by any mutation.

  Perimeter unchanged: no route, schema, migration, signing scheme, allowance or settlement behaviour moves; what moves is one tool's parse discipline (permissive → refuse) plus the comments, tests and three docs that described the permissive state. The guidance correction that makes the refusal honest shipped separately (#2359) and is already in the field.
