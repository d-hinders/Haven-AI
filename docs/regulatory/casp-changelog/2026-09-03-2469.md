# 2026-09-03 — #2469: safe-retirement: rename ChainRail off the retired-rail name, and delete the four dead x402-authorization exports (epic #1440)

Three changes, none behavioural. (1) `domain/chain-client.ts`'s client-selector type
`ChainRail = 'allowance_module' | 'delegation'` is renamed `ChainClientImpl = 'ethers' | 'viem'` —
it always selected which SDK backs a `ChainClient`, never an execution rail — with the factory in
`infra/chain/index.ts` and the two live call sites (`routes/balances.ts`,
`modules/x402/helpers.ts`) updated to match; the same client is returned for the same caller as
before. (2) Four zero-importer exports are deleted from
`infra/repositories/x402-authorizations.ts` (`refreshStaleX402Intent`,
`REFRESH_STALE_X402_INTENT_SQL`, `RefreshedX402IntentRow`,
`findActiveX402IntentByIdempotencyKey`) together with the real-DB test blocks that were their only
callers and the `db-schema-smoke.ts` PREPARE entry for the deleted SQL constant. (3) The
`getRelayer` docstring in `infra/relayer.ts` drops its "submits Safe deploys" claim (deploys were
deleted by #1988) and now names the four jobs the relayer actually submits, matching the wording
PR #2470 already shipped for the same sentence in `.env.example`/`.env.dev.example`;
`execTransaction` survives via `POST /safe/exec` and the per-chain-isolation argument is kept.

Perimeter check against the CASP guardrails: **the perimeter is unchanged** — checked, not
assumed: this diff contains no edit to `.github/money-path-globs.json` (no glob added or
removed), and every touched file was already inside existing globs
(`infra/relayer*.ts`, `infra/repositories/**`, `modules/x402/**`). No runtime behaviour differs:
one type rename (the ethers-vs-viem selection is identical — the factory's mapping is pinned by
the updated factory test), one dead-code deletion of functions nothing reachable imported
(verified against both relative and aliased import forms, excluding tests and `dist/`), and one
docstring trim. No money moved, no spend authority granted or revoked, no signing scheme touched,
no new credential surface, and no existing money-path behaviour is modified, so no
characterization test is required. The deletion is pure legacy-rail residue: the refresh SQL's
guard predicate (`allowance_nonce <> $1`) was meaningless since the column became write-only-zero,
and the AllowanceModule rail that gave it meaning answers 410 on every entry point (#1986).
