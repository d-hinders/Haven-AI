- **#2472** — **the browser-hosted manual credential fallback can now reach the existing owner-signed budget approval instead of waiting forever for a local connector report it cannot produce. The authority boundary is unchanged.**
  Registration records `install_status.manual_credential_fallback: true` only
  for the browser fallback (`browser-manual-fallback` with no runtime-config
  write capability). The frontend uses that marker only to present the normal
  in-modal budget-approval step and labels the state as a manual credential,
  never as an automatically configured runtime. The one-time private signing
  key remains generated in and held by the browser/user's trusted agent
  workspace; Haven receives the public signing address, proof, API-key hash,
  and this non-secret setup marker only.

  **CASP argument.** The marker does not activate an agent, create a
  delegation, alter an amount/token/recipient/expiry, verify a user signature,
  or make an API key sufficient to spend. Activation remains the existing
  sequence: the owner signs the on-chain budget delegation, Haven confirms the
  matching delegation, then the pending agent becomes active. Until then the
  key remains `pending_approval` and is refused by the normal authentication
  gate. No database schema, key custody, signer, payment intent, relay, or
  revocation path changes. Red Lines 1–5, *Make All Agent Authority
  User-Approved*, and the Payment-Related Merge Checklist were reviewed against
  this diff; the perimeter and enforcement remain unchanged.
