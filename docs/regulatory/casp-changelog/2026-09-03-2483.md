- **#2483** — Served instruction prose only. One sentence in the dashboard-generated setup prompt is strengthened from *the connector also supports a `--json` mode* to *if you are an AI agent running this command yourself, you should append `--json`*, and one new sentence is added: when a `--json` outcome reports `approval.required: true`, the agent's first action must be to relay the budget-approval instruction to the user before verification or any restart step, with the restart named as a separate later instruction (#1542's one-gate-at-a-time discipline). Motivation is a 2026-09 Codex field test in which the connector's approve-budget CTA printed at the right moment but sat inside a blocking prose-mode tool call, so it reached the user only after the 3-minute approval wait had already elapsed; `--json` skips that wait and returns promptly, which is the machinery the guidance now points at rather than changing. **Nothing executable changed.** The touched backend file's only edits are inside the `buildSetupPrompt` string builder and its comments — no route handler, no validation, no auth, no credential handling, no signing surface, no schema, no delegation or caveat construction. The pasted command itself is byte-identical and the consent enumeration it carries is unchanged, so no new approved action is licensed. The permitted-changes rule is verbatim untouched: appending `--json` was already one of its two permitted changes (#1719), so this makes a permitted change *recommended* and licenses no third one. What may be spent, by whom, and under which caveats is untouched; the approval gate itself is unmoved — an agent still starts `pending_approval` with zero spend authority until the owner signs a budget delegation, and this change only affects how promptly the agent tells the owner to go and do that. Perimeter unchanged. Reaches dev on deploy and production on the next `dev → main` promotion; no npm release, as the prose is backend-served.
