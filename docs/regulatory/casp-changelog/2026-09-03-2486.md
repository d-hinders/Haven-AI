- **#2486** — Served instruction prose only. The dashboard-generated setup prompt's closing sentence — *When the connector finishes, tell me to return to Haven to approve the budget.* — is replaced by a sentence scoped to prose-mode runs: if the command was run without `--json`, the connector waits for the approval itself and prints its next steps, and the agent relays the budget-approval ask only if those printed steps still ask for it, with the already-approved case named as nothing to approve. The predecessor was unconditional, which was wrong twice over now that #2483 recommends `--json`: under `--json` "the connector finishes" and "the outcome reports `approval.required: true`" are one event, so an agent obeying both sentences relayed the same ask twice in one reply; and where no approval is needed at finish (a re-run against an already-approved agent, a re-key) it still demanded one. The #2483 `--json` relay sentence is verbatim untouched. The e2e fixture mirror of the prompt (`packages/frontend/e2e/fixtures/haven-api.ts`) is brought back into line — it was also missing the network-access sentence, older drift — and a new backend test pins the mirror line-for-line to `buildSetupPrompt`'s output so the class cannot recur. **Nothing executable changed.** The touched backend file's only edits are inside the `buildSetupPrompt` string builder and its comments — no route handler, no validation, no auth, no credential handling, no signing surface, no schema, no delegation or caveat construction. The pasted command is byte-identical, the consent enumeration it carries is unchanged, and the permitted-changes sentence (#1719) is verbatim untouched, so no new approved action and no third command modification is licensed. Attribution holds: the sentence tells the agent to relay an instruction to the owner, and the owner approves the budget in Haven — Haven relays, it approves nothing. What may be spent, by whom, and under which caveats is untouched; the approval gate itself is unmoved — an agent still starts `pending_approval` with zero spend authority until the owner signs a budget delegation, and this change only affects whether the agent asks for that approval once, and only when it is still needed. Mutation-tested: re-introducing the unconditional sentence, dropping the prose-mode condition, and removing a line from the fixture mirror each turn the new tests red (`agent-connection-setups.test.ts`: the single-ask-per-mode test and the fixture-parity test). Perimeter unchanged. Reaches dev on deploy and production on the next `dev → main` promotion; no npm release, as the prose is backend-served.
