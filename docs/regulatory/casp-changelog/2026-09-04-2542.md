- **#2542** — `GET /health` now exposes only process/database liveness, while cached relayer balances, passport readiness, and proxy topology move to `GET /health/ops`, which is absent unless an operator configures `HAVEN_OPS_TOKEN` and is then protected by a constant-time token comparison. The reconciliation script presents that token only to the private route and fails before making the diagnostic request when it is missing. This narrows public operational disclosure; it changes no signing key, payment intent, transaction, budget, delegated authority, relay execution, or custody boundary. Focused API tests cover the public 200/503 shapes and operator token cases. Perimeter unchanged.
