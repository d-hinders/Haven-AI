- **#2616** — test-harness only. `db-harness.ts` gains `assertWorkerSchemaAtHead()`,
  the eight migration test files call it in `afterAll`, and
  `069_drop_safe_approver_metadata.test.ts` moves its two `up()` restores into
  `finally`. No production code path is touched: no route, no authority, no
  custody surface, no signing, no money movement — the diff is a test helper,
  test files and documentation. The defect it addresses is a TEST-INFRASTRUCTURE
  one with a compliance-adjacent cost worth naming: a migration test's `down()`
  that never reached its `up()` left the persistent worker schema off migration
  head, so the next run's first assertion failed and then healed itself,
  presenting as a flake. A suite whose red is dismissed as noise is a suite
  whose green means less, and the money-path tests share that harness. The guard
  is mutation-proven rather than asserted, on the two files whose leak was
  demonstrated — `069_drop_safe_approver_metadata.test.ts` and
  `agents-allowances-retired.test.ts`: leaving the schema off head deliberately
  makes the owning file fail with the table named, and each file was restored
  byte-identically afterwards (verified with `cmp`) with the drift the mutation
  caused dropped from the affected worker schemas. Stated as those two rather
  than as all nine, because the other seven were not individually mutated. The
  guard is registered so that vitest's reverse hook ordering runs it LAST, after
  a file's own cleanup: the question it answers is what a file LEAVES, so a file
  that repairs its own drift stays green by design. Perimeter unchanged.
