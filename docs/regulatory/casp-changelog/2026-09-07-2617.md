- **#2617** — Discovery and agent-facing copy only: `GET /discovery` gains a
  required `cli_package` field (`@haven_ai/cli@${config.connectorChannel}`,
  derived beside `CONNECTOR_PACKAGE` from the same channel constant at the same
  import in `packages/backend/src/routes/agent-connection-setups.ts`) and the
  capability manifest's `packages.cli` mirrors the connector entry's
  `channel`/`one_liner` shape from it. The runbook (`/for-agents.md`,
  `haven guide`), the onboarding prompt and the CLI README name every CLI
  invocation as `@<channel>` with the tag read from
  `/.well-known/haven.json` (`packages.cli.channel`) — a bare
  `npx @haven_ai/cli` resolves to `latest`, which nothing binds to the build a
  deployment describes, so an agent following untagged prose could install an
  older build. No custody, signing, key-handling, or payment-execution change:
  the CLI remains construct-and-hand-off (it prints links; the human signs
  every budget), the manifest and discovery document stay read-only discovery
  artifacts serving only already-public facts, and every changed sentence
  keeps the runbook's invariants — the human keeps every signature, the agent
  never asks for a password, the delegate key never leaves the machine. The
  promotion checklist gains a release precondition for docs-visible CLI
  changes so the tagged builds the docs name actually exist. Perimeter
  unchanged.
