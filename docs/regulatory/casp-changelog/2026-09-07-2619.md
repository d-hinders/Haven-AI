- **#2619** — Discovery and hand-off copy only: the capability manifest's
  `chains.supported` changes from bare ids to entries carrying `name`,
  `explorer_url` and `usdc_address`, every fact read from `@haven_ai/core`'s
  `CHAIN_REGISTRY` — the same registry the backend's own chain configuration
  derives from (`packages/backend/src/domain/chains.ts` builds `CHAINS` from
  `Object.values(CHAIN_REGISTRY)`), so the manifest and the deployment cannot
  disagree, and an id the registry does not know is dropped rather than
  guessed (a shorter list, never an unsourced fact; the static `deployable`
  half still reports it). The manifest gains `dashboard.funding: '/dashboard'`
  (the funding card is rendered by `DashboardClient` on that authenticated
  page, verified) and `attribution: { query: 'via=agent', purpose }` — the
  funnel marker the runbook's hand-off scripts already use, now readable in
  the data an agent's code reads. The funding hand-off script in the SDK
  guidance, the CLI runbook and `for-agents.md` names that page instead of
  "the dashboard shows the same", and the 402 page's signup link moves from
  `/?src=402` to `/signup?next=/agents&via=agent&src=402` — the same signup
  flow the runbook hands off, with the attribution query added. No new route,
  no new authority, no custody surface, no signing path: the manifest is a
  read-only discovery artifact, the attribution field is a funnel tag, and
  every changed sentence stays inside the Product Copy Rules (the copy still
  says the budget is the user's signed delegation, the key is made on the
  agent's machine, and nothing can be spent before the human approves). The
  new vitest suite pins the sourcing rather than the values: every supported
  entry is asserted equal to the registry's own `name`/`explorerUrl`/
  USDC-family token address with no literals in the test, an unknown id is
  asserted dropped, `dashboard.funding` is asserted to sit in
  `AUTH_MARKED_PREFIXES` (a named path must be a surface that actually
  answers), and the attribution marker is asserted equal to the one the
  runbook's scripts carry. Perimeter unchanged.
