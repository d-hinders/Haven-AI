- **#2621** — test-harness only. `db-harness.ts` gains `withMigrationReverted(revert, body, restore)`, and the five migration test files that still hand-drove `down()`/`up()` pairs with the assertions in between (`059_retire_mpp_demo_catalog`, `062_normalize_price_display`, `071_drop_allowance_nonce_watermarks`, `072_payment_intents_settlement_indexes`, `073_drop_x402_resource_tables`) route every site through it, which is the shape `070`/`075` already used inline. No production code path is touched: no route, no authority, no custody surface, no signing, no money movement. The migration modules themselves are not edited; the helper receives each migration's own `up`/`down` exports as closures, so a call site cannot drift from what the runner actually runs. The defect is the #2616 class in the files #2616 deliberately left: an assertion failing (or a `-t` filter selecting the reverting test without the restoring one) between a revert and its restore left the persistent worker schema off migration head while `schema_migrations` still read as applied, presenting as a self-healing flake on the NEXT run. The helper's `finally` is what closes the route. The proof is mutation-based, stated per class rather than as a blanket claim: **071** (guard-visible) — suppressing the restore under `-t` makes the afterAll guard name the file and `allowance_nonce_watermarks`; a body that throws leaves the table absent in the database afterwards (the `finally` ran), guard silent. **073** (guard-visible) — its registered `afterAll(up)` net heals before the guard's own registered one runs (the #2625 ordering, documented in the file), so the proof is a double-blind: helper restore and net suppressed together under `-t`, and the guard names the file plus `x402_resources` and `x402_receipts`. **072** (guard-INVISIBLE) — suppressing the restore under `-t` leaves both settlement indexes verifiably gone from `pg_indexes` for the worker schema while the run reports green and the guard says nothing: a `readSchemaShape()` table-name diff cannot see index drift, the #2625 shape-blindness arriving through the index door. For this file the helper is the only defense, which is why its green cannot be claimed from the guard. **059/062** — data-only in both directions (bare `UPDATE`s on `merchant_catalog`), so they cannot put the schema off head by construction; the helper there is the uniformity of the rule plus exception safety, and they were not mutated because there is no drift for a mutation to produce. Every mutated file was restored byte-identically, verified by hashing the working copy against the `git` blob. All drift the mutations caused was confined to worker schema `test_w1` (every proof ran single-file) and repaired there with the two migrations' own idempotent `up()` statements (`CREATE INDEX IF NOT EXISTS`, `DROP TABLE IF EXISTS`). Perimeter unchanged.
