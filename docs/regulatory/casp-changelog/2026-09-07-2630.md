- **#2630** — Boot-time warning added when `TRUST_PROXY_HOPS` is unset (`parseTrustProxyHops` in `packages/backend/src/config.ts`), reusing the `warnPublicRpc` (#2615) house pattern: branches on the RAW value, so a deliberately-set value — including an explicit `TRUST_PROXY_HOPS=0` — stays silent, while unset/empty now warns loudly that the proxy stays untrusted and the per-IP auth rate limits (signup, login, device_start, device_lookup, device_token) stay disarmed. No new authority, custody, route, or on-chain surface: this is an observability-only change to a request-origin/rate-limit input, not to spend authority, and moves no funds. `config.trustProxyHops` was already wired through `parseTrustProxyHops` before this change; the wiring is unchanged, only the unset branch gained a `console.warn`. Mutation-proven: reverting the `config.trustProxyHops` call site to a bare `Number(process.env.TRUST_PROXY_HOPS) || 0` turns `config-trust-proxy-wiring.test.ts` red (1 failed), and restoring the call site turns it green again — verified by running both directions on this branch. Rest of #2630's acceptance (establishing the production hop count, setting the Railway variable, and confirming `GET /health/ops` on production) is operator-verify and left open on the issue; no code in this PR performs or claims any of those steps. Perimeter unchanged.
