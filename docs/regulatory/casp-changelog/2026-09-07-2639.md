- **#2639** — documentation restructure; **comment-only** in the one perimeter
  file it touches. `CLAUDE.md` is rewritten as an operating manual and its
  history moved to `docs/archive/decision-log.md`; `AGENTS.md` is deduplicated
  against it. The diff reaches `packages/backend/src/rails/allowance-module.ts`
  — a runtime glob in `.github/money-path-globs.json` — for **one JSDoc
  sentence** that cited `CLAUDE.md` as the record of the module's three
  surviving reads-only exports; it now cites the decision log, which is where
  that record moved. No executable line changes: no route, no authority, no
  custody surface, no signing, no money movement, no export added or removed.
  The module's export roster is asserted mechanically in
  `packages/backend/src/testing/__tests__/mock-factory-exports.guard.test.ts`
  (`getProvider`, `getRelayerWallet`, `getTokenBalance`), and that assertion is
  untouched and still green — so the compliance claim the comment describes is
  carried by the test, not by the prose either before or after this change.
  Two adjacent comment edits in the same class: the same guard test's own
  narrative sentence and `scripts/ci/queue-framing-census.test.mjs`'s worked
  example, both of which named a `CLAUDE.md` section this change renames.
  Perimeter unchanged: no glob added, removed or repointed.
- **Retirement claims relocated, not rewritten.** The Safe-rail retirement
  sequence (#1440 → #1984 / #1986 / #1987 / #1988 / #1989 / #2020 / #2055 /
  #2413) and the session-rail retirement (#834) move verbatim or near-verbatim
  from `CLAUDE.md` into the decision log, with #2259's closure of the last
  retired-rail activation path added to that list. `casp-risk-guardrails.md`
  keeps the regulatory framing and the dates it must carry; nothing in it is
  edited by this change, and no retirement claim changes meaning. **It was
  cleared here BY THIS SHARD with its body not re-read by the gate (#2323), so
  the body was read by hand instead, and that read found a pre-existing
  staleness the shard would otherwise have buried: `:360`'s closure list stops
  at #2020 and calls legacy accounts "readable", which #2413 made an
  overstatement. Filed as
  [#2669](https://github.com/d-hinders/Haven-AI/issues/2669) rather than folded
  in, because correcting a `contract: true` CASP doc deserves its own read.**
  Recorded here because this shard is what the next shard-clearer reads.
- **One correction of a false claim, disclosed rather than folded in.**
  `CLAUDE.md` stated that `remove_passkey` "still refuses to drop below two, on
  every chain". That has been false since PR #1420 closed #1199 on 2026-08-14:
  `encodeSignerAction` refuses `remove_passkey` only at
  `passkeys.length === 1 && !owner.config.ownerAddress` and `remove_owner` only
  at `passkeys.length === 0` — both mirroring the account's on-chain
  `CannotRemoveLastSigner`, neither imposing a Haven-side ≥2 floor. The
  corrected sentence and a dated log entry record it, and
  `packages/backend/src/rails/hybrid-signer-actions.ts` is added to
  `CLAUDE.md`'s `covers:` so the next divergence is implicated. **No code
  changed** — the signer floor is exactly what it was before this PR; only the
  description of it is now accurate.
