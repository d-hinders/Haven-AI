- **#2669** — Documentation accuracy only, no perimeter move and no behaviour
  change: `casp-risk-guardrails.md`'s Safe-rail retirement paragraph (:360)
  narrated a closure list that stopped at **#2020** and described legacy
  accounts as staying "user-owned and readable". Two defects in one sentence
  pair. First, **#2055** (approval queue deleted outright — `/approvals`
  deregistered, migration 070 dropped `approval_requests`) and **#2413** (the
  dashboard stops rendering retired-rail accounts; six list queries filter to
  `account_type = 'delegator_hybrid'`; `RetiredRailNotice` deleted with every
  `account_type` branch behind it) were absent from the sequence. Second,
  unqualified "readable" had been an overstatement since #2413's owner decision
  of 2026-09-02 that retirement is deletion rather than accommodation: the rows
  are untouched and a direct database query still finds them, but no Haven
  surface displays them, and the sentence read as a product-surface guarantee.

  **The custody posture is unaffected, which is why this is an accuracy defect
  and not a compliance failure.** Nothing about who controls funds changed, no
  route changed, and the surviving `POST /safe/exec` clause in the same
  paragraph was and remains correct — an owner still signs and Haven still only
  relays for gas. What was wrong is the description of what a legacy account
  holder can still *see* through Haven, which is the sentence a regulator-facing
  reader would use to answer exactly that question.

  **Figures re-derived at this commit rather than copied from CLAUDE.md.** The
  "six list queries" are named, not counted from raw grep hits (10 occurrences
  of the predicate across 4 files, of which six are list queries):
  `LIST_SAFES_FOR_USER_SQL` and `LIST_SAFES_WITH_ACCOUNT_TYPE_FOR_USER_SQL`
  (via `DELEGATION_RAIL_ONLY`, `infra/repositories/user-safes.ts`),
  `LIST_SESSION_SAFES_FOR_USER_SQL` (same file),
  `LIST_AGENTS_FOR_USER_ALL_STATUSES_SQL` (`infra/repositories/agents.ts`), and
  `LIST_DASHBOARD_SAFES_SQL` + `LIST_DASHBOARD_AGENTS_SQL`
  (`infra/repositories/dashboard.ts`) — three account lists, the agent list, two
  dashboard lists, as CLAUDE.md says. `RetiredRailNotice` returns **0** hits in
  `packages/frontend/src`, on an instrument whose positive control (`Card`, 59
  files) was run first and found what it should.

  **How it survived, recorded because the mechanism outlives this fix.** The
  #2274 shape (PR #2322): a `contract: true` doc cleared by an added
  `satisfied-by` CASP shard, where the clearing pass reads for *contradiction*
  with the diff rather than for *completeness* against the world. #2323 made the
  coupling gate report such docs in their own section for this reason; the
  report gets you a reader, not a complete one. This shard is itself that shape,
  so the parent paragraph was re-read in full rather than diffed.

  **Perimeter:** unchanged. `.github/money-path-globs.json` is not edited. This
  PR is money-path by the file half only — `casp-risk-guardrails.md` is a
  `controlGlob`, so a human reads it and the `qa-freshness` re-run is
  deliberately excluded, because re-running the money-flow harness proves
  nothing about a documentation correction.
