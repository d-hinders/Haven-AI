- **#2687** — Comment, test-name, copy and dead-code hygiene; **no perimeter
  move, no route, no authority change, and no behavioural line altered.** The
  only altered non-deleted executable lines are one rendered string (the
  account-remove dialog) and one test name; everything else is comments or a
  deleted unreferenced helper.

  **Why it touches `covers:`-mapped files.** Two hunks are in packages this
  document covers: `packages/sdk/src/types.ts` (a JSDoc sentence on the #1306
  `execution_rail` reporting field) and `packages/connect/src/rekey.ts` (a JSDoc
  on `assertRekeyable`). Both described the retired AllowanceModule rail as
  **"import-only"**, which has been false since **#1984** closed every inflow —
  deploy *and* import answer 410 — and **#1986** fail-closed spending. Neither
  field's behaviour is touched.

  **Parent hand-read, not shard-cleared (#2323).** The gate reports a
  shard-satisfied parent as "body NOT re-read", and the shard's author is the
  change's author, so the sections were opened rather than assumed: the `#1306`
  entry, where the `execution_rail` reporting field's regulatory
  characterisation lives, and the Safe-retirement paragraph as corrected by
  #2669. Both remain accurate — `#1306` describes the field as "reporting only …
  the on-chain policy remains the actual gate either way", which a JSDoc edit
  cannot move, and the retirement paragraph already carries the #2055/#2413
  closures and the transactions-picklist exception. **Neither needed an edit,
  and that is the finding rather than an omission.**

  **Classification.** Not money-path by the file half:
  `node scripts/ci/money-path-classify.mjs` reports **0 runtime-glob +
  0 control-glob** on the perimeter above a passing self-test.
  `packages/sdk/src/signer.ts` and `packages/signer/**` are the SDK-family paths
  on the perimeter; `types.ts` is not one of them, and `packages/connect/**` is
  not either. The issue carries no `money-path` label. The CASP playbook applies
  because the coupling gate implicates this document, not because the diff
  reaches the perimeter.

  **One deliberate exclusion, recorded so the next sweep does not "fix" it.**
  `packages/backend/src/modules/agents/rekey-guards.ts:59` carries the same
  "import-only" phrase inside a **block quote of the #1694 owner decision**.
  Quoting a dated decision as of its date is not a stale claim; rewriting the
  quote would falsify the record. `haven-reviewer` reached the same conclusion
  independently on the predecessor PR.

  **Provenance.** This work was built for #2673 and is filed under #2687
  because #2673 shipped in parallel as PR #2684 while this branch was in
  review. #2684 swept the "remains readable" phrase family; these sites are a
  different false claim about the same retired rail and were outside its scope.
  The overlapping half of this branch was discarded rather than merged — #2684
  is the better change on it, having deleted the `canIssue` and
  `canConnectAgents` props outright.
