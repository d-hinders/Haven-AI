- **#2667** — Boot-time warning added when `TRUST_PROXY_HOPS` is EXPLICITLY set to a value that resolves to 0 (`parseTrustProxyHops` in `packages/backend/src/config.ts`): the branch keys on the RESOLVED hop count (after quote/whitespace stripping and the integer check), so every spelling of the disarmed state — `'0'`, `'"0"'`, `'00'`, `'-0'` — warns, with its own message, distinct from both the unset warning (#2630) and the invalid-value warning. This closes the gap where `TRUST_PROXY_HOPS=0` reproduced exactly the state #2630's unset warning exists to flag — the per-IP auth rate limits (signup, login, device_start, device_lookup, device_token) return no limit at all at 0 hops (`middleware/rate-limit.ts`) — while booting silent, because #2630's raw-value branch treated any SET variable as a deliberate configuration. The issue's own analysis (carried into the function's JSDoc) is why an explicit 0 is not respected the way #2615 respects a deliberately-set public RPC endpoint: `0` is not a configuration of the rate-limit tier, it is the absence of one, and there is no deployment where it is the intended posture. Boot behaviour is UNCHANGED — every path still returns 0 and nothing throws; the issue's option 3 (refuse `0` in production) is deliberately NOT taken, per the #2615 item-3 owner precedent that fail-open is an operator decision, not a warning PR's. Mutation-proven in both directions on this branch: removing the explicit-0 branch (0 resolves silently again) turns `config-trust-proxy.test.ts` red (the inverted #2630 silence pin and the pairwise-distinctness proof fail) and `config-trust-proxy-wiring.test.ts` red (the module-load 0-warning test fails); restoring the branch byte-identical (sha256-verified) turns both green again. The three warning messages are pinned pairwise-distinct (each matches its own sentinel fragments, none matches a sibling's), so collapsing any two messages into one reddens the proof. No new authority, custody, route, or on-chain surface: observability-only change to a request-origin/rate-limit input, moves no funds. Perimeter unchanged.
