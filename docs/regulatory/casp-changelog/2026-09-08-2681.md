- **#2681** — Retired the documentation-only `verified:` front-matter chain
  and its chain-integrity scripts. Every removed source block is preserved
  byte-for-byte in the combined archive, hash-checked by
  `scripts/docs/retire-verified-chains.mjs`; current docs retain only their
  `last-verified` date. This changes no payment route, merchant call, signing
  payload, key handling, budget, delegation, Safe permission, database state,
  or on-chain behavior. `validate-frontmatter.mjs` is a money-path
  **control-glob** because the coupling gate imports it, not because it can
  authorize payment; this change keeps that parser's required front-matter and
  covers validation intact while refusing a newly reintroduced retired block.
  Perimeter unchanged.
