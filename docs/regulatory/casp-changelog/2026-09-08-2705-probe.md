Measurement probe for #2705, never intended to merge. The diff is two comment
lines appended to `packages/backend/src/db/migrations/075_drop_inert_safe_rail_schema.ts`;
no schema statement, no exported symbol, no runtime behaviour and no migration
semantics are touched, and the file's `up`/`down` bodies are byte-identical to
`dev`. It exists solely so the `.github/CODEOWNERS` pattern
`/packages/backend/src/db/migrations/*.ts` matches, which is the only way to
observe whether GitHub requires a code-owner approval on that path: the reading
needs a gated pull request with green checks and no approving review, and every
such pull request in the repository's recent history carried an approval before
merging.

This shard exists because the contract-doc coupling gate implicates
`casp-risk-guardrails.md` on any change under `db/migrations/`, which is correct
and is itself a finding worth recording: a migration pull request cannot go green
without a CASP analysis, independently of the code-owner question #2705 asks.

Custody, authority and settlement are untouched — no rail, enforcer, delegation,
signer, relayer or route is in the diff. Perimeter unchanged.
