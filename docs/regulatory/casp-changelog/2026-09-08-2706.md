- **#2706** — An over-budget payment on the EIP-3009 x402 funding leg now refuses with the SAME typed 403 body the erc7710 branch has answered since #2082 — `error_code: "delegation_budget_exceeded"`, `phase: "insufficient_funds"`, `next_action: "fund_safe_or_raise_allowance"`, plus `remaining`/`amount`/`shortfall` in human and atomic forms — instead of the untyped 502 wrapping the raw viem/bundler simulation dump. The change is a NEW fail-fast remaining-budget pre-check in `packages/backend/src/modules/x402/delegation-authorize.ts`, placed immediately before `prepareDelegationPayment` in the funding-shape branch and reusing #2082's instrument (`readRemainingBudget`), selection (`selectDelegation(agent, token, payTo)` — the same call the prepare repeats), comparison (`remaining < amount`, exact remainder spendable), and refusal body field-for-field; the one field that differs is `merchant_address`, which carries `merchantPayTo` because on this leg `payTo` is the funding target, not the merchant. AUTHORITY/CUSTODY ARGUMENT: this widens nothing — the ERC20PeriodTransferEnforcer in the funding delegation's caveat stack remains the on-chain gate and still reverts an over-budget redemption inside `prepareRedemption`'s gas estimation; the pre-check can only refuse earlier, never admit what the chain would refuse (the comparison is on a live enforcer read, `<` not `<=`), and its fail-open posture is #2082's verbatim — a degraded read (`fromChain: false`), a thrown read, or an unparseable one all mean "no usable measurement" and proceed to prepare, so a transient RPC outage cannot become a stopped agent; a null selection skips the read so the existing no-fundable-delegation 403 keeps its own wording. The 502 REMAINS for genuine bundler/infrastructure failures — collapsing an outage into the budget taxonomy would tell a retrying agent to ask its owner to raise a budget that is fine. No new authority, custody, route, or on-chain surface: a refusal-shape change on an existing pre-funding path, moves no funds. Characterization-first per money.md: commit bdc62d0c pins the 502 world before the fix commit flips it; mutation-proven in both directions — the pre-fix source AND a branch-removal mutant each redden exactly the 3 new refusal tests in `x402-delegation.test.ts`, and the fixed source greens all 95. Nothing written on either refusal path, asserted. Perimeter unchanged.
