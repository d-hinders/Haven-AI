- **#2710** — Agent-discovery metadata now carries the canonical deployment default chain, an explicit OpenAPI mirror note, a configured-origin manifest pointer, and the device-login poll interval. The default is described and tested as information only: funding remains confirmed after login by `haven wallets funding`; no payment route, signing material, allowance, recipient, balance, approval, custody, or execution path changes. The root manifest pointer derives from configured frontend origin rather than request headers, and focused contract tests pin that boundary plus the default-chain/OpenAPI/CLI fields. Perimeter unchanged.
