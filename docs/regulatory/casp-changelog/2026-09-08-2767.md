# 2026-09-08 — #2767: the qa-dev failure bookkeeping becomes a standing tracker; the filing bar gains a home

Three dispositions and a filing bar for agent-filed issues, plus two CI-workflow
changes: the `qa-dev.yml` failure step stops filing a fresh dated issue per
failure and maintains one standing tracker, and `promotion-digest.yml` prints the
two inflow figures the bar is steered by. No payment route, funding path,
authority decision, signing material, credential, or on-chain call changes
anywhere in the diff.

**Money-path classification, re-derived rather than assumed.** The issue carries
no `money-path` label and none of the changed files moves money or grants
authority — but `.github/workflows/qa-dev.yml` is named in
`.github/money-path-globs.json`'s `controlGlobs`, the safeguard list (#1045:
the gate's own control surface gets the label and the human reading without a
harness re-run, because the harness cannot exercise the gate). The labeler
applies the union, so this PR carries `money-path` by that mechanism, not by
any surface touched here. Re-take it rather than trust this shard:

```
node scripts/ci/money-path-classify.mjs $(git merge-base origin/dev HEAD)
```

**What changed, as authority.** Nothing is authorised, permitted, funded, or
disabled. `qa-dev.yml`'s edit is confined to the `if: failure()` issue-upsert
step: the same `gh` invocation pattern, retargeted from "find any OPEN
qa-failure issue, else create a dated one" to "find THE standing qa-failure
tracker by title across all states, reopen it if closed, edit its body, append
a comment" — the `docs-audit.yml` upsert pattern the repository already runs.
The defect it closes was created by the interaction of that step with triage
habit: the step only looked at open issues while triage closes each ticket
once a run is green, so every close guaranteed the NEXT failure filed a new
dated issue — 12 in the eight days to 2026-09-08, measured with
`gh issue list --label qa-failure --state all --search created:>=2026-09-01`.
The freshness gate (`qa-freshness.mjs`, #578/#1030) reads run conclusions and
deployments, never issue bookkeeping, and its suite passes unchanged at this
head (80/80, `scripts/ci/qa-freshness.test.mjs`) — a failure is recorded the
same way, on the same trigger, with the same `Trigger:` provenance (#2271)
unchanged. `promotion-digest.yml`'s addition is a read-only metrics block
appended to the body of the digest issue it already upserts: four `gh … list`
counts, no write, no gate, skipped whole if any figure fails to compute rather
than printed half-wrong.

**The harness itself is untouched.** No scenario, leg, credential, or fixture
under `packages/qa-agent/**` changes; `QA_REQUIRE_ALL_LEGS=1`, the Coverage
completeness step (#1044/#1066), the `deployment_status` gate (#2273), and the
concurrency group are byte-identical. `contract: true` doc
`docs/operations/dev-environment.md` was re-verified against the changed hunks
with a positive control and its four `qa-dev.yml` claims recorded as unchanged
(chain entry in that file); `docs/operations/agent-qa.md`'s
§ *Automated failure reporting* is the one doc body edited, because its
"files a GitHub issue … close the qa-failure issue once green" pair stated the
defective flow as doctrine.

**Evidence.** The upsert ran LIVE against the real tracker #2769: step extracted
from the YAML (`node scripts/ci/review-isolation`-independent —
js-yaml parse, `bash -n` clean), executed with the runner's own env
(`GITHUB_REPOSITORY`, `GITHUB_EVENT_NAME`, `GITHUB_RUN_ID`); tracker closed to
simulate a triage close, the step re-run, #2769 returned OPEN with zero new
issues and the open `qa-failure` count steady at 1. The metrics block ran live
against #666: figures 207/224 and 91/213 rendered with their commands
verbatim (the product-share row excludes tooling scopes — `fix(ci)`, `fix(qa)`
and friends — because the 111 raw conventional `fix|feat|perf` titles overstate
the product share against the issue's own 36 % baseline; the classifier is
printed next to the number, per the repo's instrument-transparency rule).
`scripts/ci/*.test.mjs` 533/533, `scripts/docs` suite 252/252, `docs:check`
green. Mutation note: the metrics block is guarded by an all-figures-present
conjunction — dropping any `|| echo ''` fallback's guard changes output
visibility, which is the block's designed fail-shut: no partial metrics.

Perimeter unchanged.
