- **#2807** — a behavior-preserving contract extraction inside the hosted MCP server: the tool-name union, advertised JSON schemas, strict/permissive input-policy decisions, descriptions, and the parse/parseStrict pair move out of the monolithic tools module into three typed modules under its own directory (contracts, registry, parsing), with the old file kept as a compatibility facade and the package export surface unchanged. No tool name, advertised schema, description, input-policy decision, or direct-handler input behavior changed: the characterization suite pinning all of that was committed before the structural commit, the full mcp-server suite passes with the test-name set unchanged across the move (329 names before and after, diff empty), all 22 hosted tool names remain advertised and registered exactly once, and the strict-input refusals still read their text from the single decision maps. **Authority/custody argument: the keyless boundary is untouched.** The hosted server still constructs its client with no delegate key and the boot guard against a leaked key is byte-identical; the hosted tool error class stays in the facade rather than moving into the new modules, so error normalization keeps one instanceof branch; no signing, settlement, cap, pricing, idempotency or merchant-context validation line moved — the extraction relocated declarations and re-imported them, handler bodies are unchanged, and the money-flow invariants the epic lists are pinned by the existing suites (strict-input transport tests, description-size pins, hosted/local wire-contract tests, x402 expected-context byte contract), all green. **What is mutation-tested:** the new registration completeness guard — the runtime twin of the compile-time exhaustiveness annotations — proven red on all five failure modes (missing schema, missing description, missing input-policy decision, missing handler, duplicated ownership across the two input-policy lists), each naming the offending tool, each restore byte-identical by sha256; the server builder now refuses to boot an incomplete registry, and one documented asymmetry (a duplicate key within a single object literal collapses before runtime) is covered by its compile-time twin instead. Perimeter unchanged.
