- **#2808** — a behavior-preserving support extraction inside the hosted MCP server, the second slice of the #2806 decomposition: the cross-tool safety support moves out of the tools.ts facade into small internal modules under `tools/support/` (errors: `HostedToolError` + `runTool`/`normalizeError` and the x402 payment-window expiry mapping; guidance: `buildAgentGuidance`/`buildPurchaseSummary`; cap-price: the `readMaxAmountCap`/`priceSelectedOption`/`requireSettleableSelection` contract and `quoteWarnings`; mcp-context: transport serialization/`parseMcpTransport` shape refusal, `resolveMerchantCallContext` fail-closed validation, `deliverMerchantPayment`, `preflightMcpPaymentHeader`, the expiry-aware relay wrappers and `buildX402SigningContext`; quote-response: `buildMcpToolQuoteResponse`, `isPendingApproval`, `wrongTool`, `resolveResumeState`; signer-compat: the #1155/#1309 notice; catalog-entry: the unusable-row refusals), plus the reusable fetch/x402 test fixture under `src/test-support/hosted-mcp.ts` (adopted by `tools.test.ts` and `report-x402-outcome.test.ts`; `hosted-quote-body` and `strict-tool-input` keep their specialized stubs deliberately). No tool name, schema, description, input-policy decision, response shape, error code, refusal text, signing boundary, or payment behavior changed: handler bodies are byte-preserved and re-import the moved helpers, `tools.ts` remains the compatibility facade and the composition root for the strict-refusal thrower, `parse`/`parseStrict` stay in #2807's parsing seam, and the package export surface is unchanged (`HostedToolError`, `ResolvedMerchantCallContext`, and `signerCompatibilityNotice` re-export through the facade as before). **Authority/custody argument: the money-path guards moved file locations, not behavior or order.** The #2051 selected-option cap still binds before any intent is minted, the #2282 merchant-call-context validation still precedes every funding relay on both schemes, expiry still maps to the structured `payment_window_expired` refusal, and the keyless boundary is untouched. The committed helper-to-capability ownership map (`tools/support/shared-helper-ownership.test.ts`) enumerates every support export with the capability slices (#2809–#2812) whose handler bodies call it, refuses unmapped or phantom entries, keeps the parsing seam out of support, and source-scans the facade so no helper definition regrows in `tools.ts`. **What is mutation-tested:** the two load-bearing guards the issue names — removing the cap assertion from `priceSelectedOption` turns THE EXPLOIT red (over-cap erc7710 entry beside an under-cap standard entry) while its positive control stays green, and deferring `resolveMerchantCallContext` past funding turns all three #2282 red — each applied, proven red, and restored byte-identical (sha256-verified before/after). Full mcp-server suite green across the move with the pre-existing test-name set unchanged and the new ownership/mutation/fixture suite added on top. Perimeter unchanged.
