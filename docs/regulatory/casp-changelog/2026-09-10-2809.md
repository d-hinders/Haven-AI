- **#2809** — a behavior-preserving handler extraction inside the hosted MCP server, the third slice of the #2806 decomposition: the state, direct-payment and recovery handlers move out of the `tools.ts` facade into one capability-owned module, `packages/mcp-server/src/tools/state-direct-recovery.ts`, and are composed back into `createToolHandlers` as a typed contribution. Ten tools move with their bodies byte-preserved — `haven_get_agent`, `haven_get_allowances`, `haven_get_payment_status`, `haven_get_resume_state`, `haven_send`, `haven_pay`, `haven_submit`, `haven_sweep_delegate`, `haven_list_receipts`, `haven_verify_receipt` — and no tool name, JSON schema, description, input-policy decision, response shape, error code, refusal text, guidance field, signing boundary or payment behavior changed. **Authority/custody argument: the money-path behaviour moved file locations, not behaviour or order.** Direct-payment idempotency still forwards `idempotency_key` to the backend replay contract (#1207); the keyless signing handoff still forwards `signature_scheme` + `typed_data` verbatim on delegation-rail intents and omits them on legacy-rail ones (`delegationSignFields`, #1254); `haven_submit` still branches on `settlement_scheme` and routes BOTH schemes through the shared expiry mapping so an expired settle is the structured `payment_window_expired` refusal rather than a raw error (#2041); `haven_sweep_delegate` still refuses a below-floor balance with `below_minimum` instead of a dead-end `signature_required` (#700) and still requires `authorization` alongside `signature` on the relay phase; the keyless custody boundary is untouched and the delegate key still appears in no recorded request. **What is mutation-tested:** seven cells, each applied, proven red, and restored content-verified — a capability importing backwards into the `tools.ts` facade turns the dependency-rule test red; forking `submitSignatureWithExpiryMapping` into the capability (the one #2809↔#2812 crossing, on the signing path) turns two tests red; dropping a tool from the capability's contribution is a compile error at the facade (TS2741) and turns the composition test red; re-declaring a capability-owned tool in the facade's own literal — which `tsc` accepts, exit 0, because TS1117 does not reach across a spread — turns the new disjointness test red; and a banned queue-framing phrase planted in each of the three hosted-MCP prose surfaces this slice adds to the queue-framing census turns that census red, where the same phrase in `tools/contracts.ts` passed it green beforehand. **Test parity:** the split is measured on fully-qualified test identities collected by vitest, not on bare names — 240 identities at the merge base `2526cd12`, 216 (residual) + 24 (capability) = 240 after, sorted sets byte-identical; deleting one of a duplicated-name pair is shown to be invisible to a bare-name comparison and named by the qualified one. (The before-figure was first taken at `5d27a432`; `dev` gained one frontend-only commit between the two, and `git diff --stat 5d27a432..2526cd12 -- packages/mcp-server` is empty, so the two trees are interchangeable for this measurement. The issue's recorded baseline of **227** is a `grep -cE "^\s*(it|test)\("` count and misses 13 tests from two `it.each` blocks; 240 is the runtime figure.) The helper-to-capability ownership map is re-derived and now enforces the one-direction rule against the real module's import specifiers. Full mcp-server suite green (467). Perimeter unchanged.
