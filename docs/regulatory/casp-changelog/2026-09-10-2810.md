- **#2810** — a behavior-preserving handler extraction inside the hosted MCP server, the fourth slice of the #2806 decomposition: the catalog, quote and prepare handlers move out of the `tools.ts` facade into one capability-owned module, `packages/mcp-server/src/tools/catalog-purchase.ts`, and are composed back into `createToolHandlers` as a typed contribution. Six tools move with their bodies byte-preserved — `haven_discover_tools`, `haven_submit_catalog_entry`, `haven_quote_mcp_tool`, `haven_pay_mcp_tool`, `haven_quote_catalog_purchase`, `haven_prepare_catalog_purchase` — and no tool name, JSON schema, description, input-policy decision, response shape, error code, refusal text, guidance field, signing boundary or payment behavior changed. **Authority/custody argument: the money-path behaviour moved file locations, not behaviour or order.** This is the slice that owns price selection and caps, so specifically: a required cap is still read by `readMaxAmountCap` and still refuses when absent — an uncapped prepare or pay is still a refusal, not a default; the cap is still checked against `priceSelectedOption` of the option actually SELECTED, so a cheap advertised entry cannot be used to authorize an expensive settlement scheme (#2051), and the erc7710-only path still refuses `PRICE_EXCEEDS_MAX` before any authorize (#2054); the informational/capped split is unchanged — `haven_quote_mcp_tool` and `haven_quote_catalog_purchase` still reserve no price and create no intent, while `haven_pay_mcp_tool` and `haven_prepare_catalog_purchase` still re-quote live before preparing; the #1271/#1301 bounded same-origin discovery is untouched and still refuses an off-origin `mcp_url` unfetched, because the helper lives in `@haven_ai/sdk` and only its caller moved; idempotency keys still forward to the merchant quote; the keyless custody boundary is untouched and the delegate key still appears in no recorded request. **What is mutation-tested:** a duplicate `haven_quote_mcp_tool` written into the facade's own literal after the spread compiles clean — `tsc` exit 0, because TS1117 does not reach across a spread, the hazard #2809 recorded — and is named by the disjointness assertion, now proven to bind this capability's tuple and not only the previous one; dropping a tool from BOTH the capability tuple and its handler map produces four type errors, moves the facade+capability coverage union from 22 to 21, and fails the spelled-out contribution list. **Byte-preservation is measured, not asserted:** the 727 moved handler lines compare line-for-line identical to the pre-move file, and the facade's own diff is 734 removed (727 handler lines plus 7 imports that became dead) against 10 added, every one of the ten being the new import or the spread comment — which matters here because this slice is NON-CONTIGUOUS, the six handlers having sat in three blocks with #2811's and #2812's between them, so "it still compiles" would have been a weak claim. **Test parity:** measured on fully-qualified test identities collected by vitest, not bare names — 468 identities on the merge base, 472 after, with **zero lost** and exactly four gained: three generated automatically because `CAPABILITY_MODULES` is directory-derived and therefore enrolled the new module in the dependency-rule and helper-only checks without being told about it, plus one contribution assertion added by hand. 1980 lines of tests moved verbatim into `tools/catalog-purchase.test.ts`, classified by `.haven_*(` INVOCATION rather than by title — blocks that also drive a sibling capability's handler stayed behind rather than being split, because splitting a test that spans two capabilities is a behaviour change dressed as a move. Two source scanners in `tools.test.ts` that read only `./tools.ts` were widened to the whole hosted surface; one of them is a negative assertion that would otherwise have become unfalsifiable as handlers left the file. Full mcp-server suite green (472). Perimeter unchanged.
