- **#2811** — a behavior-preserving handler extraction inside the hosted MCP server, the fifth slice of the #2806 decomposition: the plain-HTTP x402 lifecycle handlers move out of the `tools.ts` facade into one capability-owned module, `packages/mcp-server/src/tools/plain-http-x402.ts`, and are composed back into `createToolHandlers` as a typed contribution. Four tools move with their bodies byte-preserved — `haven_quote_x402`, `haven_pay_x402_quote`, `haven_resume_x402_payment`, `haven_report_x402_outcome` — and no tool name, JSON schema, description, input-policy decision, response shape, error code, refusal text, guidance field, signing boundary or payment behavior changed. **Authority/custody argument: the money-path behaviour moved file locations, not behaviour or order.** This is the slice that owns the x402 quote/resume/reconciliation lifecycle, so specifically: the quote path still probes the merchant and returns `payment_required` without creating a Haven payment; payment-option selection, cap reading and the informational/capped split are unchanged — `haven_quote_x402` still reserves no price and creates no intent, while `haven_pay_x402_quote` still refuses `PRICE_EXCEEDS_MAX` before any funding intent and still fails closed when no payment option is settleable; resume still refuses a SUBMITTED erc7710 intent (the resume lifecycle is the funding one) and still fails closed on missing payment_id and resume_state; report reconciliation still derives settlement truth only from Haven's own payment status, never from a merchant result that merely claims payment. **The module never signs:** signing-context construction stays in shared support (`buildX402SigningContext`), the module imports only the #2807 contract/parsing seams and the #2808 shared support — never another capability module (helper or handler) — and no signer call site moved. **Byte-preservation is measured, not asserted:** the 446-line moved handler span compares byte-for-byte identical to the pre-move `tools.ts` (24,072 chars), and all four moved test blocks compare byte-identical to their pre-move spans. **Test parity:** measured on fully-qualified test identities (`describe path > name`) collected by vitest itself — 182 identities before, 182 after, empty name-set diff; the file-inclusive delta is exactly the file-prefix change (`tools.test.ts` → `tools/plain-http-x402.test.ts`), with the three standalone x402 contract suites left at their root paths, imports untouched. Colocated tests reuse the #2808 shared fixture module rather than re-declaring it. Full mcp-server suite green, registry-completeness and shared-helper-ownership suites green, x402 wire/header contract suites green. Perimeter unchanged.
