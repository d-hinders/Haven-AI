- **#2812** — the final capability slice of epic #2806: `haven_complete_mcp_tool` and `haven_settle_mcp_tool` moved out of the `tools.ts` facade into `packages/mcp-server/src/tools/paid-mcp-completion.ts`, together with the four merchant delivery / context-rehydration helpers #2808 had parked in shared support for exactly this move (`resolveMerchantCallContext` + the `ResolvedMerchantCallContext` shape, `deliverMerchantPayment`, `preflightMcpPaymentHeader`). Money-path by both halves: the issue carries the label and the classifier matched 4 of 4 changed files on the `packages/mcp-server/src/**` runtime glob (`tools.ts`, `paid-mcp-completion.ts`, `support/index.ts`, `support/mcp-context.ts`), verdict `=> MONEY-PATH … A CASP shard is required` — this shard is that requirement, never a suppressed gate. **The move is pure relocation, proven mechanically rather than by intent:** both handler bodies are byte-identical to base modulo whitespace (`haven_complete_mcp_tool` 10 lines, `haven_settle_mcp_tool` 166 lines, checked by extraction-and-diff against the base tree), and the fail-closed money-path behaviours travel with them under their existing tests: the #2282 resolve-context-BEFORE-relay ordering on both schemes, the #1456 erc7710 sequence inversion (the signature IS the settlement child, no funding leg, no preflight), the #1508 no-funding-leg flag, the #1307 rehydration rules (explicit context wins outright; exactly-one-of is refused, never merged with stored state), the #1300 verify-then-sweep merchant-timeout guidance and the #1310 rail-aware purchase summary. **No authority, custody or API claim changed:** the module never signs — no key material, no signer call site, no header construction moved into it; the funding signature and X-PAYMENT header are still produced by the local edge signer and relayed keyless. The facade is now composition-only (capability spreads plus re-exports; zero tool-specific branching), and the previously hand-verified structural rules are executable: `tools/module-boundaries.test.ts` rejects duplicate ownership (named tool + both claiming modules), missing ownership (named tools) and ANY cross-capability import — helper or handler, specifier-level — with mutation evidence on the delivered head (duplicated `haven_complete_mcp_tool` into `catalog-purchase`'s tuple and a `state-direct-recovery → paid-mcp-completion` import both turned the named tests red, then `cmp`-verified restore turned them green). **Test-set integrity:** residual `tools.test.ts` 59 + capability suite 43 = 102 = the pre-split count, with the 43 moved identities differing from the pre-split set in file path only (zero renames, zero deletions, no copied fixtures — the capability suite rides the #2808 shared fixture); the whole-package qualified census went 475 → 481 = +6, all six explicitly listed here: the three `it.each` cases the derived ownership derivation adds for the new module (handler-only, sibling-import, allow-list) and the three new boundary tests. Baselines: the #2809-recorded instrument (qualified `describe > name` identities, vitest-collected) at the pre-split head `4f4805f9`, final at the delivered head. **CI census registration:** `packages/mcp-server/src/tools/paid-mcp-completion.ts` is registered in `queue-framing-census.test.mjs` `GUARDED_FILES` (the omission that turned PR #2852 red for #2811); the capability's test files stay out, matching the completeness scan's non-test-only rule and the #2809–#2811 siblings. `node --test scripts/ci/*.test.mjs` 684/684, mcp-server suite 481/481, `tsc --noEmit` clean, `docs:check`, `docs:coupling --strict`, `lint:workspace-pins` and `lint:deps` green at the delivered head. **Perimeter unchanged — no file entered or left `.github/money-path-globs.json`; the diff moves existing money-handling code and adds no path by which value can move.**
