- **#2847** — the last live Safe-rail behaviour is deleted (epic #1440, final residue scan slice 1 of 5, owner decision 2026-09-10): `POST /safe/exec` — the relayer-funded, owner-signed Safe `execTransaction` relay, together with `infra/chain/safe-exec-contract.ts`, `infra/chain/safe-proxy-deployer.ts` (`ensurePasskeySignerDeployed`), the `'safe_exec'` relayer-budget operation, and `modules/accounts/passkey-signer.ts`; `GET /safe/:safeAddress/details` with `modules/accounts/safe-details.ts` and its OpenAPI entry; the structurally-broken `/user/owners` directory (`GET/PUT/DELETE` in `routes/user.ts` + `getCurrentOwnerDirectory` + the `owner-aliases` repository + the frontend `OwnerDirectoryContext` provider and `WalletButton` alias reads); and `POST /passkeys` (the Safe WebAuthn signer enrolment — `GET /passkeys` stays, `AuthContext` reads it every session). 4207 lines deleted against 127 added, and the diff reads as removal plus import updates — the six 410 tombstones (`/safe/deploy`, `/user/safes/deploy`, `POST /user/safes`, `PUT /user/safe`, `POST`+`DELETE /agents/:id/allowances`), the fail-closed `middleware/safe-inflow-retired.ts`, the `execution_rail='allowance_module'` refusals and `rails/execution-rail.ts` are untouched. **Authority/custody argument: the perimeter narrowed — Haven no longer operates any path that relays an arbitrary owner-signed Safe transaction.** The route was owner authority, not a policy rail (Haven never held the signing key; the relayer paid gas only), so custody was never implicated — but its existence meant Haven infrastructure could be directed to broadcast a constructed Safe transaction, and #2847 removes that surface outright rather than gating it: after this slice every Safe-rail address answers a 410 tombstone or 404, there is no live Safe-rail spend path, and the relayer can no longer be asked to execute on the legacy rail at all. The non-custody invariants are unaffected in the strengthening direction: agent spend authority still flows only through owner-signed delegations on the delegation rail, `GET /passkeys` still lets a user read their own enrolled credentials, and the deletion of the `/user/owners` probing directory removes a reader of Safe `getOwners` state without adding any. The perimeter's own file list moved with the code: the `routes/safe-exec.ts` money-path glob left `.github/money-path-globs.json` (the #1987 precedent — a glob naming a deleted file protects nothing and the phantom-glob test fails CI on it), synced into `.github/labeler.yml` and the ship-next Merge Gate line; nothing was removed that still exists. Scan B1's one comment reword landed with it: `modules/x402/settlement-observed.ts` now says two settlement schemes on the one live execution rail, not "both rails". **What is mutation-tested:** the six tombstone pins were characterized green at the merge base (82074018, 27/27) and re-run green after the cut on a byte-identical `safe-inflow-retired.test.ts` — the only change in that file is the deliberate inversion of the safe-exec assertions, which now demand 404 for anonymous AND authenticated callers (a 410-shaped tombstone or a re-added route answers neither 404-nor-404), and `routes/__tests__/retired-rail-routing.guard.test.ts` lists all seven deleted modules in `DELETED_RAIL_MODULES` so re-importing any of them fails; the owner-cli census dropped its `POST /safe/exec` pin because the refusal it pinned is structural now (no surface exists for an owner_cli token to reach). Full backend and frontend suites, both typechecks, the OpenAPI drift check (`check:api-types`), `lint:workspace-pins`, `lint:deps`, the retired-rail prose ratchet and the money-path three-copy sync gate are green at the delivered head. **Perimeter narrowed — the Safe rail has no live execution path, and the money-path file list shrank accordingly.**
