- **#2850** — Residue cleanup under the retired Safe/AllowanceModule rail (epic #1440, final residue scan slice 4): four zero-importer dependencies deleted (`@safe-global/protocol-kit`, `@rhinestone/module-sdk`, `@safe-global/safe-modules-deployments`, `ethereum-cryptography`), `rails/allowance-module.ts` renamed to `infra/chain/relayer-reads.ts`, and the SDK's legacy bare-hash `sign_data` fallback deleted. **No perimeter move in either direction.** Nothing gains authority: the renamed module's three exports (`getProvider`, `getRelayerWallet`, `getTokenBalance`) keep their bodies byte-identical and every importer is a path rewrite — no AllowanceModule contract read existed in the file to begin with. Nothing loses a guard: the fail-closed backstops (`middleware/safe-inflow-retired.ts`, the `execution_rail='allowance_module'` refusals, `rails/execution-rail.ts`) are untouched, and the retired-rail guards keep `rails/allowance-module` as a banned-name tombstone while ADDING the module's new `infra/chain/relayer-reads` name, so the entry-point module ban survives the rename under both spellings. The SDK change makes a malformed `sign_data` payload — one with no `signature_scheme` — fail loudly in the client, where it previously silently signed the bare hash (proven red-at-base: the absent-scheme test resolved a signature before the branch was deleted, and rejects after); every live emitter sets the scheme and the backend spec makes it required, so no legitimate flow loses a signature path, and `signHash` itself stays live for the EIP-3009 leg. Also removed: two stale labels (the CLI exported `delegate_sweep` as "allowance funding" — a rail claim, now "sweep"; the SDK test titled raw ECDSA as the "AllowanceModule rail" — now named the live EIP-3009 leg) and the dead `SESSION_RAIL_BUNDLER_URL` test lines (unconsulted since #882; the fail-closed `delegationRailBundlerUrl` is unchanged). The rename is reflected in the `covers:` lists of `docs/architecture/01-system-context.md`, `02-identity-and-custody.md` and `03-payment-sequence.md`, and in the retired-rail paragraph of this document, which now names the module's new path. Perimeter unchanged.
