- **#2861** — the accounting feed's entitlement gate gains a deployment-wide mode: `HAVEN_ACCOUNTING_ENTITLEMENT_MODE=all` makes every account on the deployment entitled without an `account_entitlements` row; `granted` (the default, and what an unset variable means) keeps today's row-based gate, so prod behaviour is unchanged by the merge. Any other value refuses the boot naming both modes. Two perimeter files are in the diff — `packages/backend/src/config.ts` (the parser and the `accountingEntitlementMode` field) and `packages/backend/src/routes/accounting-feed.ts` (the status response gains `entitled` and `entitlementMode`, both also required in the OpenAPI schema) — and neither touches a payment, delegation, allowance, signer or relayer path. **Authority/custody argument: nothing moves.** The gate decides who may read the settled-payment feed and push non-asserting supplier-invoice records to their own Fortnox company; the hosted and `HAVEN_ACCOUNTING_ENABLED` checks still run first and are unchanged, so mode `all` on a self-hosted or flag-off deployment is still unavailable (pinned by `accounting-feed.entitlement-mode.test.ts`, which runs the real gate and middleware with only the row lookup stubbed). Money-path unchanged: the feed constructs no transaction and holds no authority; #2861 carries no `money-path` label. **What is mutation-tested:** dropping the `mode === 'all'` short-circuit in `modules/agents/entitlements.ts` reddened the gate test and the unmocked route test (2 failed), and replacing the parser's throw with a silent `return 'granted'` reddened both refusal tests in `config-accounting-entitlement-mode.test.ts` (2 failed); the scope `src/__tests__/config-accounting-entitlement-mode.test.ts src/routes/__tests__/accounting-feed.entitlement-mode.test.ts src/modules/agents/__tests__/entitlements.test.ts` is 18 tests, 14 passed / 4 failed under both mutations planted together, 18/18 restored. **Perimeter unchanged — no file entered or left `.github/money-path-globs.json`.**
