- **#2862** — the accounting feed becomes provider-generic (epic #2858, slice 4): a provider registry (`modules/accounting/registry.ts` — Fortnox `live`; Accounted, Light, Igdrasil listed `coming_soon` by owner decision), an extended `AccountingConnector` contract (`pushTransaction`, `verify`, `getCompanyInfo`, `revoke`), a generic OAuth2 flow and a generic API-key flow, and provider-parameterised routes under `/accounting/providers` and `/accounting/connections/:provider/*` that replace `routes/fortnox.ts` (deleted; the dark legacy voucher push `POST /accounting/fortnox/push` moves verbatim into `routes/accounting.ts`, still 410 behind `HAVEN_LEGACY_BOOKKEEPING_ENABLED`). Verify, reopen, sync and status keep their feed-scoped home under `/accounting/feed/*` and dispatch to the ACTIVE connection's connector — no provider-scoped duplicates (review, 2026-09-11). **Authority/custody argument: nothing moves.** The feed reads settled payments and delivers drafts to the user's own accounting software under the user's own grant; the secrets involved are the user's credential to their ledger, never a Haven key, never a delegate key, never anything that can move funds. No migration: `feed_from` and every column used here already exist in migration 080 (#2860). The perimeter files in the diff — `routes/accounting-connections.ts` (new, covered by `routes/**`), `routes/accounting-feed.ts`, `routes/accounting.ts`, `modules/accounting/**`, `infra/repositories/accounting-connections.ts` and `accounting-feed-syncs.ts` — construct no transaction, sign nothing, touch no delegation, allowance, relayer or signer, and add no write path to any money table; the two repository changes are additive (`SET_FEED_FROM_SQL` as a third statement inside the existing clear-then-set transaction, `SET_COMPANY_INFO_SQL`, and a `$4::timestamptz IS NULL OR …` floor on the backfill selection) and leave the CAS re-encrypt, the one-active-destination transaction and disconnect-keeps-the-row semantics of #2860 byte-identical. **Three narrowings, none widenings.** (1) The OAuth `state` keeps the #1640 design (a `purpose` claim `authMiddleware` rejects, the provider carried in the claim, 10-minute expiry) and becomes **single-use**: the callback consumes the token's `jti` BEFORE the code exchange, so a replayed state never reaches the provider. The consumption store is the existing `rate_limit_counters` table (#1680) under a namespaced key — an atomic keyed upsert with expiry, durable across replicas, no new table — with the direction of its fail-open reversed here: a store that cannot answer REFUSES the callback, because a connect is retried in seconds and a replayed grant is not undone; the table is UNLOGGED, so a state issued before a Postgres crash could be accepted once more inside its ten minutes — stated, accepted. (2) A connect that would land in plaintext is refused before the authorization code is consumed at the provider (`secretsKeyConfigured()` first), the same order #2887 established for the single-use refresh token, now in the generic flow for every provider. (3) Activating a destination stamps `feed_from = now` in the same transaction as the active flag, and both the backfill selection and the settlement hook feed nothing settled before it — switching destination never re-feeds history into a new ledger — the floor is stamped by `activate` and by a first connect that takes the flag, and a user with any connection row never falls back to the registry scan, so a degraded row feeds nothing (both proven on the real database after the independent review of PR #2894 found the first cut re-fed history through the fallback); a post-push scope loss on the attachment step flips only the CONNECTION to `scope_missing` and leaves the sync row `pushed`, never re-pushable. **What is mutation-tested**, each restored from a `cp` backup and verified by `cmp`, every count from one named scope at the delivered tree — `npx vitest run --root packages/backend src/routes/__tests__/accounting-connections.test.ts src/modules/accounting/__tests__/fortnox-connector.conformance.test.ts src/modules/accounting/__tests__/in-memory-connector.conformance.test.ts src/modules/accounting/__tests__/feed-from.db.test.ts`, 42 tests green: removing the `jti` consumption from the callback — 2 failed / 40 passed (the replay test and the fail-closed store test); allowing a `coming_soon` provider through `assertConnectable` — 2 failed / 40 passed (the connect-url gate, with a connector deliberately registered so the availability check is the only thing standing, and the api-key gate); a VAT row in the Fortnox payload with the runtime guard neutered so the payload actually reaches the provider — 1 failed / 41 passed (the conformance suite's non-asserting case, on the recorded-fixture runner), and with the guard intact 5 failed / 37 passed (the guard throws before the POST, so every Fortnox push case reddens); dropping the timestamp from `activateProvider` — 3 failed / 39 passed (the route's activate test and both real-database feed-from tests, one of which shows the historical payment being enumerated and pushed). The conformance suite (`__tests__/connector-conformance.ts`: idempotent re-push, non-asserting payload, attachment degradation, verify verdicts, revoke-on-disconnect exactly per descriptor, post-push scope loss, non-SEK refusal with a SEK positive control) runs the real orchestrator, flows and repositories on the real database against `InMemoryConnector` and against the Fortnox connector with hand-authored HTTP fixtures in the shapes the #494 spike recorded — 8 green on each. One operator step, in the PR body as an unticked checklist: the Fortnox app's registered redirect URI and `FORTNOX_REDIRECT_URI` move to `/accounting/connections/fortnox/callback`; stored grants are unaffected, only the consent round-trip. Perimeter unchanged — narrowed, if anything: a signed OAuth state that could complete two callbacks now completes one.
