- **#2864** — Company info at connect, SEK-only enforcement, company-switch handling and a company-aware reopen (epic #2858, slice 6). The generic connect flows (`modules/accounting/oauth-flow.ts`, `api-key-flow.ts`) ask the connector who the grant belongs to BEFORE any secret is stored and refuse a company whose base currency is not SEK with "Haven currently feeds SEK ledgers only" (`UnsupportedBaseCurrencyError`, `provider.ts`; the API-key route answers 409 `UNSUPPORTED_BASE_CURRENCY`, the OAuth callback redirects with `reason=unsupported_currency`; an existing row is left byte-identical; the already-exchanged OAuth grant is revoked at the provider best-effort when the descriptor declares `revoke`, per #2863's rule that a grant Haven drops is also revoked there — asserted by conformance case 7 in the Fortnox runner). Fortnox: `companyinformation` joins `FORTNOX_SCOPE` (`fortnox.ts`), `getCompanyInfo` reads `GET /3/companyinformation` (`DatabaseNumber` → `external_company_id`, `CompanyName` → `external_company_name`, `SEK` by construction) and degrades — reporting `scopeMissing`, which the flow records as `scope_missing` — ONLY on a scope refusal (HTTP 403 or `[2000663]`); a network error, 401, 429 or 5xx is thrown. A reconnect that reports a different company id is a company switch (`company-info.ts`): one row, company fields replaced, `feed_from = now`, `status_reason` names both companies, the switch appended to the row's `settings.companySwitches` JSONB (no migration; `accounting_feed_syncs` gains no column and no text column is overloaded), one structured log line. The verification-gated reopen (#1365) becomes `reopenPushedPayment` (`connections.ts`): a `pushed` row created before the latest switch is refused `previous_company` and never flips — attribution is by `created_at` against the switch time, exact for connect → push → switch and with its two limits (clock skew inside the callback second; a NULL stored id is not a switch) stated in the runbook. `GET /accounting/feed/status` gains `companyName`, `GET /accounting/connections` gains `externalCompanyId`; OpenAPI and `api-types.ts` regenerated. **Authority/custody argument: nothing moves.** Every secret involved is the user's own grant to their own accounting ledger — never a Haven key, a delegate key or anything that can move funds — and this slice only decides WHETHER such a grant is stored (currency), WHAT is recorded next to it (company, switch log) and whether a sync row may be reopened. The perimeter files in the diff — `routes/accounting-connections.ts`, `routes/accounting-feed.ts`, `infra/repositories/accounting-connections.ts` — construct no transaction, sign nothing, touch no delegation, allowance, relayer or signer, and add no write path to any money table; the one repository addition is `RECORD_COMPANY_SWITCH_SQL` (company fields + `feed_from` + `status_reason` + a JSONB append on `accounting_connections`, one statement, `RETURNING` the row), registered in the schema smoke. The double-post guard is NARROWED, not loosened: `reopenMissingPushed`'s `status = 'pushed'` predicate is untouched and the new guard refuses a class of reopens that used to succeed. **What is mutation-tested**, each restored from a `cp` backup and verified by `cmp`, every count from one named scope at the delivered tree (uncommitted working tree on `feat/2864-company-info-at-connect` over `ce666e02`) — `npx vitest run --root packages/backend src/modules/accounting src/routes/__tests__/accounting-connections.test.ts src/routes/__tests__/accounting-feed.test.ts src/routes/__tests__/accounting-feed.entitlement-mode.test.ts`, 23 files / 213 tests green (new: `__tests__/company-switch.db.test.ts`, 3 real-database tests through the real OAuth2 flow, the real Fortnox connector on a fixture `fetch`, the real orchestrator; conformance cases 7c and 8 in both runners): dropping `assertSupportedBaseCurrency` from BOTH flows — **4 failed / 209 passed** (conformance 7 and 7c in the in-memory and the Fortnox runner: the EUR connect resolved and a row landed); dropping the `feed_from` stamp from `RECORD_COMPANY_SWITCH_SQL` — **3 failed / 210 passed** (the real-database switch test and conformance 8 in both runners: the floor stayed at the first connect); disabling the previous-company guard in `reopenPushedPayment` — **3 failed / 210 passed** (the real-database reopen test and conformance 8 in both runners: the pre-switch row flipped to failed); widening `getCompanyInfo`'s degrade to every `FortnoxError` — **2 failed / 33 passed** of `fortnox-connector.test.ts` (an outage became a scope_missing row). Perimeter unchanged — narrowed, if anything: a non-SEK ledger is refused before a secret lands, and a company switch can no longer trigger a mass `pushed → failed` re-feed.
