- **#2870** — the Transactions page gains a per-row accounting badge ("In Fortnox" / "Feeding…" / "Not fed"), fed by an optional `accounting` object on each `GET /transactions` (and `GET /transactions/{safeAddress}`) row, joined from the sync ledger `reporting_feed_syncs` by the payment-intent id the row already carries. Money-path by the file half only: the one perimeter file in the diff is `packages/backend/src/infra/repositories/accounting-feed-syncs.ts` (`packages/backend/src/infra/repositories/**`), and the change to it is one additional read — `LIST_SYNCS_FOR_PAYMENT_IDS_SQL`, a `SELECT provider, payment_id, status, external_ref, error … WHERE user_id = $1 AND payment_id = ANY($2)` — plus its function `listSyncsForPaymentIds`. No existing statement, write, claim or transition in that file was touched: `claimSync`'s two-statement concurrency guard, `markPushed`'s pushed-is-final rule and the verification-gated `reopenMissingPushed` are byte-identical. **Authority/custody argument: nothing moves.** The join is a read of the feed's own dedup ledger — it constructs no transaction, signs nothing, calls no provider (Fortnox is never contacted on list; the issue's optional `booked` field is deliberately omitted because the ledger stores no verify result), touches no delegation, allowance, relayer or signer, and creates no new write path. The list route is dashboard-JWT-authenticated and the read is tenant-scoped twice over: the enrichment runs only after `accountingFeedAvailable(userId)` (hosted + flag + entitlement) and a Fortnox connection row for that user, and the SQL itself carries `user_id = $1`. The delegation-rail security model's invariants 1–13 are untouched. **What is mutation-tested,** each restored from a `cp` backup verified by `cmp` and re-run green: (1) removing the `accountingFeedAvailable` check in `modules/transactions/accounting.ts` reddened both gate tests in `routes/__tests__/transactions-accounting-badge.test.ts` — 2 failed / 5 passed of 7, restored 7/7; (2) making `AccountingBadge` render for an absent `accounting` reddened the hidden-state test and both table tests in `components/accounting/__tests__/AccountingBadge.test.tsx` — 3 failed / 23 passed of the 26 in `src/components/accounting src/components/transactions`, restored 26/26 (the drawer's "no section" test stayed green because the drawer gates on `tx.accounting` itself, before the badge is reached); (3) dropping the `user_id = $1` predicate from the new SQL reddened the real-Postgres cross-tenant test in `infra/repositories/__tests__/accounting-feed-syncs.test.ts` — 2 failed / 5 passed of 7 (the unreferenced `$1` also fails the page-join test; keeping `$1` referenced isolates the leak at 1 failed / 6 passed) of 7, restored 7/7. The fail-soft branch (a ledger read throwing) is pinned at the route: without the `try/catch` the same request answered 500, with it 200 and no badge. This change is stacked on #2888, which flattened `Transaction`/`TransactionBase` and declared `fxRateSek`/`fxSource`; that work is #2888's, not this shard's — here the `accounting` property is added to the shared base object and the feed payload with a badge present is asserted end to end. Suite figures are in the PR body against the delivered head rather than restated here. **Perimeter unchanged — no file entered or left `.github/money-path-globs.json`, and the badge adds no path by which value can move.**
