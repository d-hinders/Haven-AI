- **#2871** — the transaction list gains a server-side CSV export (`GET /transactions/export.csv`), replacing the client-side one that could only ever serialize the page the browser had loaded. Money-path by the file half only: the one perimeter file in the diff is `packages/backend/src/infra/repositories/transaction-history.ts` (`packages/backend/src/infra/repositories/**`), and the change to it is two additional projected columns — `mpe.fx_rate_sek` and `mpe.fx_source` — on the two existing `SELECT`s (`FIND_PAYMENT_INTENT_AGENT_MATCHES_SQL`, `FIND_CONFIRMED_X402_PAYMENT_INTENTS_SQL`), both riding the `LEFT JOIN machine_payment_evidence` that already carried `amount_sek`. No `WHERE` clause, join, parameter or write was touched; the `pi.user_id = $N` + `us.id = ANY($N)` tenant scoping that stops a same-hash collision reaching another tenant's payment intent is byte-identical. **Authority/custody argument: nothing moves.** The export is a read of settled history — it constructs no transaction, signs nothing, touches no delegation, allowance, relayer or signer, and creates no new write path; the route is dashboard-JWT-authenticated and scoped to the caller's own accounts by the same `listBasicSafesForUser(sub)` the list route uses, with `agentId` ownership re-checked via `agentExistsForUser(id, sub)`. The delegation-rail security model's invariants 1–13 are untouched, and its §2 now records this read-surface widening explicitly. **One disclosure the guardrails document required, and now carries:** the export reads the same unfiltered `LIST_BASIC_SAFES_FOR_USER_SQL` as `GET /transactions` and `GET /transactions/filters`, which carry no `account_type` predicate — so this is a **third** surface on which a retired Safe-rail account's history stays visible, and the first on which it is downloadable as a file rather than only rendered. That enumeration named two surfaces before this change and now names three; nothing about the retirement moved, only the count of surfaces the exception covers. **What is mutation-tested:** the two new server-side filters, the export row cap and the own-account name resolution were each mutated out and the covering assertions went red — removing the `options.direction` guard reddened "applies the direction filter server-side", removing `options.chainId` reddened "applies the chain filter server-side", stubbing `exceedsExportRowCap` to `return false` reddened its boundary test (3 failed / 22 passed at `3b0ef17c`, restored 25/25), and narrowing the name map back to the `safeId`-filtered account list reddened "names the far side of a transfer between two of the user's own accounts" (1 failed / 17 passed, restored 18/18). **Correction to this PR's first review round:** an earlier draft of this shard and of `csv-export.ts` stated that the 10 000-row cap was unreachable because `infra/explorer-api.ts` fetches with `offset = 50`. That was wrong and was caught by the independent code review. The explorer legs are bounded, but the x402 leg is not — `FIND_CONFIRMED_X402_PAYMENT_INTENTS_SQL` carries no `LIMIT` and `mergeX402Transactions` appends every row — so a user with more than 10 000 confirmed x402 payment intents crosses the cap on an unfiltered export. The 413 is now exercised at the route (10 001 seeded x402 rows + 1 explorer row = 10 002 reported in the refusal), the refusal's actionable `details` reaches the user instead of being dropped by the client, and the claim has been corrected in code, in the route test and here. **One gate caught mid-flight, recorded rather than quietly fixed:** the round-2 code review reported `docs:check` red and read it as a defect inherited from `dev`. Neither half was quite right, and the distinction matters for a record like this one. The red was real but never at a committed head — the reviewer measured the builder's working tree, which at that moment carried an uncommitted edit adding a `covers:` gap to `docs/product/design-system.md` by naming a component that document's front matter cannot reach (`667a7a85` itself contains no such reference, verified by checking the doc out at that SHA). The prose was reworded rather than the coupling widened, and the instrument is green. The general lesson stands: a green-gates sentence must come from a run at the delivered head. Full backend (2569 passed, 616 skipped — the 64 real-DB files did NOT run, no Postgres on this machine) and frontend (1641 passed) suites, both `tsc --noEmit`, `npm run check:api-types`, the OpenAPI route-coverage and spec-drift tests, `docs:check`, `docs:test`, strict `docs:coupling`, `design:lint`, design-system coupling and copy lint are green at the delivered head. **Perimeter unchanged — no file entered or left `.github/money-path-globs.json`, and the export adds no path by which value can move.**
