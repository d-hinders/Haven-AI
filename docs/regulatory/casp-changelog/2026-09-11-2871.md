- **#2871** — the transaction list gains a server-side CSV export (`GET /transactions/export.csv`), replacing the client-side one that could only ever serialize the page the browser had loaded. Money-path by the file half only: the one perimeter file in the diff is `packages/backend/src/infra/repositories/transaction-history.ts` (`packages/backend/src/infra/repositories/**`), and the change to it is two additional projected columns — `mpe.fx_rate_sek` and `mpe.fx_source` — on the two existing `SELECT`s (`FIND_PAYMENT_INTENT_AGENT_MATCHES_SQL`, `FIND_CONFIRMED_X402_PAYMENT_INTENTS_SQL`), both riding the `LEFT JOIN machine_payment_evidence` that already carried `amount_sek`. No `WHERE` clause, join, parameter or write was touched; the `pi.user_id = $N` + `us.id = ANY($N)` tenant scoping that stops a same-hash collision reaching another tenant's payment intent is byte-identical. **Authority/custody argument: nothing moves.** The export is a read of settled history — it constructs no transaction, signs nothing, touches no delegation, allowance, relayer or signer, and creates no new write path; the route is dashboard-JWT-authenticated and scoped to the caller's own accounts by the same `listBasicSafesForUser(sub)` the list route uses, with `agentId` ownership re-checked via `agentExistsForUser(id, sub)`. The delegation-rail security model's invariants 1–13 are untouched, and its §2 now records this read-surface widening explicitly. **What is mutation-tested:** the two new server-side filters and the export row cap were each mutated out at `3b0ef17c` and the covering assertions went red — removing the `options.direction` guard reddened "applies the direction filter server-side", removing `options.chainId` reddened "applies the chain filter server-side", and stubbing `exceedsExportRowCap` to `return false` reddened its boundary test (3 failed / 22 passed); all three restored green (25/25) with the guards back. The row cap (10 000) is deliberately unreachable through the route today — `infra/explorer-api.ts` fetches with `offset = 50`, so the pipeline yields at most ~50 rows per source per account — which is why the predicate is unit-tested directly rather than through a request, and the route test instead pins that the export returns all 50 rows a source yields where the list route pages at 25. Full backend (2566 passed, 616 skipped — the 64 real-DB files did NOT run, no Postgres on this machine) and frontend (1635 passed) suites, both `tsc --noEmit`, and the OpenAPI route-coverage and drift gates are green at the delivered head. **Perimeter unchanged — no file entered or left `.github/money-path-globs.json`, and the export adds no path by which value can move.**
