- **#2865** — Scope-missing detection and a re-consent that keeps settings and history (epic #2858, slice 7). Three detection points, one recorded shape. (1) At the OAuth callback the granted scope string (Fortnox echoes it in the token response) is compared with the descriptor's `requiredScopes` (`compareGrantedScopes`, `provider.ts`; `recordScopeShortfall` in `oauth-flow.ts`): a shortfall STORES the connection — secrets encrypted, active flag taken if free — as `scope_missing` with `status_reason = missing scopes: <a>, <b> — …` naming the missing scopes; it is not a refusal. (2) At push time the **pre-/post-push rule** (the 2026-09-11 review blocker) is implemented in `fortnox-connector.ts` + `feed-orchestrator.ts`: a scope refusal (`[2000663]` or a bare 403, `isFortnoxScopeRefusal`) on the supplier step or on the **invoice POST itself** — nothing exists at Fortnox — returns `status: 'skipped'` with the reason, `connectionStatus: 'scope_missing'` and the scope the endpoint needed (`fortnoxScopeForPath`, because Fortnox's error does not say), and the orchestrator records a re-claimable `skipped` row AND flips the connection; a `[2000663]` on the attachment step (the invoice exists) keeps the existing behaviour — the row stays **`pushed`** with the note, only the connection flips, never re-pushed. `PushResult` gains `missingScopes` and `connectionStatus` is now meaningful on a `skipped` result too. (3) Re-consent: `POST /accounting/connections/:provider/connect-url` on an existing connection is not refused; the callback lands on the same row and `UPSERT_ACCOUNTING_CONNECTION_SQL`'s conflict path (unchanged) updates secrets, `granted_scope`, expiry, `status → connected`, `status_reason → NULL` while `settings` (including `companySwitches`), `feed_from`, `is_active_destination` and every `accounting_feed_syncs` row stay as they were; a `skipped` row is then due for the #2866 sweep (its `c.status = 'connected'` predicate, unchanged, admits it), a `pushed` row is never selected. (4) `GET /accounting/connections` and `GET /accounting/feed/status` expose `missingScopes: string[]` (`missingScopesFor`: `granted_scope` vs `requiredScopes`, plus the scopes the reason named while `scope_missing`; the feed status reads the DESTINATION row whatever its status via `getDestinationSummary`); OpenAPI and `api-types.ts` regenerated with the repo generator. No migration, no new scope. **Authority/custody argument: nothing moves.** Every secret involved is the user's own OAuth grant to their own accounting ledger — never a Haven key, a delegate key or anything that can move funds — and this slice only decides what STATUS is recorded next to such a grant, whether a sync row is `skipped` or `pushed`, and that a re-consent overwrites the grant in place. The perimeter files in the diff — `routes/accounting-connections.ts` (a comment), `routes/accounting-feed.ts` (one read-only field) — construct no transaction, sign nothing, touch no delegation, allowance, relayer or signer, and add no write path to any money table; `infra/repositories/accounting-connections.ts` is untouched. The double-post guard is NARROWED: the only new `skipped` transition is on a create call that provably created nothing, and the post-push path is now pinned in three places (conformance case 6 in both runners, the orchestrator unit test, and a real-database sequence that counts supplier-invoice POSTs). **What is mutation-tested**, each restored from a `cp` backup and verified by `cmp`, every count from one named scope at the delivered tree (uncommitted working tree on `feat/2865-scope-missing-reconsent` over `bb3a06cf`) — `npx vitest run --root packages/backend src/modules/accounting src/routes/__tests__/accounting-connections.test.ts src/routes/__tests__/accounting-feed.test.ts src/routes/__tests__/accounting-feed.entitlement-mode.test.ts`, 26 files / 259 tests green (new: `__tests__/scope-missing.db.test.ts`, 3 real-database sequences through the real OAuth2 flow, the real Fortnox connector on a fixture `fetch`, the real orchestrator and the real retry sweep with an injected clock; conformance case 6b in both runners; pre-push and helper unit tests): (a) skipping the callback scope comparison — **1 failed / 258 passed** (the narrower grant stored as `connected`); (b) making the post-push path mark the row `skipped` — **4 failed / 255 passed** (the real-database sequence, conformance 6 in both runners, the orchestrator unit test), and with that sequence's early row assertions relaxed so the run reaches the sweep, the sweep posted a **second** supplier invoice (`expected 2 to be 1`, 1 failed / 2 passed of the file); (c) resetting `settings`/`feed_from` on the upsert's conflict path — **4 failed / 255 passed** (the preservation sequence: `expected '{}' to be '{"autoFeed":false,…'`, plus three #2862/#2864 tests that also depend on the floor surviving a reconnect); (d) dropping the pre-push `scope_missing` flip — **4 failed / 255 passed** (the real-database pre-push sequence, conformance 6b in both runners, the orchestrator unit test). The negative side of the connector predicate — a 400 without the code, 401, 429, 500 on the invoice POST are thrown, never read as a missing scope — is asserted in `fortnox-connector.test.ts` ("pre-push scope refusal (#2865)"). Perimeter unchanged — narrowed, if anything: a grant short of a scope is named before its first push, and a post-push scope error can no longer be turned into a re-feed by anything downstream.
