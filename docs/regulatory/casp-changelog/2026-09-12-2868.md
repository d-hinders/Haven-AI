- **#2868** — Settings → Accounting connections card (epic #2858, slice 10, frontend only): a new `ConnectionsCard` on `/settings` lists every provider from `GET /accounting/providers` (Fortnox live; Accounted, Light, Igdrasil as *Coming soon* with a disabled Connect — listed, never endorsed) with the connection's five states rendered distinctly and one resolving action each (`connected` → Settings + Disconnect; `needs_reauthorisation` / `scope_missing` / `revoked_at_provider` → Reconnect + Disconnect; `disconnected` or no row → Connect); an inline `ConnectionSettings` form (`PATCH …/settings { suggested_account, auto_feed }`, 400 `INVALID_SETTING` shown inline naming `key`); a `BackfillDialog` on the OAuth return (`POST …/backfill { since: "YYYY-MM-DD" }`, default *Feed from now* makes no request); and a Disconnect confirmation stating that what was fed stays in the ledger and the feed history stays in Haven. The `/accounting` feed page loses its Connect / Disconnect card, keeps the sync rows, and forwards the callback's `?provider=…&connect=…&reason=…` query to `/settings` unchanged. The gate fired on `packages/frontend/src/app/(authenticated)/accounting/page.tsx` (+ its new test), which `casp-risk-guardrails.md` covers as reporting/accounting copy; no file in `.github/money-path-globs.json` is touched and no backend file changes. **Authority/custody argument: nothing moves.** Every call this surface makes is metadata about the user's OWN accounting connection over the routes #2862–#2867 already ship: a consent-URL fetch (the browser then navigates to the provider — no token ever reaches the page), a settings merge, a feed-floor move that only ever includes MORE already-settled history, and a disconnect that clears secrets server-side. Nothing here constructs or signs a transaction, touches a delegation, allowance, relayer, signer or key, and the copy keeps the accounting guardrail throughout — "appears in Fortnox with payment evidence attached; your accountant books it"; the suggested account is explained as a hint that "only suggests — it never books, and your accountant still chooses the account"; `lint:copy` is green with no new banned term. **What is mutation-tested,** each restored from a `cp` backup verified by `cmp` and the suite re-run green (69 tests across `src/components/accounting` + `src/app/(authenticated)/settings`): (a) routing `needs_reauthorisation` to Settings instead of Reconnect in `ConnectionRow.primaryActionFor` — 3 failed / 66 passed (the table pin, the state test, and the Settings page integration test); (b) dropping the inline `InlineAlert` in `ConnectionSettings` — 4 failed / 65 passed (client-side shape refusal, both `INVALID_SETTING` cases, the generic failure); (c) sending `since` as `new Date(since).toISOString()` from `BackfillDialog` instead of the field's `YYYY-MM-DD` — 2 failed / 67 passed (the dialog's body assertion and the card's end-to-end backfill POST); (d) calling DELETE straight from the row's Disconnect without the confirmation — 1 failed / 68 passed. **Perimeter unchanged — no file entered or left `.github/money-path-globs.json`, no backend change, and the card adds no path by which value can move.**
