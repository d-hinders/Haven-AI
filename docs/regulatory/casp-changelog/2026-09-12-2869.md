- **#2869** — `/accounting` feed page rework, sidebar attention marker, production *Coming soon* state (epic #2858, slice 11; frontend + one backend response-shape change). **What changed.** `GET /accounting/feed/status` — already outside `requireAccountingFeed` — now answers a COMPLETE shape in every off state: `enabled` (the `HAVEN_ACCOUNTING_ENABLED` flag under its own name, with `flagEnabled` kept as the older spelling of the same boolean), `companyName` and a new `destination` object (`provider`, `displayName`, `status`, `companyName`, `lastPushAt`) are now present, and null, when the feed is off. The gated actions (`/sync`, `/verify/:id`, `/reopen/:id`) still answer 404 in all three off cases — asserted for `hosted=false`, `hosted=true,enabled=false` and `hosted=false,enabled=true`. The dashboard reads those two flags through one helper (`accountingFeedOffState`) so three surfaces cannot disagree: the page renders *Coming soon* for `hosted && !enabled` (visible in production by owner decision 2026-09-11; exposure is a separate manual decision) and *not available on self-hosted* for `!hosted`, which must never read as coming soon; the sidebar keeps the entry with a muted Coming soon marker in the first case and hides it in the second; the Settings card (#2868) lists its providers with **no action at all** in the first and lists none in the second. With the flag on the page adds a connection summary line (where spend is going, or the attention state with a link to Settings) and the #2866 retry counts. **Authority/custody argument: nothing moves, and the diff REMOVES reach rather than adding it.** Every new surface is read-only: one additional GET of metadata about the user's own accounting connection, rendered as text and a badge. No new route, no write, no path to a transaction, delegation, allowance, relayer, signer or key; `.github/money-path-globs.json` is untouched and no file enters or leaves the money-path perimeter. The two off states deliberately expose FEWER controls than the add-on upsell they replace — no connect, reconnect, sync or verify control is reachable in either, disabled ones included, which is asserted by role in the component tests, the page tests, the screenshot scenario and the visual spec. **Copy keeps the non-asserting accounting guardrail**: the Coming soon state says settled payments "will appear in your accounting tool as drafts with the payment evidence attached. Your accountant codes and books them; Haven asserts nothing", the platform list is marked "Listed, not endorsed. Which ones go live is a separate decision", and the self-hosted copy claims nothing about a future. `lint:copy` is green with no new banned term. **What is mutation-tested** (each restored from a `cp` backup and the suite re-run green): (a) the sidebar marker rendered regardless of state; (b) `ComingSoon` rendered for `!hosted`; (c) a **Sync now** control left reachable in the off state; (d) `/status` put behind `requireAccountingFeed` so it 404s when off. Counts and the files each cell reddened are in the PR body. **Perimeter unchanged.**
