- **#2907** — Naming epic #2906, phase 0: renamed the wire contract
  ADDITIVELY in `packages/backend/src/openapi/spec.ts`. Nine Safe-named paths
  (`/user/safes*`, `/transactions|balances|portfolio/{safeAddress}`) gained
  `/user/accounts*` / `{accountAddress}` twins sharing the exact same handler
  (registered a second time under the new prefix where the path has more than
  one segment; a single dynamic segment needs no second registration — the
  URL is already served under both names). The 11 operationIds gained
  `*Account*` twins; old operationIds and paths carry `deprecated: true` and a
  removal-release sentence built from one shared `deprecatedSafeAlias()`
  template (DRY rather than 31 copies of the same string) — the literal
  source-text `grep -c "removed in" openapi/spec.ts` is 3 (the template
  definition plus one unrelated pre-existing usage plus a doc-comment
  naming the convention); `grep -c "deprecatedSafeAlias(" openapi/spec.ts`
  is 31 and is the number that reflects how many old names actually carry
  the sentence at runtime.
  Schemas `userSafe`/`sessionSafe` are unchanged in name (owner review on
  #2906 corrected the epic's original "rename `components.safe`" plan); new
  flat-duplicate schemas `account`/`sessionAccount` share one properties
  object with the old schemas (`accountAliasProperties` /
  `sessionAccountAliasProperties`) rather than an `allOf` $ref — #2888's
  established lesson: `closeObjects` registers every component CLOSED, so a
  `$ref` inside `allOf` is left open and false-fails `additionalProperties:
  false`. Safe-named properties in `Agent`, `Transaction`,
  `DashboardAgentPreview`, `TransactionsResponse` and the session-user shapes
  gained account-vocabulary twins in `properties` (declared, always emitted,
  dual-emit equality-tested — `openapi/wire-aliases.ts` /
  `wire-aliases.test.ts`), deliberately kept OUT of each schema's `required`
  array: widening `required` flips the generated `packages/core/src/
  api-types.ts` field from optional to non-optional for every downstream
  consumer repo-wide (frontend fixtures/hooks/components), a blast radius
  this additive backend slice does not own — the emission guarantee is
  enforced by the mapper's equality test instead, which is what the PR's
  mutation proof exercises. `AgentPaymentNextAction`'s served enum gained
  `fund_account_or_raise_allowance` additively (schema + `x-enumDescriptions`)
  without touching the backend taxonomy mirror, which stays parity-pinned to
  the SDK (`agent-payment-taxonomy.parity.test.ts`, a P1/#2908 surface); the
  server still only emits the old `fund_safe_or_raise_allowance` value,
  pinned by `agent-next-action-account-alias.test.ts` with a comment naming
  #2914 as the flip. `?accountId=` is accepted alongside `?safeId=` on both
  transactions list endpoints (both filter identically — an unrecognized-id
  400 and a real filtered-vs-unfiltered count difference are both asserted,
  since an unknown query key is otherwise silently ignored). `sign_data.
  components.payer_account` is a same-value twin of `components.safe` on the
  two routes that emit it (`delegation-authorize.ts`, `replay.ts`) —
  deliberately NOT a rename into `components.account`, which already means
  the delegate account address on those two shapes (a different address; an
  installed SDK reads `components.safe` as the x402 receipt's `payer` field,
  `x402-funding-leg.ts:312`, so collapsing the two would silently corrupt that
  field for every client that predates this release). `components.account`
  is untouched, and no `x402_expected_context_version` change was made
  anywhere (`sign_data` is response metadata, never part of the signed
  expected-context payload — confirmed on #2906; a version bump would have
  made every installed signer refuse to sign, `SUPPORTED_X402_EXPECTED_
  VERSIONS = [1, 2, 3]` unchanged). The dashboard CSV export gained an
  appended `account_address` column (same value as `safe_address`), per the
  export's own "append at the end, never reorder" contract. `api-types.ts`
  regenerated and the blocking drift check (`check:api-types`) is green.

  **Authority/custody argument.** Nothing here changes who can spend, from
  what account, or under what caveat: every new field, path and enum value is
  a same-value, additive alias over an existing read or an existing
  server-emitted value — no new query, no new authority check, no altered
  signed payload. The one new user-visible value
  (`fund_account_or_raise_allowance`) is listed in the served enum so
  clients can switch on it — no route takes this enum as request input at
  all, so "accepted on input" was never true of it — and changes no server
  behavior; the server's own emission is unchanged and pinned.
  Mutation-proven: (a) dropping a twin field from a `wire-aliases.ts` mapper
  fails its equality test; (b) simulating an early flip to the new enum value
  fails the emission pin; (c) removing the `?accountId=` fallback makes the
  filtered-count-differs-from-unfiltered assertion fail; (d) aliasing
  `payer_account` to `components.account` instead of `components.safe` fails
  the payer-account equality test. Perimeter unchanged.

  **Review-findings addendum (2026-09-12, same PR).** A review pass on this
  slice found and fixed one authority-shaped gap and three lower-severity
  test/doc gaps, all in the files this shard already covers:
  - **`owner_cli` allow-list gap (HIGH).** `middleware/owner-cli.ts` allow-
    listed `GET /user/safes` and `GET /user/safes/{safeId}/funding` by literal
    registered path; `routeAllowsOwnerCli` compares that literal, so the
    identical `/user/accounts*` mount (same handler module, same authority,
    registered a second time under the account-vocabulary prefix in
    `index.ts`) silently refused the same `owner_cli` token for the same data
    — a live probe showed `{"/user/safes":200,"/user/accounts":401}`. Fixed by
    adding the two twin entries to `OWNER_CLI_ALLOWED_ROUTES` (no new
    authority — the twin mount already existed and already ran the same
    handler; this closes a refusal gap, not an authorization grant). Proven
    with a request-level parity test (`routes/__tests__/user-safes-funding.
    test.ts`) and an enforcement-level parity test
    (`__tests__/owner-cli-route-census.test.ts`); mutation-proven by removing
    the two entries, which fails 4 assertions across both files.
  - **Wire-twin equality moved from mapper-only to request-level** for
    `GET /agents`, `GET /agents/{id}`, `GET /dashboard/overview`
    (`agents[]`/`transactions[]`), `GET /transactions`
    (`transactions[]`/`failedAccountIds`), and `GET /auth/me`
    (top-level + per-safe) — each mutation-proven by removing the
    corresponding `wire-aliases.ts` call at its route and confirming the new
    assertion fails. `sign_data.components.payer_account` on the two x402
    emission sites stays a source-regex assertion only
    (`openapi/payer-account-alias.test.ts`) — the orchestration fixture cost
    was judged not worth it in this pass; flagged, not fixed.
  - **`?accountId=` on `/transactions/export.csv`** had no test (reverting it
    to `safeId`-only left the suite green); added a filtered-vs-unfiltered
    row-count test and an identical-output-to-`?safeId=` test, both mutation-
    proven.
  - **`scripts/ci/safe-account-rename-census.mjs`** printed its seven
    per-token counts and always exited 0 — a per-token zero (the exact
    silent-failure mode the issue's AC calls out) would not have failed CI.
    Now exits non-zero on any zeroed token, wired into
    `node --test scripts/ci/*.test.mjs` (the same `ci_config_checks` job every
    other `scripts/ci/*.test.mjs` runs under) via a new companion
    `safe-account-rename-census.test.mjs`; mutation-proven.
  - Two doc-accuracy fixes: the four `/user/accounts/{accountId}...` twin-path
    descriptions in `spec.ts` said `{accountId}` where the actual registered
    key is `{safeId}` (the path parameter is not renamed in P0 — corrected,
    with a sentence on why); and `?accountId=`/`?safeId=` precedence
    (`accountId` wins when both are given) is now stated in the spec and
    covered by a test.
  None of the above changes who can spend, from what account, or under what
  caveat — the HIGH item closes an over-refusal (the token could already do
  everything through `/user/safes*`), and the rest are additional wire-twin
  and test-instrument coverage over the same additive, already-reviewed
  shape. `grep -c "removed in" openapi/spec.ts` is still 3 and
  `grep -c "deprecatedSafeAlias(" openapi/spec.ts` was 31 in the first review
  round — #2914's contraction pass should grep `deprecatedSafeAlias(`, not
  `"removed in"`, since the latter counts the template definition and doc
  comments rather than the per-field deprecation sites.

  **Second review-findings addendum (2026-09-12, same PR) — the reviewer's
  triaged-out list, delivered.** Four findings from the first review were
  filed for size rather than fixed. All four are additive, same-value twins
  over already-fetched rows — no new query, no new authority, no altered
  signed payload:

  - **`CreateAgentRequest.account_id`** — the one genuine INPUT change in
    this whole slice: `POST /agents` now accepts `account_id` alongside
    `safe_id` (either alone wins; both present and DISAGREEING is a 400
    naming both keys, rather than silently preferring one and hiding a
    caller bug). This is what unblocks P1/#2908, which makes the SDK send
    `account_id`. Route-tested for old-only, new-only (distinguished from
    the default-safe fallback by asserting the two-param
    `FIND_USER_SAFE_ID_FOR_USER_SQL` call, not just the response body — a
    weaker first draft of this test passed even with the `?? account_id`
    fallback deleted), both-equal, and both-disagree
    (`routes/__tests__/agents.test.ts`).
  - **`activityPayment.safe_id/safe_address/safe_name`** (`GET
    /agents/{id}/activity`, the combined feed) → `account_id/account_address/
    account_name` twins via a new `withActivityPaymentAccountAlias` mapper in
    `openapi/wire-aliases.ts`, applied at both emit sites in
    `routes/agent-activity.ts`; request-level twin===old assertions added to
    `routes/__tests__/agent-activity.test.ts` for both the single-agent and
    all-agent feed shapes.
  - **`account_address` twins on `userProfile.safe_address`
    (`PUT /user/profile`), `userIdentity.safe_address` (`PUT /user/wallet`),
    the passkey list item's `safe_address` (`GET /passkeys`),
    `DelegateBalance.safe_address` (`GET /agents/{id}/delegate-balance`),
    `MachinePaymentAgent.safe_address` (`GET /agent`, the MCP agent-identity
    read) and `AllowanceSummary.safe_address`** (`GET /allowances`) — each
    dual-emitted at its one emitting route (`withSessionAccountAddressAlias`
    for the three nullable-string shapes, `withAccountAddressAlias` for the
    two non-nullable `AgentContext`-sourced shapes, and a direct
    `account_address: agent.safe_address` at the delegate-balance route,
    since `agent.safe_address` there is nullable pre-linking and the generic
    mapper's `SafeAddressed` constraint requires a non-null string), each
    with a request-level twin===old assertion
    (`routes/__tests__/user.test.ts`, `routes/__tests__/machine-payments.test.ts`,
    `routes/__tests__/agents-characterization.test.ts`).
  - **`PaymentReceipt.payment.account`** (`modules/payments/receipt.ts`) —
    `account` is free on this shape (no other field on `payment` already
    claims that name), so the twin is `payment.account`, not
    `payment.payer_account` — unlike `sign_data.components`, where `account`
    already means the delegate account address and the twin is named
    `payer_account` to avoid colliding with it. `PaymentReceipt` is a
    published `@haven_ai/sdk` type (byte-identical in this slice, per the
    epic's package boundary): the object literal is built with the extra
    field and widened with a type assertion rather than changing the SDK
    interface. Verification is unaffected — `verifyPaymentReceipt` recovers
    the signer from the STORED `authorization.signHash`; it never recomputes
    a hash over `payment`, so an additive field changes nothing about what
    the delegate's signature covers (asserted directly in
    `modules/payments/__tests__/receipt.test.ts`).
  - **The `safes` envelope key gains an `accounts` twin key, same array** —
    on `GET /user/safes` / `GET /user/accounts` (`routes/user-safes.ts`) and
    on every session-shaped response (`POST /auth/signup`, `POST
    /auth/login`, `GET /auth/me`) via a new `withAccountsEnvelopeAlias`
    mapper. The `accounts` array's item schema is `sessionAccount` — until
    now defined in `spec.ts` and never referenced by any operation, so it
    never reached `packages/core/src/api-types.ts` and `sessionSafe`'s
    deprecation sentence ("same value as `sessionAccount`") was aspirational
    rather than true. It is true now (item C on this pass), pinned by the
    new `openapi/session-account-schema.test.ts`.
  - **The `AgentPaymentNextAction` schema's own doc comment and this shard**
    said `fund_account_or_raise_allowance` is "accepted on input" — false: no
    route takes this enum as request input at all; it is response-only. Both
    now say "listed in the enum so clients can switch on it." The enum pin
    (`openapi/agent-next-action-account-alias.test.ts`) already asserted the
    schema-level claim; this pass adds real-response assertions on BOTH
    emission sites (`modules/x402/delegation-authorize.ts` — the erc7710
    pre-check ~line 198 and the EIP-3009 funding-leg pre-check ~line 433,
    exercised respectively by the existing `x402 delegation-rail settlement`
    and `EIP-3009 funding-leg budget refusal` blocks in
    `routes/__tests__/x402-delegation.test.ts`) and a real-response
    `sign_data.components.payer_account === components.safe` assertion on
    both the erc7710 funding 201 and the idempotent-replay 201 — replacing
    the source-regex-only pin the shard previously flagged as not worth
    fixing in the first round.
  - **`sign_data.components.payer_account`** also twins `components.safe` on
    the one remaining site that carried `safe` with no twin:
    `routes/payments.ts`'s legacy `replayIntentBody` fall-through (the
    `execution_rail !== 'delegation'` branch) — the "clean case" the issue
    names, since that shape carries no `account` key at all. Per the
    existing code comment at that site, this branch is unreachable in
    production (the only live insert path always pins
    `executionRail: 'delegation'`); the twin is added for completeness and
    is not independently route-tested for that reason — flagged here rather
    than silently left inconsistent with every other `components.safe` site.

  Mutation-proven (cp-backup + diff-verified restore, this pass): dropping
  the `withActivityPaymentAccountAlias` call at one of its two emit sites
  fails exactly that site's request-level assertion; deleting the `??
  account_id` fallback in `routes/agents.ts` fails the account_id-only
  create-agent test (via the two-param SQL-call assertion, not just the
  response body); flipping ONE of the two `AgentPaymentNextAction` emission
  sites fails exactly that site's real-response assertion and leaves the
  other green; pointing `payer_account` at the delegate account address
  instead of `safe_address` fails the real-response equality assertion.
  `node scripts/ci/safe-account-rename-census.mjs 6e3ea1dc` reproduces the
  same total (2591) and all seven per-token counts unchanged — this pass
  added no new Safe-vocabulary source text, only twins of it.

  **Not done in this pass, and why.** AC #1's characterization set (a
  response recording from a separate worktree checked out at the PR's base,
  replayed at HEAD with the new twin keys stripped and asserted deep-equal)
  was not built: the AC is about the additive rename as a whole and was
  already the first round's design target (every mapper is an equality
  transform over an already-emitted value, tested that way at the unit and,
  after the first review, the request level), but a dedicated recorded-fixture
  harness across ten-plus routes in an isolated `npm ci`'d worktree is a
  separate, larger piece of work than this fix-round's four triaged findings
  and was not attempted here — filed as follow-up rather than fabricated.

  **AC #1 delivered (2026-09-12, follow-up pass).** The old-name response
  characterization filed above as follow-up is now built. A scratch worktree
  checked out at the PR's base commit (`6e3ea1dc`, node_modules copied rather
  than `npm ci`'d — the lockfile is byte-identical between base and HEAD,
  verified by `git diff` before copying) recorded 13 real responses on the
  mocked-db/mocked-repository harness style every sibling route test in this
  tree already uses (fixed literal rows, no real Postgres data, so the
  recording is deterministic): `GET /user/safes` (list), `GET /user/safes/
  {id}/funding`, `GET /agents` (list), `GET /agents/{id}`, `POST /agents`
  (create), `GET /agents/{id}/activity`, `buildPaymentReceipt` (the pure
  function backing the receipt shape), `POST /auth/login` (401 branch),
  `GET /auth/me`, `GET /passkeys`, `GET /machine-payments/agent`,
  `PUT /user/profile`, `PUT /user/wallet` —
  `packages/backend/src/openapi/__fixtures__/p0-characterization/*.json`,
  each carrying `_base: "6e3ea1dc"`, the exact request, and the recorded
  status/body. One normalization, documented in the recorder
  (`record.ts`): a JWT's `iat`/`exp` are time-variant by construction, so the
  fixture stores the SIGNING CLAIMS (`authClaims: { sub, email }`) instead of
  the literal bearer token, and the replay mints a fresh token from the same
  claims. One exclusion: `POST /agents`'s `api_key` is a freshly generated
  secret (`crypto.randomBytes(24)`) by design — format-checked
  (`/^sk_agent_[0-9a-f]{48}$/`) on both sides rather than value-pinned.

  The replay suite (`packages/backend/src/openapi/p0-characterization.test.ts`,
  16 tests) runs every recorded request against HEAD's route modules —
  including a second call against the `/user/accounts/{id}/funding` path
  twin for the funding fixture — and asserts three things per route: (1) the
  status code is unchanged; (2) the OLD-named fields are byte-for-byte
  unchanged (recorded body and live body, both twin-keys-stripped, deep-
  equal); (3) every twin key present in the live response equals its old-name
  sibling. The twin-key list (6 pairs: `safe_id`/`account_id`,
  `safe_address`/`account_address`, `safe_name`/`account_name`,
  `safe_chain_id`/`account_chain_id`, `safe`/`account` on
  `PaymentReceipt.payment`, and the `safes`/`accounts` envelope pair) is
  declared once in the test file and checked against the
  `deprecatedSafeAlias(...)` registry in `spec.ts` (every field-level NEW key
  is asserted to be a real `deprecatedSafeAlias('<key>')` argument; the
  envelope pair is asserted NOT to be one, since the `safes` property itself
  carries no `deprecated: true` — only its item shape gains the twin).
  Mutation-proven (cp-backup, diff-verified revert): (a) forcing
  `withAgentAccountAlias`'s `account_id` to `null` fails the twin-equality
  assertion (`body.account_id (null) !== body.safe_id ("b1d7c9a4-…")`); (b)
  dropping `safe_name` from the `GET /agents/{id}` legacy-rail response
  fails the byte-for-byte comparison (diff shows the missing key); (c)
  removing the `safe_id`/`account_id` pair from the twin-key list makes the
  now-unstripped `account_id` surface as an extra key in the byte-for-byte
  diff. Gates run clean on the resulting tree: `typecheck`,
  `check:api-types`, `lint:db-mocks` (the new files are `db-mock-exempt`, the
  same convention `user-safes-funding.test.ts` already uses — no baseline
  bump), `lint:wire-types`, `lint:copy`, `docs:check`, `docs:coupling`
  (28 docs flagged as possibly touched — pre-existing state of this
  three-commit branch against `origin/dev`, unrelated to this pass's diff),
  `docs:covers-gaps` (0 new gaps). The scratch base worktree was removed
  after recording.
