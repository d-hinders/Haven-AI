- **#2909** — naming epic #2906 phase 2a: rename the persistence layer's identifiers (repositories + SQL constants) while the schema stays `user_safes`. **What changed.** `infra/repositories/user-safes.ts` → `smart-accounts.ts` (`git mv`); every `*SAFE*_SQL` constant across `infra/repositories/**` (24 in the renamed file, plus 12 more across `agents.ts`, `agent-connection-setups.ts`, `transaction-history.ts`, `dashboard.ts`, `machine-payments.ts` and `user-passkeys.ts`) renamed to `*ACCOUNT*_SQL`; exported row types (`UserSafeRow` → `SmartAccountRow`, `SafeInfoRow` → `AccountInfoRow`, `TransactionSafeRow` → `TransactionAccountRow`, `DashboardSafeRow` → `DashboardAccountRow`, `StoredPasskeySafeRow` → `StoredPasskeyAccountRow`, `HybridOwnerSafeRow` → `HybridOwnerAccountRow`, `SessionSafeRow` → `SessionAccountRow`, `OwnedSafeWithTypeRow` → `OwnedAccountWithTypeRow`, `SafeWithAccountTypeRow` → `AccountWithTypeRow`, `AgentSafeBindingRow` → `AgentAccountBindingRow`, `SafeOwnershipRow` → `AccountOwnershipRow`) and their exported functions (`findOwnedSafesWithType` → `findOwnedAccountsWithType`, `listSafesWithAccountTypeForUser` → `listAccountsWithTypeForUser`, and 15 more listed in the PR body) renamed to match; the internal `safeId`/`safeAddress` positional parameters became `accountId`/`accountAddress` inside `infra/repositories/**` only. Every importer (`routes/user-safes.ts`, `routes/auth.ts`, `routes/agents.ts`, `routes/agent-connection-setups.ts`, `routes/balances.ts`, `routes/portfolio.ts`, `routes/transactions.ts`, `routes/dashboard.ts`, `rails/execution-rail.ts`, `rails/hybrid-account-config.ts`, `modules/transactions/**`, `scripts/db-schema-smoke.ts`) had its import path and the renamed symbol's usages updated — nothing else in those files changed. The three `routes/__tests__/user-safes-*.test.ts` files whose keys sit in `db-mock-baseline.json` (`characterization`, `delete`, `list`) moved to `smart-accounts-*.test.ts` (`git mv`), and the baseline's three keys moved with them at unchanged values (57 files, same counts, before and after — `--update` only re-sorted). `infra/repositories/__tests__/user-safes.test.ts` moved to `smart-accounts.test.ts` alongside its subject file. **Schema, wire and behaviour unchanged.** The table stays `user_safes`; every SQL string literal is byte-identical to `origin/dev` — proven by extracting all 115 SQL template/string literals from the eight touched repository files (old ref vs new working tree) and diffing them: zero differences. `openapi/spec.ts`, `api-types.ts`, route paths, response shapes and `routes/safe-deploy.ts` are untouched; the `dep-lint-callsite-ceiling.json` key for that file therefore does not move. The deleted-module denylist in `routes/__tests__/retired-rail-routing.guard.test.ts:95-112` is untouched. **Authority/custody argument: this is a pure internal rename.** No route, no SQL predicate, no tenant-scoping clause (`WHERE user_id = $1` stays verbatim everywhere), no delegation, allowance, relayer, signer or key touches this diff — only the TypeScript-side names attached to the same queries. The existing real-DB repository suite (run against Postgres, not mocks) is the characterization: it exercises the renamed functions against the unchanged schema and passes with no behavioural assertion altered. `.github/money-path-globs.json` is untouched. Money-path classification is by file glob only: `node scripts/ci/money-path-classify.mjs` self-test passes (6/6 positive, 6/6 negative controls); the changed-file check (run manually against the working tree, since this slice does not commit) matches `packages/backend/src/infra/repositories/**` (15 files) plus the two named rails files and one named route importer already on the perimeter. **Non-mechanical hunks:** the identifier rename itself is fully scripted (quoted verbatim in the PR body); 15 additional hand-edited hunks were needed to reword prose comments/docs that named the old file (`user-safes.ts`) or an old identifier (`UserSafeRow`, `updateUserSafeAddress`, `insertUserSafe`, `setLegacyUserSafeAddress`, `seedUserSafe`) in a sentence rather than as live code, because the AC grep (`git grep -n "user-safes\|UserSafe\|SafeRow\|findOwnedSafes\|listSafesWith" -- 'packages/backend/src/infra/**' 'packages/backend/scripts/**'`) is a literal substring match with no comment/code distinction — each is listed in the PR body with its file and reason; none changes behaviour, an SQL literal, or a schema/wire claim. **Perimeter unchanged.**
