- **#2910** — naming epic #2906 phase 2b: rename the remaining backend identifiers outside the persistence layer (routes, modules, rails, `infra/chain/**`, `infra/relayer*`) while the schema stays `user_safes`/`safe_address` and the wire contract is untouched. **What changed.** `safeId`/`safeAddress` positional parameters and locals renamed to `accountId`/`accountAddress` in `routes/user-safes.ts` (destructure-rename only — the Fastify `Params` type keys and the `:safeId`/`:safeAddress` path segments are untouched, since route paths are #2914's slice), `routes/balances.ts`, `routes/portfolio.ts`, `routes/transactions.ts`, `routes/dashboard.ts`, `routes/agents.ts`, `routes/agent-connection-setups.ts`, `routes/payments.ts`, `modules/transactions/{aggregate,orchestration,ordering,cache-key,types,index}.ts`, `modules/accounts/portfolio.ts`, `modules/mpp/sweep.ts`, `rails/sweep.ts`, `rails/hybrid-account-config.ts` and their tests. The deferred P2a fields — `NewAgent.safeId` → `accountId`, `CreatedAgent.safeInfo` → `accountInfo` (`infra/repositories/agents.ts`), `NewSetup.safeId` → `accountId` and the anonymous `insertPendingAgent` input's `safeId` → `accountId` (`infra/repositories/agent-connection-setups.ts`), `NewDelegationIntent.safeAddress`/`NewSendIntent.safeAddress` → `accountAddress` (`infra/repositories/payment-intents.ts`) — are renamed with their route call sites and repository tests, exactly as #2909's PR body flagged them for this slice. Exported symbols renamed: `fetchSafeTransactions` → `fetchAccountTransactions`, `FetchSafeTransactionsParams`/`Result` → `FetchAccountTransactionsParams`/`Result`, `AggregateSafeTransactionsResult` → `AggregateAccountTransactionsResult`, `aggregateSafeTransactions` → `aggregateAccountTransactions`, `SafeTransactionsPageParams`/`Page` → `AccountTransactionsPageParams`/`Page`, `buildSafeTransactionsPage` → `buildAccountTransactionsPage`, `sessionSafePayload` → `sessionAccountPayload` (`modules/accounts/mainnet-gate.ts`, called from `routes/auth.ts`), `fetchPortfolioForSafe` → `fetchPortfolioForAccount` (`modules/accounts/portfolio.ts`, called from `routes/portfolio.ts` and `routes/dashboard.ts`), `resolveUserSafe` → `resolveAccountForSetup` (`routes/agent-connection-setups.ts`, private). The dead `'safe_deploy'` member of `RelayerOperation` (`infra/relayer-spend-guard.ts`) is removed, along with its `RULES` entry; `relayer_gas_events.operation` rows written before the retirement keep the literal value as history (owner decision, epic #2906 Notes) — the repository layer (`infra/repositories/relayer-gas-events.ts`) types that column `string` throughout and never re-validates it against `RelayerOperation`, so a historical read does not throw, proven by a new test in `infra/__tests__/relayer-spend-guard.test.ts` (`relayerSpendSummary` reads a row with `operation: 'safe_deploy'` without throwing) plus a `@ts-expect-error` compile-time guard that the literal no longer assigns to the type, mutation-proved by temporarily re-adding the member (the `@ts-expect-error` then becomes an unused-directive error, failing `tsc`). The `users.safe_address` legacy-mirror writer (`setDefaultAccountForUser`/`deleteAccountForUser` in `infra/repositories/smart-accounts.ts`) was already renamed by #2909 — verified unchanged here, no further edit needed.

  **Deliberately NOT renamed, and why.** `openapi/wire-aliases.ts` is untouched (explicit scope boundary), and its exported mapper functions (`withTransactionAccountAlias`, `withDashboardAgentAccountAlias`, `withFailedAccountIdsAlias`) constrain their input generically on the EXACT field names `safeId`/`safeAddress`/`safeName`/`failedSafeIds`. Renaming `EnrichedTransaction.safeId`/`.safeAddress`/`.safeName` (`modules/transactions/types.ts`) or `AggregateSafeTransactionsResult.failedSafeIds` would make every call site that feeds one of those mappers (`routes/transactions.ts`'s `enrichedPage.map(withTransactionAccountAlias)` and `withFailedAccountIdsAlias({ ..., failedSafeIds })`; `routes/dashboard.ts`'s two `withTransactionAccountAlias({...})`/`withDashboardAgentAccountAlias({...})` object-literal constructions) fail to typecheck without also editing `wire-aliases.ts` — out of scope for this slice. These field names, and every object-literal construction site that must still spell them (`orchestration.ts`'s `merged.push({...})` and the page-builder's tx map, `dashboard.ts`'s two alias call sites, `x402.ts`, `enrichment.ts`, `ordering.ts`'s two `EnrichedTransaction`-reading functions, `csv-export.ts`), are left exactly as they were and remain in the post-rename AC grep, justified there rather than silently dropped. Also untouched: Fastify route **path** strings and the `Params`/`Querystring` TYPE field names bound to them (`:safeId`, `:safeAddress`, the dual-accept `?safeId=`/`?accountId=` query param from #2907) — route paths are #2914's slice; the `owner_cli` route-coverage allow-list literal paths (`middleware/owner-cli.ts`, `__tests__/owner-cli-route-census.test.ts`) — rewriting those strings would make the allow-list stop matching the actually-registered routes; `openapi/spec.ts`, `openapi/wire-aliases.ts`(+`.test.ts`), `openapi/spec.test.ts` (wire files); the retired-rail routing guard denylist; `db/migrations/**` (no migration edits — the one migration test with local `safeId`/`seedUserSafe` identifiers, `069_drop_safe_approver_metadata.test.ts`, is left for #2911/#2912's slice).

  **Schema, wire and behaviour unchanged.** The table stays `user_safes`; the columns stay `safe_id`/`safe_address`/`safe_name`/`safe_chain_id`. No SQL string literal in any touched file changed — the touched files with inline SQL (`infra/repositories/agents.ts`, `agent-connection-setups.ts`, `payment-intents.ts`, `relayer-gas-events.ts` unchanged) carry SQL text that mentions only `safe_id`/`safe_address` (snake_case columns, never matching the `safeId`/`safeAddress`/`SafeRow`/`UserSafe` identifier tokens this slice renames) or is untouched entirely. `openapi/spec.ts`, `api-types.ts`, and every route path are untouched; `check:api-types` stays clean. `.github/money-path-globs.json` is untouched. **Money-path classification (manual, working-tree, since this slice does not commit):** touched files matching a runtime glob — `infra/relayer-spend-guard.ts` (`infra/relayer*.ts`), `infra/repositories/{agents,agent-connection-setups,payment-intents}.ts` (`infra/repositories/**`), `modules/accounts/mainnet-gate.ts`, `modules/mpp/sweep.ts` (`modules/mpp/**`), `rails/{sweep,hybrid-account-config}.ts`, `routes/{agent-connection-setups,payments}.ts` — 8 files on the perimeter. **MONEY-PATH**; a dev→main promotion needs a covering green money-flow QA run. **Authority/custody argument: this is a pure internal rename plus one dead-enum-member removal.** No route, no SQL predicate, no tenant-scoping clause, no delegation, allowance, relayer-budget RULE VALUE, signer or key semantic changes — the relayer budget caps, identity columns and window minutes for `hybrid_deploy`/`allowance_transfer`/`sweep` are byte-identical; only the retired `safe_deploy` key (whose route was already deleted in #1988) is removed from the map. The existing backend suite plus the two new relayer-spend-guard tests are the characterization.

  **Non-mechanical hunks:** the mechanical identifier rename is scripted (`rename-2910.mjs`, quoted verbatim in the PR body — two passes: a blanket word-boundary pass over 21 files verified to carry no Fastify-path/wire-tied occurrence, and ~30 targeted exact-substring replacements for files that mix wire-tied and internal occurrences of the same token). Hand-written, non-mechanical: the two new relayer-spend-guard.test.ts tests (read-path characterization + `@ts-expect-error` compile guard) and this shard; zero prose/doc rewording hunks were needed beyond the `docs/operations/package-dev-channel.md` re-verification paragraph (its `covers:` names `routes/agent-connection-setups.ts`, touched here by an identifier-only diff — the strict coupling gate requires the re-check, matching #2909's precedent).
