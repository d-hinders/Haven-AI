- **#2915** — the sync/backfill answer now reports what the feed actually DELIVERED instead of what it enumerated: `syncUser` counted every enumerated candidate as "fed" even when `feedSettledPayment` returned `not_fed` (FX not ready, below the floor), `skipped` (a dead grant or a connector skip) or `failed` (the push threw), so the dialog said "N earlier payments fed." while the /accounting list stayed empty and the same claim repeated on every Sync. `fed` now counts only the `{ outcome: 'pushed' }` results and the sync and backfill wire answers `{ fed, total }` (total = enumerated; additive, `required: [fed, total]`, openapi + `packages/core/src/api-types.ts` regenerated in sync), with the dialog copy naming the remainder ("X of Y earlier payments fed." with a Sync-now pointer, en+sv). Pure bookkeeping-delivery REPORTING over records of ALREADY-settled payments: `feedSettledPayment` is byte-identical (push, claim, skip and fail all unchanged — the only delta in `feed-orchestrator.ts` is the counting loop in `syncUser` and its doc comment), the selection statement still enumerates every settled candidate with no pushed row (`amount_sek IS NOT NULL OR fx_rates IS NOT NULL`, the #2877 either-form capture), no accounting assertion is added (the feed remains non-asserting, #491), and no payment authority, custody, budget or allowance surface moves — the wire change only makes an existing count honest. Perimeter unchanged.
