- **#2851** — safe-retirement epic #1440, final slice (5 of 5): one migration
  drops the last three dead Safe-rail tables and fixes a wrong default. One
  file in the money-path perimeter: `packages/backend/src/db/migrations/083_drop_dead_safe_rail_tables.ts`
  (new), registered in `db/migrations/index.ts`; `packages/backend/src/infra/repositories/smart-accounts.ts`
  loses one prepared statement in the same change. **The migration is
  schema-only, destructive of nothing live, and touches zero rows.** `up()` is
  three `DROP TABLE IF EXISTS` statements — no `CASCADE`, no `DELETE FROM`
  first, the same discipline `070`/`075`/`081` established (#2055) — plus one
  `ALTER TABLE user_safes ALTER COLUMN account_type SET DEFAULT` flip.
  `self_sign_agents` and `self_sign_payment_intents` (migrations `001`/`002`)
  are the pre-delegation self-sign identity/payment pair, with zero live
  INSERT or SELECT; the child (`self_sign_payment_intents.agent_id →
  self_sign_agents(id) ON DELETE CASCADE`) is dropped before its parent, so
  `CASCADE` is never needed. `owner_aliases` (migration `009`) lost its
  routes, repository and smoke statements in slice 1 (#2847, merged
  2026-09-11); no table declares a foreign key into it. **Authority/custody
  argument: nothing moves.** No transaction is constructed or signed, no
  delegation, allowance, relayer, signer or key is touched, and the owner
  decision on #1440 (2026-09-10) that gates this slice — retired **rows** in
  `user_safes` and their agents/intents stay as inert history — is respected
  exactly: this migration drops *tables* with zero live readers or writers and
  fixes a *default*, and no `user_safes` row is rewritten (proven by a
  dedicated test seeding a legacy `account_type='safe'` row, re-running
  `up()`, and asserting the row is unchanged). The default fix closes the one
  remaining inconsistency `075` (#2263) left open: `075` repointed
  `execution_rail`'s default at `'delegation'` but left `account_type` on the
  retired `'safe'`, so an insert naming neither column was born
  self-contradictory; it now reads `'delegator_hybrid'` on both columns. The
  three recorded keeps — `user_safes`, `user_passkeys`,
  `payment_intents.allowance_nonce` — are each asserted present and untouched
  by this migration's own test. `down()` restores the three tables verbatim
  (`001`/`002`/`009`'s shapes) and the `'safe'` default; both directions are
  idempotent, and data is not restored, since nothing dropped held a live row
  a rollback could meaningfully recreate. The repository-layer change removes
  `ORPHAN_SELF_SIGN_AGENTS_FOR_ACCOUNT_SQL` and its call inside
  `deleteAccountForUser`'s unlink transaction — it existed solely to null out
  `self_sign_agents.safe_id` ahead of a delete, so its own RESTRICT-FK reason
  for existing disappears with the table — and the matching
  `db-schema-smoke.ts` entry and import. Two tests that previously asserted
  the statement ran BEFORE the delete (`smart-accounts.test.ts`,
  `smart-accounts-delete.test.ts`) now assert it never runs at all — absence,
  not reordering. `user_safes` is unrenamed as of this migration; P3 (#2911)
  renames the table after this lands, and the migration's header comment
  tells that author the `account_type` default is already
  `'delegator_hybrid'` by then. **What is mutation-tested**, each planted
  alone and restored from a `cp` backup: (a) commenting out the
  `self_sign_agents` drop — the absence test for that table fails, and the
  shared worker schema is left off migration head, which the harness's own
  drift guard catches on the next test file rather than silently accepting a
  stale schema (repaired with `db:reap-test-schemas`, the harness's own
  documented remedy); (b) leaving `account_type`'s default at `'safe'` — the
  default-value test and the omitting-insert test both fail, reporting
  `'safe'` where `'delegator_hybrid'` was expected. Both restored and the full
  migration test (12 assertions) plus the two updated repository/route tests
  and `075`'s own suite (16 assertions, now nested through this migration's
  `down()`/`up()` to keep `075`'s narrower pre-#2851 contract provable) re-run
  green. Migration → CODEOWNERS review required; this PR carries the
  `money-path` label. Census note: the row census this slice's issue asks the
  owner to run on `#1440` before merge (a read-only operator step, not part of
  this diff) sizes the drop but gates nothing about correctness — this
  migration's behaviour is proven independent of row counts, since it deletes
  no rows. Perimeter unchanged — narrowed, if anything: two more dead tables
  and one dead prepared statement leave the schema, and the only live
  behavioural change is a default that makes a previously self-contradictory
  insert self-consistent.
