- **#2913** — Naming epic #2906, phase 4: the frontend reads and sends only
  the account vocabulary. Routing: `app/(authenticated)/accounts/[safeId]` →
  `[accountId]` with `useParams` keys renamed — the URL segment name does not
  change the URL, so `/accounts/<id>` needs no redirect (verified: no `?safe=`
  query key exists anywhere in the frontend to redirect). State:
  `ACTIVE_SAFE_STORAGE_KEY` renamed to `haven_active_account_id` with the
  one NEW logic in this slice — a
  one-time migration read (old key present, new absent → write new, delete
  old, selection preserved), unit-tested in
  `src/lib/__tests__/auth-storage.test.ts` including the old-key-absent and
  both-keys-present orders. Hooks/contexts: `useUserSafes` → `useAccounts`,
  `useSafeFunding` → `useAccountFunding`, `useSafeOperationGate` →
  `useAccountOperationGate` (legacy arm accepted residue from #1440, rename
  only), `UserSafe*` types → `SmartAccount*` matching the backend table.
  Prop renames (`safeId`/`safeAddress` → `accountId`/`accountAddress`)
  across ~115 files. `TransactionsClient` sends `?accountId=` (accepted by
  the server since #2907) with the request URL asserted in the feed test.
  Agent handoff (`lib/agent-handoff.ts`) emits `HAVEN_ACCOUNT_ADDRESS` FIRST
  plus `HAVEN_WALLET_ADDRESS` and `HAVEN_SAFE_ADDRESS` during the window
  (both old names removed at #2914, per the #2906 decision). Fixtures and
  harness: `FIXTURE_SAFE` → `FIXTURE_ACCOUNT`, `screenshot.mjs` seeds the new
  storage key, `screenshot-fixture.test.ts` parity updated, the `[safeId]`
  route-path entry in `retired-rail-prose-baseline.json` +
  `retired-rail-prose-justifications.json` renamed in lockstep.

  **Authority/custody argument.** This is a rename-only slice on money-path
  globs: `src/lib/signer.ts` (46 changed lines) and
  `src/hooks/useAgentRekey.ts` (1 changed line) — the diffstat vs dev is
  `useAgentRekey.ts | 2 +-` and `signer.ts | 46 ++++++++++++++------------`,
  and every hunk is identifier-level: function-arg renames in option objects
  (`safeAddress` → `accountAddress`), one local destructure alias, doc
  comments. No reordered await, no changed error path, no changed storage-key
  format (`haven_passkey_<address>_<chainId>` byte-identical), no changed
  signer-resolution order, no signed payload touched. Two deliberate KEEP
  decisions, both the stored-data analogue of the epic's one-release window:
  (1) the persisted localStorage signer-blob field is still named
  `safeAddress` (renaming the STORED JSON key would orphan every signer
  enrolled before this release — a money-path data break, not a rename; the
  arg surface is renamed, the blob is read through a destructure alias with a
  comment naming the constraint; the field rename is #2914's together with
  the wire flip); (2) the active-account storage key keeps a one-release
  migration read of the old key (the AC's single allowed exception). Nothing
  here changes who can spend, from what account, or under what caveat: no
  new query, no new authority check, no altered signed payload, no permission
  widening. The acceptance grep
  (`git grep -n -E "\b(safeId|safeAddress|useUserSafes|useSafeFunding|useSafeOperationGate|FIXTURE_SAFE|ACTIVE_SAFE|haven_active_safe_id)\b" origin/dev -- 'packages/frontend/**'`)
  falls from 461 matches on dev to 38 at this head, every one classified:
  6 are the allowed migration read itself (`auth-storage.ts` + its test);
  15 are the persisted signer-blob field compat (keep-decision 1: `signer.ts`
  9, `signer.test.ts` 3, `signer-storage.test.ts` 1, `AuthContext.tsx` 1,
  `useAccountOperationGate.test.ts` 1); 17 are forced by the GENERATED wire
  schema until #2914 — `ApiSchema<'Transaction'>`/`'TransactionBase'` still
  declare `safeId`/`safeAddress` REQUIRED (deprecated twin of
  `accountId`/`accountAddress`, removed in the release after #2908 per the
  schema's own sentence; #2907's shard documents why `required` was
  deliberately not widened then — it flips optionality repo-wide), so test
  fixtures typed as `AggregatedTransaction` must carry the old keys, and the
  two production sites (`TransactionDetailPanel.tsx:235` fallback read,
  `AgentDetailClient.tsx:135-136` deprecated-twin writes) read/write those
  same wire fields with the new keys already first. Zero hand-written wire
  shapes added (`lint:wire-types` green, 11 baseline shapes unchanged).
  Rendered evidence: `/accounts/<id>` and the dashboard header captured on
  this branch and on a clean dev checkout, light+dark, desktop+mobile,
  full-page + header clips — 15 of 16 pairs pixel-identical, all 8 header
  clips zero-diff, the one full-page delta reproduced on a SAME-build rerun
  (live KPI value, not a render change). The `/design-system` visual
  baselines did NOT move: the issue-mandated sentence change at
  `design-system/page.tsx:1838-1843` sits in the page body, outside the
  sidebar/topbar regions those baselines clip
  (`design-system.visual.spec.ts` clips sidebar-desktop, topbar-desktop,
  topbar-mobile only), and `git diff origin/dev...HEAD --
  e2e/__screenshots__` is empty. Gates at the delivered head
  (`d3aea831`): `node --test scripts/ci/*.test.mjs` 691/691, frontend
  typecheck + unit suite green, `design:lint`, `lint:copy`,
  `lint:wire-types`, `docs:check` green. Perimeter unchanged — narrowed, if
  anything: the frontend no longer sources the account vocabulary from
  Safe-named identifiers.
