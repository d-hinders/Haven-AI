- **#2929** — dark-mode epic #2925 slice 3/3: the product-surface colour sweep, the `--color-scheme` capture flag, the `chromium-desktop-dark` pixel gate, and the contrast table. **What changed on a covered file:** one line in `packages/frontend/src/components/AddFundsModal.tsx` — the modal container's literal `bg-white` became `bg-[var(--v2-bg)]`, so the Add-funds dialog renders on the dark ground in the dark theme instead of a white card on a dark page. The on-ramp provider link and every funding instruction inside that dialog are byte-identical: same `href`, same provider, same amounts, same copy, same disabled/submit wiring. No class that carries a value a regulatory statement rests on (the amounts, the "Coming soon" gating copy, the licensed-provider attribution) was touched. No file under `packages/backend/**`, no route, module, rail, repository, migration, policy, relayer read, or key path is in this diff; `.github/money-path-globs.json` is untouched; nothing in `packages/signer/**` or the SDK payment path is touched. The one workflow this diff does edit, `.github/workflows/update-visual-baselines.yml`, gains a dark-baselines regeneration step beside the light one, and `.github/workflows/ci.yml` gains the matching `test:visual:dark` invocation — both under the *design visual regression* job. Neither can make a payment path behave differently: they change which PNGs are blessed and which comparison runs, not what ships. **Verification rather than assertion:** the 32 token pairs in `packages/frontend/scripts/contrast-check.mjs` pass in both themes at the thresholds the doc's contrast rule asks for (this shard's own run: `contrast-check: all 32 pairs pass in both themes`); `design:lint` reports 0 baselined violations with `design-lint-baseline.json` at `{}` — the white/black ratchet at zero for product surfaces, which is the whole evidence this slice owes; the sweep's before/after counts are in the PR body, generated by the two greps the issue names. **Perimeter unchanged.**
