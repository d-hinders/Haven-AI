- **#2945** — analytics slice A records what the guardrails refuse: a new `payment_refusals` table (migration `086`, additive only — no existing table, column, constraint or index is altered) plus fire-and-forget writes from the four refusal paths (the fail-fast budget pre-check on BOTH x402 settlement legs, the no-delegation-for-target 403, the relayer-budget sign-route 429, and the caveat-enforcer revert surfaced by `prepareRedemption`'s gas estimation). The enforcement model is untouched in every respect: the on-chain caveat enforcers remain the authority that refuses (Red Line #4 — the fail-fast reads stay a convenience, not the gate), the pre-checks are byte-identical with and without the ledger, and each writer runs strictly AFTER the refusal response is decided — a ledger write that fails is logged and swallowed, and dedicated characterization tests pin every refusal response byte-identical with the ledger succeeding AND failing. The row is a record for the user, never an input to a decision: `reason` is a closed five-value CHECK with a named writer per value (`recipient_not_allowed` deliberately absent — no writer emits it), `source` a closed three-value CHECK, `detail` a JSONB ALLOWLIST enforced at the database (`error_code`, `phase`, `next_action`, `remaining_atomic`, `budget_atomic` — nothing else can be copied, the #2907/#2908 whole-body-copy finding made structurally impossible), fiat booked through the same `getFiatValuesForTokenAmount` path settled payments use (an outage books NULLs, never a fabricated zero), and a 60-second dedupe on `(agent_id, reason, resource_url)` folds retry storms into `attempts` on one row. No key, no signature, no transfer, no authority read is added or changed; the hosted MCP's `PRICE_EXCEEDS_MAX` cap refusal stays out of the ledger on purpose — it happens in the agent's own runtime before any backend call and is the user's own instruction; a machine-side report of it is a possible follow-up, not filed. No retention policy: the rows are small and they are the evidence trail § Maintain Evidence That Haven Does Not Control Funds asks the codebase to keep. Perimeter unchanged.
