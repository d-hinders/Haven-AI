- **#2946** — analytics slice B (`GET /analytics/overview`) is a read-only aggregate over data other slices already book: `payment_intents.usd_value`/`eur_value` (booked once, at confirmation, by the existing confirm UPDATEs), `payment_refusals` (read exclusively through `#2945`'s own repository functions — `listRefusalsForUser`/`aggregateRefusalsForUserByAgent` — no new SQL touches that table), `payment_fees`, `relayer_gas_events`, `agent_delegations` and `user_daily_portfolio_snapshots`. It writes nothing, moves nothing, and reads no key or signature material. `money-path-globs.json`'s `packages/backend/src/infra/repositories/**` glob matches the new `infra/repositories/analytics.ts` by DIRECTORY, not by behavior — the file's own gauge (`infra/transaction.ts`'s `Executor`) is the same read/write-agnostic pattern every repository in that directory uses, and every statement in it is a `SELECT`. The one on-chain read (`readRemainingBudget`, unchanged, already used by the agent page) is called once per active delegation to REPORT `used_atomic`/`remaining_from_chain` — never to authorize or decline a payment; the enforcement is unchanged and unread of by this slice's own reasoning (the caveat enforcer is still the sole authority, per `docs/security/delegation-rail-security-model.md`). Rail scoping is additive-narrowing only: every section joins through `agents.account_id -> smart_accounts.account_type = 'delegator_hybrid'`, the same `DELEGATION_RAIL_ONLY` posture `infra/repositories/smart-accounts.ts` and `dashboard.ts` already carry, so a legacy-Safe row is invisible here exactly as it is everywhere else. Tenant isolation is a required `userId` parameter on every exported function (repository convention), asserted by a route test that a second user's rows never appear. No migration, no schema change, no new table, no write path. Perimeter unchanged.
