- **#2946** — analytics slice B (`GET /analytics/overview`) is a read-only aggregate over data other slices already book: `payment_intents.usd_value`/`eur_value` (booked once, at confirmation, by the existing confirm UPDATEs), `payment_refusals` (dedupe/upsert and the per-reason breakdown stay exclusively in `#2945`'s own repository — `listRefusalsForUser`/`aggregateRefusalsForUserByAgent` — but review of this slice added two READ-ONLY bounded aggregates directly in `infra/repositories/analytics.ts`, `AGGREGATE_REFUSALS_FOR_USER_SQL` and `REFUSALS_BY_DAY_SQL`, replacing a 10,000-row full fetch that was folding `refused_amount`/by-day counts in application code; neither writes to the table), `payment_fees`, `relayer_gas_events`, `agent_delegations` and `user_daily_portfolio_snapshots`. It writes nothing, moves nothing, and reads no key or signature material. `money-path-globs.json`'s `packages/backend/src/infra/repositories/**` glob matches the new `infra/repositories/analytics.ts` by DIRECTORY, not by behavior — the file's own gauge (`infra/transaction.ts`'s `Executor`) is the same read/write-agnostic pattern every repository in that directory uses, and every statement in it is a `SELECT`. The one on-chain read (`readRemainingBudget`, unchanged, already used by the agent page) is called once per active delegation (up to 4 in flight at once since review, each still on its own timeout — a concurrency bound, not a new authority path) to REPORT `used_atomic`/`remaining_from_chain` — never to authorize or decline a payment; the enforcement is unchanged and unread of by this slice's own reasoning (the caveat enforcer is still the sole authority, per `docs/security/delegation-rail-security-model.md`). **Rail scoping is NOT "every join," corrected on review**: most sections join through `agents.account_id -> smart_accounts.account_type = 'delegator_hybrid'` (the same `DELEGATION_RAIL_JOIN` posture `infra/repositories/smart-accounts.ts` and `dashboard.ts` already carry), so a legacy-Safe row is invisible there exactly as it is everywhere else — but `BALANCE_BY_DAY_SQL` (`user_daily_portfolio_snapshots`, one row per user per day, no `account_id`/`agent_id` column to join through) is rail-agnostic BY CONSTRUCTION, and `GAS_EVENTS_BY_CHAIN_SQL` (`relayer_gas_events`) is deliberately tenant-scoped ONLY: that table's own migration (054) makes `agent_id` NOT a foreign key so a deleted agent's gas attribution survives it, and lets `user_id` carry a gas event directly for user-level operations (Safe/Hybrid deploys) with no owning agent at all — a hard rail join would silently drop both of those legitimately-attributed row shapes. Tenant isolation is a required `userId` parameter on every exported function (repository convention). **Corrected on review**: the earlier draft of this shard said tenant isolation was "asserted by a route test that a second user's rows never appear" — the route test in question (`routes/__tests__/analytics-overview.test.ts`) mocks the repository layer and asserts only that each repository function is CALLED with the caller's own `sub`, never that row-level filtering holds; that is a route-layer contract check, not a tenant-isolation proof. The actual proof is in `infra/repositories/__tests__/analytics.test.ts`'s real-Postgres suite: a two-tenant fixture seeded across `payment_intents`, `payment_refusals`, `payment_fees`, `relayer_gas_events`, `user_daily_portfolio_snapshots`, `contacts`, `machine_payment_evidence`/`merchant_receipts` and `agent_delegations`, asserted for every one of the 11 exported list/sum functions, and mutation-proven on 8 of them (`BY_DAY_SPEND_SQL`, `TOP_MERCHANTS_SQL`, `BALANCE_BY_DAY_SQL`, `GAS_EVENTS_BY_CHAIN_SQL`, `COUNT_UNSETTLED_SUBMITTED_SQL`, `RECEIPT_MERCHANT_NAMES_SQL`, `ACTIVE_DELEGATIONS_FOR_USER_SQL`, `FEES_TOTALS_SQL`) by neutralising each tenant predicate to `($1::uuid IS NOT NULL)` (kept load-bearing on the parameter, so the mutation cannot be confused with a parameter-count error) — every one of the 8 turned red. `tz` validation now requires an actual IANA zone name (`Intl.supportedValuesOf('timeZone')`, plus `'UTC'`): a UTC offset or a fixed abbreviation (`+05:00`, `EST`, `GB`, `Zulu`) is a 400, because Postgres's `AT TIME ZONE` and JavaScript's `Intl`/`Date` machinery can interpret an offset string with opposite sign conventions, and because those inputs used to construct successfully and then bucket a day on the wrong side of midnight. The 400 body never echoes the raw `tz` value. No migration, no schema change, no new table, no write path. Perimeter unchanged.
