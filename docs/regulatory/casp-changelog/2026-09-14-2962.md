- **#2962** — Connect's `--doctor` now classifies a superseded credential only from the existing authenticated, read-only agent-identity endpoint: a 401/403 is reported as already revoked, a successful identity read remains spend-capable, and network or malformed responses remain explicitly unverifiable. The hosted MCP `tools/list` call remains a reachability handshake only; its output no longer claims bearer authorization, including during post-setup runtime installation. No private key, signature, budget, delegation, payment route, or spend authority changes; this narrows a diagnostic claim to evidence that actually authenticates the stored API key. Regression tests cover live, revoked, network-error, and malformed-response classifications. Perimeter unchanged.
