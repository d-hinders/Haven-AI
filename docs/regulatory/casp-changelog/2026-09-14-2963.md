- **#2963** — `@haven_ai/connect --doctor`: the `signer_runtime` check reported an
  outdated-but-intact local signer runtime as "stale or empty" because it
  measured intactness against the connector's pinned manifest instead of the
  sidecar's record of what npm installed; the version-drift message was
  unreachable. Diagnostic only: the check now reads the sidecar for intactness
  and the manifest for currency, and names both versions. No custody, signing,
  network, authority or payment-path code changes — `packages/connect/src/doctor.ts`
  and its test only; the repair action (`--doctor --repair`) is unchanged and
  the installers keep comparing against the manifest. Mutation-tested: the
  pre-#2963 comparison makes the new drift test fail. Perimeter unchanged.
