- **#2970** — "Settled means verified": the hosted erc7710 `haven_settle_mcp_tool`
  branch now reports `settled: true` only once the backend has confirmed a
  merchant-reported settlement hash on-chain (reusing the existing evidence
  report the EIP-3009 path already makes,
  `HavenClient.completeX402MerchantCall` → `MerchantCompletion.reportEvidence` →
  `POST /machine-payments/evidence` → `observeErc7710Settlement`, #2092's
  fail-closed seam, unmodified) — otherwise it answers `delivered_unsettled` or
  `settlement_pending`, both with `next_action: check_status_later` and the
  merchant's result still returned. `haven_complete_mcp_tool` has no erc7710
  branch: it always takes the funding-leg path, which 409s on a `submitted`
  erc7710 intent before any merchant call, pre-existing and unchanged here.
  `haven_get_payment_status` gains one
  additive `next_action` value, `awaiting_settlement_evidence`, for a
  `submitted` erc7710 intent whose settlement window
  (`MAX_SETTLEMENT_WINDOW_SECONDS`) has passed with no evidence — replacing a
  `check_status_later` answer that promised a resolution nothing would
  produce; Haven's settlement sweep (120s tick, its own window plus a 120s
  clock-skew allowance) can still attribute the settlement for roughly two
  minutes after this value first appears, so the agent-facing message polls
  once more before telling the user the goods were delivered without
  verified evidence. The demo merchant's two zero-hash paths (the `MERCHANT_SKIP_SETTLE_
  PRODUCT` QA hook and `AuthorizationAlreadyUsedError` recovery) stop printing
  "Purchase confirmed"/a `Tx:` line for a hash that is not a real transaction;
  their invoice JSON gains one additive Swedish status value for those two
  paths only. No new authority, no new route, no migration, no change to what
  the merchant receives from Haven or to what Haven's evidence-verification
  seam (`settlement-observed.ts`) accepts as proof — this issue only decides,
  at the AGENT-FACING surface, how honestly Haven's existing verdict is
  reported. `PAYMENT-RESPONSE`/`x-receipt-json` wire shape to the buyer is
  unchanged; the zero-hash path still carries `transaction: ZERO_TX_HASH`
  exactly as before. Mutation-proven: reverting the backend confirmation gate
  back to the merchant's bare HTTP-ok flag re-fails the zero-hash hosted-tool
  test; reverting the settlement-window predicate to unconditional
  `check_status_later` re-fails the status test; restoring the confirmation
  text's `Tx:` line on a zero hash re-fails the demo-merchant render test.
  Perimeter unchanged.
