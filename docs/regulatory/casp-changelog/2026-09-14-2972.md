- **#2972** — a new hosted-only tool, `haven_report_settlement_evidence
  { payment_id, settlement_tx_hash }`, lets an erc7710 agent hand Haven the
  merchant's real settlement transaction hash out of band — the remedy #2970's
  guidance could not name, because no hosted tool accepted a hash
  (`haven_report_x402_outcome` takes none and refuses a non-`confirmed`
  intent). The tool causes an on-chain VERIFICATION of the agent's OWN
  payment, never a write of unverified data: it posts to the SAME backend
  seam `haven_settle_mcp_tool`/`haven_complete_mcp_tool` already call
  (`POST /machine-payments/evidence` → `observeErc7710Settlement`, #2092's
  fail-closed seam — VERIFY on-chain, then write, unmodified by this change),
  and reports the identical three outcomes #2970 introduced: `settled: true`
  only once Haven confirmed the hash against this payment's transfer shape
  and window, else `code: 'DELIVERED_UNSETTLED'` (409 `settlement_unverified`,
  or any other terminal refusal — including a foreign `payment_id`, which the
  backend's own `WHERE agent_id = $` scoping 404s before this tool's logic
  ever runs) or `code: 'SETTLEMENT_PENDING'` (503 `settlement_unobservable`,
  `retryable: true`). Own payments only, enforced entirely at the existing
  backend boundary — this tool carries no separate ownership check, and a
  foreign payment_id never reaches a write. A zero settlement hash
  (`0x00…00`, the demo merchant's own "delivered, not settled" marker) is
  refused CLIENT-SIDE, before any network call
  (`HavenClient.reportSettlementEvidence` /
  `MerchantCompletion.reportSettlementEvidence`, throwing
  `HavenZeroSettlementHashError`) — never posted, so it can never be mistaken
  for a real report. No new authority, no new backend route, no migration,
  no change to `settlement-observed.ts` or what it accepts as proof — this
  issue only adds a new CALLER of the existing verified-evidence seam, plus
  guidance (the `DELIVERED_UNSETTLED` / `SETTLEMENT_PENDING` reasons on the
  #2970 settle/complete gate, and `agent-payment-status.ts`'s
  `awaiting_settlement_evidence` message) naming this tool as the next step
  when the agent holds a hash, `haven_get_payment_status` unchanged as the
  next step when it does not. Mutation-proven: mapping the SDK's 409
  (`settlement_unverified`) refusal to `confirmed` re-fails the 409 outcome
  test; dropping the client-side zero-hash boundary check re-fails the
  zero-hash test. Money-path classification
  (`node scripts/ci/money-path-classify.mjs origin/dev`, working tree):
  `packages/mcp-server/src/**` matches the touched contract/handler/test
  files (the same glob #2970 and #2300 matched on) — MONEY-PATH, a dev →
  main promotion needs a covering green money-flow QA run. Perimeter
  unchanged: no new glob, no new route, no signing surface, no custody
  change.
