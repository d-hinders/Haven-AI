- **#2978** — catalog discovery badges: `verified_payable` on operator-curated
  rows (`source: 'operator'`) was hard-coded `false` in
  `packages/backend/src/routes/catalog.ts` although `refreshCatalog` probes
  every non-delisted row for a real 402 challenge; it now reflects that
  probe (`status === 'active' && verified_at !== null`). `domain_verified`
  stays `false` for operator rows — no ownership proof runs for them and the
  badge must not claim one. The SDK's `discoverTools({ verified: 'verified' })`
  filters on the badge (`verifiedPayable === true`, either source) instead of
  on provenance, so the filter the tool description recommends no longer
  returns an empty catalog on a deployment with only operator entries.
  Read-only discovery surface: no payment, signature, route, migration or
  authority change; the badges remain "we watched this endpoint answer a
  live quote", never a claim about merchant honesty. Mutation-tested:
  restoring the hard-coded `false` fails two route tests on the real DB;
  restoring the provenance predicate (or re-narrowing the badge filter to
  ingestion rows) fails the SDK test. Perimeter unchanged.
