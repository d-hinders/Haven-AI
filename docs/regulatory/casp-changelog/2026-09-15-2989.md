- **#2989** — demo merchant disclosure of the `MERCHANT_SKIP_SETTLE_PRODUCT`
  QA fixture (#603, chain-gated to Base Sepolia): the fixture product was
  listed, quoted and challenged exactly like a product that settles, so an
  agent learned only from the receipt (#2969/#2970) that no settlement was
  ever attempted. `list_products` structured output, the discovery document
  and the product's 402 `description` now carry
  `qa_fixture: { kind: 'skip_settle', settles_on_chain: false }` / a
  one-line disclosure on that product only — absent (not null) on every
  other product and on every chain where the flag cannot be set. Metadata
  and prose only: no payment, settlement, custody or authority change, the
  fixture itself is unchanged, and the demo merchant is not on the money-path
  runtime globs. Mutation-tested: dropping the marker branch fails the
  positive test; hard-coding it fails both absence tests; dropping the 402
  description suffix fails the positive test. Perimeter unchanged.
