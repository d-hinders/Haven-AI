- **#3000** — Follow-up from the review of #2987 (#2983): #2983 fixed the
  scheme-awareness gap on the hosted paid-retry REJECTION refusal
  (`MERCHANT_REJECTED_AFTER_FUNDING`), one code over from the paid-retry
  TIMEOUT refusal (`MERCHANT_UNRESPONSIVE_AFTER_FUNDING`,
  `deliverMerchantPayment` in
  `packages/mcp-server/src/tools/paid-mcp-completion.ts`), which still said
  "verify-then-sweep" unconditionally. On erc7710 (`options?.noFundingLeg`)
  there is no funding leg, so a merchant that never answers the paid retry
  leaves nothing staged in a delegate wallet — there is nothing to strand and
  nothing to sweep, exactly the same fact #2983 established for the rejection
  case. The timeout refusal now branches the same way: `next_action:
  check_status_later` stays on both schemes (the merchant may still settle
  late, whichever scheme is in play), but the erc7710 branch drops every
  sweep/stranded-funds mention and tells the agent to check
  `haven_get_payment_status` and retry once before re-quoting, never before
  considering a sweep it cannot perform. The eip3009 branch is unchanged:
  funding is confirmed on-chain before this refusal on that scheme, so the
  delegate wallet genuinely may hold stranded funds, and the sweep guidance
  stays. The four code-keyed texts (`AgentPaymentFailureCodeDescriptions` and
  the enum JSDoc in `packages/sdk/src/types.ts`, the skill copy in
  `packages/sdk/src/skill-content.ts` and its frontend mirror
  `packages/frontend/src/lib/agent-skill-bundle.ts`, and `packages/sdk/README.md`'s
  table and x402 tool-window flow lines) now describe both schemes for
  `MERCHANT_UNRESPONSIVE_AFTER_FUNDING` the same way they already did for
  `MERCHANT_REJECTED_AFTER_FUNDING`; the skill copy keeps the pinned
  "Stop-and-sweep" / "Verify-then-sweep" case-sensitive labels for the eip3009
  branch and adds the erc7710 clause alongside them, and the SDK/frontend
  copies stay byte-for-byte identical. No funds-path change and no tool
  description edit: this is a refusal-reporting correction on a path that
  already refused, and `description-size`'s mean is unchanged at 867.22 bytes
  across 23 tools (#2983's own figure), still under the 874-byte cap.
  Mutation-proven: removing the `options?.noFundingLeg` branch on the timeout
  path re-fails the erc7710 timeout test (it regresses to the eip3009 message
  and `sweep_stranded_funds`); reverting the erc7710 branch's `nextAction` to
  `sweep_stranded_funds` re-fails the same test's `next_action` assertion.
  Perimeter unchanged.
