- **#3013** — `GET /analytics/overview` (packages/backend/src/routes/analytics-overview.ts) reports one new field on its existing response: `basis.refusals_recorded_from` (`YYYY-MM-DD` | `null`), the earliest `payment_refusals` day for the requesting user, read by a new single-statement `SELECT TO_CHAR(MIN(created_at) AT TIME ZONE 'UTC', 'YYYY-MM-DD') … WHERE user_id = $1` in packages/backend/src/infra/repositories/payment-refusals.ts (`firstRefusalDayForUser`). The field is declared required-but-nullable on the `basis` block in packages/backend/src/openapi/spec.ts and rides the regenerated wire types in packages/core/src/api-types.ts (spec is the single source, #984); the slice C renderer (`EmptyStates.RefusalsRecordedFromFootnote`, already on dev) displays the value verbatim via the response's own date — the client never derives or invents it. Authority/custody argument: the change is SELECT-only over the refusal ledger the product already owns — it writes nothing, alters no refusal decision (the fire-and-forget writer in `modules/payments/refusal-ledger.ts` and the guardrail pre-checks are untouched), changes no spend path, no delegation/authority check, no settlement or redemption behaviour, and introduces no credential, key, or custody surface. It is a reporting-coverage claim so a window behind the ledger's first row can say "nothing was recorded" instead of asserting a clean zero. Tests pin the claim at both seeds: real-Postgres proofs in the payment_refusals repository suite (empty ledger → `null`; earliest row's day wins across a UTC midnight and out-of-order inserts; tenant-scoped; honours no window bound), route-level seeds in `analytics-overview.test.ts` (empty → `null`, rows → day; the repo read takes only the user, never the requested window), and frontend pins that the footnote renders from a non-null field and renders nothing from `null`. Perimeter unchanged.
