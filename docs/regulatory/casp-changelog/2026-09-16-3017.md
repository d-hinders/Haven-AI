- **#3017** — Accounted becomes the second live accounting provider, slice 1 of epic #3016: the registry descriptor flips to `authKind: 'api_key'` / `availability: 'live'`, a new `AccountedConnector` answers `getCompanyInfo` over a read-only bearer `GET /api/v1/companies` (base currency left null; the feed books in `DEFAULT_LEDGER_CURRENCY`), a multi-company key is refused 409 `MULTI_COMPANY_KEY` before anything is stored, and the frontend paste modal submits the key to the existing api-key connect route. No new route class, no new authority surface: the key is a third-party read/write credential for the user's own accounting platform, stored encrypted through the existing `HAVEN_SECRETS_KEY` secrets path (the Fortnox class), never echoed, and revocable by the user in Accounted's dashboard. Nothing here reaches spend, budgets, settlement, agent authority, or the refusal WRITE path — the connector's push/verify/revoke are explicit slice-1 no-ops (#3018 owns the push half), and the connect flow refuses before storing on any validation failure. Covered by fixture-backed connector tests (live-recorded, redacted), the real-DB connect test asserting ciphertext at rest and plaintext never echoed, and the route 409 mapping test. Perimeter unchanged.
