- **#3029** — Request validation slice 1/4: a new `packages/backend/src/openapi/request-validation.ts`, an installable Fastify plugin registered by `packages/backend/src/index.ts` and by route tests (never inline), whose `onRoute` hook resolves each route's spec operation via `fastifyPathToOpenApi` + method and injects `schema: { body, params, querystring }` compiled by the spec-driven ajv factory `makeSpecAjv` in `packages/backend/src/openapi/ajv.ts` (request-side options `coerceTypes: 'array'`, `useDefaults: true`, `removeAdditional: false`, `allErrors: false`, no `closeObjects`), with a `schemaErrorFormatter` emitting the house error envelope (`error`, `statusCode: 400`, `details`, `error_code: 'invalid_request'`); mode from `HAVEN_REQUEST_VALIDATION=off|shadow|enforce` (default `shadow`) — in shadow every route logs one structured `request_validation.would_refuse` line and increments an in-process counter reported under `request_validation` on `GET /health/ops`, `enforcedPrefixes` flips a module regardless of the env, and in this slice enforce is exercised only on the contacts proof module (`packages/backend/src/routes/contacts.ts`, whose `typeof` ladders are deleted in favour of spec-conformant handlers proven by `expectRejectsOffSpec`); a shrink-only ratchet (`scripts/lint-request-schemas.mjs`, baseline `scripts/lint-request-schemas-baseline.json`, `npm run lint:request-schemas`) pins the shadow/`typeof` census, and the plugin file is added to `covers:` in `docs/regulatory/casp-risk-guardrails.md`. Authority/custody argument: the plugin is a validator only — it reads the shape of a request the spec already governs, refuses or logs, and never authorizes, alters, or constructs spend intent; all semantic money-path refusals (entitlement, budget, delegation, settlement checks) keep their exact position and body, no credential, key, or custody surface is introduced, and payment routes remain shadow-only until slices 2-4; where a request body's spec is looser than the handler's real expectations (the settlementScheme gap) the slice demonstrates the gap in shadow rather than fixing it — that is #3031. Mutation-tested: removing `contacts` from `enforcedPrefixes` reddens `lint:request-schemas`; swapping the request-side ajv for the response-instance compiler reddens the typed-query coercion test (`'10'` string vs `10` number); removing the would-refuse counter increment reddens the `/health/ops` `request_validation` assertions. Perimeter unchanged.
