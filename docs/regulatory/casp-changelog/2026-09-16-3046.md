- **#3046** — `parseBooleanFlag` moved from `config.ts` into the
  dependency-free `config/boolean-flag.ts` (re-exported from `config.ts`, so
  its six call sites and their tests are unchanged), and
  `infra/chain/x402-binding-signer.ts` imports it from there. #3023 had
  pointed the binding signer at `config.ts`, whose module-level `config`
  object requires `DATABASE_URL` at evaluation; the mcp-server's
  wire-contract suite imports the binding signer across the package boundary
  precisely to prove the hosted/signer/backend x402 expected-context contract
  byte-for-byte, and the MCP CI job (no backend env) could no longer load it
  — the contract proof was silently skipped on every dev run since #3023
  merged (the MCP job is surface-routed) and failed on the first mcp-server
  PR. No change to how the flag is parsed, to what is signed, or to any
  perimeter; a backend guard test now imports the binding signer with
  `DATABASE_URL`/`JWT_SECRET` unset and is proven red when the import is
  pointed back at `config.ts`. Perimeter unchanged.
