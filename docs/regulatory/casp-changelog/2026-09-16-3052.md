- **#3052** — the EIP-3009 funding leg of the x402 authorize path
  (`runDelegationAuthorize` in
  `packages/backend/src/modules/x402/delegation-authorize.ts`) gains the two
  `payment_refusals` ledger writers its siblings for the identical condition
  always had: the `prepareDelegationPayment` 502 `catch` now classifies the
  error with the same `classifyRevertForLedger` the byte-identical
  `POST /payments` sibling already ran (its `prepareDelegationPayment` catch,
  unwrapped), and books a row only when that classification names a policy
  refusal — an expired caveat as `delegation_expired`, the period-budget
  enforcer's revert as `delegation_budget_exceeded`, any other estimation
  revert as `onchain_revert` — while a non-revert (a transport failure, an RPC
  auth/config error) classifies `null` and the ledger books NOTHING, because
  an outage is not a refusal; and the no-fundable-delegation 403 (prepare
  resolving `null`, an over-budget `catch`-then-`null`) books
  `no_delegation_for_target`, the value the erc7710 no-delegation 403 and the
  `POST /payments` funding 403 have carried from the start. Both writers are
  `void`-ed fire-and-forget after the response is decided (the pre-check
  writer at the #2706 block is the precedent they copy field for field), and
  the 429 at the `ensureHybridDeployed` gas-sponsor cap was deliberately left
  OUT of scope: it is a sponsorship-availability answer, not a guardrail
  refusal, and the migration 086 CHECK already reserves `relayer_budget` for
  the sign-route that `#2945` booked. Authority/custody argument: this change
  is a pure READ of a decision the existing code had ALREADY made and an
  APPEND to an audit table — it cannot authorize, alter, gate, or reorder any
  spend intent. Neither writer's return value is read; neither awaits the
  write; the recorder's own contract (refusal-ledger.ts) is to return
  synchronously, run the write detached, and swallow its failure — so no
  ledger outcome, slow or broken, can change, delay, or break a refusal
  response, which the characterization tests pin byte-for-byte for BOTH
  affected responses with the ledger succeeding AND failing. On the 502 the
  classification is applied to a redacted dump the response has always
  returned verbatim, and on the 403 the pre-check still stands as the named
  refusal gate (this writer sits behind it and fires only on the null/
  exception path, so it cannot preempt the pre-check, cannot become a second
  source of truth for an over-budget condition, and cannot refuse a payment
  the enforcers would have allowed). No new authority, no custody, no route,
  no change to the hourly cap ordering, to which a payment intent is ever
  written, or to what the on-chain caveat enforcers will redeem — the on-chain
  instruments stay the only real spend control and the API credential stays
  insufficient to spend. `merchant_to` is booked as `merchantPayTo` (the real
  merchant), never the funding-leg `payTo` (the agent's own delegate EOA), so
  the audit trail cannot confuse the funding target with the merchant; the
  migration 086 reason/source CHECKs and the detail allowlist constrain the
  row's shape. Mutation-tested: removing the 502-`catch` writer turns red
  "a DEGRADED read (fromChain:false) plus an enforcer revert at the prepare
  seam books delegation_budget_exceeded" (the epic's degraded-read promotion
  box, proven against real Postgres) and its route-file twin in
  `x402-delegation.test.ts`; removing the 403-branch writer turns red
  "a null prepare answers the unpinned-budget 403 and books it as
  no_delegation_for_target"; the outage control proves the classifier's null
  path writes nothing at all. Perimeter unchanged.
