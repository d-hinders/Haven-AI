- **#3053** — slice 2 of epic #3056 (refusals 2/4): the refusal choke point
  and the enumerating census. `modules/payments/refuse.ts` (new) is now the
  one way a policy refusal in `modules/x402/delegation-authorize.ts` and
  `routes/payments.ts` becomes a response: it takes the already-decided
  response (`{ code, body }` pair, or a Fastify reply the handler has sent
  inside its own argument expression) plus the ledger input, records the
  `payment_refusals` row fire-and-forget through the unchanged #2945
  recorder, and returns the response object verbatim. All eight ledger
  writers in the two files (five x402 — including slice 1's two — and three
  direct-path) plus four deliberately writer-less policy-status sites
  (settlement-child build 502, relayer-budget 429, delegate-deploy 502, the
  post-claim execution 502) route through it; the per-agent hourly cap 429
  stays a bare return on the owner's standing decision. The census guard
  (`modules/payments/__tests__/refusal-census.test.ts`) reads both files
  through the TypeScript compiler API (never a regex) and asserts BOTH
  directions: every `403|429|502` return/reply-send is inside `refuse(`
  or named on the allowlist with a one-line reason, AND the refuse( call
  sites equal the enumerated list exactly — a removed writer is as red as an
  added refusal. The predicate is the HTTP status set, deliberately not
  `error_code` (owner verdict note 1: two of the guarded writers' bodies
  carry no `error_code`, so an `error_code`-keyed guard never sees its own
  targets); computed-status passthroughs (retired tombstones, replay and
  scheme-shape forwards, resume_state) are pinned by literal fragment counts
  because a numeric-literal walker cannot classify them. Positive control
  proven by mutation: removing the wrapped allowlist entry at
  `routes/payments.ts:781` and unwrapping its `refuse(..., null)` back to a
  bare `reply.code(502).send(...)` reddens the census on both directions;
  restored byte-identical (`git diff --quiet HEAD` clean) and green again.
  Authority/custody argument: refuse() adds no authority, no route, no key
  role and no spend path — it is a bookkeeping seam over the existing
  fire-and-forget recorder, and the one rule (a ledger write can never
  change, delay, or block the refusal it records) is re-proven at the new
  choke point (`refuse.test.ts`: identity of the returned response with the
  recorder succeeding, asynchronously rejecting, and throwing synchronously;
  the write is issued synchronously and never awaited; the recorder's own
  never-propagates contract stays `refusal-ledger.test.ts` on the real
  harness, green). The guard's header names the out-of-scope files
  explicitly (`modules/x402/settle.ts`'s 409/502 family including the `:38`
  state-loss 502, and `routes/agent-delegations.ts`'s deploy 502/429 family)
  so a later worker does not enumerate them as residue (owner note 4). The
  `refusal-ledger`/`payment_refusals` perimeter, the migration-086 CHECKs
  and the detail allowlist are untouched — no new source, reason, or ledger
  field, and bodies stay byte-identical (characterization suites green).
  Perimeter unchanged.
