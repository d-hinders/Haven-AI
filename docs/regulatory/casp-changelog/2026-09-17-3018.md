- **#3018** — Accounted feed slice 2 (epic #3016): the connector's push half goes live — `pushTransaction` renders the verifiable receipt underlag and uploads it as ONE document to the user's own Accounted company (`POST /api/v1/companies/{companyId}/documents`, multipart `file` + `upload_source: api` only), proving delivery by echoing the provider's `data.sha256_hash` against the local SHA-256 before any ref is stored; `verify` is answered from Haven's own sync record with zero provider calls (`capabilities.verify: false` — no `GET /documents/{id}` exists and `/download` writes an audit event per call), and the wire contract gains `AccountingVerification.invoice_number: number | null` + `document_ref: string | null` (Fortnox keeps emitting its number). Perimeter unchanged: the connector writes nothing but the evidence document into the user's own accounting account, after settlement, under the user's own pasted key — it cannot move money, cannot touch spend, budgets, agent authority, settlement, or the refusal WRITE path, and the upload's `additionalProperties: false` shape plus the outbound allowlist test (`GET /api/v1/companies` + `POST .../documents` only; no `/download`, `journal-entries`, `supplier-invoices`, `link`) pin that no journal-entry or invoice-writing surface can be added silently. The key stays a third-party credential for the user's own platform (the #3017 class), never echoed, revocable in Accounted's dashboard; a key lacking `documents:write` is refused at first push as `scope_missing` (#2865), `IDEMPOTENCY_KEY_REUSE` is terminal-skipped so a WORM document is never re-filed over, and the sha256 proof is mutation-tested (a mismatched echo is `skipped`, never `pushed`). Perimeter unchanged.
