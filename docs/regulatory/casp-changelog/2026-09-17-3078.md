- **#3078** — slice 1 of epic #3077 (Marketplace): a `merchants` table over the
  catalog (migration 088), `merchant_catalog.merchant_id NOT NULL` after an
  explicit host→merchant backfill, `GET /merchants` and `GET /merchants/{slug}`,
  `CatalogEntry.merchant` on the wire, server-side chain scoping of the
  catalog for non-agent reads (`HAVEN_MARKETPLACE_CHAIN_IDS`), and a
  dashboard-only, flag-gated listing of `coming_soon` prospects. The
  migration is on the money-path glob by directory, not by content: it
  touches no payment, intent, delegation, allowance or account table, and
  adds only catalog metadata (a merchant row per seller, six Ampersend
  offers with `verified_at NULL` so the existing catalog probe — not the
  seed — vouches for them). Every route added or changed is read-only: no
  intent is created, nothing is signed, no allowance moves; the only new
  writer (`findOrCreateMerchantByHost`) inserts catalog metadata. Custody and
  authority are untouched — Haven still holds no key and no funds, and an
  agent's discovery read keeps its own chain clause unchanged (a Sepolia
  agent on prod still finds the Sepolia demo store, proven by a route test).
  Mutation-tested: the migration's backfill map and fallback (real DB), the
  NOT NULL, the chain scope's agent/non-agent split, the prospect gate's
  three conditions (flag, no mainnet listed, dashboard user), and the
  404-not-403 of a hidden prospect. Perimeter unchanged.
