- **#3080** — slice 3 of epic #3077 (Marketplace prospects): migration 089
  seeds two `coming_soon` merchants (Berget AI, Redpine) with no offers, a
  banned-word guard over their copy, and the zero-offers rule for prospects
  in the one catalog writer (`findOrCreateMerchantByHost` never returns a
  `coming_soon` merchant and `assertMerchantAcceptsOffers` refuses to attach
  an offer to one). On the money-path glob by directory only (`db/migrations`,
  `infra/repositories`, an `mcp-server` test): no payment, intent,
  delegation, allowance or account table is touched, no route contract
  changes, nothing is signed and no funds move — the rows are catalog
  metadata visible only to an authenticated dashboard user on a deployment
  that lists no mainnet chain with `HAVEN_MARKETPLACE_PROSPECTS` on (#3078's
  gate, unchanged here). Custody and authority untouched. Mutation-tested:
  the word guard (a "partner" in a seed reddens it), the zero-offers
  assertion and the SQL filter (both halves), and `down()`'s refusal to
  delete a promoted or offer-bearing row. Perimeter unchanged.
