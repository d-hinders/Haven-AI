- **#3054** — slice 3 of epic #3056 (refusals 3/4): the hosted prepare's
  over-budget refusal reaches the ledger, server-decided. New backend route
  `POST /machine-payments/budget-precheck`
  (`routes/machine-payments.ts`, orchestration in
  `modules/mpp/budget-precheck.ts`): agent auth from the plugin-wide hook,
  `moneyPathRateLimit`, camelCase body `{ chainId, token, amountAtomic,
  merchantTo, resourceUrl }`. The handler runs the SAME compare the hosted
  MCP's guided prepare used to run in the agent's runtime
  (`catalog-purchase.ts` step 5b/6) over the SAME derived-budget read
  `GET /machine-payments/allowances` uses (`deriveDelegationBudgets` +
  the #1145 `readRemainingBudget` enforcer read, never
  `agent_allowances`), chain-scoped to the authenticated agent's own chain,
  on sufficiency answering `{ sufficient: true, remaining_atomic,
  remaining_is_from_chain? }` and on insufficiency refusing through the
  #3053 choke point `refuse()` with `reason:
  'delegation_budget_exceeded'`, `source: 'hosted_prepare'` and the same
  taxonomy body (`phase`, `next_action`, remaining/shortfall atomic+human)
  the x402 legs emit. Authority/custody argument: the ledger row is
  server-decided and never agent-asserted — there is no self-report
  endpoint; `source` is a literal in the handler (grep: one writer), every
  wire field is derived or compared server-side, and nothing about the
  caller's claim is trusted beyond which quote it asks about, so an
  agent-key holder cannot book arbitrary amounts into the owner's Refused
  tile (Daniel S1, adopted). The refusal still lands BEFORE any intent is
  created (owner decisions #1090/#2055 unchanged) — this endpoint creates
  nothing, authorizes nothing, and moves no funds; it is a measurement and
  a refusal, and enforcement stays on-chain (the budget delegation's
  ERC20PeriodTransferEnforcer still reverts an over-budget redemption).
  Migration 087 widens `payment_refusals_source_check` with
  `'hosted_prepare'` (same additive drop-and-re-add shape as 086's reason
  widening), the repository union
  (`infra/repositories/payment-refusals.ts`) moves in the same PR so the
  TypeScript mirror cannot compile a value the database rejects, and the
  dedupe fold key is UNCHANGED — `(agent_id, reason, resource_url)` within
  the 60-second window, `source` NOT in the key, so a hosted refusal
  followed by a backend pre-check on the same URL folds into ONE row with
  `attempts = 2`. `down()` refuses loudly while `hosted_prepare` rows
  exist (the #1139 structural-down() rule: the rows are audit records of
  Haven's own guardrail decisions; deleting or rewriting them is not an
  option) and otherwise restores the 086 shape exactly. Migration 086's
  docblock and CHECK comment are updated in the same PR per the
  mirror-the-latest rule. Tool side: the guided prepare's step 6 now calls
  the new SDK `client.precheckBudget(...)` (additive, pinned in
  `client-boundaries.test.ts`'s `publicMethods`) and relays the decided 403
  byte-identically — the `DELEGATION_BUDGET_EXCEEDED` shape is
  characterization-pinned unchanged (95/95 in
  `catalog-purchase.test.ts`) — with one network round trip replacing the
  local compare (the allowances GET it replaced was also one, so the
  preflight's Haven traffic is unchanged, #1348 budget); ANY other
  precheck failure — transport down, a retired rail's 410, an older
  backend without the route — degrades to the existing `sufficient: null`
  warning and NEVER to a refusal (test-pinned), because a transport
  failure is not a guardrail decision and must not look like one. The
  `haven_pay_mcp_tool` path is untouched (it has no pre-check today and
  needs none), and price-cap refusals (`PRICE_EXCEEDS_MAX`) stay excluded
  by the 2026-09-13 owner decision. New route-level real-DB proof
  (`routes/__tests__/budget-precheck.test.ts`, zero mocks except the
  prices stub the #2945 ledger suite established): over-budget → 403 +
  exactly one row through refuse(); sufficient → 200 and no row; the
  dedupe fold → one row with `attempts = 2` and `source =
  'hosted_prepare'`; both retired rails → 410 fail-closed with no write;
  malformed bodies → 400 before any comparison. Deploy order backend →
  hosted MCP (an older MCP against the new backend degrades to the
  warning path; a new MCP against an older backend likewise). Perimeter
  unchanged.
