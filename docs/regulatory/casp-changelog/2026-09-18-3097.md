- **#3097** — the paid x402 retry's target. A merchant's 402 declares
  `resource.url`; the SDK adopted that declaration as the URL the paid request
  (carrying `PAYMENT-SIGNATURE`) was sent to whenever the caller's own URL was
  not in hand — the hosted quote → pay path, which had no `url` input. The
  Ampersend sandbox declares `http://` for a resource served over https (live,
  2026-09-17; 308 → https), so the signed header travelled in clear on the
  first hop. Now: the caller's quoted URL is the retry target whenever one
  exists (hosted: `url` on `haven_pay_x402_quote` / `haven_resume_x402_payment`,
  returned as `retry_url`; the quote reports `request_url` and
  `resource_url_differs_from_request`), and a retry target that is not `https`
  (nor loopback / a reserved test host) is refused with `INSECURE_RETRY_TARGET`
  before any intent exists — on the SDK side at the one seam every paid retry
  crosses, `MerchantTransport.deliverPayment`. Money-path by file
  (`packages/mcp-server/src/**`) and by substance: a refusal is ADDED and a
  transport target is constrained; no selection, cap, funding, signing or
  settlement decision changes. Custody unchanged — Haven still holds no key
  and no funds. Mutation-tested: the seam refusal (remove → the http public
  target is delivered), the hosted refusal (remove → an intent is created for
  an http-only challenge), the resolver's preference (swap → the merchant's
  declaration wins), and the quote's disagreement flag. Perimeter tightened.
