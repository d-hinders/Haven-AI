- **#3098** — money-path perimeter parity (control surface: `.github/money-path-globs.json`,
  `.github/labeler.yml`, `scripts/ci/money-path.test.mjs`, the ship-next Merge
  Gate; this document's own § *What `covers:` must span*): the demo merchant's
  settlement surface `packages/demo-merchant-mcp/src/**` joins the runtime
  money-path globs — `x402.ts` submits the buyer's EIP-3009 authorization
  on-chain and the prod instance runs on Base mainnet — after two
  settlement-semantics changes (#2969/#2977, #2979/#2980) shipped through it
  unlabelled while this document had covered the package since #1736. A new
  test reads the fourth copy in the other direction: every package-wide glob
  in this document's `covers:` is on the money-path list or named doc-only
  with its reason (`sdk`, `cli`, `connect`, `mcp`: client packages the
  deployed harness never runs). Positive control recorded in the PR: both new
  tests red on the unchanged tree; the classifier said not-money-path for the
  #3114 merge commit, which changed `x402.ts`, and says MONEY-PATH after.
  Perimeter WIDENED by one deployed package, on the classification side only —
  what this document covers is unchanged. No custody, settlement, funding or
  refusal decision changes. Perimeter unchanged for Haven's own custody
  posture.
