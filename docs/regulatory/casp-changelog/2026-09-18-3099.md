- **#3099** — demo merchant (`packages/demo-merchant-mcp/src/x402.ts`, on the
  CASP guardrails' coverage list though outside the classifier's globs — #3098):
  a cleanup clause in `verifyAndSettle` whose outcome no code path could observe
  (`!settled.has(productKey)` on the attempt delete) is removed with its reason
  in place, and the chain's already-used recovery (#1519) is pinned end to end:
  an authorization the chain reports as already spent settles as
  `already_settled_earlier` with exactly one submit, the same product is
  answered from the cache, a different product on that authorization is
  refused. No settlement, verification, dedupe or refusal decision changes —
  the deleted clause guarded a delete whose outcome is unobservable once the
  `settled` cache holds the key. Mutation-tested: dropping the cache write in
  the recovery branch makes the second call resubmit (red). Custody unchanged.
  Perimeter unchanged.
