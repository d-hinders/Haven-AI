- **#3100** — slice 1 of epic #3105 (typed next steps): discovery hands the
  agent arguments its suggested tool accepts verbatim. `haven_discover_tools`
  (hosted and local) gains `suggested_arguments` in the suggested tool's own
  vocabulary; hosted MCP entries now point at the cap-free
  `haven_quote_catalog_purchase { catalog_id }` (the prepare tool requires a
  spending cap the server must never invent, so a verbatim hint for it cannot
  exist — the quote tool's description leads to prepare with the user's cap), HTTP
  entries at `haven_quote_x402 { url }`; the local runtime keeps its pay tools.
  A row the suggested tool would refuse (no `tool_name`; hosted: degraded)
  carries `suggested_tool_omitted_reason` and no hint, on both surfaces.
  A hosted strict refusal names the declared keys and a rejected key's declared
  alias (`resource_url` → `url`, `id` → `catalog_id`; folded-spelling equality
  otherwise; never similarity) on both parse paths, and the
  `haven_quote_x402` reason no longer claims the surface has no body field.
  Money-path by file (`packages/mcp-server/src/**`) only: discovery is
  read-only, a refusal's TEXT changed and gained a hint, no spend, cap, funding,
  signing or settlement decision changes; nothing this slice emits can be
  turned into a payment without the caps the pay tools already require.
  Custody unchanged. Mutation-tested: the alias table entry (remove → the hint
  disappears), the emitted `catalog_id` key (rename → the verbatim-acceptance
  parity test refuses), the transport-path error map (drop → the SDK-level
  refusal loses the hint). Perimeter unchanged.
