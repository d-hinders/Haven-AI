- **#3101** — epic #3105 slice 2/5 (typed next steps): guidance typing only,
  no spend decision. The hosted server's `next_tool` / `next_tool_server` /
  `next_tool_name` / `next_tool_server_role` / `next_arguments` are now
  rendered by the SDK's `createNextStepBuilder` from a bare tool name and
  server role over a target map derived from the hosted `toolSchemas` plus
  the two signer handoff shapes the hosted server declares itself (it never
  imports the edge signer at runtime; a test pins them to the signer's
  schemas), so a wrong key, a missing required key, an unregistered tool
  name or an omitted `nextTool` at any of the 17 emission sites is a compile
  error (seven `@ts-expect-error` twins, each mutation-proven) and, for a caller
  that bypasses the types, a fail-safe omission with the reason rather than a
  throw after money moved. Wire strings byte-identical on the 9 sites the
  epic did not re-decide; all 17 `buildAgentGuidance` call sites (a census
  the characterization test enforces) are pinned by that test, written
  before the change, the 8 re-decided ones marked; the three refusals that
  handed `{ payment_id: null }` to `haven_get_payment_status` now omit the
  tool and say why, and five no-tool sites gain `next_tool_omitted_reason`
  (decision 3). A `HostedToolError` may carry the same typed step, which
  `normalizeError` emits on the refusal (decision 7). Money-path by file
  (`packages/mcp-server/src/**`, `packages/signer/**` — the signer change is
  the `as const satisfies` annotation and six optional failure fields). No
  cap, funding, signing, settlement, custody or refusal DECISION changes;
  what changes is how the agent is told what to call next. Perimeter
  unchanged.
