- **#3102** — epic #3105 slice 3/5 (typed next steps): guidance only, no
  spend decision. Every hosted refusal that names a `next_action` now also
  names its next tool with arguments that tool declares, or says why none
  follows — the 27 steps thrown as `HostedToolError` (25 sites, two branching)
  and the SDK's payment-state error passed through `normalizeError`, which
  takes its step from the per-action default table: `HostedToolError` takes a typed `nextStep` (built by the same SDK
  builder and hosted target map as the success path) instead of a bare
  `nextAction`, so a refusal that forgets is a compile error. 27 refusal
  sites converted (census-pinned by `refusalNextStep` calls); 5 name a tool
  (the status read after a post-funding timeout or an erc7710 rejection; the
  sweep after an eip3009 rejection or a funded insecure target — each the
  tool its own message names), 22 carry
  `next_tool_omitted_reason` (stop-and-tell-user, retry-with-explicit-context,
  fund-account, window-expired). `next_action` and `suggested_tool` are
  byte-identical on every refusal, pinned by a characterization test written
  before the change; the other refusal fields are untouched by the diff and
  pinned by the handler suites. A registry walk parses every named tool's
  arguments under its strict schema. Mutation-tested: a reason dropped, a bare
  `nextAction`, and a wrong argument key are each a typecheck error (quoted in
  the PR); the sweep tool dropped at the eip3009 rejection turns two handler
  tests red.
  Money-path by file (`packages/mcp-server/src/**`). No cap, funding, signing,
  settlement, custody or refusal DECISION changes; what changes is that a
  refusal tells the agent what to call next in the same structured fields a
  success does. Perimeter unchanged.
