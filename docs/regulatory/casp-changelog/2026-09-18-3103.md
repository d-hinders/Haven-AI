- **#3103** — epic #3105 slice 4/5 (typed next steps): guidance only, no
  signing decision. The edge signer's five `next_action` decision sites and
  the local runtime's one now carry a typed next step: a backend refusal of
  the signing context names the hosted status read with the payment id
  through the runtime-neutral role fields (the signer cannot know the
  client's server names); a transport failure, malformed body, expired window
  or version skew names no tool and says why in `next_tool_omitted_reason`.
  The signer declares the hosted handoff shapes it emits (it never imports
  the hosted server) and the hosted server's suite pins them to its schemas.
  `HavenSignContextError` and both packages' failure envelopes gain the
  fields additively; the local runtime dual-emits `nextAction`/`next_action`
  for one release. Every previously emitted refusal field is byte-identical,
  pinned by characterization tests written before the change. Money-path by
  file (`packages/signer/**`). No change to `SUPPORTED_X402_EXPECTED_VERSIONS`,
  the sweep binding versions, any signature, key handling or custody. An old
  local signer paired with a new hosted server keeps working (additive
  fields only). Perimeter unchanged.
