- **#3104** — epic #3105 slice 5/5 (typed next steps), closes the epic:
  tests and a gate, guidance only. The hosted next-step fixtures move to a
  test-support module and `next-step-signer-parity.test.ts` becomes the
  cross-surface walk — every hosted emission fixture (19 for the 17 success
  sites, 28 for the refusal steps) built for real, its `next_arguments` parsed with the named tool's
  strict schema on the surface its role names (hosted → hosted, hosted →
  signer from the signer's built package), the signer's declared hosted
  shapes under the hosted schemas, the local runtime's discovery hints under
  its own tools; an action with a default-table mapping must name its tool.
  `scripts/lint-next-steps.mjs` is a shrink-only ratchet with a committed
  zero baseline over emission blocks naming neither a tool nor a reason and
  discovery entries without arguments, across all three packages; positive
  control at the epic's base `4ed69592`: 44 + 2 across 10 files; at the
  epic's head: 0 / 0. Mutation-tested: an emitted argument name changed in
  each package turns its parity pin red (hosted: the prepare site's handler
  pin in `catalog-purchase.test.ts`; signer: the hosted parity test; local:
  the discovery parity in `packages/mcp/src/tools.test.ts`). Money-path by file
  (`packages/mcp-server/src/**`). No emission, cap, funding, signing,
  settlement, custody or refusal decision changes. Perimeter unchanged.
