- **#3122** — **Setup warns about other live-keyed agents BEFORE the first
  credential write, and each setup records the MCP server name it bound
  locally. Connector-only, local files only; no backend file, no network call
  added; `area:backend` / `money-path` waived on this slice by owner decision
  (epic #3119, decisions 1 and 2).**
  Before this change `packages/connect/src/runtime.ts` knew at
  `detectWiringCollision` that the machine carried other agents with live keys
  and printed the "your previous agent(s) still exist with their own keys"
  heads-up only after the credentials, the registration and the runtime
  config were written. Now `listExistingKeyedAgents` reads every credential
  directory's `identity.json` / `agent.json` (the stored API key's presence
  and the account address — never `signer.json`) before the key is minted,
  logs the agents with the account each can spend from, and the run proceeds:
  a refusal would change behaviour for every non-interactive caller including
  the dashboard's connect flow, so the win is timing and legibility, not a
  gate. `--json` gains `existing_agents_before_write`, additive within
  `schema_version: 1`. Each completed setup writes `mcp-server-binding.json`
  (`{ version, server_name, signer_name, agent_id, api_url, hosted_mcp_url?,
  bound_at }`, 0o600, per credential directory) beside the outcome record;
  before the write, a name another directory's record holds is named with
  its previous agent, backend URL and bound-at, and a backend change is
  flagged (`server_name_rebound_from`) — the case the backend's own
  `agents.mcp_server_name` cannot answer, and the one that silently repoints a
  saved session. The backend stays the authority for the same backend; the
  local record never asserts itself as its current truth and every message
  reading it says "locally recorded". `--unwire` and the `--replace`
  retirement release the record; `--doctor` reports two records claiming one
  name as the `mcp_server_name_rebound` advisory (#3121 level). Perimeter
  argument: the connector gains no write of key material and no read of the
  backend; the new file carries no secret by construction (a test greps the
  record for the API key, the private key and the field names of either, and
  a second test proves URL userinfo — `https://user:pass@host` — is stripped
  from `api_url` before the record is written, the shape row 9a of the
  delegation-rail security model already treats as a secret); the doctor
  advisory counts every non-retired directory (a superseded directory still
  holds a key a stale host may act as) and skips tombstoned ones;
  nothing is revoked (#1688); the collision REFUSAL of #2551 (a taken name
  slot) is unchanged — this slice adds a warning about the wallet, never a
  refusal. Mutation-tested: the pre-write notice silenced (the ordering test
  red); the `--json` list forced empty (two red); rebind detection dropped
  (two red); `backend_changed` forced false (red); the record not written
  (red); `--unwire` keeping the record (two red); the doctor advisory
  dropped (red); the retired-directory filter removed (red); the `--replace`
  wording forced to the takeover alarm (red). Measured with `scripts/ci/money-path-classify.mjs
  <merge-base>` against `.github/money-path-globs.json`: no touched file
  matches a runtime or control glob (`packages/connect/src/**` is `DOC_ONLY`
  in `scripts/ci/money-path.test.mjs`). Custody unchanged — Haven still holds
  no key and no funds. Perimeter unchanged.
