- **#3134** — **The four receipt/transaction vocabulary pairs that name one
  concept now share one name, decided at the SDK mapper (epic #3130 slice
  4/4; owner decision 1). Reads only — no funds move, no signing path, no
  authority changes; `money-path` by the epic's classification of this slice
  (it reaches agents through `packages/mcp-server/src/**`, whose
  `haven_list_receipts` passes the SDK's mapped rows through), not by file —
  the classifier prints the denominator: 0 of the changed files on the
  perimeter.** Before this change a settled x402 payment read through
  `haven_list_receipts` and through the wallet feed carried four values under
  eight names: `proofStatus`/`paymentProofStatus`,
  `merchantAddress`/`x402MerchantAddress`, `resourceUrl`/`x402ResourceUrl`
  and `rail`/`source`, and #3131's vocabulary map held them as
  `converge-pending` (open count 4). Now `mapPaymentReceipt` emits the
  transaction-side name for every pair — the transactions wire is frozen
  under decision 1, `proofStatus` is ambiguous on a generic row,
  `x402`-prefixed names carry the protocol provenance a receipt always has,
  and `source` beside nested `scope.source` is the pair every transaction row
  already carries — beside the old receipt name as a deprecated twin for one
  full release. The backend receipts wire (`RawHavenPaymentReceipt`, snake
  case) is untouched: no route, no migration, no column. The removal
  condition is written on the mapper, in substance: the twins go only when
  `npm view @haven_ai/sdk dist-tags` and `npm view @haven_ai/mcp dist-tags`
  both read a `latest` at or above the release naming them AND the hosted
  mcp-server deploy reports a `serverInfo.version` on MCP `initialize`
  (`HOSTED_SERVER_VERSION`) at or past that release — read against the
  registry and the live server's handshake, never inferred from a green
  promotion. The map records the
  four concepts as `converged` with one reason each, the four old names as
  `singleSurface.receipt` rows carrying that removal condition, and
  `lint:vocabulary`'s baseline is ratcheted 4 → 0 open (`{}`); a converged
  pair cannot reopen silently. Neither MCP runtime reshapes receipt rows, so
  both hosted and local `haven_list_receipts` carry the twins once the SDK
  that maps them ships; no tool description changed (none names these
  fields; the #1591 budget has 2 bytes of headroom and was left alone). The
  dashboard reads no receipt row (its `resourceUrl` / `merchantAddress` hits
  are catalog and transaction types), so "the dashboard moves in lockstep"
  is satisfied vacuously and stated here. Perimeter argument: one read
  surface gains four keys it already carried under other names; no write,
  no new authority, Haven still holds no key and no funds. Mutation-tested
  (each restored byte-identically): `source` pointed at `proof_status`
  (red); `x402MerchantAddress` pointed at `payer_address` (red);
  `x402ResourceUrl` pointed at `tx_hash` (red); the deprecated `proofStatus`
  twin dropped (red, and a type error); a converged survivor misspelled in
  the map (guard red: "the field moved, was renamed, or the pair
  converged"); a `singleSurface.receipt` row dropped (guard red: undeclared
  field). Custody unchanged. Perimeter unchanged.
