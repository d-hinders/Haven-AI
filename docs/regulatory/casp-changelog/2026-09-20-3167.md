- **#3167** — Agent labels: free-form tags on agents (`agent_labels` + `agent_label_assignments`, migration 093), a CRUD vocabulary API (`GET/POST /labels`, `PUT/DELETE /labels/:id`), a full-replacement assignment route (`PUT /agents/:id/labels`), and `labels[]` riding along on every agent read — plus the chips, tag editor and label manager in the dashboard.

  **Authority and custody: what moved, what did not.** The issue's own hard guard is the analysis: labels are display/categorization ONLY, and the diff is built so nothing else can read them as authority. The two new tables carry no amount, no caveat, no delegation reference and no key material — `agent_labels` is a per-user name and a colour name from a four-value palette, `agent_label_assignments` is a bare (agent_id, label_id) pair. The data access lives in one narrow module (`infra/repositories/agent-labels.ts`) whose header states the boundary, and nothing under `rails/`, the payment pre-checks, the delegation lifecycle, or the caveat-enforcement path imports it — the import graph keeps the guarantee auditable rather than merely asserted. No existing route's request handling changed in a way that could widen spend: the two new route files are born ENFORCED through the request-validation plugin (`enforcedModules` in `index.ts` grew by exactly these two entries), the assignment route accepts a list of label ids the user already owns (verified inside the replace transaction) and nothing else, and the `/agents` response gains a read-only projection. No new consent, signature or key-handling surface exists: renaming, recolouring, deleting or assigning a label never signs anything, never moves anything, and never changes what an agent may spend — the delegation lifecycle, budgets and on-chain enforcement are byte-identical to #3166's state.

  **What is mutation-tested / pinned:** the migration's real-DB suite pins the unique-per-user lowercased-name rule ("prod" and "Prod" collide, 23505), the blank-name check constraint, and CASCADE in both directions (deleting a label removes only join rows; agents are untouched — the issue's acceptance criterion; deleting an agent removes its assignments). The route suites pin: per-user scoping (a foreign label id is 404, never a leak), the 400/404/409 semantic guards (blank name, unknown colour, rename onto an existing name → 409), the ALL-statuses rule on assignment (archived/revoked agents are labelable), the 32-labels-per-agent cap, the transactional replace, and the ride-along `labels[]` on `GET /agents`, `GET /agents/:id` and `PUT /agents/:id`. Frontend suites pin the chip row's three-visible/+N overflow, the v2-token-only palette (no raw hex), and that the manager's delete confirm states agents keep everything else.

  **Perimeter verdict:** labels never touch delegation authority, budgets, or on-chain enforcement, and nothing that decides what an agent may spend may join these tables — that is the design boundary the issue draws and this diff draws too. Perimeter unchanged.
