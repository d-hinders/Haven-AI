- **#3202** — PR for epic #3077 decision 14 (Marketplace prospects beside the
  mainnet merchants on dev): the prospects gate in
  `modules/catalog/marketplace-scope.ts` keys on the **presence of an explicit
  testnet** in `HAVEN_MARKETPLACE_CHAIN_IDS` (new
  `marketplaceListsTestnetExplicitly`) instead of the absence of mainnet, so
  Berget AI and Redpine's `coming_soon` cards can stand beside the six mainnet
  merchants on dev's `84532,8453` list. The other halves of decision 12 are
  unchanged: a prospect is served only to an authenticated dashboard user,
  never to an agent, never in the credential-less shape, and its slug answers
  404, never 403. The deploy-chain fallback and "every chain" never open the
  gate (prod deploys `8453,84532`), and an unregistered chain id is not a
  testnet (`isMainnetChain` fails closed), so prod — list `8453` — cannot show
  a prospect with the flag copied over; a prod list that named `84532` would
  already be leaking testnet merchants to dashboard users, a misconfiguration
  the grid shows before it shows a prospect. Not on the money-path perimeter
  by file (none of the PR's files match `globs` or `controlGlobs` in
  `money-path-classify.mjs`; `modules/catalog/**` is not a runtime glob) — this shard is
  the forward pointer the immutable #3078/#3080 shards need, and it satisfies
  `casp-risk-guardrails.md`'s coupling on the `config.ts` comment edit. No
  payment, intent, delegation, allowance, account or custody surface is
  touched; nothing is signed and no funds move. Mutation-tested: the old
  "no mainnet" gate back (red), the fallback list counted (red), the gate
  removed (red), an unregistered id treated as a testnet (red), the route
  test on `84532,8453` with the old gate (red). Perimeter unchanged.
