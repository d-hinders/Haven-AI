- **#3030** — epic #3028 slice 2 (request validation): every non-money route
  module — the 22 files `accounting*`, `agent-activity`, `analytics*`, `auth`,
  `balances`, `catalog*`, `dashboard`, `discovery`, `health`, `openapi`,
  `passkeys`, `passport-verify`, `portfolio`, `safe-deploy`, `transactions`,
  `user`, `user-accounts*`, plus `accounting-webhooks` (#3196, landed in the
  slice's base commit; found shadowed in review) and the inline `index.ts`
  pair — joins the
  request-validation plugin's `enforcedModules`, so an off-spec request to
  any of them answers the documented 400 envelope from the OpenAPI schema
  before the handler runs, and the hand-rolled shape checks the schemas
  replaced (`typeof` ladders, uuid/address/enum/bound checks) are deleted
  from the handlers; the semantic checks (blank-after-trim, ownership, chain
  support, the email form, control characters) stay exactly where they
  were. Flipped on the epic's fallback — owner decision 2026-09-21 on #3028:
  the in-process shadow counter resets on every dev deploy and carries no
  per-route traffic, so the dev read could not prove these modules; each
  module's route tests (off-spec → the envelope, conformant → unchanged) are
  the instrument and `enforce` on dev is the reading. Not on the money-path
  perimeter by file: none of the PR's files is a runtime glob in
  `money-path-classify.mjs` — the plugin file
  `openapi/request-validation.ts` (which IS on the perimeter) is untouched,
  and the nine money-path modules (`payments`, `x402`, `machine-payments`,
  `agents`, `agent-delegations`, `agent-rekey`, `agent-passports`,
  `agent-connection-setups`, `hybrid-accounts`) stay shadowed for slices 3–4
  (#3031/#3032). Spec corrections in this slice narrow only what the handlers
  already refused (`chain_id`/`chainId` positive, `paymentId` uuid on the
  accounting feed, ISO date prefixes on the accounting period bounds,
  `agentId` as `user`-or-uuid, `tokenKey` as `<chain>:<address|native>`,
  safe-integer ceilings on `offset`/`page`), loosen one the description
  already promised (`backfill.since` accepts a plain date, as the dashboard
  sends), require the body `POST /auth/device/start` always receives (the
  CLI login; its `client_label` stays truncated to 80, never refused — a
  long hostname must not fail `haven login`), and declare one field the
  dashboard had been sending undeclared (`signup.via`) — the two
  `@haven_ai/core` type changes are `signup.via` and the now-required
  device-start body. The three enforced routes whose
  `authMiddleware` ran as a `preHandler` (after validation) — the two device
  routes and `GET /analytics/funnel` — now run it `onRequest`, so an
  anonymous caller is 401 before any 400 there too. For the catalog read path `04-x402-payment-sequence.md`
  describes: `GET /catalog` is still strictly read-only and still filters
  `category` case-insensitively after trim, `search` after trim and
  whitespace collapse, and `rail` by the same set — the difference is that a
  repeated key, an over-long search or an unknown rail is refused by the
  schema (the enum is pinned to `VALID_RAILS` by test) instead of by the
  handler; nothing here authorizes a payment or changes what a catalog
  price means. The retired Safe-inflow tombstones (`POST /user/accounts`,
  `PUT /user/account`, `/deploy`) answer their 410 from a route-level
  `onRequest` hook now, so a malformed body is still told the flow is gone
  rather than refused for its shape — the closed inflow stays closed, one
  hop earlier. No payment, intent, delegation, allowance, account or custody
  surface is touched; nothing is signed and no funds move. Mutation-tested
  per module: the module dropped from `enforcedModules` → its off-spec test
  red (the request reaches the handler or the repository); the spec enum or
  bound removed → red; the 410 hook removed → 400. Perimeter unchanged.
