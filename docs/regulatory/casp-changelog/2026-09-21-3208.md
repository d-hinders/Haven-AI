- **#3208** — the shadow-reading instrument epic #3028's slices 3–4 wait on.
  `openapi/request-validation.ts` (a money-path runtime glob, so this shard)
  gains two OBSERVATION fields on its in-process snapshot — `since` (the
  plugin install, ISO) and `seenByRoute` (requests that reached validation
  per SHADOWED route, whatever the verdict) — and one structured log line,
  `request_validation.seen`, at most once per route per minute carrying the
  running total; `GET /health/ops` declares the `request_validation` object
  it had been serving undeclared, with the two new fields. Nothing on the
  refusal path moves: `recordSeen` runs inside the existing shadow-only
  `preValidation` guard (`routeMode(request) !== 'shadow'` returns first),
  writes only to its own map and the log, and no verdict — shadow or
  enforce — reads it; the body snapshot/restore (#3082), the raw-Buffer guard
  (#3196) and the error formatter are untouched. Mutation-tested: `recordSeen`
  removed → the `seenByRoute` test red and nothing else in the 41-test plugin
  suite changes (the counter cannot change a verdict); the minute guard
  removed → 100 lines for 100 requests (red). The reading itself is
  `scripts/ci/shadow-reading.mjs`: it aggregates a window of the backend's
  log stream — which survives the dev deploys the in-process map does not —
  into the per-module table the epic asks for, sums a route's traffic per
  process segment (a boot line restarts the running total), and prints
  **NOT PROVEN** for a route with no traffic in the window instead of letting
  a zero pass as clean (mutation: verdict without the `seen` join → red);
  `--min-window-hours` refuses a window narrower than the runbook's minimum.
  It reads logs the operator pipes in; no token enters the repository. No
  payment, intent, delegation, allowance, account or custody surface is
  touched; nothing is signed and no funds move. Perimeter unchanged.
