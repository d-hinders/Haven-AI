- **#3210** — **flagless `--doctor` reaches the doctor; `--repair` still
  requires an explicit runtime. Diagnostic reachability only; no authority,
  custody, funding, signing or settlement boundary moves.**
  `packages/connect/src/args.ts` refused `--doctor` and `--repair` alike
  without `--runtime`, in the parser, before any code ran — a guard from
  #1597, when no setup record existed to resolve a runtime from. #3120 then
  taught `packages/connect/src/doctor.ts` to resolve the runtime from the
  agent directory's `last-connect-outcome.json`, but every real invocation
  still ended at the parser, so that resolution was reachable only through a
  direct `runDoctor` call; the field observation "`--doctor` fails without
  `--runtime`" was correct, and the #3119/#3120 bodies that withdrew it had
  measured `doctor.ts` alone (the shard for #3120 is a correct historical
  record of the doctor layer it describes). The guard is now narrowed to
  `--repair`: a flagless `--doctor` passes through and the doctor resolves the
  runtime from the record, or reports the existing `failed` "Runtime is
  unknown — the runtime config was NOT checked" verdict (exit 1). `--repair`
  rewrites the user's editor config, so which config it rewrites stays a
  command-line statement rather than a value inherited from a record on disk;
  the parser refusal for `--repair` is kept and its message names the reason
  (a whitespace-only value counts as absent). Three consequences of the path
  becoming reachable, found in review and closed here: a record-resolved
  runtime is named with its source file on the `runtime_config` verdict;
  every `--repair` hint carries `--runtime <runtime>` and the allowed values
  instead of a bare `--doctor --repair` the parser would refuse; and the
  informational `restart` check no longer env-detects a runtime nobody named.
  Perimeter argument: the doctor is read-only — it reads the user's own
  credential directory and config, performs the same probes as before, writes
  nothing, revokes nothing, deletes nothing; making it reachable without a
  flag opens no write path, and the one write path (`--repair`) keeps the
  stricter of the two requirements. The report schema, check ids and
  exit-code semantics are unchanged (a flagless `--doctor --json` now emits a
  `DoctorReport` where it emitted the parse-failure record); no new network
  call. Mutation-tested: the parser guard's condition widened back to
  `!options.runtime` with the new message kept → 3 red (`args.test.ts`
  "`--doctor` parses with no `--runtime`"; `cli.test.ts` "with a recorded
  runtime and NO --runtime flag" and "with NO --runtime and NO resolvable
  record"), 80 unchanged; with the pre-#3210 message restored as well → 5
  red, 78 unchanged (the two `args.test.ts` message assertions join —
  reproduced by the independent code pass); the CLI's `?? ''` fallback
  replaced by a guessed runtime (`?? 'codex-cli'` → the two flagless
  end-to-end `cli.test.ts` cases red); the restart guard removed
  (`input2.runtime === '' ? false :` dropped → the env-guess test red); the
  `repairCommandFor` placeholder branch reverted to a bare `--doctor
  --repair` → the unknown-runtime repair test and the `cli.test.ts` no-record
  case red. Measured
  against `.github/money-path-globs.json`: none of the touched files matches
  any glob or control glob. Custody unchanged — Haven still holds no key and
  no funds. Perimeter unchanged.
