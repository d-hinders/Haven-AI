- **#3213** — **`haven_check_funds` accepts the token symbol the other hosted
  reads hand the agent; refusal copy names the right remedy. Read-only tool,
  no authority, custody, funding, signing or settlement boundary moves.**
  `packages/mcp-server/src/tools/state-direct-recovery.ts` (a money-path
  runtime glob, so this shard): the sufficiency check's `token` argument was
  described as the contract address only, while `haven_get_agent` and
  `haven_get_allowances` report `tokenSymbol` beside `tokenAddress`; live on
  dev (2026-09-21 quality scan, D1) the symbol form refused twice — first
  with the purchase tools' "spending cap is REQUIRED before a paid merchant
  call" copy on a check that contacts no merchant, then with
  `MAX_AMOUNT_UNCONVERTIBLE` telling the agent to re-send atomic units, the
  wrong remedy since the address form of the same call answers `covered:
  true`. Now: an input matching `0x` + 40 hex takes the #3126 path unchanged;
  anything else is resolved against THIS agent's own allowances (one existing
  authenticated read) to the address of the single allowance carrying that
  symbol, and the backend coverage read still receives an address — the route
  and its guards are untouched. A symbol with zero or several matching
  allowances refuses `INVALID_INPUT` naming the agent's allowances with their
  addresses and handing off to `haven_get_allowances`; the no-cap refusal on
  this tool opens with "An amount is REQUIRED for the sufficiency check"
  (`readMaxAmountCap` takes an optional opener; the purchase tools' copy is
  byte-identical). Characterization test
  `packages/mcp-server/src/tools/check-funds-token.test.ts` replays the three
  live calls plus the ambiguity, unknown-address and address-bypass cases.
  Mutation-tested: symbol path bypassed → 3 red; ambiguity check relaxed to
  first-match → 1 red; tool-specific opener dropped → 1 red; coverage read
  sent the raw input instead of the resolved address → 1 red. No payment,
  intent, delegation, allowance, account or custody surface is touched;
  nothing is signed and no funds move. Perimeter unchanged.
