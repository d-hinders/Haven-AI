- **#3164** — agent organizations: a per-user folder tree (`agent_organizations`,
  migration `094_agent_organizations`) that files agents for display on
  `/agents`, with `routes/agent-organizations.ts` (CRUD + a delete that
  promotes contents one level up) and an optional `organization_id` on
  `PUT /agents/:id` (`packages/backend/src/routes/agents.ts`; the shard sits
  here because the classifier routes on `db/migrations/**` +
  `infra/repositories/**` — `agent_organizations.ts` in the latter and 094 in
  the former — NOT because `routes/agents.ts` is on the money-path glob list;
  it is not). This is categorization only, the same boundary the
  #3167 label shard drew one migration earlier: no amount, no caveat, no
  delegation reference, no join into `agent_delegations` or any budget view —
  the repository module (`infra/repositories/agent-organizations.ts`) states
  the display-only boundary in its header and nothing in `rails/` or the
  payment pre-checks imports it. The only `agents`-table write the feature
  adds is the nullable `organization_id` placement on `updateAgentProfile`
  (tenant-scoped WHERE as before; a foreign org id is refused with 404 before
  the write), and deleting an organization never touches an agent beyond
  moving that placement up one level — the real-Postgres route tests assert
  the agent row's `name` and `status` after the delete (the two fields they
  pin), not a whole-row equality. No signed intent, amount, token,
  recipient, route or timing moves; no allowance, delegation, custody or
  revocation surface is touched; the on-chain allowance remains the real
  spend control and an API credential remains insufficient to spend. The
  route module is born ENFORCED under the request-validation rollout.
  Round-3 rework hardened the tree writes (per-user advisory-locked move and
  delete, cycle/depth refusals, user-scoped promote) with no change to the
  boundary above. Perimeter unchanged.
