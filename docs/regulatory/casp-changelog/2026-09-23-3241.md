- **#3241** — the connector doctor's multi-directory test fixtures are made
  deterministic; test-only, no product code. `doctor.test.ts` (+47/−1) seeds
  several credential directories in quick succession, adjacent writes tie on
  the kernel's coarse clock, and `discoverCredentialDirectory`'s newest-wins
  selection (by `identity.json` mtime) then followed readdir order — a
  different mutation-proof cell failed per run depending on scheduling. A
  `stampAgentMtimes()` helper re-stamps every seeded `identity.json` with
  strictly increasing synthetic utimes, so the fixtures honor the doctor's
  selection contract deterministically; the contract itself is unchanged and
  all mutation-proof assertions are unchanged and re-proven load-bearing. No
  signing decision, key handling, custody, sweep-binding or
  `SUPPORTED_X402_EXPECTED_VERSIONS` change. Perimeter unchanged.
