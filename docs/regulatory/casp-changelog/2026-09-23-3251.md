- **#3251** — `connect`'s tombstone ledger follows the credential root. **No custody or authority boundary moves.** The `--tombstone`, `--unwire` and `--replace` writers never passed a ledger location, so every retirement record was mirrored into the ambient `~/.haven/tombstones`. That included test runs, which left `agent-old.json` / `unknown.json` records in a developer's real home. The mirror now lands in `tombstones/` beside the root that holds the retired directory, and `--doctor` reads the ledger beside the root it scans. For the default root (`~/.haven/agents`) the path is `~/.haven/tombstones`, byte-identical to before. The record is unchanged: `agent_id`, `retired_at`, the redacted `reason` and an optional `replaced_by`. It carries no key material and adds no signing path, and nothing is revoked (#1688: connect reports, the user decides). A tombstone remains a marker, never a revocation. The doctor still reads ledgers from the root it was pointed at and never from the machine-wide default under an explicit `--credentials-dir` (REGRESSION B2 discipline). The connect test suite now runs every worker against a scratch `HOME` / `USERPROFILE` / `TMPDIR`, so no test can write under the real `~/.haven`. No route, credential scope, key-material handling or signing path is added or widened, and the parent doc's custody and authority claims read identically before and after this diff. Perimeter unchanged.
