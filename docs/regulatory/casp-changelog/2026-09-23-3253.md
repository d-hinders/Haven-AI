- **#3253** — the hosted MCP server's `initialize` instructions (`HOSTED_INSTRUCTIONS`, `packages/mcp-server/src/server.ts`) now name the plain-HTTP x402 sequence: quote with `haven_quote_x402`, prepare with `haven_pay_x402_quote` passing the quote's `request_url` as `url` (never the merchant-declared `resource_url`), then either the EIP-3009 branch (local `haven_sign_x402`, `haven_submit`, the agent's own merchant retry, `haven_report_x402_outcome`) or the erc7710 branch (local `haven_sign`, `haven_submit` with `settlement_scheme: "erc7710"`, the agent's own retry, no outcome report). The session bootstrap wording now allows `haven_discover_tools` to run in parallel with `haven_get_agent` when discovery is needed. This is orientation prose read by the model: no tool, schema, `next_tool` value, cap, funding, signing, settlement or refusal behaviour changes, Haven still never contacts the plain-HTTP merchant, and every spend is still bounded by the agent's signed budget delegation enforced on-chain and requires the local signer's signature. `server.test.ts` pins the handoff shape, each branch's tool order, the absence of `resource_url` as `url`, the agent-performs-the-retry wording, and that every tool named exists on the hosted registry or the signer; each assertion was shown red under a targeted mutation of the text. Perimeter unchanged.
