- **#3031 (epic #3028 slice 3)** — the request schema is now ENFORCED on the three money-path route files #3221 left shadowed: `routes/payments.ts`, `routes/agent-delegations.ts`, `routes/machine-payments.ts` (x402.ts was flipped by #3221). **No custody or authority boundary moves.** What a caller may DO is still decided exactly where it was — budget, rail, scheme-agreement, expiry and the uint96 cap stay semantic refusals in the handlers (`modules/x402/authorize.ts`, `modules/x402/scheme-selection.ts`, the rail gates) — what changed is that a request whose SHAPE the OpenAPI spec does not declare is refused before the handler runs, with the plugin's 400 envelope (`error_code: invalid_request`, `details` naming the field) instead of reaching hand-rolled per-route rungs. The hand-rolled rungs are deleted in those files (`typeof` gauge 0; the request-schemas ratchet lowered accordingly). Every accepted request shape in the payment suites passes byte-identically before and after (the money-path characterization gate), and every semantic refusal answers unchanged.

  Enforcement without the two #3221 spec corrections would have refused every erc7710 payment; conversely the compiler's `removeAdditional` stays `false`, so an undeclared field is REFUSED, never silently stripped (pinned on `/x402/authorize` by `expectRejectsOffSpec` — an unknown field cannot reroute a payment to the 3009 rail un-pinned).

  Residual risk read: the refusal surface NARROWS client input but does not widen any permission. An attacker who could previously reach a handler with a malformed body now gets a 400 from the same edge; nothing that was refused before is now accepted, and nothing accepted before is refused (proven per-route: the suites assemble the app the production way — `installRequestValidation` with the module enforced — per the #3082 convention). The one plugin change, an optional-body operation (`requestBody.required: false`) now validating "declared shape OR absent", only restores the body-less POSTs dev already accepted (revoke-prepare, revoke-all prepare, rekey abandon); a required body still refuses absence. The sweep route's request schema now states the full 8-field EIP-3009 authorization the shipped SDK sends (`packages/sdk/src/sweep.ts SweepAuthorization`); mock-world minimal fixtures were made conformant rather than the schema loosened. Tested: 342 backend test files green including the four incident pins — #2282 (snake_case refused by schema, #3221), #1469 (null in accepts[] stays free-form, #3221), #1464 (malformed uuid path param refused by the schema naming `params/id` BEFORE any query; the 22P02→400 mapping past the schema stays the central handler's, unchanged), #2245 (a caller-supplied `settlementScheme` still cannot divert the tombstone; schema accepts, rail gate refuses). No migration. Perimeter unchanged: no new route, no new credential surface, no new outbound call.

- **#3031 round 2 rework (review verdict CHANGES REQUESTED; owner decision epic #3028 2026-09-24T21:24:44Z closing #3223)** — two corrections to the paragraph above:

  **The standing residue is NAMED and SHADOWED again.** `POST /machine-payments/reconciliation-events` is NOT enforced — it moved to its own route file, `routes/machine-payments-reconciliation-events.ts`, so the file-keyed flip (#3135/#3167, one file = one `enforcedModules` entry, no per-operation opt-out in the plugin) can enforce the rest of the machine-payments surface without dragging it along. The owner decision names it the rollout's residue: the route is only posted on a genuine merchant rejection after a confirmed payment, and driving it synthetically would write a false record into a payment's ledger (`machine_payment_reconciliation_events`). It waits for a real rejection or a QA scenario that produces one; until then its shape rungs STAY in the handler (a shadowed route must keep answering exactly as it did — shadow only measures), its shadow residue is baselined honestly (`routes/machine-payments-reconciliation-events.ts: shadow 1, typeof 5` in `lint-request-schemas-baseline.json`), and the `dev → main` promotion checklist carries the note the decision requires. The earlier claim that "every money-path module is now enforced" was wrong in round 1 and is corrected in `docs/operations/dev-environment.md`.

  **The retired `/machine-payments/authorize` tombstone moved to `onRequest`, as ordered.** The refusal (`mppDemoRetired()`, 410) now runs in a route-level `onRequest` hook — the #3030 retired-tombstone pattern (`routes/user-accounts-retired.ts` pins the same property) — and the round-1 permissive `MachinePaymentAuthorizeRequest` schema (`properties: {}, additionalProperties: true`) was reverted to the strict pre-#3031 declaration (`challenge` + `idempotencyKey` required, `signature` patterned, `additionalProperties: false`; `packages/core/src/api-types.ts` regenerated). The mechanism matters because fastify runs `onRequest` hooks BEFORE schema validation: the tombstone answers every body — a full old challenge, `{}`, a signed one-shot, garbage — with the honest 410, never a 400, and the module-level `agentAuthMiddleware` (added first, also `onRequest`) keeps the unauthenticated caller's 401. The ordering is measured on fastify 5.8.x and pinned by the characterization suite. The deviation round 1 shipped (permissive schema instead of the ordered move, recorded nowhere) is closed: the ordered mechanism is implemented and this entry is its record. Round 1's "no new route" perimeter line is updated by this rework: the reconciliation route's FILE moved (a new file, same single registered route, same prefix — no new operation, no new caller surface).
