- **#3032 (independent prep of epic #3028 slice 4)** — request validation readiness for `routes/agent-connection-setups.ts` and `routes/agent-passports.ts`. **No custody or authority boundary moves, and nothing is enforced yet.**
  - **Four request-schema loosenings in `openapi/spec.ts`.** Each declares a field today's clients already send and the handler already reads:
    - `local_mcp` on `POST /agent-connection-setups` (dashboard);
    - `mcp_server_name` on `POST /agent-connection-setups/register` (`@haven_ai/connect`);
    - `skill_installed` and `superseded_agent_ids` on `POST /agent-connection-setups/{setupId}/install-status` (`@haven_ai/connect`).

    Without them, enforcing the module would refuse a working connector with a 400. They add no constraint, so nothing accepted today is refused. Handler-side sanitisation is unchanged: `mcp_server_name` is normalised to null, and `superseded_agent_ids` is trimmed, path- and secret-filtered and capped. `packages/core/src/api-types.ts` is regenerated.
  - **Auth order.** `agent-passports.ts`'s module hook and the four owner routes of `agent-connection-setups.ts` authenticate in `onRequest` instead of `preHandler`, so once the modules are enforced an anonymous caller gets 401 before any schema 400. The same `authMiddleware` runs, earlier; who may call which route is unchanged. It is pinned against both modules enforced, with an authenticated off-spec control that must answer 400.
  - **Not changed:** the connector routes, which authenticate in-handler by setup token or agent API key; `enforcedModules`; the `HAVEN_REQUEST_VALIDATION` default. Those land with the rest of #3032, after #3031.

  No route, credential scope, key-material handling, signing path or spend authority is added or widened. Perimeter unchanged.
