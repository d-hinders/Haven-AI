- **#3264**: comments, `.env*.example` comments and docs only. There is no runtime change. The changed lines touch money-path files (`infra/relayer.ts`, `infra/outbound-queue.ts`, `rails/sweep.ts`) only in comments. Two stale claims are corrected, along with every live copy of them. The first is that the relayer submits "owner-signed Safe `exec` relays": `POST /safe/exec` was deleted in #2847 and answers 404, pinned by `safe-inflow-retired.test.ts`. The relayer's submitters today are delegator activation, passport attestations and revocations, sweeps, and the outbound queue's fee bumps and lane cancels. Agent payments are paymaster-sponsored UserOps and never use the relayer. The second is that multi-replica correctness is "gated on the Safe-bound legacy sites (#1440)": those sites are deleted. The comments now name the gap that remains. `openOutboundRecord` is fail-open by policy, so on a database error the submitter passes a null `recordId`, `submitRecorded` skips the stamp-before-broadcast fence, and only the in-process `withRelayerSendLock` serialises that one submission. Across replicas, a collision on that path fails a broadcast and misdirects nothing. The same sweep corrected `ABOUT_HAVEN.md`, `03-payment-sequence.md` and `account-recovery.md`, which still called `POST /safe/exec` open to legacy Safe owners, and the relayer-paid operation lists in `04-x402-payment-sequence.md` and the delegation-rail security model. **No authority, custody or execution boundary moves.** The relayer stays gas-only, what may be spent and under which caveats is untouched, and the fail-open policy itself is unchanged: this records it, it does not alter it. Perimeter unchanged.
