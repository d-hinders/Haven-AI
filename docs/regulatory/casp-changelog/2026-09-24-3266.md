- **#3266** — frontend-only rename of leftover Safe-era local identifiers and repair of test fixtures that still spoke the retired wire. **No custody, authority, or payment-execution change.** One file this document covers is touched, `packages/frontend/src/components/AddFundsModal.tsx`, and the change inside it is comments only: three prose references that described live state with Safe-era nouns ("a safe that arrived without `chain_id`", "no safe") now say account, and a history comment's `getChainConfig(safe.chain_id)` call-site reference is reworded to the same expression's current spelling (`account.chain_id`) — the same modernisation that call's file already received in the same PR. No executable line in the file moves: the diff is comment text.

  Everywhere else the diff reaches files this document does not cover: component and hook locals (`safes`, `SafeCard`, `defaultSafe`, `selectedActionSafe`, and friends) renamed to account vocabulary with no behaviour change, and test fixtures that mocked a retired `/auth/me` envelope key (`safes:`) or a retired set-default route (`PUT /user/safes/:id/default`) repaired to the live ones. Repairs went one step further where the fixture alone did not make the test honest: two `DashboardClient` backup-nudge tests ("unfunded delegation-rail account", "transient balance-fetch failure") never stored a signer set, so the component's unknown-signer silence — not the funded-state gate their names claim — suppressed the nudge. Both now store a known single-signer set (mutation-checked: funding the account flips the first test to failure), so the gate in the test's name is the load-bearing one. That is a test-only change; the component is untouched. The stored passkey blob's `safeAddress` field, `lib/signer.ts` (a money-path file), and the `safe_funded` funnel event are deliberately untouched. No route, credential scope, key-material handling, signing path, or generated credential artifact changes. Perimeter unchanged.
