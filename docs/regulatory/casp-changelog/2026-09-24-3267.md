- **#3267** — the backend's leftover Safe-era identifiers on live account data are
  renamed to account vocabulary: `userSafeId` → `accountId`, `safes` /
  `allSafes` / `ownedSafes` → `accounts` / `allAccounts` / `ownedAccounts` and
  the singular `safe` loop local → `account` (transactions orchestration and
  the x402 merge, routes: transactions, dashboard, balances, portfolio),
  `safeExecutionRail` → `executionRail` (the `rails/execution-rail.ts` seam and
  its mpp/x402 callers), the `has_bound_safe` → `has_bound_account` SQL column
  ALIAS on `AGENT_BY_API_KEY_SQL` (a projected boolean, not a column — agents.ts,
  agentAuth, mpp/sweep), `safeTxHash` → `accountTxHash` (an input field that
  already wrote the `account_tx_hash` column — agent-connection-setups repo and
  route), the db-schema-smoke check labels (every renamed check still PREPAREs
  the same SQL constant), two test-file renames and six stale comment sites.
  **No custody or authority boundary moves.** Nothing changes on the wire, in
  the schema or in behaviour: a spec-review confirmed none of the renamed names
  is serialised, logged or metric-tagged, response keys and route paths are
  untouched, and the `/user/safes*` tombstone contract, the retired-name
  refusals (`safeIdVerdict`, `safe_id`, `safeId`), the `safe_funded` funnel
  event, the excluded retired-name tests and all migration files are unchanged.
  No key material, credential scope, signing path, approval boundary or
  spend-control mechanism moves: `resolveExecutionRail` keeps identical wire
  values and fail-closed semantics (`'session_key'` → `retired_session`,
  `'delegation'` → `delegation`); `agentAuth` keeps the `has_bound_account === false`
  fail-closed refusal and the `?? true` legacy fallback for rows predating the
  alias; every SQL statement keeps its predicates, tenant scope and the same
  `account_tx_hash` write target. Perimeter unchanged.
