- **#3279 (SDK slice)** — documentation-only edits in `@haven_ai/sdk`: JSDoc comments, one code comment, the README and the CHANGELOG. The sweep's destination is now named as the agent's Haven account instead of "the originating Safe". This matches the code: `delegate-sweep.ts` sends to `agent.accountAddress`, and the backend `rails/sweep.ts` sets `to: params.accountAddress`. The x402 funding leg is now described as account → delegate EOA, redeemed through the agent's budget delegation, not as a Safe AllowanceModule transfer. `AgentPaymentRail.Direct` is now described as a budget-delegation redemption. No type, value, export or behaviour changes: the diff touches only comment lines, README prose and a CHANGELOG entry. Mentions of the retired Safe rail as history are kept. **No custody, authority or execution boundary moves.** Perimeter unchanged.
