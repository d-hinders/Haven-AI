- **#3279 (signer + MCP + sweep-route copy, the remainder after the #3280 SDK slice)** — user-facing and agent-facing text only, across `packages/signer/src/{consent,tools,core}.ts` (+ their tests), `packages/mcp/src/consent.ts` (+ its test), `packages/backend/src/openapi/spec.ts`, `packages/backend/src/modules/mpp/sweep.ts`, `packages/backend/src/rails/sweep.ts`, the regenerated `packages/core/src/api-types.ts`, two docs pages and two package CHANGELOGs. The signer's first-launch consent block now names **the agent's signed budget delegation** as the real spend gate instead of "On-chain Safe rules"; the MCP consent screen prints `Haven wallet: <address>` instead of `Haven wallet (Safe): <address>`; every sweep copy site names the destination as **the agent's account (Haven wallet)** instead of the delegate's "own Safe", and the sweep copy now states the real destination check — it runs **only when the local credential carries an account address** (`core.ts`'s `expectedSafe &&` conditional, #2247) and otherwise rests on Haven's binding signature — where the old copy overclaimed an unconditional check.

  Authority and custody argument: the diff touches only string literals in copy positions (tool descriptions, rendered consent lines, JSDoc, OpenAPI summary/description/schema text, two API error bodies, comments) and test assertions that pin those strings. No hash input changes: neither `computeSignerConsentHash` nor `computeConsentHash` hashes rendered text (identity + tool names + surface version / identity + tool set + allowance summary respectively), and `SIGNER_CONSENT_SURFACE_VERSION` stays 2 — newly pinned by a test — so **nobody is re-prompted**. No signing, verification, binding, relaying or sweep logic moves: the `expectedSafe &&` guard, the `from` check, the binding verification and the canonical-USDC assertion are byte-identical; `to: params.accountAddress` is untouched; the identifier `expectedSafe` keeps its name and `SweepSignatureInput` remains unexported by name (no export surface change). The two changed API error bodies are free-text `error` strings with no client matching on them (grep-verified), so no wire consumer moves. What the copy now says is what the code already did: the delegate key stays the only key, funds move only delegate EOA → the agent's own Haven account, and the spend gate is the agent's signed budget delegation. Perimeter unchanged.
