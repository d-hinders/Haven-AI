- **#3306** — **The four deprecated `HavenPaymentReceipt` twins that #3134
  dual-emitted are removed at the SDK mapper (epic #3130, contraction of slice
  4/4). Reads only — no funds move, no signing path, no authority changes;
  `money-path` by the epic's classification of this slice (it reaches agents
  through `packages/mcp-server/src/**`, whose `haven_list_receipts` passes the
  SDK's mapped rows through), not by file.** `mapPaymentReceipt` no longer
  emits `rail`, `proofStatus`, `resourceUrl` or `merchantAddress`, and
  `HavenPaymentReceipt` no longer declares them; the survivors `source`,
  `paymentProofStatus`, `x402ResourceUrl` and `x402MerchantAddress` carry the
  same columns unchanged. The removal condition #3134 wrote on the mapper held
  at merge on all three clocks: `@haven_ai/sdk` `latest`, `@haven_ai/mcp`
  `latest` and the hosted prod mcp-server's `serverInfo.version` on
  `initialize` read `0.5.0-alpha.1`, at or above the twin-bearing
  `0.5.0-alpha.0` (owner decision on #3306, evidence on #3130). The vocabulary
  map drops the four `singleSurface.receipt` rows, and `lint:vocabulary`'s
  undeclared and stale directions both hold: re-adding `rail` to the type and
  mapper turns the guard red as an undeclared field, and re-adding a
  `proofStatus` row turns it red as a stale declaration (each restored
  byte-identically). The inverted mapper test and the exact-shape
  `listReceipts` test both go red when the mapper re-emits a twin. The backend
  receipts wire (`RawHavenPaymentReceipt`) is untouched: no route, no
  migration, no column. The change is breaking for readers of the old keys and
  is recorded as such in the SDK and MCP CHANGELOGs, with a MINOR bump under
  the 0.x convention for the release carrying it. Perimeter argument: one read
  surface drops four duplicate keys; no write, no new authority, Haven still
  holds no key and no funds. Custody unchanged. Perimeter unchanged.
