- **#3319 (follow-up to #3307)** — the agent-side allowance and identity reads join the checksum rule: `handleGetAllowances` (`packages/backend/src/modules/mpp/allowances.ts`), `GET /machine-payments/agent` (`packages/backend/src/routes/machine-payments.ts`) and the `haven_check_funds` backend echo (`packages/backend/src/modules/mpp/balance-coverage.ts`) emit their Haven-owned addresses (`delegate_address`, per-allowance and echoed `token_address`, plus the identity read's already-checksummed `account_address`/`delegate_account_address`) EIP-55 checksummed through the shared `toCanonicalAddress`, the same rule #3129 and #3307 apply. For one settled payment, `haven_get_payment_status`'s `asset` and `allowance.token_address` are now byte-equal (the joined block reads the fixed allowances read), and `haven_get_agent`'s `delegateAddress` matches the checksummed `delegate` on the agent's receipts.

  Authority and custody argument: this is a value-casing change on read-only informational surfaces. No key material, signing, binding, verification, relaying, or payment execution moves; no spender, approval, or credential surface is created. Storage stays lowercase (`LOWER(...)` writes and the `agent_delegations` CHECK are untouched — a write-side change would be the authority-adjacent one, and none was made); the budget view and its comparing callers (budget-precheck, balance-coverage matching) keep the stored casing, so no spend check, budget comparison or enforcement path re-cases anything. The x402 binding signer lowercases `delegate_address` independently, so no signed byte moves. The OpenAPI `address` pattern is case-agnostic, so no wire schema changes; no in-repo consumer parses these fields by exact case.

  Deliberately untouched: owner-side reads (`GET /agents*`, `/dashboard`, `/agents/:id/delegations*`) keep stored casing — the owner/agent casing asymmetry on one delegation is declared, every frontend consumer compares case-insensitively; the budget-precheck refusal keeps its deliberate lowercase `merchant_address`; the spec's owner-side `Delegation` schema keeps its "Stored lowercase" note. The x402 quote/prepare responses were not re-surveyed beyond the status and settle paths.

  Tests: `routes/__tests__/machine-payments-address-casing.test.ts` (real Postgres, real agent-credential path) seeds lowercase rows — the only form the CHECK constraint admits — asserts every seed differs from its checksum form (a self-checksumming seed would prove nothing), pins the checksummed responses on all three surfaces, and re-reads the raw rows to prove the boundary is a read-side one. The two existing lowercase pins (`machine-payments.test.ts:474`'s allowance `token_address`, `balance-coverage.test.ts`'s coverage echo) are updated to checksummed, not deleted. Perimeter unchanged.
