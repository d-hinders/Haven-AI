- **0.6.0-alpha.0 release: `0.5.0-alpha.1` → `0.6.0-alpha.0`, a MINOR bump.** It is MINOR because the range carries one breaking change to tool output. #3306 (#3352) removes the four deprecated `HavenPaymentReceipt` twins, which `haven_list_receipts` rows no longer carry. The 0.x convention recorded in `docs/operations/mcp-runtime-compatibility.md` and the mcp CHANGELOG makes that a minor step. All other changes in the range are additive or narrow authority; the one widening is #3329's: the redemption allowlist admits one bounded chain shape, and the key learns to sign that task child and its authority-reducing close, recorded below. Each merged change carries its own shard, and this entry records what the release bundles and what it does not prove.

  **Shipped delta, measured with `npm run build && npm run release:scope` at `origin/dev` `8944227d`:** 25 files, +1696/−87, over 28 commits from `origin/main` `6f7ec1a8`.
  - Per package: `@haven_ai/sdk` **15**, `@haven_ai/signer` **5**, `@haven_ai/cli` **3**, `@haven_ai/mcp` **2**, which sums to 25. `@haven_ai/connect` has no shipped source change and moves only its version and exact pins.
  - Excluded: 243 files. 215 are outside a published package, 23 are unreachable test files, and 5 are in a package but outside the tarball.
  - The run exits 1 on one file, `sdk/src/__fixtures__/valid-userop.ts`. Only tests import it, which is the script's benign cause 2.
  - The instrument reads refs, so it measures the committed range before this bump.

  **Re-measured at the door, at `origin/dev` `90c27a24`** (the promotion head after the release PR merged), with `npm run build && npm run release:scope`: 30 commits, 35 files, +1713/−104.
  - The two new commits are #3337 (#3377), a qa-failure CI and docs change that ships nothing, and #3382, this release's own bump.
  - Per package: `@haven_ai/sdk` **17**, `@haven_ai/signer` **7**, `@haven_ai/cli` **4**, `@haven_ai/mcp` **4**, `@haven_ai/connect` **3**. The 10 files added since the cut are exactly the bump's version fields, exact pins and version constants: five `package.json`s, `connect`'s `runtime.ts` and `runtime-manifest.ts`, `mcp`'s and `signer`'s `server.ts`, and `sdk`'s `client-identity.ts`. `cli`'s `CLI_VERSION` sits in `commands.ts`, which was already counted.
  - Excluded: 249 files. The same single unresolved file, `valid-userop.ts`, is cause 2.
  - So the scope moved only by the bump itself. #3377 joins the CI list below, and every other figure in this entry stays as measured at the cut.

  **What actually promoted: `main` merge `7ea8c62e` (#3384, 2026-09-26 18:39Z), head `origin/dev` `1bbe267d`.** Four commits landed on `dev` after the measurement above and before the merge, so they ship inside this release too. `release:scope` at `1bbe267d` gives 34 commits; the shipped delta is still **35 files, +1713/−104**, with the same per-package split. None of the four reaches a tarball:
  - #3336 (#3381): an RPC conformance CI script and docs.
  - #3383: the `/releases` visual baselines and this entry's own door amendment.
  - #3344 (#3385): the qa-agent reads the chain on the observer node. `@haven_ai/qa-agent` is private.
  - #3371 (#3387): the backend RPC transport scrubs provider API keys from errors in place. It is **money-path** (`infra/relayer.ts`, `infra/outbound-queue.ts`, `infra/chain/rpc-transport.ts`), deploys with the backend, and carries its own shard, `2026-09-26-3371.md`.

  A green `money-flow` run on `1bbe267d` (run 36262472510) covered #3387 before the merge, and `qa-freshness` passed on that head.

  **What the release carries, by effect on authority:**
  - **#3329 (#3354), task budgets.** An agent can open a time-boxed, self-delegated child of its own budget delegation. It is redeemed as a two-link `[task child, budget]` chain and closed with an authority-reducing `disableDelegation`. This can only narrow what the account may already redeem, and it has its own shard.
  - **#3375 (#3376), SDK funding-leg recipient pin.** `HavenClient`'s EIP-3009 funding leg must pay the quoted amount into the agent's own delegate EOA, which narrows residual 1 of epic #3284. It has its own shard.
  - **#3378 (#3379), task budget passed through on four paths.** `task_budget_id` now reaches the wire on the SDK's `payX402Quote` and on the hosted `haven_send`, `haven_pay` and `haven_pay_x402_quote`, each of which had been charging the whole budget delegation instead of the named task budget. Every drop existed only on `dev` (the SDK's `dev` channel and the hosted runtime's `dev` deploy), because #3329 itself ships in this release. The change narrows spending to the task budget the caller named, and it has its own shard.
  - **#3277 (#3380), direct-payment handoff.** The hosted direct payments name the byte-free `haven_sign({ payment_id })` handoff and add old-signer refusal recovery. That is guidance and relay only, with no new signing surface, and it has its own shard.
  - **#3304 (#3367) and #3305 (#3372), public release data.** Read-only data in `/discovery`, `haven.json` and `/releases`, and the bump now generates that data from the CHANGELOGs. #3304 also ships guidance text: the agent runbook's "If something breaks" section, in `sdk/src/agent-guidance.ts` and its `haven guide` copy in `cli/src/agent-guidance-text.ts`, which is the third shipped CLI file. It points an agent at `client_update` and `/releases`. **This release is the first bump to regenerate it**, so `/releases` shows `0.6.0-alpha.0` once this is deployed. It never writes `client-compat.ts`, and every threshold is still null, so no client is hinted or refused by this release.
  - **#3306 (#3352), the break above.** Tool-output reshape only, with no authority change.
  - **#3318 (#3349), CLI balance freshness.** `haven wallets balances` renders the backend's `stale` and `unavailable` freshness markers instead of printing them as current. Display only, and it has its own shard. It accounts for 2 of the 3 shipped CLI files.
  - **Changes that reach no tarball**, 20 PRs at the cut plus #3377 after it, each carrying its own record where it is money-path:
    - Backend: #3326, #3327, #3350, #3353, #3356, #3357, #3365.
    - CI: #3355, #3359, #3360, #3363, #3366, #3373, #3374, and #3377 (merged after the cut, not in the 20).
    - Frontend: #3358.
    - Docs and skills: #3322, #3334, #3351, #3369, #3370.
  - At the cut, those 20 plus the 8 above account for all 28 commits in the range; at the door, #3377 and #3382 make 30; the four after the door (#3381, #3383, #3385, #3387) make the 34 that promoted.

  **Money-flow acceptance.**
  - A green `money-flow` job ran on `8944227d`, the promotion head at the cut: run 36247767246, 14/14 scenarios including coverage completeness. That closed #2769.
  - This release commit edits the signer CHANGELOG, which is a money-path file. So freshness must be re-established after the release PR merges: dispatch qa-dev on `dev` and re-run `qa-freshness` on the promotion PR. Done: see the green run on `1bbe267d` above, qa-dev's post-deploy run, which covers #3387 too.
  - The production smoke is an operator step.

  Perimeter unchanged. The delegate-key signing surface moves in both directions, each bounded: it is narrower on the x402 funding leg (#3375), and it is widened by exactly one redemption shape, the two-link `[task child, budget]` chain (#3329), which the parent budget delegation still bounds on-chain.
