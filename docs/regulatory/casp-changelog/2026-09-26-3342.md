- **#3342** — the #3327 anchor-UID repair is hardened on three points: the sweep's selector now excludes rows durably CONFIRMED against their anchor receipt (`uid_repair_confirmed_at`, migration 096 — one nullable TIMESTAMPTZ column on `agent_passports`, no backfill, every existing row reads unconfirmed and is re-read exactly once on the first post-deploy tick), so reads per tick stay bounded instead of round-robining the same oldest rows forever while a live agent's phantom behind them starved; the repair's and the #1043 recovery's reader is replaced by one proven-ours reader (`readProvenAnchorUid`), which applies the mint path's invariant — the `Attested` log must carry the pinned passport schema, be attested by the mined transaction's OWN sender (not the current, rotatable relayer address), and sit in a transaction whose `to` is the pinned EAS contract — and both callers write a UID only on `ours`; and every row the sweep cannot answer is reported per row (`agent_id` + reason), with a repair that throws deferred an hour instead of heading the queue every tick.

  Authority and custody argument: this is a correctness and evidence-integrity change on the passport attestation record; no key material, signing, binding, verification, relaying, or payment execution moves, and no spender, approval, or credential surface is created or widened. The chain reads stay exactly what they were (receipt + tx body via the relayer's provider); the only writes are the pre-existing compare-and-set UPDATE that swaps a stored UID for the receipt's, the new marker stamp, and the deferral bump — all on `agent_passports`, none on any payment, budget, or entitlement table. The change NARROWS the write perimeter: before it, a foreign `Attested` log (foreign schema, foreign attester, or a tx not targeting EAS) reached `agent_passports` verbatim through the repair or the recovery; after it, such a log is refused and reported, never written — the UID the merchant verifier is handed is provably the one Haven's own anchor tx emitted. Migration 096 adds one nullable timestamp column and moves no data; it grants no authority and records nothing but the fact of a confirmation.

  Deliberately untouched: the mint path's reader (`readMinedAttestationUid`) semantics, the relayer, the EAS deployment pins, the retire/revoke path, `FIND_BY_AGENT_ADDRESS_SQL`'s `updated_at DESC` tie-break (the marker is never read as "verified" anywhere else, so the verifier's answer does not churn), and the selector's pacing otherwise.

  Tests: the real-DB suite (`anchor-uid-repair-sweep-db.test.ts`) pins the stall fix (N ≥ limit healthy older rows + one phantom repaired within ceil(N/limit)+1 ticks), bounded reads in steady state, and the throw-deferral; the mocked suite pins the invariant refusals — foreign schema, foreign attester, foreign log before ours (ours is chosen), a tx that did not target EAS, and a row minted by a PREVIOUS relayer still repairable; the attester-guard fixture is padded to a 32-byte topic so the guard, not the ABI decoder, refuses foreign logs and the attester mutation turns a test red. Perimeter unchanged.
