- **RPC error key scrub (2026-09-26 incident)** — every ethers provider `infra/relayer.ts` builds (the primary and the broadcast fallback) now scrubs its own RPC URL — and so any embedded provider API key — out of a transport-level error before it leaves the provider: `ScrubbingJsonRpcProvider` overrides `_send`, the one place ethers' `FetchResponse.assertOk()` throws with the URL embedded in the message and in `info.requestUrl`/`request`/`response`. The 2026-09-26 incident was a dRPC 408 timeout on the PRIMARY provider printing the full URL, key included, to logs and the `outbound_txs` error column; the fallback's own send already had this fix (`fallbackSendError`, #3323/#2769) but the primary did not. JSON-RPC-level classification (`code`, `shortMessage`, a nested error body) is preserved untouched, so `isPendingTagRefusal`, `isDeterministicRevert` and NONCE_EXPIRED handling are unaffected.

  Authority and custody argument: no key material, signing, binding, verification, relaying or payment execution moves; no new spender, approval or credential surface is created. This is a defensive rewrite of an ERROR OBJECT after a transport failure, never of a request, a signature or a balance check — the relayer's nonce view, its single-provider binding (#1533) and the fallback's one consumer (#2769) are untouched. Perimeter unchanged.
