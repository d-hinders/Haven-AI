{
  "name": "haven",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w packages/backend\" \"npm run dev -w packages/frontend\"",
    "build": "npm run build -w packages/sdk && npm run build -w packages/mcp && npm run build -w packages/backend && npm run build -w packages/frontend",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "test": "npm run test --workspaces --if-present",
    "test:unit": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "quality": "npm run typecheck && npm run test:unit && npm run build",
    "test:e2e": "npm run test:e2e -w packages/frontend"
  },
  "overrides": {
    "axios": "^1.16.1",
    "fast-uri": "^3.1.2",
    "follow-redirects": "^1.16.0",
    "hono": "^4.12.23",
    "postcss": "^8.5.15"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
